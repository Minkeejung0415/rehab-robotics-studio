---
phase: 15-opensim-quaternion-live-link
plan: "03"
type: execute
wave: 3
depends_on:
  - 15-02
files_modified:
  - backend/rehab_robotics_bridge/opensim_test_publisher.py
  - backend/launch/rehab_robotics.launch.py
  - backend/setup.py
  - backend/test/test_opensim_launch.py
  - docs/opensim-quaternion-live-link.md
autonomous: true
requirements:
  - LINK-01
  - LINK-02
  - LINK-03
  - LINK-04
  - LINK-05
  - LINK-06
requirements_addressed:
  - LINK-01
  - LINK-02
  - LINK-03
  - LINK-04
  - LINK-05
  - LINK-06
must_haves:
  truths:
    - "An operator can launch the bridge with configurable native Imu topics, model path, frame mappings, stale timeout, and optional deterministic test publisher."
    - "The deterministic publisher emits known master identity and slave 90-degree orientation messages without connected ESP hardware."
    - "Launch/source contract tests prove parameter wiring and legacy UDP removal without importing a live ROS launch runtime."
    - "Exact known publisher messages traverse the configured master/slave bridge callbacks into a fake adapter with correct role, mapped frame, normalized rotation, status, and independent counters."
    - "Operator documentation states the exact quaternion convention, status schema, native visualizer meaning, and non-IK scope."
  artifacts:
    - path: "backend/rehab_robotics_bridge/opensim_test_publisher.py"
      provides: "Deterministic native Imu publisher and pure known-message construction helper"
      exports:
        - "OpenSimTestPublisher"
        - "known_orientations"
        - "main"
    - path: "backend/launch/rehab_robotics.launch.py"
      provides: "Live-link launch arguments and optional hardware-free publisher"
    - path: "backend/test/test_opensim_launch.py"
      provides: "Publisher and launch/setup source-contract tests"
    - path: "docs/opensim-quaternion-live-link.md"
      provides: "Operator launch, convention, status, and verification guide"
  key_links:
    - from: "backend/launch/rehab_robotics.launch.py"
      to: "opensim_bridge"
      via: "LaunchConfiguration parameters for topics, frames, model, timeout, and status"
      pattern: "master_imu_topic|slave_imu_topic|model_path|stale_timeout_s"
    - from: "backend/launch/rehab_robotics.launch.py"
      to: "opensim_test_publisher"
      via: "default-off IfCondition test publisher node"
      pattern: "enable_opensim_test_publisher"
    - from: "backend/setup.py"
      to: "backend/rehab_robotics_bridge/opensim_test_publisher.py"
      via: "console script"
      pattern: "opensim_test_publisher = rehab_robotics_bridge.opensim_test_publisher:main"
    - from: "backend/rehab_robotics_bridge/opensim_test_publisher.py"
      to: "backend/rehab_robotics_bridge/opensim_node.py"
      via: "known_orientations Imu fixtures delivered through the configured master/slave callback paths into an injected fake adapter"
      pattern: "known_orientations|OpenSimPublisherBridgeIntegrationTests"
---

<objective>
Complete the operator launch and deterministic hardware-free proof for the quaternion live link.

Purpose: Make the prototype runnable and inspectable from standard ROS 2 commands while preserving local unit verification on machines without ROS/OpenSim.
Output: Launch integration, test publisher, console entry point, source-contract tests, operator documentation, and a Phase 15 regression gate.
</objective>

<execution_context>
@C:/Users/justi/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/justi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/15-opensim-quaternion-live-link/15-CONTEXT.md
@.planning/phases/15-opensim-quaternion-live-link/15-01-SUMMARY.md
@.planning/phases/15-opensim-quaternion-live-link/15-02-SUMMARY.md
@backend/launch/rehab_robotics.launch.py
@backend/setup.py
@backend/package.xml
@docs/two-esp-usb-operations-test.md

<locked_decisions>
- D-01: native master/slave Imu topic defaults.
- D-02: independent newest-orientation publishing, no timestamp pairing.
- D-03: configurable mappings with femur/tibia defaults.
- D-04: either sensor can demonstrate independently.
- D-05/D-06: bridge-hosted native visualizer with labeled mapped sensor triads.
- D-07: documentation and naming must not imply model pose/joint angles are solved.
- D-08/D-09: configurable model path and observable non-visual fallback.
- D-10: compact JSON status plus logs.
- D-11/D-12: documented validation/normalization and independent stale timeout.
- D-13/D-14: adapter/fake tests plus deterministic identity, axis, configuration, invalid, stale, and unavailable-runtime verification.
</locked_decisions>

<interfaces>
From Plan 15-02:
- `opensim_bridge` parameters are `master_imu_topic`, `slave_imu_topic`, `master_frame`, `slave_frame`, `model_path`, `stale_timeout_s`, and `status_topic`.
- Defaults are `/esp32/master/imu`, `/esp32/slave/imu`, `femur_r_imu`, `tibia_r_imu`, empty model path, `1.0`, and `/opensim/status`.
- Status messages use `rehab.opensim_live_link.1` and expose visualization plus both roles.

Existing launch/package patterns:
- `backend/launch/rehab_robotics.launch.py` declares arguments and passes `LaunchConfiguration` values in each `Node(parameters=[...])`.
- `backend/setup.py` already installs the launch file and declares `opensim_bridge`.
- `backend/package.xml` already declares `rclpy`, `sensor_msgs`, and `std_msgs`; no new package-manager dependency is required.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a deterministic native-Imu test publisher and package entry point</name>
  <files>backend/rehab_robotics_bridge/opensim_test_publisher.py, backend/setup.py, backend/test/test_opensim_launch.py</files>
  <behavior>
    - Pure known-orientation construction returns master identity and slave positive 90-degree Z rotation in ROS `(x,y,z,w)` order.
    - Publisher parameters can override master/slave topics and publish rate.
    - Each timer tick publishes one Imu per role with stable role-specific frame IDs and current ROS timestamps.
    - Setup exposes `opensim_test_publisher` without adding dependencies.
    - Existing unrelated `setup.py` console scripts and dirty hunks are preserved byte-for-byte outside the explicit new entry.
  </behavior>
  <action>Before editing, capture `git status --short -- backend/setup.py backend/launch/rehab_robotics.launch.py` and `git diff -- backend/setup.py backend/launch/rehab_robotics.launch.py` into the execution log/SUMMARY as inherited dirty state. Work from the current copies with surgical edits only: never run reset, checkout, restore, clean, or whole-file replacement, and stage only the explicit Phase 15 paths. Create a narrow `opensim_test_publisher.py` that publishes deterministic `sensor_msgs/Imu` fixtures to the configured native topics, defaulting to master identity and slave +90 degrees about Z. Keep quaternion fixture creation in a pure helper so source tests run with the same lightweight ROS stand-ins used elsewhere. Publish both roles independently on each timer tick; do not simulate TCP hardware, calibration, synchronization, IK, or joint states per D-02 and D-07. Add only the `opensim_test_publisher` console script line to the current `setup.py`; retain every pre-existing console entry point and all inherited dirty hunks. Add tests for exact quaternion fields, topics, entry point, repeatability, and preservation of the existing setup entry points per D-14. After editing, compare the new diff against the captured baseline and record inherited versus Phase 15 hunks separately.</action>
  <verify>
    <automated>$env:PYTHONPATH='backend'; python -m unittest backend.test.test_opensim_launch.OpenSimTestPublisherContractTests -v</automated>
  </verify>
  <done>The packaged publisher deterministically supplies the two native Imu inputs without ESP hardware, and its known quaternion values are proven by pure local tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire launch parameters, document operation, and run the phase gate</name>
  <files>backend/launch/rehab_robotics.launch.py, backend/test/test_opensim_launch.py, docs/opensim-quaternion-live-link.md</files>
  <behavior>
    - Source/AST contracts prove exact launch defaults, conditions, parameter wiring, preservation of every unrelated launch node, and removal only of legacy OpenSim UDP settings.
    - The exact master identity and slave +90Z Imu objects returned by `known_orientations()` are delivered through bridge instances configured with master/slave topic and frame overrides into an injected fake adapter.
    - The cross-component test asserts master/slave role, mapped frame, normalized quaternion/rotation, `live` status, independent update counters/freshness, and that updating either path does not change the other's adapter call or state.
    - Always-run fake/source contracts pass without ROS or OpenSim; the installed-runtime smoke from Plan 15-01 is allowed to skip only when `opensim` is absent.
  </behavior>
  <action>Starting from the captured dirty launch-file copy, replace only legacy `opensim_udp_host`/`opensim_udp_port` arguments and filtered-topic wiring with launch arguments for `master_imu_topic`, `slave_imu_topic`, `master_frame`, `slave_frame`, `model_path`, `stale_timeout_s`, `status_topic`, `enable_opensim_bridge` (default true), and `enable_opensim_test_publisher` (default false). Pass every live-link value to `opensim_bridge` and conditionally start the deterministic publisher using the same topic arguments. Preserve all unrelated bridge, filter, recorder, status, observer, rosbridge nodes, their conditions/parameters, and inherited dirty hunks. Add source/AST contract tests that do not import ROS launch modules locally and assert locked defaults, conditions, parameter wiring, retained unrelated nodes/setup entry points, and absence of legacy OpenSim UDP settings.

Add `OpenSimPublisherBridgeIntegrationTests` as a hardware-free cross-component contract, using the same ROS stand-ins as the node tests. Instantiate the bridge/controller twice or route both configured callback paths on one injected node, feed the exact Imu message objects from `known_orientations()` (master identity, slave +90Z), and assert end to end: configured role and frame reach the fake adapter, the rotation is normalized and matches the golden identity/+90Z matrix, each role becomes live with its own counter/freshness, visualization status remains that of the fake, and either role updates independently. Do not replace this assertion with separate publisher-field and bridge-callback tests; LINK-06 requires the producer output to be the callback input.

Write `docs/opensim-quaternion-live-link.md` with build/launch commands, model-path override, frame/topic overrides, hardware-free test-publisher command, `/opensim/status` inspection, expected identity/+90Z result, the ROS `(x,y,z,w)` to normalized scalar-first/OpenSim active-rotation convention, rejection/staleness behavior, optional-runtime fallback, and the explicit D-07 boundary that triads are sensor orientations rather than solved model pose or joint angles. Run the always-run fake/source/cross-component Phase 15 suite and all unittest-discoverable backend contracts, then run the installed-runtime smoke separately so an absent OpenSim is visibly skipped rather than conflated with contract success. Never run reset/checkout/restore/clean, stage explicit paths only, and compare post-task launch/setup diffs to the captured baseline; record inherited dirty hunks separately from Phase 15 changes.</action>
  <verify>
    <automated>$env:PYTHONPATH='backend'; python -m unittest backend.test.test_opensim_adapter.QuaternionConversionTests backend.test.test_opensim_adapter.OpenSimAdapterContractTests backend.test.test_opensim_node backend.test.test_opensim_launch.OpenSimTestPublisherContractTests backend.test.test_opensim_launch.OpenSimPublisherBridgeIntegrationTests backend.test.test_opensim_launch.OpenSimLaunchContractTests -v; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; python -m unittest discover -s backend/test -p 'test_*.py' -v; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; python -m unittest backend.test.test_opensim_adapter.OpenSimInstalledRuntimeSmokeTests -v</automated>
  </verify>
  <done>Launch exposes and wires all prototype parameters; the exact known publisher messages traverse both configured bridge paths into the fake adapter with correct mapping/rotation/status and independent updates; docs explain the convention/status/non-IK meaning; legacy UDP settings alone are removed; unrelated launch nodes, setup entries, and inherited dirty hunks are preserved; always-run contracts pass and the real-runtime smoke passes or skips only because OpenSim is absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Launch CLI to node parameters | Operator-provided topics, frames, paths, and timeouts configure runtime behavior. |
| Test publisher to ROS graph | Synthetic messages are indistinguishable from live topic data unless clearly opt-in. |
| Documentation to operator | Ambiguous claims could misrepresent sensor triads as biomechanical pose results. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-08 | Spoofing | Deterministic publisher | mitigate | Keep it default-off behind `enable_opensim_test_publisher` and use explicit test frame IDs/documentation. |
| T-15-09 | Tampering | Launch configuration | mitigate | Wire named parameters directly, test exact defaults, and let adapter/node validation surface invalid model/frame/timeout values. |
| T-15-10 | Repudiation | Demonstration semantics | mitigate | Document that visible triads are raw mapped sensor orientations, not IK, calibration, joint angles, or clinical output. |
| T-15-SC | Tampering | Package supply chain | accept | No package installation is introduced; existing ROS message dependencies are sufficient. |
</threat_model>

<source_coverage>
## Multi-Source Coverage Audit

| Source | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | - | Native ESP Imu topics through opensim_bridge into mapped OpenSim frame-orientation updates | 01-03 | COVERED | Pure boundary, ROS wiring, native visualizer adapter, and launch proof. |
| REQ | LINK-01 | Configurable master/slave Imu topics | 02-03 | COVERED | Node parameters and launch arguments. |
| REQ | LINK-02 | One documented ROS-to-OpenSim convention | 01-03 | COVERED | Golden pure conversion plus docs. |
| REQ | LINK-03 | Configurable model-frame mappings | 01-03 | COVERED | Exact frame resolution, node parameters, and launch defaults. |
| REQ | LINK-04 | Valid input updates native OpenSim visualizer demonstration | 01-03 | COVERED | Model-owned adapter and independent node forwarding. |
| REQ | LINK-05 | Visible runtime/model/input/mapping/stale failures | 01-03 | COVERED | Unavailable adapter plus logs and versioned JSON status. |
| REQ | LINK-06 | Hardware-free deterministic proof | 01-03 | COVERED | Fake adapter tests, ROS-stubbed node tests, and opt-in publisher. |
| RESEARCH | prototype constraints | Explicit quaternion boundary, fake seam, optional runtime, observable stale/error behavior | 01-03 | COVERED | Current CONTEXT narrows and overrides the research IK/C++/Jetson recommendations. |
| CONTEXT | D-01 | Two native Imu subscriptions | 02-03 | COVERED | No filtered JSON input. |
| CONTEXT | D-02 | Independent newest orientation | 02-03 | COVERED | No timestamp pairing. |
| CONTEXT | D-03 | Configurable mappings with locked defaults | 01-03 | COVERED | femur/tibia defaults. |
| CONTEXT | D-04 | Update available sensor; report other waiting/stale | 02 | COVERED | Independent state and callbacks. |
| CONTEXT | D-05 | Bridge owns model/native visualizer | 01-02 | COVERED | Adapter instantiated once by node. |
| CONTEXT | D-06 | Labeled mapped coordinate triads | 01 | COVERED | Decorative sensor frames. |
| CONTEXT | D-07 | No solved pose implication | 01-03 | COVERED | No model coordinates/IK; explicit docs. |
| CONTEXT | D-08 | Configurable model path and clear load error | 01-03 | COVERED | Adapter status and launch/docs. |
| CONTEXT | D-09 | Non-visual fallback without runtime | 01-03 | COVERED | Lazy optional import and status. |
| CONTEXT | D-10 | JSON status plus logs | 02-03 | COVERED | Versioned compact schema. |
| CONTEXT | D-11 | Reject invalid, normalize valid | 01-02 | COVERED | Pure boundary used by callbacks. |
| CONTEXT | D-12 | Independent configurable staleness | 02-03 | COVERED | Monotonic state timer and launch arg. |
| CONTEXT | D-13 | Adapter/fake seam | 01-02 | COVERED | Protocol and injected fake. |
| CONTEXT | D-14 | Required deterministic cases | 01-03 | COVERED | Golden rotation, configuration, invalid, stale, unavailable, publisher, and launch tests. |
</source_coverage>

<verification>
Run the always-run fake/source/cross-component command, then backend discovery, then `OpenSimInstalledRuntimeSmokeTests` separately. Inspect the launch/setup preservation assertions and compare their final diffs with the captured pre-task baselines; only explicit Phase 15 paths may be staged, and inherited dirty hunks must remain distinguishable in the SUMMARY.
</verification>

<success_criteria>
- Standard ROS launch controls topics, mappings, model path, stale timeout, status topic, and optional test publisher.
- Known native Imu messages can be generated locally without ESP hardware.
- Exact `known_orientations()` messages cross the bridge callbacks into the fake adapter with proven role/frame/rotation/status independence.
- Source-contract tests pass without ROS launch or OpenSim installed.
- Unrelated dirty launch/setup content is preserved through baseline diff capture, surgical edits, explicit-path staging, and post-edit comparison.
- Operator docs make successful, unavailable, invalid, mapping-error, and stale states inspectable.
- All LINK requirements and D-01 through D-14 are covered with no IK, calibration, joint state, Jetson packaging, Studio 3D, or desktop-GUI automation.
</success_criteria>

<output>
Create `.planning/phases/15-opensim-quaternion-live-link/15-03-SUMMARY.md` when done.
</output>
