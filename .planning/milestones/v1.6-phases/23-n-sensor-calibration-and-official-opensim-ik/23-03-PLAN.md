---
phase: 23-n-sensor-calibration-and-official-opensim-ik
plan: 03
type: tdd
wave: 2
depends_on:
  - 23-01
files_modified:
  - backend/rehab_robotics_bridge/opensim_node.py
  - backend/test/test_opensim_node.py
autonomous: true
requirements:
  - IK-03
must_haves:
  truths:
    - "IK solve is suppressed when any device timestamp deviates from median by more than sync_skew_ms"
    - "IK solve is suppressed when any _DeviceInput.post_reconnect_fresh is False"
    - "Acquisition and recording continue unaffected when IK is suppressed"
    - "/rehab/opensim/input_validity is published every IK cycle with per-device validity flags"
    - "sync_skew_ms ROS parameter defaults to 50 and is read at node startup"
  artifacts:
    - path: backend/rehab_robotics_bridge/opensim_node.py
      provides: "_check_sync_skew, _publish_input_validity, sync_skew_ms param wiring, IK gate in _solve_and_publish_ik_n"
      contains: "_check_sync_skew"
  key_links:
    - from: "opensim_node._solve_and_publish_ik_n"
      to: "_check_sync_skew"
      via: "all_valid gate before calling IK solver"
    - from: "_check_sync_skew"
      to: "_DeviceInput.last_ts_ns and post_reconnect_fresh"
      via: "median timestamp computation + per-device skew check"
    - from: "_solve_and_publish_ik_n"
      to: "/rehab/opensim/input_validity publisher"
      via: "_publish_input_validity called on every cycle regardless of gate result"
---

<objective>
Implement the sync-skew gate and reconnect-freshness guard for N-sensor IK, and wire both
into _solve_and_publish_ik_n(). Publish per-device validity on every cycle.

Purpose: IK-03 — joint states must publish only when every required input is valid, fresh,
post-reconnect, and within the synchronization-skew bound. Degraded inputs suppress new
IK output while acquisition and recording continue unaffected.

Output:
- sync_skew_ms ROS parameter (default 50) declared in OpenSimBridgeNode
- _check_sync_skew(inputs) -> dict[str, dict] with per-device {valid, fresh, skew_ms, all_valid}
- _publish_input_validity() called every IK cycle via /rehab/opensim/input_validity (String)
- IK gate in _solve_and_publish_ik_n: suppress solve when not all_valid
- IK-03 contract tests (offline, no hardware)
</objective>

<execution_context>
@C:/Users/justi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/justi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/23-n-sensor-calibration-and-official-opensim-ik/23-CONTEXT.md
@.planning/phases/23-n-sensor-calibration-and-official-opensim-ik/23-RESEARCH.md
@.planning/phases/23-n-sensor-calibration-and-official-opensim-ik/23-01-SUMMARY.md

<interfaces>
<!-- _DeviceInput from Plan 23-01 (already present after Plan 01 execution): -->
```python
@dataclass
class _DeviceInput:
    device_id: str
    frame: str
    subscription_handle: object
    last_xyzw: tuple[float,float,float,float] | None = None
    last_ts_ns: int | None = None
    post_reconnect_fresh: bool = False
    last_seen_monotonic: float | None = None
```

Sync-skew check algorithm (D-07):
  reference_ts = median(entry.last_ts_ns for entry if entry.last_ts_ns is not None)
  For each entry:
    if entry.last_ts_ns is None: valid=False, skew_ms=None
    else: skew_ms = abs(entry.last_ts_ns - reference_ts) / 1e6
          valid = skew_ms <= sync_skew_ms AND entry.post_reconnect_fresh

Per D-08: if any device not valid -> suppress IK for that cycle.
Acquisition (IMU callbacks, recording) is NOT gated. The gate only applies to IK solve.

/rehab/opensim/input_validity payload:
{
  "schema": "rehab.n_input_validity.1",
  "all_valid": true | false,
  "device_validities": {
    "esp32:aabbccddeeff": {"valid": true, "fresh": true, "skew_ms": 2.3},
    "esp32:112233445566": {"valid": false, "fresh": false, "skew_ms": 75.1}
  }
}

_solve_and_publish_ik_n() stub (to be created; full wiring in Plan 23-04):
  - snapshot _mac_inputs under lock
  - build inputs list sorted alphabetically by device_id
  - call _check_sync_skew(inputs_snapshot)
  - call _publish_input_validity(validity_dict)
  - if not all_valid: return (suppress IK, continue)
  - ... (Plan 23-04 adds the actual solver call)
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: sync_skew_ms param and _check_sync_skew implementation</name>
  <files>backend/rehab_robotics_bridge/opensim_node.py</files>
  <behavior>
    - sync_skew_ms declared via declare_parameter("sync_skew_ms", 50) in __init__
    - _check_sync_skew([]) returns {"all_valid": False, "device_validities": {}}
    - _check_sync_skew with 2 inputs both at same timestamp: both valid=True, skew_ms=0.0, all_valid=True (assumes fresh=True)
    - _check_sync_skew with inputs where one timestamp is 100ms behind median: that device valid=False, all_valid=False
    - _check_sync_skew with fresh=False on any device: that device valid=False, all_valid=False (even if skew is within bound)
    - _check_sync_skew with last_ts_ns=None on any device: that device valid=False, skew_ms=None, all_valid=False
    - _check_sync_skew returns device_validities keyed by device_id with {valid, fresh, skew_ms} per entry
  </behavior>
  <action>
    Add to OpenSimBridgeNode.__init__ parameter_defaults dict:
      "sync_skew_ms": 50
    Read after declare_parameter loop:
      try: self._sync_skew_ms = max(1, int(values["sync_skew_ms"]))
      except (TypeError, ValueError): self._sync_skew_ms = 50

    Implement _check_sync_skew(self, inputs: list[_DeviceInput]) -> dict:
      Returns dict with keys "all_valid" (bool) and "device_validities" (dict keyed by device_id).
      Each device_validity: {"valid": bool, "fresh": bool, "skew_ms": float | None}

      Algorithm:
        if not inputs: return {"all_valid": False, "device_validities": {}}

        timestamps = [entry.last_ts_ns for entry in inputs if entry.last_ts_ns is not None]
        if not timestamps:
          # No timestamps at all — all invalid
          return {
            "all_valid": False,
            "device_validities": {
              entry.device_id: {"valid": False, "fresh": entry.post_reconnect_fresh, "skew_ms": None}
              for entry in inputs
            }
          }

        # Compute median
        sorted_ts = sorted(timestamps)
        n = len(sorted_ts)
        if n % 2 == 0:
          reference_ts = (sorted_ts[n // 2 - 1] + sorted_ts[n // 2]) / 2.0
        else:
          reference_ts = float(sorted_ts[n // 2])

        device_validities = {}
        for entry in inputs:
          fresh = entry.post_reconnect_fresh
          if entry.last_ts_ns is None:
            device_validities[entry.device_id] = {"valid": False, "fresh": fresh, "skew_ms": None}
          else:
            skew_ms = abs(entry.last_ts_ns - reference_ts) / 1_000_000.0
            within_skew = skew_ms <= self._sync_skew_ms
            valid = within_skew and fresh
            device_validities[entry.device_id] = {"valid": valid, "fresh": fresh, "skew_ms": round(skew_ms, 3)}

        all_valid = all(v["valid"] for v in device_validities.values())
        return {"all_valid": all_valid, "device_validities": device_validities}

    Inputs parameter is a list[_DeviceInput] (a snapshot already taken outside the lock).
    This method is pure computation — takes no locks itself.
  </action>
  <verify>
    <automated>cd backend && python -m pytest test/test_opensim_node.py -x -q -k "IkThree" 2>&1 | tail -20</automated>
  </verify>
  <done>
    sync_skew_ms parameter defaults to 50. _check_sync_skew correctly identifies skew violations,
    freshness failures, and missing timestamps. Returns all_valid=True only when all devices pass
    both the skew bound and post_reconnect_fresh checks.
  </done>
</task>

<task type="tdd">
  <name>Task 2: _publish_input_validity and _solve_and_publish_ik_n stub with gate</name>
  <files>backend/rehab_robotics_bridge/opensim_node.py</files>
  <behavior>
    - _n_input_validity_publisher created for "/rehab/opensim/input_validity" (String, depth=10)
    - _publish_input_validity(validity: dict) serialises and publishes JSON with schema "rehab.n_input_validity.1"
    - _solve_and_publish_ik_n() snapshots _mac_inputs under lock, builds alphabetically-sorted DeviceInput list
    - _solve_and_publish_ik_n() calls _check_sync_skew then _publish_input_validity on every call
    - When all_valid=False: returns without calling IK solver (no JointState published on N-sensor path)
    - When all_valid=True but _n_calib_artifact is None: still suppresses IK (calibration required)
    - _solve_and_publish_ik_n() is called from _on_mac_imu after updating entry (replaces/supplements _solve_and_publish_ik)
  </behavior>
  <action>
    Add to OpenSimBridgeNode.__init__:
      self._n_input_validity_publisher = self.create_publisher(
          String, "/rehab/opensim/input_validity", 10
      )

    Implement _publish_input_validity(self, validity: dict) -> None:
      payload = {
        "schema": "rehab.n_input_validity.1",
        "all_valid": bool(validity.get("all_valid", False)),
        "device_validities": validity.get("device_validities", {}),
      }
      message = String()
      message.data = json.dumps(payload, sort_keys=True, separators=(",", ":"))
      self._n_input_validity_publisher.publish(message)

    Implement _solve_and_publish_ik_n(self) -> None:
      # Snapshot inputs under lock (alphabetical sort per D-03)
      with self._input_lock:
        inputs_snapshot = sorted(
            self._mac_inputs.values(),
            key=lambda e: e.device_id,
        )

      if not inputs_snapshot:
        return  # No mapping applied; nothing to do

      validity = self._check_sync_skew(inputs_snapshot)
      self._publish_input_validity(validity)

      if not validity["all_valid"]:
        return  # Suppressed — acquisition continues unaffected

      if self._n_calib_artifact is None:
        return  # Calibration required before IK

      # Solver call wired in Plan 23-04 (_solve_and_publish_ik_n_solve)
      # Stub: log at debug level that IK gate passed but solver not yet wired
      # Do NOT call the old single-sensor _solve_and_publish_ik() from here.

    Call _solve_and_publish_ik_n() from _on_mac_imu, after the existing lock-update block,
    OUTSIDE the lock (lock is released before this call).

    The existing _solve_and_publish_ik() (single-sensor master/slave path) is NOT removed —
    it continues to be called from _on_imu() as before. Both paths coexist.

    Also: at end of _on_mac_imu, after updating entry, release lock and then call
    _solve_and_publish_ik_n() to trigger N-sensor IK evaluation on each new frame.
  </action>
  <verify>
    <automated>cd backend && python -m pytest test/test_opensim_node.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    /rehab/opensim/input_validity publisher exists. _publish_input_validity emits correct schema.
    _solve_and_publish_ik_n returns without calling solver when all_valid=False or calib absent.
    Called from _on_mac_imu. Existing single-sensor path unaffected. All tests pass.
  </done>
</task>

<task type="tdd">
  <name>Task 3: IK-03 contract tests</name>
  <files>backend/test/test_opensim_node.py</files>
  <behavior>
    - IK-03-A: _check_sync_skew with 2 devices at same timestamp, both fresh: all_valid=True
    - IK-03-B: _check_sync_skew with one device 100ms behind median: all_valid=False, that device valid=False
    - IK-03-C: _check_sync_skew with fresh=False on one device: all_valid=False, that device valid=False
    - IK-03-D: _check_sync_skew with last_ts_ns=None on any device: all_valid=False, skew_ms=None
    - IK-03-E: _solve_and_publish_ik_n() with skewed input does NOT publish on /opensim/joint_states (N-sensor path); DOES publish on /rehab/opensim/input_validity
    - IK-03-F: _solve_and_publish_ik_n() with all valid inputs and valid calib does pass the gate (stub behavior; no solver crash)
    - IK-03-G: sync_skew_ms parameter overridden to 200 allows 100ms skew to pass
    - IK-03-H: Existing single-sensor _solve_and_publish_ik() remains callable and unaffected by _check_sync_skew
  </behavior>
  <action>
    Add IkThreeContractTests(unittest.TestCase) to test_opensim_node.py.

    Test helpers:
    - _make_device_input(device_id, frame, last_ts_ns, post_reconnect_fresh, last_xyzw=(0,0,0,1)):
        Create a _DeviceInput instance directly (import from opensim_node after stubs installed).
    - _make_mapping_msg(assigned_list): build a _String with .data = JSON {"assigned": [...], "applied_revision": 1}

    For IK-03-A through IK-03-D: construct a bare OpenSimBridgeNode (with FakeOrientationIkSolver
    injected), then call node._check_sync_skew(inputs_list) directly and assert result fields.
    _DeviceInput objects constructed directly — no need to call _on_mapping_current for pure unit tests.

    For IK-03-E: build node, call _on_mapping_current with 2 devices, then call _on_mac_imu for each
    device with different timestamps (100ms apart). Assert:
      - _n_input_validity_publisher has at least one message
      - Most recent validity message all_valid == False
      - _joint_states_publisher has no messages (or no new messages since node init)

    For IK-03-F: all devices fresh=True, same timestamp, _n_calib_artifact set on node directly
    (or via capture service if practical). Call _solve_and_publish_ik_n(). No exception raised.

    For IK-03-G: override parameter sync_skew_ms=200 via _StubNode.parameter_overrides before
    constructing node. Then skew of 100ms should give all_valid=True.

    For IK-03-H: call node._solve_and_publish_ik() directly (old path); verify it still works
    with existing _sensor_states setup.
  </action>
  <verify>
    <automated>cd backend && python -m pytest test/test_opensim_node.py -x -q -k "IkThree" 2>&1 | tail -20</automated>
  </verify>
  <done>
    All IK-03-A through IK-03-H tests pass. _check_sync_skew correctness verified for all edge
    cases. Gate suppression verified (no joint states published). Param override works. No
    regressions in existing tests.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IMU timestamps from /esp/raw/mac_* | ROS Imu header.stamp from ESP32 devices; could be wrong/stale/spoofed |
| sync_skew_ms parameter | Set at launch by operator; treated as trusted configuration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03-01 | Tampering | Stale device timestamp bypasses skew check | mitigate | Skew check uses relative median — any device deviating from median by >skew_ms is flagged; stale device has large deviation |
| T-23-03-02 | Denial of Service | All devices frozen at same timestamp, both stale | accept | post_reconnect_fresh=False guards this case independently; both guards must pass |
| T-23-03-03 | Denial of Service | Single device with bad clock causes perpetual IK suppression | accept | Operator can identify via /rehab/opensim/input_validity per-device report; hardware issue |
| T-23-03-04 | Information Disclosure | /rehab/opensim/input_validity exposes device_ids and timing | accept | Internal ROS bus; no external exposure; device_ids already published on fleet registry |
| T-23-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages; pure stdlib |
</threat_model>

<verification>
- python -m pytest backend/test/test_opensim_node.py -x -q (all IK-03 + existing tests green)
- python -m pytest backend/test/ -x -q (full suite no regressions)
- Manual check: node with sync_skew_ms=50 and two devices with 100ms timestamp difference
  should publish all_valid=False on /rehab/opensim/input_validity
</verification>

<success_criteria>
- sync_skew_ms parameter declared, defaults to 50, configurable at launch
- _check_sync_skew uses median-reference computation per D-07
- post_reconnect_fresh=False suppresses IK independently of skew per D-09
- _publish_input_validity publishes on every IK evaluation cycle (even when suppressed)
- _solve_and_publish_ik_n() gate returns before solver call when all_valid=False
- Acquisition path (IMU callbacks, recording) unaffected by gate
- IK-03-A through IK-03-H pass offline
</success_criteria>

<output>
Create .planning/phases/23-n-sensor-calibration-and-official-opensim-ik/23-03-SUMMARY.md when done
</output>
