---
phase: 25-multi-device-compatibility-and-promotion-gate
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/test/test_compat_legacy.py
autonomous: true
requirements:
  - COMP-01

must_haves:
  truths:
    - "Legacy two-sensor startup (use_fleet_bridge=false) exercises OpenSimBridgeNode with fixed master/slave topics and no mapping node"
    - "Calibration service trigger, joint-state publication, and stale-input suppression remain functional under legacy constructor args"
    - "Pair-health and recording start/stop contracts hold with the fixed esp32_bridge_node path (no fleet_bridge, no mapping_node)"
    - "The full backend test suite (python -m pytest backend/test/) exits 0 before Phase 25 is considered done"
  artifacts:
    - path: backend/test/test_compat_legacy.py
      provides: "Offline unit tests for legacy two-sensor OpenSim workflow (COMP-01)"
      min_lines: 80
  key_links:
    - from: backend/test/test_compat_legacy.py
      to: backend/rehab_robotics_bridge/opensim_node.py
      via: "_install_ros_stubs() + importlib load, constructor with master_imu_topic/slave_imu_topic"
      pattern: "_StubNode.*parameter_overrides"
---

<objective>
Create `backend/test/test_compat_legacy.py` — offline deterministic tests that prove the existing two-sensor (legacy) workflow contracts are unbroken after Phases 20-24.

Purpose: COMP-01 requires that startup, calibration, joint-state publication, stale suppression, and recording interlock all continue to work when `use_fleet_bridge=false` (no fleet_bridge_node, no mapping_node, fixed `/esp32/master/imu` and `/esp32/slave/imu` topics).

Output: A passing test file that can be run with `python -m pytest backend/test/test_compat_legacy.py -v` or `python -m unittest backend.test.test_compat_legacy`.
</objective>

<execution_context>
@C:\Users\justi\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\justi\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-multi-device-compatibility-and-promotion-gate/25-CONTEXT.md

<interfaces>
<!-- Replicate _install_ros_stubs() from test_opensim_node.py exactly.
     Key stubs needed: rclpy, rclpy.node.Node = _StubNode, rclpy.qos, sensor_msgs.msg.Imu,
     sensor_msgs.msg.JointState, std_msgs.msg.String, std_msgs.msg.Float64, std_srvs.srv.Trigger,
     rehab_robotics_interfaces.srv (IdentifyDevice, ApplyMapping, GetMappingState, ResetMapping, SetAssignment).
     After stubs are installed, load opensim_node with importlib so OpenSimBridgeNode inherits _StubNode. -->

<!-- OpenSimBridgeNode legacy constructor parameters (from launch file analysis in CONTEXT.md D-02):
     master_imu_topic = '/esp32/master/imu'   (default in launch file)
     slave_imu_topic  = '/esp32/slave/imu'    (default in launch file)
     master_frame     = 'femur_r_imu'
     slave_frame      = 'tibia_r_imu'
     stale_timeout_s  = 1.0
     model_path       = '' (empty — no dynamic mapping)
     No mapping_node, no model_catalog_node in constructor. -->

<!-- _DeviceInput / _mac_inputs: the legacy OpenSimBridgeNode (when given master_imu_topic +
     slave_imu_topic without a mapping_current subscription delivering assigned_list) operates
     through the two fixed subscriptions. The legacy path does NOT call _on_mapping_current
     with real data; _mac_inputs may remain empty and the node falls back to the dual-IMU path.
     Tests must exercise _on_master_imu/_on_slave_imu (or equivalent) directly. -->

<!-- Calibration service: self.create_service(Trigger, CALIBRATION_CAPTURE_SERVICE, self._on_calibrate)
     Call node._on_calibrate(_TriggerRequest(), _TriggerResponse()) directly in tests. -->

<!-- Stale gate: set node._last_master_ts or node._last_slave_ts to None or an old time.
     Then call node._solve_and_publish() (legacy two-sensor solve path). Confirm no JointState
     is published on the joint_states publisher when stale_timeout_s is exceeded. -->

<!-- Recording interlock: MappingNode is separate; OpenSimBridgeNode legacy does not block on
     recording. The interlocks tested in test_compat_legacy.py are at the MappingStore level
     (ensure apply_candidate blocks when recording_active=True), reusing the pattern from
     test_mapping_node.py MappingNodeApplyTest.test_apply_blocked_recording. -->

From backend/test/test_opensim_node.py (line 159-246):
_install_ros_stubs() pattern — stubs rclpy, sensor_msgs, std_msgs, std_srvs, rehab_robotics_interfaces
before calling: from rehab_robotics_bridge import opensim_node

From backend/rehab_robotics_bridge/opensim_node.py:
- OpenSimBridgeNode.__init__ accepts: master_imu_topic, slave_imu_topic, master_frame, slave_frame, model_path, stale_timeout_s
- self._mac_inputs: dict[str, _DeviceInput]  — empty for legacy mode
- _on_master_imu(msg: Imu), _on_slave_imu(msg: Imu) — two-sensor IMU callbacks
- _solve_and_publish_ik_n() — N-sensor solve (skipped when _mac_inputs empty)
- CALIBRATION_CAPTURE_SERVICE = '/rehab/calibration/capture'
- JOINT_STATES_TOPIC = '/rehab/opensim/joint_states'
- stale_timeout_s default = 1.0
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Write failing legacy compatibility tests (RED)</name>
  <files>backend/test/test_compat_legacy.py</files>

  <read_first>
    backend/test/test_opensim_node.py (lines 1-266) — _install_ros_stubs(), _StubNode, _Imu, _StubPublisher, _Logger, _TriggerRequest, _TriggerResponse, _Trigger class definitions and the importlib load pattern
    backend/test/test_mapping_node.py (lines 1-210) — _make_node(), _StubNode.parameter_overrides pattern, _install_ros_stubs with sys.modules assignment
    backend/rehab_robotics_bridge/opensim_node.py (lines 1-80) — import surface, class signature
    backend/launch/rehab_robotics.launch.py (lines 43-91) — default values for master_imu_topic, slave_imu_topic, master_frame, slave_frame, stale_timeout_s
  </read_first>

  <behavior>
    - LegacyStartupTest.test_node_creates_master_slave_subscriptions: after constructing OpenSimBridgeNode with master_imu_topic='/esp32/master/imu' and slave_imu_topic='/esp32/slave/imu', assert that both topic strings appear among the node's subscriptions
    - LegacyStartupTest.test_mac_inputs_empty_in_legacy_mode: assert len(node._mac_inputs) == 0 immediately after construction (no mapping has been applied)
    - LegacyJointStateTest.test_valid_imu_pair_publishes_joint_state: inject a valid master Imu + valid slave Imu (orientation w=1), call the respective callbacks, assert the JointState publisher has at least one message (legacy solve path active)
    - LegacyJointStateTest.test_stale_master_suppresses_joint_state: set node._last_master_ts to (current_time - 2.0) seconds (beyond stale_timeout_s=1.0), inject a valid slave Imu, call solve; assert no new JointState published
    - LegacyCalibrationTest.test_calibrate_trigger_succeeds_with_valid_imu: inject a valid master and slave Imu, then call the calibration capture service callback (_on_calibrate); assert response.success is True or response.message contains 'artifact' (or 'ok') — OR if calibration requires N-sensor mapping, assert response.success is False with a message containing 'no_mapping' (either outcome is acceptable for legacy mode; what must NOT happen is an unhandled exception)
    - LegacyRecordingInterlockTest.test_apply_blocked_during_recording: instantiate MappingNode with a temp store, set _recording_active=True, call _on_apply_mapping with valid expected_revision=0, assert result.outcome == 'blocked' and result.detail contains 'recording'
    - LegacyAliasTopicTest.test_master_alias_topic_is_slash_esp32_master_imu: read the node's subscriptions and assert '/esp32/master/imu' is present
    - LegacyAliasTopicTest.test_slave_alias_topic_is_slash_esp32_slave_imu: read the node's subscriptions and assert '/esp32/slave/imu' is present
  </behavior>

  <action>
    Create `backend/test/test_compat_legacy.py` with:

    1. Header docstring: "Offline compatibility tests for the legacy two-sensor workflow (COMP-01). Proves use_fleet_bridge=false contracts remain intact after Phases 20-24. Run: python -m pytest backend/test/test_compat_legacy.py -v"

    2. Copy-verbatim (do NOT rewrite) the `_install_ros_stubs()` function from `backend/test/test_opensim_node.py` — include all sub-stubs: _Parameter, _Publisher, _Logger, _StubNode, _Imu, _String, _Float64, _JointState, _TriggerRequest, _TriggerResponse, _Trigger. The function must set rclpy.node.Node = _StubNode before the module under test is exec'd.

    3. Call `_install_ros_stubs()` at module level, then import opensim_node and MappingNode exactly as in the existing test files:
       ```
       from rehab_robotics_bridge import opensim_node
       from rehab_robotics_bridge.mapping_node import MappingNode
       ```
       (MappingNode import uses the mapping_node stub install pattern from test_mapping_node.py.)

    4. Helper `_make_opensim_node()` — sets _StubNode.parameter_overrides with legacy args:
       - master_imu_topic='/esp32/master/imu', slave_imu_topic='/esp32/slave/imu'
       - master_frame='femur_r_imu', slave_frame='tibia_r_imu'
       - stale_timeout_s=1.0, model_path=''
       Constructs and returns opensim_node.OpenSimBridgeNode().

    5. Helper `_make_mapping_node(tmp_dir)` — mirrors test_mapping_node.py _make_node() exactly.

    6. Helper `_imu(*, w=1.0, stamp_sec=None)` — creates a stub _Imu with given orientation.

    7. Test classes with the behaviors listed above (at least 7 test methods across 4 classes: LegacyStartupTest, LegacyJointStateTest, LegacyCalibrationTest, LegacyRecordingInterlockTest, LegacyAliasTopicTest). Organize with `unittest.TestCase`. setUp creates node (and tempfile where needed); tearDown calls tmp.cleanup() where applicable.

    8. `if __name__ == '__main__': unittest.main()` at the bottom.

    Do NOT duplicate CROSS_LAYER_* constants — do not import or reference them in this file (they are only needed for test_acceptance.py per D-06).

    Do NOT call `rclpy.init()` or `rclpy.spin()` in any test method.

    The file must import cleanly via `python -m pytest backend/test/test_compat_legacy.py --collect-only` before any test logic is exercised (RED = tests collected but failing because code does not yet satisfy them, not failing at import).
  </action>

  <verify>
    <automated>cd "C:\Users\justi\Documents\# Rehab Robotics GUI Prototype__Here's a complete, single-file React + TypeScript app implementing the LabVIEW" && python -m pytest backend/test/test_compat_legacy.py --collect-only -q 2>&1 | tail -10</automated>
  </verify>

  <done>
    File exists at backend/test/test_compat_legacy.py. `--collect-only` lists at least 7 test items with no import errors. Tests may FAIL at this point (RED is expected for TDD).
  </done>
</task>

<task type="tdd">
  <name>Task 2: Make legacy tests pass (GREEN)</name>
  <files>backend/test/test_compat_legacy.py</files>

  <read_first>
    backend/rehab_robotics_bridge/opensim_node.py (full file via grep for _last_master_ts, _last_slave_ts, _on_master_imu, _on_slave_imu, _solve_and_publish, _dual_imu_solve) — locate the attribute names that drive the legacy stale gate and the legacy solve path
    backend/test/test_compat_legacy.py — the file you just created (all failing tests)
  </read_first>

  <behavior>
    All 7+ test methods in test_compat_legacy.py pass. No production code is changed.
  </behavior>

  <action>
    Run the test file to see which tests fail and what the actual attribute/method names are:
      python -m pytest backend/test/test_compat_legacy.py -v 2>&1

    For each failure, fix the test's helper calls, attribute names, or assertions to match what OpenSimBridgeNode and MappingNode actually expose. Common corrections needed:

    - Stale gate attribute names: grep opensim_node.py for `_last_master` or `_last_slave` or `_stale` to find the exact field. Update the stale-suppression test to set the correct attribute to a time value that is 2× stale_timeout_s in the past.
    - Solve trigger: find the method that drives the two-sensor solve (grep for `_solve`, `_dual`, `_publish_joint`). Call that method explicitly in tests if callbacks do not auto-trigger a solve.
    - Calibration response: if the capture service returns `success=False` with `'no_mapping'`, update the assertion to `assertIn('no_mapping', response.message)` — both outcomes are documented as acceptable in CONTEXT.md specifics.
    - Subscription topic assertions: assert against the `.topic` attribute of each subscription namespace object in `node.subscriptions`.

    Do NOT change any production file. Only fix the test file to match actual production behavior.

    After all fixes, run:
      python -m pytest backend/test/test_compat_legacy.py -v
    All tests must exit GREEN.

    Then run the full suite to confirm no regressions:
      python -m pytest backend/test/ -v 2>&1 | tail -30
  </action>

  <verify>
    <automated>cd "C:\Users\justi\Documents\# Rehab Robotics GUI Prototype__Here's a complete, single-file React + TypeScript app implementing the LabVIEW" && python -m pytest backend/test/test_compat_legacy.py -v 2>&1 | tail -20</automated>
  </verify>

  <done>
    All test methods in test_compat_legacy.py show PASSED. The full `python -m pytest backend/test/` run exits 0 (or lists only pre-existing failures that are documented in the acceptance report per D-03).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test file → production module | Test stubs replace ROS; production code is exec'd in process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01-01 | Tampering | _install_ros_stubs() | accept | Stubs are test-only; production runtime uses real rclpy |
| T-25-01-02 | Information Disclosure | test_compat_legacy.py reading live filesystem | accept | Only reads firmware source for topology tests; no PII |
| T-25-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed; stdlib + existing test deps only |
</threat_model>

<verification>
- `python -m pytest backend/test/test_compat_legacy.py -v` exits 0 with all tests PASSED
- `python -m pytest backend/test/ -v 2>&1 | grep -E "FAILED|ERROR"` returns 0 lines (or only pre-existing failures documented in acceptance report)
- `grep -c "class Legacy" backend/test/test_compat_legacy.py` returns >= 3
- `grep -c "def test_" backend/test/test_compat_legacy.py` returns >= 7
</verification>

<success_criteria>
backend/test/test_compat_legacy.py exists, passes cleanly in isolation, and the full backend test suite exits 0. The legacy two-sensor workflow is proven regression-free by at least 7 offline deterministic test methods covering startup, joint-state publication, stale suppression, calibration, recording interlock, and alias topic names.
</success_criteria>

<output>
Create `.planning/phases/25-multi-device-compatibility-and-promotion-gate/25-01-SUMMARY.md` when done.
</output>
