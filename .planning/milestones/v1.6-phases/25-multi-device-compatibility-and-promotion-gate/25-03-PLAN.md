---
phase: 25-multi-device-compatibility-and-promotion-gate
plan: 03
type: execute
wave: 2
depends_on:
  - 25-01
  - 25-02
files_modified:
  - docs/hardware-acceptance-report.md
  - scripts/acceptance_gate.py
  - backend/launch/rehab_robotics.launch.py
autonomous: true
requirements:
  - COMP-03

must_haves:
  truths:
    - "docs/hardware-acceptance-report.md exists with all 8 required evidence sections, each containing STATUS: PENDING and a structured template the operator can fill in"
    - "scripts/acceptance_gate.py reads the report, exits 1 when any section is STATUS: PENDING or FAIL, exits 0 only when all sections are STATUS: PASS, and prints which sections are open"
    - "Running acceptance_gate.py on the as-shipped template exits 1 (gate is OPEN) and the launch file use_fleet_bridge default remains 'false'"
    - "If acceptance_gate.py had returned 0, the use_fleet_bridge default would be flipped to 'true' — the conditional flip logic is implemented in the plan task"
  artifacts:
    - path: docs/hardware-acceptance-report.md
      provides: "Hardware acceptance evidence template with STATUS: PENDING placeholders"
      contains: "STATUS: PENDING"
    - path: scripts/acceptance_gate.py
      provides: "Machine-checkable promotion gate script"
      exports: ["exit 0 when all PASS", "exit 1 otherwise"]
    - path: backend/launch/rehab_robotics.launch.py
      provides: "use_fleet_bridge launch arg with default determined by gate outcome"
  key_links:
    - from: scripts/acceptance_gate.py
      to: docs/hardware-acceptance-report.md
      via: "Path(__file__).parent.parent / 'docs' / 'hardware-acceptance-report.md'"
      pattern: "re.findall.*STATUS"
    - from: backend/launch/rehab_robotics.launch.py
      to: scripts/acceptance_gate.py
      via: "Gate is run in this plan task; result determines default_value for use_fleet_bridge"
      pattern: "use_fleet_bridge.*default_value"
---

<objective>
Create the hardware acceptance evidence template (`docs/hardware-acceptance-report.md`), the machine-checkable promotion gate script (`scripts/acceptance_gate.py`), and conditionally flip the `use_fleet_bridge` default in the launch file only if the gate exits 0.

Purpose: COMP-03 requires that dynamic mode becomes the default only after documented hardware evidence is gathered. The gate provides a machine-checkable binary answer. Since physical hardware evidence cannot be gathered in CI, the gate will exit 1 on this automated run, leaving `use_fleet_bridge` default as `'false'`. This is the documented correct outcome per D-11 and D-12.

Output: Three files created/modified. `acceptance_gate.py` runs successfully (exits 1, gate OPEN). ROADMAP.md Phase 25 note updated to "Gate OPEN — hardware evidence pending".
</objective>

<execution_context>
@C:\Users\justi\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\justi\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-multi-device-compatibility-and-promotion-gate/25-CONTEXT.md
@backend/launch/rehab_robotics.launch.py

<interfaces>
<!-- acceptance_gate.py pattern from CONTEXT.md D-09 + D-specifics:
     - Uses only stdlib: re, sys, pathlib.Path
     - Reads docs/hardware-acceptance-report.md
     - Finds all STATUS: (PENDING|PASS|FAIL) markers with re.findall(r'STATUS:\s*(\w+)', text)
     - Exits 0 only if all statuses == 'PASS' and len(statuses) > 0
     - Exits 1 otherwise, printing which sections are PENDING or FAIL
     - Also prints the section heading preceding each non-PASS marker for operator UX (per D-specifics) -->

<!-- hardware-acceptance-report.md sections (from CONTEXT.md D-07):
     1. Fleet configuration tested
     2. Identify safety
     3. Acquisition continuity
     4. Recording continuity
     5. Reconnect under load
     6. Radio/relay load
     7. OpenSim solve latency
     8. Compatibility aliases
     Each section must have exactly one STATUS: PENDING marker (so re.findall returns 8 values).
     Each section must have a "Tested by / Date:" field (per D-specifics).
     use_fleet_bridge=false (rollback mode) must be documented in the Compatibility Aliases section (per D-01). -->

<!-- Launch file current state:
     DeclareLaunchArgument('use_fleet_bridge', default_value='false', ...)
     Target state if gate exits 0: default_value='true'
     Target state if gate exits 1: default_value='false' (unchanged)
     The PLAN task reads the gate exit code and conditionally patches the file. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create hardware acceptance report template and gate script</name>
  <files>docs/hardware-acceptance-report.md, scripts/acceptance_gate.py</files>

  <read_first>
    .planning/phases/25-multi-device-compatibility-and-promotion-gate/25-CONTEXT.md (D-07, D-08, D-09, D-specifics section) — exact section list, STATUS marker format, operator UX requirements for gate script
    .planning/REQUIREMENTS.md (COMP-03) — what hardware acceptance must cover
  </read_first>

  <action>
    Create `docs/hardware-acceptance-report.md` with the following structure:

    ```
    # Hardware Acceptance Report — Rehab Robotics Multi-Device Workflow

    **Phase:** 25 — Multi-Device Compatibility and Promotion Gate
    **Requirement:** COMP-03
    **Purpose:** Documents hardware evidence that the dynamic N-sensor fleet workflow (use_fleet_bridge=true) is safe to set as the default launch configuration.

    **Promotion Gate:** Run `python scripts/acceptance_gate.py` — exits 0 only when all sections are STATUS: PASS.

    > **Note on rollback mode:** The legacy two-sensor workflow remains available at any time by launching with `use_fleet_bridge:=false`. This is the current default and will remain so until this gate closes.

    ---

    ## 1. Fleet Configuration Tested

    Document the devices under test: device IDs, roles, firmware version, and fleet size.

    | Field | Value |
    |-------|-------|
    | Fleet size (Master + Slaves) | PENDING HARDWARE TEST |
    | Master device ID | PENDING HARDWARE TEST |
    | Slave device IDs | PENDING HARDWARE TEST |
    | Firmware version | PENDING HARDWARE TEST |
    | Test date | PENDING HARDWARE TEST |
    | Tested by / Date: | PENDING |

    STATUS: PENDING

    ---

    ## 2. Identify Safety

    Confirm that the LED Identify action works for each device individually without disrupting acquisition or other devices.

    **Procedure:** Issue Identify to Master; confirm LED blinks; confirm Slave IMU data continues to publish at target rate. Repeat for each Slave.

    | Device | Outcome | Acquisition unaffected | Notes |
    |--------|---------|----------------------|-------|
    | Master | PENDING | PENDING | |
    | Slave 1 | PENDING | PENDING | |

    Tested by / Date: PENDING

    STATUS: PENDING

    ---

    ## 3. Acquisition Continuity

    Master plus N-1 Slave acquisition at target Hz with no data loss during continuous operation.

    **Target rate:** ≥100 Hz per device. **Duration:** ≥60 seconds.

    | Device | Measured rate (Hz) | Dropped frames | Notes |
    |--------|--------------------|---------------|-------|
    | Master | PENDING | PENDING | |
    | Slave 1 | PENDING | PENDING | |

    Tested by / Date: PENDING

    STATUS: PENDING

    ---

    ## 4. Recording Continuity

    Full record/stop cycle with N sensors. Verify no dropped frames at device reconnect during recording.

    **Procedure:** Start recording, power-cycle one Slave mid-recording, stop recording, verify file integrity.

    | Metric | Result | Notes |
    |--------|--------|-------|
    | Recording start/stop without error | PENDING | |
    | Frame count consistent after reconnect | PENDING | |
    | SD file playback valid | PENDING | |

    Tested by / Date: PENDING

    STATUS: PENDING

    ---

    ## 5. Reconnect Under Load

    Physically power-cycle one Slave during active acquisition; confirm recovery and re-attachment.

    **Procedure:** All devices streaming → power-cycle Slave → wait for reconnect → confirm IMU data resumes on canonical MAC topic → confirm assignment re-attaches automatically.

    | Metric | Result | Notes |
    |--------|--------|-------|
    | Reconnect time (seconds) | PENDING | |
    | Assignment re-attaches automatically | PENDING | |
    | Other devices unaffected during reconnect | PENDING | |

    Tested by / Date: PENDING

    STATUS: PENDING

    ---

    ## 6. Radio/Relay Load

    Measured throughput at full fleet size; compare to single-device baseline.

    **Procedure:** Single device → measure UDP rate. Full fleet → measure per-device UDP rate. Record ratio.

    | Configuration | Measured rate (Hz) | Packet loss (%) | Notes |
    |---------------|--------------------|-----------------|-------|
    | Single device baseline | PENDING | PENDING | |
    | Full fleet per-device | PENDING | PENDING | |
    | Rate ratio (fleet/baseline) | PENDING | PENDING | |

    Tested by / Date: PENDING

    STATUS: PENDING

    ---

    ## 7. OpenSim Solve Latency

    Wall-clock latency from IMU frame arrival to `/rehab/opensim/joint_states` publication for N sensors.

    **Procedure:** Timestamp IMU frame on arrival; timestamp JointState publish. Compute delta over 1000 samples. Report mean and 99th percentile.

    | Metric | Value | Notes |
    |--------|-------|-------|
    | Mean latency (ms) | PENDING | |
    | 99th percentile latency (ms) | PENDING | |
    | Sensor count (N) | PENDING | |

    Tested by / Date: PENDING

    STATUS: PENDING

    ---

    ## 8. Compatibility Aliases

    Confirm `/esp32/master/imu` and `/esp32/slave/imu` still publish matching data when fleet_bridge is active with alias bindings configured.

    **Procedure:** Launch with `use_fleet_bridge:=true alias_master_device_id:=<ID> alias_slave_device_id:=<ID>`. Subscribe to both alias topics. Confirm data matches canonical MAC topics.

    **Rollback verification:** Launch with `use_fleet_bridge:=false`; confirm legacy esp32_bridge_node starts and publishes on both alias topics without fleet_bridge.

    | Topic | Publishes | Matches canonical | Notes |
    |-------|-----------|------------------|-------|
    | /esp32/master/imu (fleet mode) | PENDING | PENDING | |
    | /esp32/slave/imu (fleet mode) | PENDING | PENDING | |
    | /esp32/master/imu (legacy mode) | PENDING | N/A (native) | |
    | /esp32/slave/imu (legacy mode) | PENDING | N/A (native) | |

    Tested by / Date: PENDING

    STATUS: PENDING

    ---

    ## Gate Summary

    All 8 sections must show `STATUS: PASS` before dynamic mode is promoted to the default.

    Run: `python scripts/acceptance_gate.py`

    Current gate status: OPEN — hardware evidence pending.
    ```

    Then create `scripts/acceptance_gate.py` using only stdlib (re, sys, pathlib):

    - Path resolution: `Path(__file__).parent.parent / 'docs' / 'hardware-acceptance-report.md'`
    - Read the report text
    - Use `re.findall(r'(?m)^##\s+\d+\.\s+(.+?)\s*$', text)` to extract section headings
    - Use `re.findall(r'STATUS:\s*(\w+)', text)` to extract all status values
    - Validate: if `not statuses`, print "Gate ERROR: no STATUS markers found in report" and sys.exit(2)
    - For each STATUS value, collect the non-PASS ones
    - If any non-PASS: print each open section (match by index: section_headings[i] if available, else f"Section {i+1}"), then print "Gate OPEN — N section(s) not PASS", sys.exit(1)
    - If all PASS: print "Gate CLOSED — all N sections PASS", sys.exit(0)
    - Include a `if __name__ == '__main__':` guard and a module-level `main()` function for clean testability

    The script must be runnable as: `python scripts/acceptance_gate.py`
  </action>

  <verify>
    <automated>cd "C:\Users\justi\Documents\# Rehab Robotics GUI Prototype__Here's a complete, single-file React + TypeScript app implementing the LabVIEW" && python scripts/acceptance_gate.py; echo "Exit code: $?"</automated>
  </verify>

  <acceptance_criteria>
    - `docs/hardware-acceptance-report.md` exists and `grep -c "STATUS: PENDING" docs/hardware-acceptance-report.md` returns 8
    - `python scripts/acceptance_gate.py` exits with code 1 (gate OPEN, all sections PENDING)
    - `python scripts/acceptance_gate.py` output contains "Gate OPEN" and lists at least one section name
    - `python -c "import scripts.acceptance_gate"` does not raise an ImportError (script is importable)
  </acceptance_criteria>

  <done>
    docs/hardware-acceptance-report.md and scripts/acceptance_gate.py both exist. Running acceptance_gate.py exits 1 with "Gate OPEN" output listing all PENDING sections. Eight STATUS: PENDING markers confirmed in the report.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run acceptance gate and conditionally flip use_fleet_bridge default; update ROADMAP</name>
  <files>backend/launch/rehab_robotics.launch.py, .planning/ROADMAP.md</files>

  <read_first>
    backend/launch/rehab_robotics.launch.py — current use_fleet_bridge DeclareLaunchArgument with default_value='false'
    scripts/acceptance_gate.py — just created
    docs/hardware-acceptance-report.md — just created
    .planning/ROADMAP.md — Phase 25 section to update with gate status note
  </read_first>

  <action>
    Step 1: Run the acceptance gate and capture its exit code:
      python scripts/acceptance_gate.py
      GATE_EXIT=$?

    Step 2: Based on the exit code:

    IF GATE_EXIT == 0 (all sections STATUS: PASS):
      Edit backend/launch/rehab_robotics.launch.py — change the use_fleet_bridge DeclareLaunchArgument:
        FROM: default_value='false'
        TO:   default_value='true'
      Do NOT change the description string or any other argument. Only the default_value string for use_fleet_bridge.

    IF GATE_EXIT == 1 (gate OPEN — expected outcome for this automated run):
      Do NOT modify backend/launch/rehab_robotics.launch.py.
      The default_value='false' remains correct and intentional.

    Step 3: Update .planning/ROADMAP.md — locate the Phase 25 section and add a gate status line immediately after the phase heading or under the Plans section. The line must read one of:
      - "Gate OPEN — hardware evidence pending" (if gate exits 1)
      - "Gate CLOSED — dynamic mode is default" (if gate exits 0)

    The gate status line may be added as a blockquote or bold text under the Phase 25 heading:
      > **Gate status:** Gate OPEN — hardware evidence pending.

    Step 4: Verify the launch file still parses (syntax check):
      python -c "import ast; ast.parse(open('backend/launch/rehab_robotics.launch.py').read())"

    Step 5: Verify the gate script still exits correctly after any file changes:
      python scripts/acceptance_gate.py; echo "Gate exit: $?"
  </action>

  <verify>
    <automated>cd "C:\Users\justi\Documents\# Rehab Robotics GUI Prototype__Here's a complete, single-file React + TypeScript app implementing the LabVIEW" && python -c "import ast; ast.parse(open('backend/launch/rehab_robotics.launch.py').read())" && echo "Launch file parses OK" && python scripts/acceptance_gate.py; echo "Gate exit: $?"</automated>
  </verify>

  <acceptance_criteria>
    - `python -c "import ast; ast.parse(open('backend/launch/rehab_robotics.launch.py').read())"` exits 0 (file is valid Python)
    - If gate was OPEN: `grep "default_value='false'" backend/launch/rehab_robotics.launch.py | grep use_fleet_bridge` returns 1 match (default unchanged)
    - If gate was CLOSED: `grep "default_value='true'" backend/launch/rehab_robotics.launch.py | grep use_fleet_bridge` returns 1 match (default flipped)
    - `.planning/ROADMAP.md` contains "Gate OPEN" or "Gate CLOSED" (whichever matches the actual gate result) in the Phase 25 section
    - `python scripts/acceptance_gate.py` exits with the same code as in Task 1 (no regression)
  </acceptance_criteria>

  <done>
    The launch file is syntactically valid. The use_fleet_bridge default reflects the gate outcome. ROADMAP.md Phase 25 section contains the gate status line. The acceptance gate script exits consistently.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| acceptance_gate.py → filesystem | Script reads docs/hardware-acceptance-report.md; path is computed relative to script location |
| gate script → launch file | Conditional write to launch file based on gate exit code — only done if all sections PASS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-03-01 | Tampering | hardware-acceptance-report.md STATUS markers | accept | Report is an evidence document; operator must set STATUS: PASS manually after physical testing — cannot be auto-approved |
| T-25-03-02 | Elevation of Privilege | acceptance_gate.py flipping use_fleet_bridge to true | mitigate | Gate only flips if ALL 8 sections are STATUS: PASS; shipped template has all PENDING so automated run cannot trigger flip |
| T-25-03-03 | Repudiation | No traceability for who ran hardware tests | mitigate | Each section has "Tested by / Date:" field; operator must fill in identity before marking PASS |
| T-25-03-04 | Information Disclosure | Report stored in docs/ | accept | Contains only hardware test metadata, no secrets or PII |
| T-25-03-SC | Tampering | npm/pip/cargo installs | accept | acceptance_gate.py uses stdlib only; no new packages |
</threat_model>

<verification>
- `python scripts/acceptance_gate.py` exits 1 and output contains "Gate OPEN"
- `grep -c "STATUS: PENDING" docs/hardware-acceptance-report.md` returns 8
- `grep -c "STATUS:" docs/hardware-acceptance-report.md` returns 8
- `python -c "import ast; ast.parse(open('backend/launch/rehab_robotics.launch.py').read())"` exits 0
- `grep "use_fleet_bridge" backend/launch/rehab_robotics.launch.py | grep "default_value='false'"` returns 1 line (gate was OPEN so default unchanged)
- `grep -i "gate" .planning/ROADMAP.md` returns at least 1 line containing "OPEN" or "CLOSED"
- Full test suite still passes: `python -m pytest backend/test/ -q 2>&1 | tail -5` shows 0 failures
</verification>

<success_criteria>
docs/hardware-acceptance-report.md exists with 8 structured evidence sections and STATUS: PENDING markers. scripts/acceptance_gate.py runs, exits 1 (gate OPEN), and lists open sections. The launch file default for use_fleet_bridge is unchanged at 'false' (gate was OPEN). ROADMAP.md Phase 25 section notes "Gate OPEN — hardware evidence pending". Phase 25 is considered complete per D-12: test suites (COMP-01 + COMP-02) are green and the acceptance template + gate script exist.
</success_criteria>

<output>
Create `.planning/phases/25-multi-device-compatibility-and-promotion-gate/25-03-SUMMARY.md` when done.
</output>
