---
phase: 25-multi-device-compatibility-and-promotion-gate
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/test/test_acceptance.py
autonomous: true
requirements:
  - COMP-02

must_haves:
  truths:
    - "Full-MAC collision: two devices sharing the same low-32 bits have distinct canonical IDs"
    - "Arbitrary discovery order: fleet registry is stable regardless of first-seen order"
    - "DHCP/reconnect: identity and assignment survive IP change and TCP reconnect"
    - "Identify failure: timeout, offline, and rejected outcomes leave mapping state unaffected"
    - "Partial-Apply rollback: apply with stale revision leaves applied_revision at its previous value"
    - "Corrupt persistence: corrupted mapping_store.json triggers atomic recovery (revision=0, assignments={})"
    - "Stale/skewed sample: stale or skewed IMU input suppresses IK output; acquisition path continues"
    - "Interlock: apply_candidate is blocked during recording; recording is not stopped"
    - "Repeated cleanup: calling _on_mapping_current 20 times alternating device lists leaves len(_mac_inputs) bounded to the last applied device count"
  artifacts:
    - path: backend/test/test_acceptance.py
      provides: "Deterministic acceptance tests for all 9 COMP-02 edge-case categories"
      min_lines: 250
  key_links:
    - from: backend/test/test_acceptance.py
      to: backend/test/test_stepesp_firmware_topology.py
      via: "import CROSS_LAYER_SELF_ID, CROSS_LAYER_PEER_IDS, CROSS_LAYER_LOW32_COLLISION_IDS, CROSS_LAYER_IDENTIFY_OUTCOMES (per D-06)"
      pattern: "from backend.test.test_stepesp_firmware_topology import CROSS_LAYER_"
    - from: backend/test/test_acceptance.py
      to: backend/rehab_robotics_bridge/mapping_node.py
      via: "_install_ros_stubs() + importlib load; apply_candidate, _on_fleet_registry"
      pattern: "MappingNode|MappingStore"
    - from: backend/test/test_acceptance.py
      to: backend/rehab_robotics_bridge/opensim_node.py
      via: "_on_mapping_current, _check_sync_skew, _mac_inputs"
      pattern: "_mac_inputs|_on_mapping_current"
---

<objective>
Create `backend/test/test_acceptance.py` — offline deterministic tests for all 9 COMP-02 edge-case categories. Each category is its own test class. Tests import CROSS_LAYER_* constants from test_stepesp_firmware_topology.py (per D-06) rather than duplicating them.

Purpose: COMP-02 requires deterministic coverage of full-MAC collisions, arbitrary ordering, DHCP/reconnect, Identify failures, partial-Apply rollback, corrupt persistence, stale/skewed samples, interlocks, and repeated resource cleanup. These are the edge cases identified across Phases 20-24 that could regress under the multi-device path.

Output: A passing test file with 9 test classes and at least 18 test methods (2 per class minimum), runnable as `python -m pytest backend/test/test_acceptance.py -v`.
</objective>

<execution_context>
@C:\Users\justi\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\justi\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-multi-device-compatibility-and-promotion-gate/25-CONTEXT.md

<interfaces>
<!-- CROSS_LAYER_* constants (from test_stepesp_firmware_topology.py, import do NOT copy):
     CROSS_LAYER_SELF_ID         = 'esp32:aabbccddeeff'
     CROSS_LAYER_PEER_IDS        = ('esp32:112233445566', 'esp32:77bbccddeeff')
     CROSS_LAYER_LOW32_COLLISION_IDS = ('esp32:1111ccddeeff', 'esp32:2222ccddeeff')
     CROSS_LAYER_IDENTIFY_OUTCOMES   = ('confirmed','sent_unconfirmed','timeout','offline','unsupported','rejected','invalid_target') -->

<!-- MappingStore API (from backend/rehab_robotics_bridge/mapping_node.py):
     MappingStore(store_path: Path)
     .set_model_hash(hash_str: str)
     .set_frame_list(frame_list: list[dict])
     .set_assignment(device_id, segment, frame, state) -> (outcome, detail)
     .apply_candidate(expected_revision, frame_list, interlock_active=False, interlock_reason='') -> dict
     .revision: int
     .applied_revision: int
     .assignments: dict
     Corruption recovery: write garbage JSON to store_path and store_path.with_suffix('.bak'),
     then MappingStore(store_path) -> revision=0, assignments={} -->

<!-- MappingNode API (from backend/rehab_robotics_bridge/mapping_node.py):
     _on_fleet_registry(msg: String) — JSON {"devices": [{"device_id": ..., "route_state": "connected"}]}
     _recording_active: bool — set directly in tests
     _on_apply_mapping(req, resp) -> resp
     _store: MappingStore -->

<!-- OpenSimBridgeNode API (from backend/rehab_robotics_bridge/opensim_node.py):
     _on_mapping_current(msg: String) — JSON {"applied_revision":N, "model_hash":"...", "assigned_list":[{"device_id":...,"frame":...}]}
     _mac_inputs: dict[str, _DeviceInput]   — len must stay bounded after repeated remaps
     _check_sync_skew(inputs: list[_DeviceInput]) -> dict  — returns per-device validity
     _solve_and_publish_ik_n() — suppresses publish when any input invalid/stale
     _DeviceInput fields: device_id, frame, subscription_handle, post_reconnect_fresh, last_ts_ns -->

<!-- Stale/skew test pattern (from CONTEXT.md D-specifics):
     Set _DeviceInput.last_ts_ns to (time.monotonic_ns() - 3_000_000_000) to exceed stale_timeout_s=1.0.
     Set _DeviceInput.post_reconnect_fresh = False to simulate post-reconnect freshness failure.
     Then call node._solve_and_publish_ik_n(); assert len(joint_states_publisher.messages) == 0. -->

<!-- Identify outcomes test pattern:
     CROSS_LAYER_IDENTIFY_OUTCOMES covers: 'confirmed','sent_unconfirmed','timeout','offline','unsupported','rejected','invalid_target'.
     Tests assert that for non-'confirmed' outcomes the MappingStore.applied_revision is unchanged
     (Identify does not modify mapping state). Identify is handled at the fleet_bridge_node level;
     test constructs a stub that records the outcome without touching MappingStore. -->
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Write failing acceptance tests for all 9 COMP-02 categories (RED)</name>
  <files>backend/test/test_acceptance.py</files>

  <read_first>
    backend/test/test_mapping_node.py (lines 1-210) — _install_ros_stubs(), _StubNode, _make_node(), _apply_req/_apply_resp helpers, _fleet_msg, MappingStore direct instantiation
    backend/test/test_stepesp_firmware_topology.py (lines 1-56) — CROSS_LAYER_* constant definitions (to understand what to import)
    backend/test/test_opensim_node.py (lines 159-266) — _install_ros_stubs() with sensor_msgs.msg.Imu stub
    backend/rehab_robotics_bridge/mapping_node.py (grep for MappingStore, apply_candidate, _on_fleet_registry, _recording_active)
    backend/rehab_robotics_bridge/opensim_node.py (grep for _mac_inputs, _on_mapping_current, _check_sync_skew, _DeviceInput)
  </read_first>

  <behavior>
    9 test classes, each with at least 2 test methods:

    FullMacCollisionTests:
      - test_low32_collision_ids_have_distinct_canonical_ids: bytes.fromhex(CROSS_LAYER_LOW32_COLLISION_IDS[0][6:])[-4:] == bytes.fromhex(CROSS_LAYER_LOW32_COLLISION_IDS[1][6:])[-4:] AND canonical IDs are not equal (proves full-48-bit key is used, not low-32)
      - test_collision_devices_get_distinct_assignments: assign CROSS_LAYER_LOW32_COLLISION_IDS[0] to femur_r and CROSS_LAYER_LOW32_COLLISION_IDS[1] to tibia_r; assert both appear as separate keys in store.assignments

    ArbitraryDiscoveryOrderTests:
      - test_fleet_registry_stable_regardless_of_order: call _on_fleet_registry with [PEER_A, PEER_B], then with [PEER_B, PEER_A]; assert store.assignments keys are the same set after both calls
      - test_new_device_order_does_not_overwrite_existing: after assigning SELF_ID, call _on_fleet_registry with [SELF_ID, PEER_A]; assert SELF_ID assignment state is unchanged

    DhcpReconnectTests:
      - test_device_stays_assigned_after_fleet_registry_reconnect: assign SELF_ID, apply, then call _on_fleet_registry with SELF_ID; assert assignments[SELF_ID]['state'] == 'assigned'
      - test_new_mac_at_same_route_gets_unassigned: after assigning SELF_ID, call _on_fleet_registry with a different device_id (PEER_IDS[0]); assert PEER_IDS[0] ends up as 'unassigned'

    IdentifyFailureTests:
      - test_non_confirmed_identify_outcomes_do_not_alter_mapping: for each outcome in CROSS_LAYER_IDENTIFY_OUTCOMES where outcome != 'confirmed', assert store.applied_revision is unchanged (simulate by reading applied_revision before and after calling a stub identify handler that sets outcome but never calls apply_candidate)
      - test_confirmed_identify_does_not_alter_mapping: same assertion for 'confirmed' — Identify is always mapping-neutral

    PartialApplyRollbackTests:
      - test_stale_revision_apply_leaves_applied_revision_intact: apply at revision N successfully, then call apply_candidate(expected_revision=N-1); assert applied_revision == N
      - test_apply_incomplete_leaves_applied_revision_intact: apply at revision N, add an unassigned device (bumps to N+1), call apply_candidate(expected_revision=N+1); assert result['outcome'] == 'incomplete' and applied_revision == N

    CorruptPersistenceTests:
      - test_both_files_corrupt_triggers_fresh_start: write garbage to mapping_store.json and .bak; instantiate MappingStore; assert revision==0 and assignments=={}
      - test_corrupt_main_with_valid_bak_recovers_from_bak: write a valid JSON to .bak; write garbage to mapping_store.json; instantiate MappingStore; assert assignments are non-empty (recovered from backup) OR revision==0 (fresh start) — either is acceptable, but no exception must be raised

    StaleSkewedSampleTests:
      - test_stale_imu_suppresses_joint_state_publish: set a _DeviceInput.last_ts_ns to time.monotonic_ns() - 3_000_000_000 (3 seconds ago, beyond stale_timeout_s=1.0); call node._solve_and_publish_ik_n(); assert the joint_states publisher received no new messages
      - test_skewed_sample_suppresses_joint_state_publish: inject two _DeviceInput entries with timestamp difference > sync_skew_ms=50; assert no new JointState published

    InterlockTests:
      - test_apply_blocked_when_recording_active: set node._recording_active=True; call _on_apply_mapping with valid expected_revision; assert result.outcome == 'blocked' and result.detail contains 'recording'
      - test_recording_not_stopped_by_blocked_apply: set node._recording_active=True; call _on_apply_mapping; assert node._recording_active is still True after the call

    RepeatedResourceCleanupTests:
      - test_repeated_remap_does_not_grow_mac_inputs: call _on_mapping_current 20 times alternating between [SELF_ID, PEER_A] and [PEER_B]; assert len(node._mac_inputs) <= 2 after all calls
      - test_repeated_remap_does_not_grow_subscriptions: same 20 calls; assert len(node.subscriptions) stays bounded (does not grow monotonically — subscriptions for removed devices are destroyed)
  </behavior>

  <action>
    Create `backend/test/test_acceptance.py` with:

    1. Header docstring: "Deterministic acceptance tests for COMP-02 edge cases. Covers all 9 categories from Phases 20-24. Run: python -m pytest backend/test/test_acceptance.py -v"

    2. Imports — stdlib only at top: `import json, sys, tempfile, time, types, unittest` and `from pathlib import Path`.

    3. Import CROSS_LAYER_* constants from test_stepesp_firmware_topology (per D-06 — import, do NOT copy):
       ```python
       import sys
       from pathlib import Path
       _REPO_ROOT = Path(__file__).parents[2]
       sys.path.insert(0, str(_REPO_ROOT))
       from backend.test.test_stepesp_firmware_topology import (
           CROSS_LAYER_SELF_ID,
           CROSS_LAYER_PEER_IDS,
           CROSS_LAYER_LOW32_COLLISION_IDS,
           CROSS_LAYER_IDENTIFY_OUTCOMES,
       )
       ```

    4. Copy `_install_ros_stubs()` verbatim from `backend/test/test_opensim_node.py` (lines 159-246) — include all needed stubs: rclpy, sensor_msgs, std_msgs, std_srvs, rehab_robotics_interfaces. Call it at module level before importing production modules.

    5. After `_install_ros_stubs()`, load production modules with importlib exactly as test_mapping_node.py does for MappingNode, and as test_opensim_node.py does for opensim_node.

    6. Define all request/response factory helpers needed (_req, _apply_req, _apply_resp, _set_assign_req, _set_assign_resp, _str_msg) — copy from test_mapping_node.py.

    7. Define `_make_mapping_node(tmp_dir)` and `_make_opensim_node()` helper functions following existing patterns.

    8. Define `_mapping_msg(assigned_list, revision=1, model_hash='a'*64)` — returns a _str_msg with JSON for `_on_mapping_current`:
       `{"applied_revision": revision, "model_hash": model_hash, "assigned_list": assigned_list}`

    9. Implement all 9 test classes with the behaviors listed. Each class uses `unittest.TestCase`. setUp creates a fresh node and/or tempfile; tearDown calls cleanup.

    10. For StaleSkewedSampleTests: after constructing the opensim node and calling _on_mapping_current to populate _mac_inputs, directly mutate `node._mac_inputs[device_id].last_ts_ns` to the stale value. Then call `node._solve_and_publish_ik_n()`. Capture the joint_states publisher by finding the publisher whose topic equals the JOINT_STATES_TOPIC constant, and assert its messages count has not increased.

    11. For RepeatedResourceCleanupTests: the node.subscriptions list (from _StubNode) tracks all subscriptions created; destroy_subscription removes them. Assert that after 20 alternating _on_mapping_current calls the count of active subscriptions for IMU MAC topics does not grow beyond the last applied mapping's device count.

    12. `if __name__ == '__main__': unittest.main()` at the bottom.

    Do NOT use subprocess, do NOT call rclpy.init() or rclpy.spin(). No live ROS. No external packages.
  </action>

  <verify>
    <automated>cd "C:\Users\justi\Documents\# Rehab Robotics GUI Prototype__Here's a complete, single-file React + TypeScript app implementing the LabVIEW" && python -m pytest backend/test/test_acceptance.py --collect-only -q 2>&1 | tail -15</automated>
  </verify>

  <done>
    File exists at backend/test/test_acceptance.py. `--collect-only` lists at least 18 test items across 9 classes with no import errors. Tests may FAIL at this point (RED is expected for TDD).
  </done>
</task>

<task type="tdd">
  <name>Task 2: Make all acceptance tests pass (GREEN)</name>
  <files>backend/test/test_acceptance.py</files>

  <read_first>
    backend/test/test_acceptance.py — the file you just created (all test classes and methods)
    backend/rehab_robotics_bridge/opensim_node.py (grep for _DeviceInput, last_ts_ns, post_reconnect_fresh, sync_skew_ms, _solve_and_publish_ik_n) — find exact attribute names for stale/skew manipulation
    backend/rehab_robotics_bridge/mapping_node.py (grep for applied_revision, _on_apply_mapping, _recording_active) — confirm attribute names
  </read_first>

  <behavior>
    All 18+ test methods in test_acceptance.py pass. No production code is changed.
  </behavior>

  <action>
    Run: python -m pytest backend/test/test_acceptance.py -v 2>&1

    For each failing test, fix the test's attribute names or call signatures to match the actual production code. Common corrections:

    - _DeviceInput attribute names: grep opensim_node.py for the dataclass or namedtuple definition. Confirm `last_ts_ns`, `post_reconnect_fresh`, `subscription_handle`, `frame`, `device_id` — update tests if names differ.
    - Stale gate threshold: the stale check uses `stale_timeout_s`. Confirm the check compares `(now_ns - last_ts_ns) / 1e9 > stale_timeout_s`. Set last_ts_ns to `time.monotonic_ns() - int(stale_timeout_s * 2 * 1e9)` (double the timeout).
    - Sync skew: grep for `sync_skew_ms` usage in `_check_sync_skew`. Set two _DeviceInput.last_ts_ns values differing by `(sync_skew_ms + 100) * 1_000_000` nanoseconds.
    - Subscription count bound: the _StubNode.subscriptions list is modified by create_subscription and destroy_subscription. After 20 alternating remaps, assert `len([s for s in node.subscriptions if '/esp/raw/mac_' in s.topic]) <= 2`.
    - Corrupt persistence bak recovery: if MappingStore.apply_candidate checks bak before returning fresh_start, the test may need to verify a specific recovery behavior. Accept either outcome as documented in the behavior block above.
    - CorruptPersistenceTests: ensure the temp files are at the actual store_path location used by MappingStore. Use `self.store.store_path` or the path you provided to MappingStore().

    Do NOT change any production file. Only fix the test assertions to match actual code behavior.

    After all fixes:
      python -m pytest backend/test/test_acceptance.py -v
    All tests must exit GREEN.

    Then run full suite:
      python -m pytest backend/test/ -v 2>&1 | tail -30
  </action>

  <verify>
    <automated>cd "C:\Users\justi\Documents\# Rehab Robotics GUI Prototype__Here's a complete, single-file React + TypeScript app implementing the LabVIEW" && python -m pytest backend/test/test_acceptance.py -v 2>&1 | tail -25</automated>
  </verify>

  <done>
    All 18+ test methods in test_acceptance.py show PASSED. The full `python -m pytest backend/test/` run exits 0 (or only pre-existing failures documented in acceptance report).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test file → production modules | Stubs replace ROS; production code exec'd in-process |
| CROSS_LAYER_* import | Constants imported from test_stepesp_firmware_topology.py — single source of truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-02-01 | Tampering | MappingStore corruption recovery | mitigate | Test verifies that corrupt persistence triggers clean state, never silently uses corrupt data |
| T-25-02-02 | Denial of Service | Repeated _on_mapping_current calls | mitigate | Test asserts subscription count stays bounded (≤ last-mapping device count) |
| T-25-02-03 | Spoofing | Full-MAC collision via low-32 bits | mitigate | Test proves canonical ID uses full 48-bit; collision IDs get distinct keys |
| T-25-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages; stdlib + existing test deps only |
</threat_model>

<verification>
- `python -m pytest backend/test/test_acceptance.py -v` exits 0 with all tests PASSED
- `grep -c "class.*Tests" backend/test/test_acceptance.py` returns 9
- `grep -c "def test_" backend/test/test_acceptance.py` returns >= 18
- `grep -v "^#" backend/test/test_acceptance.py | grep -c "CROSS_LAYER_LOW32_COLLISION_IDS\s*="` returns 0 (constants are imported, not redefined)
- `python -m pytest backend/test/ -v 2>&1 | grep -E "FAILED|ERROR" | grep -v "pre-existing"` returns 0 lines
</verification>

<success_criteria>
backend/test/test_acceptance.py exists with 9 test classes and at least 18 test methods. All tests pass. CROSS_LAYER_* constants are imported from test_stepesp_firmware_topology.py. The full backend test suite exits 0.
</success_criteria>

<output>
Create `.planning/phases/25-multi-device-compatibility-and-promotion-gate/25-02-SUMMARY.md` when done.
</output>
