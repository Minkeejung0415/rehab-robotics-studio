---
phase: 20-full-identity-and-confirmed-identify
plan: "04"
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - firmware/step_node/step_node.ino
  - firmware/step_node_slave/step_node_slave.ino
  - backend/test/test_stepesp_firmware_topology.py
autonomous: true
requirements: [ID-03]
must_haves:
  truths:
    - "The official Seeed Studio XIAO ESP32S3 build advertises onboard user LED GPIO 21 with active-low semantics."
    - "The LED configuration is reachable only through an exact XIAO ESP32S3 board-target guard; unknown targets remain unsupported."
    - "Software tests prove guard parity, non-blocking state ownership, bounded timing, and prior-state restoration logic without claiming physical LED evidence."
  artifacts:
    - path: "firmware/step_node/step_node.ino"
      provides: "Exact guarded XIAO ESP32S3 Identify LED configuration"
    - path: "firmware/step_node_slave/step_node_slave.ino"
      provides: "Matching guarded XIAO ESP32S3 Identify LED configuration"
    - path: "backend/test/test_stepesp_firmware_topology.py"
      provides: "Hardware-independent board guard and state-machine contract coverage"
  key_links:
    - from: "official XIAO ESP32S3 board target macro"
      to: "STEPESP_IDENTIFY_LED_*"
      via: "exact compile-time guard selecting GPIO 21 active-low"
      pattern: "ARDUINO_XIAO_ESP32S3|GPIO_NUM_21|STEPESP_IDENTIFY_LED_ACTIVE_LEVEL"
---

<objective>
Encode and test the manufacturer-defined XIAO ESP32S3 Identify LED configuration without blocking software delivery on physical access.

Purpose: Enable the supported board target safely while preserving fail-closed behavior for every unknown target.
Output: Matching guarded firmware configuration and hardware-independent regression coverage; physical blink evidence remains an explicit final human check.
</objective>

<execution_context>
@C:/Users/justi/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/justi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-full-identity-and-confirmed-identify/20-01-SUMMARY.md
@.planning/phases/20-full-identity-and-confirmed-identify/20-CONTEXT.md
@firmware/step_node/step_node.ino
@firmware/step_node_slave/step_node_slave.ino
@backend/test/test_stepesp_firmware_topology.py

<worktree_rules>
Both sketches and the topology test are untracked user work. Read their current contents and inspect `git status --short` plus relevant diffs before editing. Patch only the board guard, Identify LED state, and matching test regions; preserve all identity, acquisition, recording, and user content; never switch branches; stage only these exact paths; and commit atomically.
</worktree_rules>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the official XIAO ESP32S3 LED behind an exact board guard</name>
  <files>firmware/step_node/step_node.ino, firmware/step_node_slave/step_node_slave.ino, backend/test/test_stepesp_firmware_topology.py</files>
  <read_first>
    - `20-CONTEXT.md` D-20-06/D-20-07 — GPIO 21 active-low, exact board guard, bounded non-blocking timing, and restoration source of truth.
    - `20-01-SUMMARY.md` — implemented configuration identifiers and Identify state-machine seams.
    - Both firmware sketches — current `STEPESP_IDENTIFY_LED_*`, target macro, saved prior state, deadline, cadence, and loop-owned `identifyTick()`.
    - `backend/test/test_stepesp_firmware_topology.py` — existing paired-sketch schema and no-blocking assertions.
  </read_first>
  <behavior>
    - "Both builds under ARDUINO_XIAO_ESP32S3 select GPIO 21 active-low and advertise Identify support."
    - "A build without ARDUINO_XIAO_ESP32S3 advertises unsupported and never writes GPIO 21."
    - "Master and Slave board tuples cannot drift."
    - "Identify remains loop-owned, uses millis deadlines, accepts 1000-5000 ms with 3000 ms default, and restores the captured prior state."
  </behavior>
  <action>Begin with failing topology assertions for the exact `ARDUINO_XIAO_ESP32S3` target guard, GPIO 21, active-low level, Master/Slave parity, and unknown-target unsupported behavior per D-20-07. Then update only the compile-time board configuration regions so that exact macro selects `STEPESP_IDENTIFY_LED_PIN=21`, active-low semantics, and verified capability; do not use a broad ESP32S3-family guard or runtime board-name guess. Keep all other targets unsupported. Preserve compile-time diagnostic overrides only when they cannot bypass the checked-in exact-target production guard. Retain the Plan 01 loop-owned state machine, duration clamp/default, and exact prior application LED restoration. Extend source-contract tests to isolate `identifyTick()` and reject `delay()`, callback GPIO writes, or assignments to streaming, recording, SD, filter, sample-rate, and acquisition deadline state. Compile both sketches with the official FQBN. Record software/configuration evidence only; do not claim that compilation or fixtures prove physical blink, polarity, restoration, or timing. Preservation gate: reread all three files immediately before patching, stage only these paths, and commit atomically.</action>
  <verify>
    <automated>python -m unittest backend.test.test_stepesp_firmware_topology -v; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; arduino-cli compile --fqbn esp32:esp32:XIAO_ESP32S3 firmware/step_node; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; arduino-cli compile --fqbn esp32:esp32:XIAO_ESP32S3 firmware/step_node_slave</automated>
  </verify>
  <acceptance_criteria>
    - `[configuration]` Both roles select the official onboard LED at GPIO 21 active-low only under the exact XIAO ESP32S3 target guard.
    - `[fail-closed]` Unknown board targets advertise unsupported and contain no reachable Identify GPIO write.
    - `[parity]` Topology tests fail on Master/Slave board tuple or guard drift.
    - `[timing]` Tests prove loop ownership, duration bounds/default, saved-state restoration code, and absence of blocking/acquisition mutations.
    - `[evidence boundary]` Summary labels all results software/configuration evidence and leaves physical behavior pending for Plan 06 human UAT.
  </acceptance_criteria>
  <done>The supported board is manufacturer-configured and locally verified while unknown targets remain fail-closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build target macro -> GPIO capability | Compile-time target selection controls whether firmware may drive an onboard LED. |
| Identify LED state -> acquisition/recording loop | Diagnostic state shares timing-sensitive firmware execution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-16 | Elevation of privilege | board guard/GPIO selection | high | mitigate | Require the exact official XIAO ESP32S3 target guard and GPIO 21 active-low tuple in both roles; all unknown targets stay unsupported. |
| T-20-17 | Denial of service | Identify timing | high | mitigate | Source tests enforce loop-owned millis deadlines, bounded durations, no delay, and no acquisition/recording mutation. |
| T-20-18 | Tampering | Master/Slave board parity | high | mitigate | Paired-source tests pin identical guard, pin, level, capability, and restoration semantics. |
| T-20-SC | Tampering | package/tool installs | low | accept | No dependency install is authorized. |
</threat_model>

<verification>Run the firmware topology suite and compile both official XIAO ESP32S3 sketches. This proves configuration and source invariants only; Plan 06 carries the physical observation as a verifier-harvested human check.</verification>
<success_criteria>The exact supported board compiles with GPIO 21 active-low Identify, unknown boards remain unsupported, and no software result is mislabeled as physical evidence.</success_criteria>
<output>Create `.planning/phases/20-full-identity-and-confirmed-identify/20-04-SUMMARY.md` when done.</output>
