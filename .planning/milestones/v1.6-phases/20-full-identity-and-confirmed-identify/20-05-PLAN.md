---
phase: 20-full-identity-and-confirmed-identify
plan: "05"
type: execute
wave: 4
depends_on: ["20-03", "20-04"]
files_modified:
  - backend/test/test_stepesp_firmware_topology.py
  - backend/test/test_stepesp_udp_relay.py
  - backend/test/test_esp32_controls.py
  - docs/stepesp-identity-identify.md
autonomous: true
requirements: [ID-01, ID-03]
must_haves:
  truths:
    - "Hardware-independent regression proves one self record, distinct peer inventory, an explicit count/terminator, and self-only host session binding across firmware, relay, and bridge."
    - "Regression proves exact full-MAC targeting and all Identify outcomes without creating Phase 21 per-MAC publisher lifecycle."
    - "Operators have an exact runbook that separates local proof from the pending one-target physical UAT and reserves fleet capacity/promotion for Phase 25."
  artifacts:
    - path: "backend/test/test_stepesp_firmware_topology.py"
      provides: "Firmware identity inventory and guarded Identify invariants"
    - path: "backend/test/test_stepesp_udp_relay.py"
      provides: "Relay self-only binding and complete inventory fixtures"
    - path: "backend/test/test_esp32_controls.py"
      provides: "Bridge self/peer parsing, Identify correlation, and Phase 21 publisher-boundary fixtures"
    - path: "docs/stepesp-identity-identify.md"
      provides: "Operator identity/Identify contract and explicit physical UAT checklist"
  key_links:
    - from: "firmware IDENTITY_OK/IDENTITY_PEER/IDENTITY_END"
      to: "relay and bridge session identity"
      via: "complete counted inventory with record=self as the only bindable record"
      pattern: "record=self|record=peer|peer_count"
    - from: "backend/rehab_robotics_bridge/esp32_bridge_node.py"
      to: "Phase 21"
      via: "canonical token contract without per-MAC publisher instantiation"
      pattern: "mac_[0-9a-f]{12}"
---

<objective>
Close cross-layer identity/Identify contract gaps with deterministic regression coverage and an operator runbook.

Purpose: Make self binding, peer inventory, confirmed outcomes, phase boundaries, and the remaining physical evidence impossible to misinterpret.
Output: Cross-layer regression fixtures and a runbook that explicitly defers per-MAC publishers to Phase 21 and final capacity/promotion to Phase 25.
</objective>

<execution_context>
@C:/Users/justi/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/justi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-full-identity-and-confirmed-identify/20-01-SUMMARY.md
@.planning/phases/20-full-identity-and-confirmed-identify/20-02-SUMMARY.md
@.planning/phases/20-full-identity-and-confirmed-identify/20-03-SUMMARY.md
@.planning/phases/20-full-identity-and-confirmed-identify/20-04-SUMMARY.md
@.planning/phases/20-full-identity-and-confirmed-identify/20-CONTEXT.md
@backend/test/test_stepesp_firmware_topology.py
@backend/test/test_stepesp_udp_relay.py
@backend/test/test_esp32_controls.py
@docs/stepesp-wireless-setup.md

<worktree_rules>
All three tests overlap dirty or untracked user work. Read each current file and inspect status/diffs before editing. Extend existing fixtures in place, preserve all current assertions, never switch branches, stage only the task's exact paths, and commit each task atomically.
</worktree_rules>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the self-peer-terminator contract across all host layers</name>
  <files>backend/test/test_stepesp_firmware_topology.py, backend/test/test_stepesp_udp_relay.py, backend/test/test_esp32_controls.py</files>
  <read_first>
    - All four Phase 20 summaries — exact firmware, relay, bridge, and board-guard identifiers produced upstream.
    - All three focused test files — current source fixtures, async route doubles, ROS stubs, mixed-stream helpers, and dirty-worktree assertions.
    - `20-CONTEXT.md` — D-20-01 through D-20-08 plus explicit Phase 21 per-MAC publisher deferral.
  </read_first>
  <behavior>
    - "Firmware emits one IDENTITY_OK record=self, exactly peer_count IDENTITY_PEER record=peer rows, and one matching IDENTITY_END."
    - "Relay and bridge reject peer-as-self, missing/duplicate self, peer-before-self, duplicate peers, count mismatch, and missing/mismatched terminator."
    - "Two identities sharing low 32 bits remain distinct at every parser and target lookup."
    - "A matching application ACK alone confirms Identify; wrong-target, duplicate, late, lost, unsupported, offline, rejected, sent-unconfirmed, and timeout cases cannot false-confirm."
    - "Bridge tests permit the pure canonical token helper but fail if Phase 20 creates, caches, destroys, or publishes per-MAC ROS publishers."
  </behavior>
  <action>Extend the three existing suites as a single cross-layer contract matrix. Pin the exact ordered id-v1 sequence and require both host layers to bind only `record=self` after a complete counted terminator. Add adversarial fixtures where a peer appears first, a peer reuses the expected ID, self is missing or duplicated, peer IDs duplicate, counts differ, terminator is absent/wrong, and two MACs share the same low 32 bits. Reuse the existing async/mixed-stream fixtures to prove one malformed inventory or Identify timeout cannot poison unrelated acquisition/control traffic. Cover all seven outcomes and require matching command ID plus target for confirmed. Add a source/behavior gate that allows canonical `mac_<12hex>` token normalization but rejects any new `/esp32/mac_` publisher construction or publisher lifecycle in the bridge, preserving only existing fixed-role publishers. Retain guarded GPIO, no-delay, prior-state restoration, acquisition/recording non-mutation, relay queue bounds, existing ROS control/range, and dirty-worktree tests. Stage only these three tests and commit atomically.</action>
  <verify>
    <automated>python -m unittest backend.test.test_stepesp_firmware_topology backend.test.test_stepesp_udp_relay backend.test.test_esp32_controls -v</automated>
  </verify>
  <acceptance_criteria>
    - `[protocol]` One protocol matrix proves exact self/peer/count/end semantics in firmware, relay, and bridge.
    - `[binding]` Neither relay nor bridge can bind a session to peer inventory, even when the peer matches an expected ID.
    - `[identify]` Only a correlated target-loop ACK confirms; every non-confirmed outcome remains distinct.
    - `[phase boundary]` Tests fail on any Phase 20 per-MAC publisher instantiation while preserving the canonical token foundation for Phase 21.
    - `[preservation]` All prior acquisition, recording, range, queue, parser, and topology regressions remain green.
  </acceptance_criteria>
  <done>Hardware-independent regression locks the complete identity and confirmed Identify contract across all adjacent layers.</done>
</task>

<task type="auto">
  <name>Task 2: Publish the operator contract and pending physical UAT</name>
  <files>docs/stepesp-identity-identify.md</files>
  <read_first>
    - Phase 20 summaries — actual command tokens, service fields, existing ROS surfaces, board guard, and local test results.
    - `docs/stepesp-wireless-setup.md` — established operator tone and startup workflow.
    - `20-CONTEXT.md` — identity forms, outcomes, GPIO 21 active-low decision, physical evidence boundary, and deferred phases.
  </read_first>
  <action>Create the runbook with canonical/display identity forms; the exact self/peer/count/end transcript; separate role/IP/base/STA/AP/ESP-NOW metadata; expected-self relay/bridge binding; `IDENTITY?` and typed Identify examples; definitions for all seven outcomes; 1-5 second bounds and 3-second default; official XIAO ESP32S3 GPIO 21 active-low guard; mixed-firmware behavior; and existing role-topic inspection. State explicitly that Phase 20 defines/tests `mac_<12hex>` but does not instantiate canonical per-MAC publishers, which Phase 21 owns. Separate fixture/compile evidence from the pending physical check. Include a one-selected-target checklist with another device present for non-target observation, 1/3/5-second blink timing, exact prior-state restoration, and Identify during streaming and SD recording. State that the execute-phase verifier records this as HUMAN-UAT rather than auto-approving it, and that Phase 25 remains the final multi-device capacity/promotion gate. Stage only the new document and commit atomically.</action>
  <verify>
    <automated>python -c "from pathlib import Path; p=Path('docs/stepesp-identity-identify.md'); s=p.read_text(encoding='utf-8'); required=['IDENTITY_OK','record=self','IDENTITY_PEER','record=peer','IDENTITY_END','peer_count','esp32:aabbccddeeff','GPIO 21','active-low','confirmed','sent_unconfirmed','timeout','offline','unsupported','rejected','invalid_target','Phase 21','Phase 25','HUMAN-UAT']; missing=[x for x in required if x not in s]; assert not missing, missing"</automated>
  </verify>
  <acceptance_criteria>
    - `[operator]` Exact identity transcript, Identify service/terminal outcomes, and failure meanings are actionable.
    - `[identity]` Session self, peer inventory, stable identity, and mutable metadata are never conflated.
    - `[hardware boundary]` The document does not claim fixtures or compiles prove physical LED behavior.
    - `[scope]` Per-MAC publishers are assigned to Phase 21 and final hardware capacity/promotion to Phase 25.
  </acceptance_criteria>
  <done>Operators have a reproducible contract and an explicit pending physical UAT checklist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firmware identity inventory -> relay/bridge | Multiple identity rows could be reordered or substituted to spoof the connected device. |
| Local proof -> operator claims | Software evidence can be overstated as physical or fleet-capacity proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-19 | Spoofing | self/peer session binding | high | mitigate | Cross-layer adversarial tests require one self, exact peer count, matching terminator, and self-only binding. |
| T-20-20 | Repudiation | Identify outcomes | high | mitigate | Fixtures retain command ID, target, and exact reason-coded outcome across every layer. |
| T-20-21 | Tampering | Phase 21 publisher boundary | high | mitigate | Source/behavior tests reject Phase 20 per-MAC publisher creation while preserving token helpers. |
| T-20-22 | Repudiation | evidence classification | high | mitigate | Runbook separates automated evidence, pending HUMAN-UAT, and Phase 25 capacity/promotion proof. |
| T-20-SC | Tampering | package/tool installs | low | accept | No install is authorized. |
</threat_model>

<verification>Run all three hardware-independent suites and validate the runbook's protocol, outcome, phase-boundary, and HUMAN-UAT terms.</verification>
<success_criteria>Cross-layer tests prevent peer misbinding and false confirmation, no canonical publisher lifecycle enters Phase 20, and physical evidence remains explicitly pending rather than auto-approved.</success_criteria>
<output>Create `.planning/phases/20-full-identity-and-confirmed-identify/20-05-SUMMARY.md` when done.</output>
