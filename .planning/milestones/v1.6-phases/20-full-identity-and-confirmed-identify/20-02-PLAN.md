---
phase: 20-full-identity-and-confirmed-identify
plan: "02"
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - scripts/stepesp_tcp_udp_relay.py
  - scripts/start_stepesp_wireless.ps1
  - backend/test/test_stepesp_udp_relay.py
autonomous: true
requirements: [ID-01, ID-03]
must_haves:
  truths:
    - "A relay session is keyed by a strictly parsed canonical device identity, while IP endpoint and Master/Slave role remain mutable metadata."
    - "The relay binds a connection only from IDENTITY_OK record=self and keeps IDENTITY_PEER rows as inventory; a peer row can never become the connected session identity."
    - "DHCP or reconnect changes preserve the same device identity; a different full MAC at an old endpoint is never mutated into the old device."
    - "Identity and Identify lines pass unchanged and one route's timeout cannot block another live route."
  artifacts:
    - path: "scripts/stepesp_tcp_udp_relay.py"
      provides: "Identity-confirmed relay session metadata and transparent Identify forwarding"
    - path: "scripts/start_stepesp_wireless.ps1"
      provides: "Identity-probed fixed-role launch without DHCP-order selection"
    - path: "backend/test/test_stepesp_udp_relay.py"
      provides: "Hardware-free route/reconnect/collision/failure-isolation fixtures"
  key_links:
    - from: "scripts/start_stepesp_wireless.ps1"
      to: "scripts/stepesp_tcp_udp_relay.py"
      via: "expected canonical identity and separately resolved endpoint arguments"
      pattern: "device.id|expected.*identity|esp32:"
    - from: "scripts/stepesp_tcp_udp_relay.py"
      to: "firmware IDENTITY_OK / IDENTIFY_* lines"
      via: "strict handshake parser plus byte-transparent forwarding"
      pattern: "IDENTITY_OK|IDENTIFY_ACK|IDENTIFY_ERR"
---

<objective>
Bind the Windows relay's mutable routes to verified full identities and preserve correlated Identify replies.

Purpose: Eliminate DHCP/discovery-order identity changes without expanding Phase 20 into the Phase 21 N-route fleet manager.
Output: Strict relay identity handshake, identity-aware legacy launcher, and failure-isolated async fixtures.
</objective>

<execution_context>
@C:/Users/justi/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/justi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-full-identity-and-confirmed-identify/20-01-SUMMARY.md
@.planning/phases/20-full-identity-and-confirmed-identify/20-CONTEXT.md
@.planning/phases/20-full-identity-and-confirmed-identify/20-PATTERNS.md
@scripts/stepesp_tcp_udp_relay.py
@scripts/start_stepesp_wireless.ps1
@backend/test/test_stepesp_udp_relay.py

<interfaces>
- Firmware identity sequence: exactly one `IDENTITY_OK protocol=id-v1 record=self peer_count=N device_id=...`, exactly N `IDENTITY_PEER protocol=id-v1 record=peer ...` rows, then `IDENTITY_END protocol=id-v1 peer_count=N`.
- Firmware Identify terminal lines: `IDENTIFY_ACK` or `IDENTIFY_ERR` with protocol, command_id, target, outcome, duration_ms, and detail.
- Relay remains two compatibility sessions in Phase 20; Phase 21 owns the generalized N-route registry.
</interfaces>

<worktree_rules>
The relay and relay test are untracked user files and the launcher is operationally sensitive. Read each current file and inspect status/diff before editing. Patch functions and parameters in place, retain all current transport translation and queue behavior, do not switch branches, stage only exact task paths, and commit each task atomically.
</worktree_rules>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make relay sessions identity-confirmed and control-transparent</name>
  <files>scripts/stepesp_tcp_udp_relay.py, backend/test/test_stepesp_udp_relay.py</files>
  <read_first>
    - `scripts/stepesp_tcp_udp_relay.py` — `StepEspRelay` ownership/cleanup, handshake forwarding, control-prefix logging, `UdpRouter` queues, CLI construction.
    - `backend/test/test_stepesp_udp_relay.py` — existing isolated async route fixtures and source import pattern.
    - `20-01-SUMMARY.md` — exact emitted id-v1 and identify-v1 tokens implemented by firmware.
    - `20-CONTEXT.md` D-20-01 through D-20-05 and D-20-08 — identity mutation, acknowledgement, outcome, replay, and mixed-firmware rules.
    - `20-PATTERNS.md` relay assignment — session metadata, identity gate, and failure-isolation constraints.
  </read_first>
  <behavior>
    - "Exactly 12 hex digits normalize to one canonical ID and display MAC; malformed, partial, or unknown identity lines never bind a verified route."
    - "A peer inventory row that arrives before self, substitutes for self, duplicates self, exceeds the advertised count, or lacks a matching terminator fails the handshake."
    - "Same full MAC at a new endpoint preserves the canonical key; different full MAC at an old endpoint is a distinct identity."
    - "Two IDs sharing low 32 bits remain distinct."
    - "IDENTIFY_ACK/ERR bytes are forwarded unchanged and one stalled route does not block another route."
  </behavior>
  <action>Add pure strict id-v1 parsing/normalization helpers plus separate `SessionIdentity` and `PeerIdentity` records containing canonical device ID, display/base/STA/AP/ESP-NOW transport MACs, role, capabilities, verification state, and current endpoint where applicable. During each ESP session, issue `IDENTITY?` before enabling the identity-confirmed route and consume the complete ordered sequence defined by Plan 01: exactly one bounded `IDENTITY_OK record=self`, exactly its advertised `peer_count` of `IDENTITY_PEER record=peer` rows, then a matching `IDENTITY_END`. Bind the connection only from the self record; peer rows populate inventory metadata and are never eligible for session binding. Reject missing/duplicate self, peer-before-self, count mismatch, duplicate peer IDs, malformed rows, or a missing/mismatched terminator. Never infer identity from `name`, IP, discovery order, or low-32 data. Preserve legacy acquisition by representing old firmware as explicitly unverified/unsupported rather than falsely verified. Keep endpoint changes metadata-only for an equal self ID; reject/quarantine a changed self identity within a session. Add IDENTITY/IDENTIFY prefixes to logging and forwarding without rewriting bytes or manufacturing an ACK. Preserve shared UDP port, bounded per-route queues, transport translation, and independent worker cleanup. Extend async fixtures for self-versus-peer binding, reordered/missing/duplicate records, count/terminator mismatch, DHCP rebind, old-IP/new-MAC, low-32 collision, malformed/changed identity, old firmware, unchanged Identify replies, and cross-route timeout isolation. Preservation gate: reread both current files, patch only the session/handshake/prefix/test regions, stage only these two files, and commit atomically.</action>
  <verify>
    <automated>python -m unittest backend.test.test_stepesp_udp_relay -v; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; python -m py_compile scripts/stepesp_tcp_udp_relay.py backend/test/test_stepesp_udp_relay.py</automated>
  </verify>
  <acceptance_criteria>
    - `[identity]` A verified relay session exposes canonical identity independently of name, endpoint, role, and transport MAC.
    - `[inventory]` The relay accepts only a complete counted self/peer/end sequence and binds only the self record.
    - `[reconnect]` Same-MAC endpoint change retains one key; changed MAC is not merged; low-32 collisions remain distinct.
    - `[identify]` Firmware terminal replies remain byte-identical and no relay/link event upgrades an outcome to confirmed.
    - `[isolation]` Tests prove malformed or stalled identity on one route does not block another route's forwarding.
    - `[compatibility]` UDP queue bounds, handshake ordering, transport translation, and two legacy sessions remain intact.
  </acceptance_criteria>
  <done>The relay owns identity-confirmed sessions without making mutable routes or role labels persistent identities.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Select wireless routes by verified identity instead of DHCP order</name>
  <files>scripts/start_stepesp_wireless.ps1, backend/test/test_stepesp_udp_relay.py</files>
  <read_first>
    - `scripts/start_stepesp_wireless.ps1` — current parameters, Wi-Fi join, auto Slave ping scan, relay arguments, and fixed Master/Slave ROS launches.
    - `backend/test/test_stepesp_udp_relay.py` — Task 1 parser/session fixtures and any existing launcher source assertions.
    - `scripts/stepesp_tcp_udp_relay.py` — Task 1 canonical parser and CLI argument names.
    - `20-CONTEXT.md` D-20-01/D-20-02/D-20-08 — full identity, metadata separation, and legacy compatibility.
    - `20-PATTERNS.md` launcher assignment — replace first-responsive selection while preserving aliases.
  </read_first>
  <behavior>
    - "Ping may discover candidates but only an id-v1 response can select one."
    - "Optional expected Master/Slave canonical IDs select the exact device; ambiguous auto-discovery fails closed."
    - "Fixed master/slave aliases remain role labels and verified IDs are passed separately."
  </behavior>
  <action>Add optional canonical expected-ID parameters for Master and legacy Slave, validate their exact `esp32:` form, and probe every candidate endpoint using the bounded, complete `IDENTITY_OK record=self` plus counted `IDENTITY_PEER` plus `IDENTITY_END` exchange before launching relays. Replace first-responsive DHCP selection with exact self-identity selection; peer inventory must never satisfy an expected endpoint identity. With no expected Slave ID, accept only one candidate whose self record is verified and fail with all discovered self IDs when ambiguous. Verify the fixed Master endpoint self identity too. Pass expected/verified self identity separately to the relay and ROS bridge launch parameters while retaining `node_id:=master|slave`, ports, logs, OpenSim setup, GUI startup, and all unrelated arguments. Add launcher source/contract assertions to the existing Python test so the hardware-free gate proves ping order and peer inventory cannot select identity and role labels are not rewritten as IDs. Preservation gate: reread both current files, surgically replace only discovery/argument construction, stage these two files only, and commit atomically.</action>
  <verify>
    <automated>python -m unittest backend.test.test_stepesp_udp_relay -v; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; $null = [scriptblock]::Create((Get-Content -Raw scripts/start_stepesp_wireless.ps1)); python -m py_compile scripts/stepesp_tcp_udp_relay.py</automated>
  </verify>
  <acceptance_criteria>
    - `[selection]` DHCP response order never determines the selected device; expected ID selects exact full MAC and ambiguity fails visibly.
    - `[metadata]` Master/Slave role, endpoint, and canonical identity are distinct launch values.
    - `[compatibility]` Existing fixed ports, node aliases, rosbridge/OpenSim/GUI startup, and two-route rollback behavior remain.
    - `[tests]` Relay tests cover arbitrary candidate order and exact canonical-ID selection without network hardware.
    - `[preservation]` No unrelated launcher setup or dirty-worktree file is rewritten/staged.
  </acceptance_criteria>
  <done>The legacy wireless launcher discovers routes but binds them only after full-identity confirmation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ESP TCP endpoint -> Windows relay | An IP endpoint supplies untrusted identity/control text. |
| Windows discovery -> relay/ROS launch | DHCP candidates and operator expected IDs select operational routes. |
| Relay route -> WSL bridge | Binary samples and inline control lines share a stream. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-06 | Spoofing | route identity binding | high | mitigate | Strict complete id-v1 sequence, self-only binding, counted peer inventory, terminator validation, and optional expected-self-ID match precede route verification. |
| T-20-07 | Tampering | session identity changes | high | mitigate | Quarantine changed identity; never mutate the existing device record or infer missing bits. |
| T-20-08 | Information disclosure | relay logs | medium | mitigate | Log bounded token-safe identity/outcome metadata only; never dump unbounded binary payloads. |
| T-20-09 | Denial of service | multi-route relay | high | mitigate | Bounded independent queues/timeouts and tests proving one route cannot block the other. |
| T-20-10 | Repudiation | Identify forwarding | high | mitigate | Forward correlated firmware replies unchanged; relay never manufactures confirmation. |
| T-20-SC | Tampering | package/tool installs | low | accept | No package install or dependency change is authorized. |
</threat_model>

<verification>
Run isolated relay tests, Python compile checks, and PowerShell parse validation. These hardware-free checks cover arbitrary order, DHCP rebind, changed identity, low-32 collision, malformed/legacy identity, Identify transparency, and route isolation.
</verification>

<success_criteria>
The current two compatibility routes bind through full identity rather than IP/order, Identify results remain application-authored, and existing UDP/acquisition startup behavior is preserved.
</success_criteria>

<output>Create `.planning/phases/20-full-identity-and-confirmed-identify/20-02-SUMMARY.md` when done.</output>
