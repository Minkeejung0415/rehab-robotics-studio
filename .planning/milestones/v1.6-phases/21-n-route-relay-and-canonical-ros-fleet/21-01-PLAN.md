---
phase: 21-n-route-relay-and-canonical-ros-fleet
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/stepesp_tcp_udp_relay.py
  - scripts/start_stepesp_wireless.ps1
  - backend/test/test_stepesp_udp_relay.py
  - docs/stepesp-wireless-setup.md
autonomous: true
requirements: [ID-02, FLEET-03]
must_haves:
  truths:
    - "The Windows relay accepts the Master plus every verified Slave self-identity up to the firmware peer slot cap of 6, each on an isolated TCP listen port and shared UDP demux."
    - "A device keeps the same canonical identity binding when its DHCP/IP endpoint refreshes; topic tokens are never derived from IP or discovery order."
    - "Duplicate MAC discovery fails closed, and a new verified self at a formerly occupied endpoint leaves the prior MAC offline while registering the new MAC as distinct."
    - "Per-route UDP queues remain maxsize=256 drop-oldest and continue forwarding independently when another route is absent or broken."
  artifacts:
    - path: "scripts/stepesp_tcp_udp_relay.py"
      provides: "N-slave identity-bound relay sessions, host remap, drop-oldest queues"
    - path: "scripts/start_stepesp_wireless.ps1"
      provides: "Discovery and launch of all verified slaves ≤6 with expected IDs and listen ports"
    - path: "backend/test/test_stepesp_udp_relay.py"
      provides: "Hardware-free multi-route identity, demux, and launcher contract tests"
  key_links:
    - from: "scripts/start_stepesp_wireless.ps1"
      to: "scripts/stepesp_tcp_udp_relay.py"
      via: "repeatable slave route args with expected_device_id per host"
      pattern: "slave-route|slave-host|expected-device-id"
    - from: "SessionIdentityRegistry.bind"
      to: "UdpRouter.routes"
      via: "canonical device_id stable across current_endpoint refresh"
      pattern: "current_endpoint|device_id"
---

<objective>
Extend the STEP_ESP32 Windows relay and wireless launcher so every verified Slave (≤6) gets an identity-keyed TCP/UDP route without ambiguous first-responder selection.

Purpose: Make N physical devices independently routable before ROS fleet publishers exist.
Output: Multi-slave relay CLI, launcher discovery that routes all verified slaves, and deterministic relay/launcher tests.
</objective>

<execution_context>
@C:/Users/justi/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/justi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-n-route-relay-and-canonical-ros-fleet/21-CONTEXT.md
@.planning/phases/21-n-route-relay-and-canonical-ros-fleet/21-RESEARCH.md
@scripts/stepesp_tcp_udp_relay.py
@scripts/start_stepesp_wireless.ps1
@backend/test/test_stepesp_udp_relay.py
@docs/stepesp-wireless-setup.md

<locked_decisions>
- D-21-01: Route all verified slave self-identities discovered on STEP_ESP32, capped at firmware peer slot limit 6.
- D-21-02: Fail closed on duplicate MAC; if a known route later reports a different stable identity, retain the original as offline and register the new identity as distinct.
- D-21-03: Master at 192.168.4.1 and Windows STA remain required; slaves are N additional TCP/UDP routes.
- D-21-04: Bind each relay session to canonical esp32:aabbccddeeff and refresh IP without changing the canonical identity key.
- D-21-10 (partial): Keep per-route bounded UDP queues (maxsize=256, drop-oldest); drop_count exposure completes in plan 04.
</locked_decisions>

<interfaces>
From scripts/stepesp_tcp_udp_relay.py:
- SessionIdentity / SessionIdentityRegistry.bind(session) — MAC-keyed, endpoint mutable
- StepEspRelay(name, esp_host, esp_port, expected_device_id=..., identity_registry=...)
- UdpRouter(udp_port, routes: dict[str, StepEspRelay]) with Queue(maxsize=256) drop-oldest
- CLI today: --slave-host, --slave-listen-port, --slave-expected-device-id (singular)

From scripts/start_stepesp_wireless.ps1:
- -ExpectedSlaveDeviceId singular; throws when verifiedSlaveCandidates.Count -gt 1
- RelayPort=5002, SlaveRelayPort=5003, UdpPort=55001
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Multi-slave identity-bound relay routes</name>
  <files>scripts/stepesp_tcp_udp_relay.py, backend/test/test_stepesp_udp_relay.py</files>
  <read_first>
    - scripts/stepesp_tcp_udp_relay.py — StepEspRelay, SessionIdentityRegistry, UdpRouter, run_relays, argparse
    - backend/test/test_stepesp_udp_relay.py — identity and dual-route demux fixtures
    - 21-CONTEXT.md multi-slave discovery decisions
    - 21-RESEARCH.md relay CLI and DHCP remap guidance
  </read_first>
  <behavior>
    - "Repeatable slave route configuration accepts up to 6 slaves with distinct hosts, listen ports, and expected device_ids."
    - "Mismatched list lengths, duplicate hosts/ports/device_ids, or slave count greater than 6 fail closed before serve."
    - "Three or more source IPs demux independently into their downstream writers."
    - "Updating a session endpoint/IP keeps the same device_id registry key; a different verified self at an endpoint marks the prior MAC offline (current_endpoint=None) and binds the new MAC."
    - "Singular --slave-host compatibility remains for one-slave launches."
  </behavior>
  <action>Per D-21-01 through D-21-04: first extend test_stepesp_udp_relay.py with failing contracts for N-route CLI parsing, 3-host UdpRouter demux, DHCP/endpoint refresh without identity change, and distinct-identity-at-old-endpoint offline retention. Then extend stepesp_tcp_udp_relay.py so run_relays builds one StepEspRelay per master plus each verified slave route. Prefer a repeatable --slave-route HOST:LISTEN_PORT:EXPECTED_DEVICE_ID form (or equal-length repeatable --slave-host/--slave-listen-port/--slave-expected-device-id lists — pick one and document in argparse help). Cap slaves at 6. Keep UdpRouter drop-oldest maxsize=256. Add a safe remap helper so when SessionIdentityRegistry refreshes current_endpoint, UdpRouter host keys/queues follow the new IP without dropping the identity binding. Do not yet require drop_count metrics (plan 04). Preserve byte-identical Identify/binary forwarding and existing dual-route tests.</action>
  <verify>
    <automated>python -m unittest backend.test.test_stepesp_udp_relay -v</automated>
  </verify>
  <done>Relay can own Master + N≤6 identity-bound slave sessions with isolated UDP demux and stable MAC keys across IP refresh.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Launcher discovers and routes all verified slaves</name>
  <files>scripts/start_stepesp_wireless.ps1, backend/test/test_stepesp_udp_relay.py, docs/stepesp-wireless-setup.md</files>
  <read_first>
    - scripts/start_stepesp_wireless.ps1 — candidate ping, identity probe, ambiguity throw, relay spawn args
    - backend/test/test_stepesp_udp_relay.py — launcher source-contract tests
    - docs/stepesp-wireless-setup.md — current one-slave docs
    - 21-CONTEXT.md D-21-01/D-21-03
  </read_first>
  <behavior>
    - "When multiple verified slave selves are discovered, the launcher routes all of them up to 6 instead of requiring ExpectedSlaveDeviceId solely to disambiguate."
    - "Duplicate MAC across candidates or slave identity equal to master fails closed."
    - "More than 6 verified slaves fails closed citing the firmware peer slot limit."
    - "Optional ExpectedSlaveDeviceIds filter may restrict the set; absence means route-all-verified."
    - "Listen ports allocate contiguously from SlaveRelayPort (default 5003)."
    - "Relay process receives every selected slave host, listen port, and expected device_id."
  </behavior>
  <action>Per D-21-01 and D-21-03: replace the Count -gt 1 hard throw with route-all-verified logic capped at 6. Add optional multi-ID filter parameter (e.g. -ExpectedSlaveDeviceIds) without removing the singular -ExpectedSlaveDeviceId alias if useful for COMP-01. Exclude Windows STA and master host from slave candidates. Allocate listen ports as SlaveRelayPort + index. Build relay argument list for N slaves using the Task 1 CLI. Keep MasterHost default 192.168.4.1. Update launcher source-contract tests in test_stepesp_udp_relay.py and refresh docs/stepesp-wireless-setup.md for N-slave discovery. Do not yet switch ROS to a single fleet process (plans 02–03); temporary dual-bridge launch for the first two slaves is acceptable only if clearly marked transitional — prefer leaving bridge spawn lines untouched until plan 03 if that avoids thrash, but relay must receive all N routes now.</action>
  <verify>
    <automated>python -m unittest backend.test.test_stepesp_udp_relay -v</automated>
  </verify>
  <done>Launcher discovers every verified slave ≤6, fails closed on duplicates/overflow, and starts the multi-route relay with identity-stable args.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ESP TCP/UDP → Windows relay | Untrusted firmware/network payloads enter host routing |
| Discovery probe → launcher selection | Candidate IPs must not become identity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Spoofing | relay identity bind | mitigate | Bind only verified id-v1 record=self; expected_device_id mismatch fails closed |
| T-21-02 | Tampering | duplicate MAC / endpoint swap | mitigate | Fail closed on duplicate MAC; displaced prior MAC marked offline, new MAC distinct |
| T-21-03 | Denial of service | UDP flood on one host | mitigate | Per-route Queue(maxsize=256) drop-oldest; other routes unaffected |
| T-21-04 | Elevation | peer inventory as session | mitigate | Peer rows never create relay sessions |
| T-21-SC | Tampering | package installs | accept | No new packages in this plan |
</threat_model>

<verification>
python -m unittest backend.test.test_stepesp_udp_relay -v
</verification>

<success_criteria>
- Relay CLI and UdpRouter support Master + N≤6 identity-bound slaves
- Launcher routes all verified slaves (or filtered subset) without first-responder selection
- IP/endpoint refresh preserves device_id; identity change retains prior offline MAC
- Existing dual-route and identity contract tests still pass
</success_criteria>

<output>
Create `.planning/phases/21-n-route-relay-and-canonical-ros-fleet/21-01-SUMMARY.md` when done
</output>
