---
phase: 21-n-route-relay-and-canonical-ros-fleet
plan: "03"
type: execute
wave: 3
depends_on: ["21-01", "21-02"]
files_modified:
  - backend/rehab_robotics_bridge/fleet_bridge_node.py
  - backend/rehab_robotics_bridge/esp32_bridge_node.py
  - scripts/start_stepesp_wireless.ps1
  - backend/launch/rehab_robotics.launch.py
  - backend/test/test_fleet_bridge.py
  - backend/test/test_stepesp_udp_relay.py
  - docs/stepesp-identity-identify.md
  - docs/stepesp-wireless-setup.md
autonomous: true
requirements: [FLEET-02, ID-02]
must_haves:
  truths:
    - "Fixed /esp/raw/master, /esp/raw/slave, /esp/status/master, and /esp/status/slave remain explicit aliases bound to configured identities and carry the same payload as the matching canonical mac_ topics."
    - "Alias binding comes from alias_master_device_id / alias_slave_device_id (or first verified role identities when params unset) — never discovery order alone."
    - "/esp/status/pair continues to publish when aliases are bound; registry remains authoritative for N>2."
    - "Wireless launcher starts one fleet_bridge_node with the full route table and alias parameters."
  artifacts:
    - path: "backend/rehab_robotics_bridge/fleet_bridge_node.py"
      provides: "Alias publishers + pair health when aliases bound"
    - path: "scripts/start_stepesp_wireless.ps1"
      provides: "Single fleet process launch with alias and routes params"
    - path: "backend/test/test_fleet_bridge.py"
      provides: "Alias/canonical payload parity and binding tests"
  key_links:
    - from: "alias_master_device_id"
      to: "/esp/raw/master"
      via: "mirror publish of canonical mac_ payload"
      pattern: "alias_master_device_id|/esp/raw/master"
    - from: "start_stepesp_wireless.ps1"
      to: "fleet_bridge_node"
      via: "routes_json + alias_* params replaces dual esp32_bridge_node"
      pattern: "fleet_bridge_node|alias_master_device_id"
---

<objective>
Wire explicit Master/Slave compatibility aliases, pair health, and launcher/launch parameters onto the fleet manager.

Purpose: Preserve COMP-01 two-sensor workflows while N-device canonical topics remain authoritative.
Output: Alias binding params, mirrored payloads, pair health when aliases bound, and single-process fleet launch wiring.
</objective>

<execution_context>
@C:/Users/justi/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/justi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-n-route-relay-and-canonical-ros-fleet/21-CONTEXT.md
@.planning/phases/21-n-route-relay-and-canonical-ros-fleet/21-RESEARCH.md
@.planning/phases/21-n-route-relay-and-canonical-ros-fleet/21-02-PLAN.md
@scripts/start_stepesp_wireless.ps1
@backend/launch/rehab_robotics.launch.py
@docs/stepesp-identity-identify.md

<locked_decisions>
- D-21-06: Keep /esp/raw|status/master|slave as explicit aliases bound to configured identities with same payload as canonical topics.
- D-21-07: Alias binding owned by launch/bridge parameters (alias_master_device_id / alias_slave_device_id, or first verified role identities) — never whoever connected first.
- D-21-16: Keep /esp/status/pair publishing when aliases are bound for COMP-01; registry authoritative for N>2.
</locked_decisions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Explicit identity-bound Master/Slave aliases and pair health</name>
  <files>backend/rehab_robotics_bridge/fleet_bridge_node.py, backend/rehab_robotics_bridge/esp32_bridge_node.py, backend/test/test_fleet_bridge.py</files>
  <read_first>
    - backend/rehab_robotics_bridge/esp32_bridge_node.py — current /esp/raw/{node_id}, pair health publisher
    - backend/rehab_robotics_bridge/fleet_bridge_node.py — canonical publishers and registry from plan 02
    - 21-CONTEXT.md alias decisions
  </read_first>
  <behavior>
    - "When alias_master_device_id is set to a bound device, /esp/raw/master and /esp/status/master publish identical payloads to that device's mac_ topics."
    - "When alias_slave_device_id is set, /esp/raw/slave and /esp/status/slave mirror that device's canonical payloads."
    - "If alias params are unset, bind aliases to the first verified master-role and first verified slave-role identities respectively — still explicit role-based selection, not TCP connect order."
    - "Unbound or offline alias targets make alias topics go stale/unavailable without tearing down other devices."
    - "When both aliases are bound, /esp/status/pair publishes oe_esp32.pair_health.v1 (or current pair schema) derived from those identities."
  </behavior>
  <action>Per D-21-06, D-21-07, D-21-16: add ROS parameters alias_master_device_id and alias_slave_device_id. Implement alias publishers that mirror canonical String payloads (and document whether /esp32/master|slave imu topics also mirror — preserve existing OpenSim launch expectations if still consumed). Resolve default aliases from verified role metadata when params empty. Publish /esp/status/pair only when aliases are bound. Include alias_* fields on registry JSON. Extend test_fleet_bridge.py for payload parity and non-first-connect binding.</action>
  <verify>
    <automated>python -m unittest backend.test.test_fleet_bridge backend.test.test_esp32_controls -v</automated>
  </verify>
  <done>Aliases and pair health are identity-bound and payload-parity tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Launcher and launch params for single fleet process</name>
  <files>scripts/start_stepesp_wireless.ps1, backend/launch/rehab_robotics.launch.py, backend/test/test_stepesp_udp_relay.py, docs/stepesp-wireless-setup.md, docs/stepesp-identity-identify.md</files>
  <read_first>
    - scripts/start_stepesp_wireless.ps1 — dual esp32_bridge_node spawn lines
    - backend/launch/rehab_robotics.launch.py — existing bridge params if any
    - backend/test/test_stepesp_udp_relay.py — launcher contracts expecting dual bridges
    - docs/stepesp-wireless-setup.md and docs/stepesp-identity-identify.md
  </read_first>
  <behavior>
    - "Launcher starts one fleet_bridge_node with routes for master + all selected slaves and passes alias_master_device_id / alias_slave_device_id."
    - "Dual esp32_bridge_node master/slave process spawn is no longer the default wireless path."
    - "GUI/OpenSim env still can consume /esp/raw/master and /esp/raw/slave aliases."
    - "Launcher source contracts and docs describe N-route fleet launch without requiring STEP_ESP32 for agent verification."
  </behavior>
  <action>Per D-21-07 and D-21-13: replace dual WSL bridge Start-Process lines with one fleet_bridge_node invocation carrying routes_json (or equivalent), alias_master_device_id=$verifiedMasterDeviceId, and alias_slave_device_id set to the first selected verified slave (or operator override). Preserve rosbridge, OpenSim, processing observer, and GUI startup. Update launcher tests that currently assert two esp32_bridge_node lines. Update launch file parameters if the ROS launch path is used for fleet mode. Refresh wireless and identity docs for aliases + registry topic. Do not claim Phase 24 UI work.</action>
  <verify>
    <automated>python -m unittest backend.test.test_stepesp_udp_relay backend.test.test_fleet_bridge -v</automated>
  </verify>
  <done>Wireless stack launches one fleet manager with explicit alias bindings and documented contracts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Launch params → alias binding | Operator-supplied device_ids select compatibility topics |
| Alias topics → legacy GUI/OpenSim | Compatibility surface must not silently rebind |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-09 | Spoofing | alias topics | mitigate | Aliases bind only to verified known device_ids; never first TCP client |
| T-21-10 | Tampering | launcher route table | mitigate | Expected IDs from verified probes; fail closed on duplicates |
| T-21-11 | Repudiation | pair health | accept | Pair health is telemetry; registry is authoritative for N>2 |
| T-21-SC | Tampering | package installs | accept | No new packages |
</threat_model>

<verification>
python -m unittest backend.test.test_fleet_bridge backend.test.test_stepesp_udp_relay backend.test.test_esp32_controls -v
</verification>

<success_criteria>
- Master/Slave aliases mirror canonical payloads for configured identities
- Pair health publishes when aliases bound
- Launcher uses one fleet_bridge_node with alias and route params
- Docs and launcher contracts updated
</success_criteria>

<output>
Create `.planning/phases/21-n-route-relay-and-canonical-ros-fleet/21-03-SUMMARY.md` when done
</output>
