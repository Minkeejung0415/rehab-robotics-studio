---
phase: 21-n-route-relay-and-canonical-ros-fleet
plan: "02"
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - backend/rehab_robotics_bridge/fleet_bridge_node.py
  - backend/rehab_robotics_bridge/esp32_bridge_node.py
  - backend/setup.py
  - backend/test/test_esp32_controls.py
  - backend/test/test_fleet_bridge.py
autonomous: true
requirements: [ID-02, FLEET-01, FLEET-02]
must_haves:
  truths:
    - "Each bound device publishes IMU/health on /esp/raw/mac_<12hex> and /esp/status/mac_<12hex> derived from device_topic_token, stable across reconnect and DHCP."
    - "One fleet manager process owns all identity sessions and publishes aggregate /esp/fleet/registry JSON with layered discovery/command/route/orientation_freshness/synchronization/rate fields."
    - "Role, IP, and transport MAC remain metadata on registry/health rows and never redefine the topic token."
  artifacts:
    - path: "backend/rehab_robotics_bridge/fleet_bridge_node.py"
      provides: "Multi-session fleet manager + registry publisher"
    - path: "backend/rehab_robotics_bridge/esp32_bridge_node.py"
      provides: "Shared session/helpers; device_topic_token used by canonical publishers"
    - path: "backend/test/test_fleet_bridge.py"
      provides: "Deterministic registry and canonical topic contracts"
  key_links:
    - from: "device_topic_token(device_id)"
      to: "/esp/raw/mac_<12hex>"
      via: "create_publisher lifecycle keyed by canonical identity"
      pattern: "device_topic_token|/esp/raw/mac_"
    - from: "fleet sessions"
      to: "/esp/fleet/registry"
      via: "periodic JSON snapshot oe_esp32.fleet_registry.v1"
      pattern: "oe_esp32.fleet_registry.v1"
---

<objective>
Introduce a single multi-session fleet bridge that publishes canonical per-MAC topics and a layered MAC-keyed fleet registry.

Purpose: Make every known IMU observable on stable identity topics for operators and later mapping UI.
Output: fleet_bridge_node entry point, canonical publishers, registry schema, and unit tests; legacy aliases wired in plan 03.
</objective>

<execution_context>
@C:/Users/justi/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/justi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-n-route-relay-and-canonical-ros-fleet/21-CONTEXT.md
@.planning/phases/21-n-route-relay-and-canonical-ros-fleet/21-RESEARCH.md
@.planning/phases/21-n-route-relay-and-canonical-ros-fleet/21-01-PLAN.md
@backend/rehab_robotics_bridge/esp32_bridge_node.py
@backend/setup.py
@backend/test/test_esp32_controls.py

<locked_decisions>
- D-21-05: Canonical topics use device_topic_token → /esp/raw/mac_<12hex> and /esp/status/mac_<12hex>.
- D-21-08: Publish one aggregate /esp/fleet/registry (String/JSON).
- D-21-12: Retain offline/stale MAC rows with last-seen; do not drop solely because TCP died.
- D-21-13: Prefer one fleet manager / multi-session bridge owning all identity routes and the registry.
- D-21-14: Registry rows expose layered discovery, command, route, orientation freshness, synchronization, rate (plus drops/reconnects in plan 04).
- D-21-15: Backend/ROS contracts first; full multi-row mapping UI is Phase 24.
</locked_decisions>

<interfaces>
From esp32_bridge_node.py:
- normalize_device_id / display_mac / device_topic_token(value) → mac_<12hex>
- Esp32BridgeNode health schema oe_esp32.health.v1 with connection_state, reconnect_count, identity snapshot
- Bind only complete verified self; peer inventory capped

Discretion (RESEARCH):
- Registry schema name oe_esp32.fleet_registry.v1
- New console_script fleet_bridge_node; keep esp32_bridge_node as thin single-session wrapper
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fleet registry builder and canonical topic contracts</name>
  <files>backend/test/test_fleet_bridge.py, backend/rehab_robotics_bridge/fleet_bridge_node.py, backend/rehab_robotics_bridge/esp32_bridge_node.py</files>
  <read_first>
    - backend/rehab_robotics_bridge/esp32_bridge_node.py — device_topic_token, health_snapshot, identity bind, publishers
    - backend/test/test_esp32_controls.py — Phase 20 bans on mac_ publisher lifecycle (must be updated carefully)
    - 21-RESEARCH.md registry JSON proposal
  </read_first>
  <behavior>
    - "device_topic_token('esp32:aabbccddeeff') yields publishers on /esp/raw/mac_aabbccddeeff and /esp/status/mac_aabbccddeeff."
    - "Registry JSON schema is oe_esp32.fleet_registry.v1 with devices[] keyed by device_id and layered state fields present on every row."
    - "Offline devices remain in devices[] with route offline/stale and last_seen populated."
    - "Two MACs that share low-32 bits still get distinct topic tokens."
  </behavior>
  <action>Per D-21-05, D-21-08, D-21-12, D-21-14: create backend/test/test_fleet_bridge.py with failing unit tests for registry serialization and canonical topic token wiring (no live ROS master required — pure helpers / stub publishers acceptable). Implement fleet_bridge_node.py (or shared helpers imported by it) that builds registry documents matching RESEARCH schema. Reuse device_topic_token from esp32_bridge_node; do not reimplement MAC normalization. Factor session-scoped publish helpers if needed without breaking single-node Esp32BridgeNode construction. Update test_esp32_controls.py Phase 20 “no mac_ publishers yet” assertions so they allow Phase 21 lifecycle in fleet code while still forbidding accidental /esp32/mac_ prefixes if that contract remains intentional.</action>
  <verify>
    <automated>python -m unittest backend.test.test_fleet_bridge backend.test.test_esp32_controls -v</automated>
  </verify>
  <done>Registry builder and canonical topic token contracts exist with failing-then-passing unit coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Multi-session fleet_bridge_node entry point</name>
  <files>backend/rehab_robotics_bridge/fleet_bridge_node.py, backend/rehab_robotics_bridge/esp32_bridge_node.py, backend/setup.py, backend/test/test_fleet_bridge.py</files>
  <read_first>
    - backend/rehab_robotics_bridge/esp32_bridge_node.py — TCP session loop, reconnect, frame publish, Identify service
    - backend/setup.py — console_scripts
    - backend/rehab_robotics_bridge/imu_aggregator_node.py — anti-pattern (do not copy incomplete construction)
    - 21-CONTEXT.md D-21-13
  </read_first>
  <behavior>
    - "fleet_bridge_node accepts a route table parameter (JSON string or structured params) listing host/port/expected_device_id for Master and each Slave."
    - "One process owns all sessions; each session publishes canonical mac_ topics after verified bind."
    - "Registry topic /esp/fleet/registry updates when membership or layered states change."
    - "A session reporting a different self identity retains the prior MAC as offline and registers the new MAC."
    - "esp32_bridge_node remains importable/runnable as a single-session wrapper for debug/COMP-01."
  </behavior>
  <action>Per D-21-13 and D-21-05: implement fleet_bridge_node as the primary multi-session owner. Declare ROS parameters for routes (e.g. routes_json) plus optional body_segment map keyed by device_id. Each session performs the same verified-self bind rules as Esp32BridgeNode. Publish canonical /esp/raw|status/mac_* and retain /esp32/{topic_token}/imu|raw or keep role-based /esp32 prefixes only for aliases in plan 03 — for this plan, ensure at least canonical String raw/health topics exist. Publish /esp/fleet/registry periodically. Register console_script in setup.py. Do not implement alias_master_device_id / pair health here (plan 03). Do not require STEP_ESP32 hardware — tests use stubs/fakes for sessions.</action>
  <verify>
    <automated>python -m unittest backend.test.test_fleet_bridge backend.test.test_esp32_controls -v</automated>
  </verify>
  <done>One fleet manager process can own multiple identity sessions, publish canonical mac_ topics, and emit layered registry JSON.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Relay listen ports → fleet sessions | Host/port table is operator-configured; payloads still untrusted |
| Fleet registry → GUI/rosbridge subscribers | Registry is operator-visible telemetry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-05 | Spoofing | canonical publishers | mitigate | Publishers created only after verified self bind matching expected_device_id |
| T-21-06 | Information disclosure | registry JSON | accept | Local ROS domain; no secrets; MAC identities are operational identifiers |
| T-21-07 | Denial of service | multi-session loops | mitigate | Per-session reconnect; one failed session must not stop others (enforced/tested in plan 04) |
| T-21-08 | Tampering | topic token | mitigate | Token solely from normalize_device_id; never from IP/role/order |
| T-21-SC | Tampering | package installs | accept | No new packages |
</threat_model>

<verification>
python -m unittest backend.test.test_fleet_bridge backend.test.test_esp32_controls -v
</verification>

<success_criteria>
- Canonical per-MAC raw/status topics exist and are identity-stable
- /esp/fleet/registry publishes oe_esp32.fleet_registry.v1 with layered states
- Offline MACs retained; single fleet process owns sessions
- esp32_bridge_node remains as thin single-session entry
</success_criteria>

<output>
Create `.planning/phases/21-n-route-relay-and-canonical-ros-fleet/21-02-SUMMARY.md` when done
</output>
