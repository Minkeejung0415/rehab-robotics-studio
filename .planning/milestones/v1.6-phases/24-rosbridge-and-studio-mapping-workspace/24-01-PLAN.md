---
phase: 24-rosbridge-and-studio-mapping-workspace
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - rehab-robotics-studio/src/state/mappingStore.ts
  - rehab-robotics-studio/src/state/mappingStore.test.ts
autonomous: true
requirements:
  - UI-01
  - UI-03
  - UI-04

must_haves:
  truths:
    - "useMappingStore holds all typed MappingRow and MappingStore state per D-04"
    - "updateFromFleetRegistry upserts rows by deviceId without clearing backend assignment fields (D-06)"
    - "updateFromMappingCurrent overwrites backendSegment/backendFrame/backendState without clearing draftSegment (D-06)"
    - "mappingStatus computed correctly for all five states per D-07 rules on every store update"
    - "clearDraft removes draftSegment/draftFrame/draftNotUsed for one device; clearAllDrafts removes for all"
    - "calibrationInterlocked set to true when calibration state is 'capturing' (from updateFromCalibrationStatus)"
    - "setIdentifyResult auto-clears after 5 000 ms via setTimeout in the store action (D-18)"
  artifacts:
    - path: "rehab-robotics-studio/src/state/mappingStore.ts"
      provides: "Zustand store with MappingRow, MappingStore types and all store actions"
      exports: ["useMappingStore", "MappingRow", "MappingStore", "ModelCatalogSnapshot", "MappingCurrentSnapshot", "FleetRegistrySnapshot", "NCalibrationStatusSnapshot", "InputValiditySnapshot"]
    - path: "rehab-robotics-studio/src/state/mappingStore.test.ts"
      provides: "Vitest unit tests covering all store actions and mappingStatus computation"
      contains: "describe('mappingStore'"
  key_links:
    - from: "rehab-robotics-studio/src/state/mappingStore.ts"
      to: "rehab-robotics-studio/src/components/mapping/MappingWorkspace.tsx"
      via: "useMappingStore hook (created in Plan 24-03)"
      pattern: "export.*useMappingStore"
    - from: "rehab-robotics-studio/src/state/mappingStore.ts"
      to: "rehab-robotics-studio/src/data/appDataSource.ts"
      via: "useMappingStore.getState() callbacks wired in Plan 24-04"
      pattern: "useMappingStore\\.getState"
---

<objective>
Create `mappingStore.ts` — the Zustand store holding all UI state for the Mapping Workspace. Covers MappingRow and MappingStore type definitions (D-04), row identity rules (D-05, D-06), computed mappingStatus per D-07, calibration interlock flag, and the 5-second auto-clear for Identify results (D-18). All logic is covered by vitest unit tests written before the implementation (TDD).

Purpose: This store is the single client-side state container for Phase 24. Plans 24-03 and 24-04 depend on it.
Output: `mappingStore.ts` (store) + `mappingStore.test.ts` (unit tests).
</objective>

<execution_context>
@c:/Users/justi/Documents/# Rehab Robotics GUI Prototype__Here's a complete, single-file React + TypeScript app implementing the LabVIEW/rehab-robotics-studio/src/state/systemStore.ts
</execution_context>

<context>
@c:/Users/justi/Documents/# Rehab Robotics GUI Prototype__Here's a complete, single-file React + TypeScript app implementing the LabVIEW/.planning/phases/24-rosbridge-and-studio-mapping-workspace/24-CONTEXT.md
@c:/Users/justi/Documents/# Rehab Robotics GUI Prototype__Here's a complete, single-file React + TypeScript app implementing the LabVIEW/.planning/ROADMAP.md

<interfaces>
<!-- Zustand pattern to replicate — from systemStore.ts -->
```typescript
import { create } from 'zustand';

// Store shape: interface + create<Interface>((set) => ({ ...initialState, ...actions }))
export const useSystemStore = create<SystemStore>((set) => ({
  // state
  status: INITIAL_STATUS,
  // action
  setMotor: (value, level) => set((s) => ({ status: { ...s.status, motor: { ...s.status.motor, value, level } } })),
}));
```

<!-- Topic payload schemas (per 24-CONTEXT.md and 24-UI-SPEC.md) -->
// /rehab/model/catalog   → { schema, model_hash, model_path, frame_list: [{path, name}] }
// /rehab/mapping/current → { schema, revision, applied_revision, model_hash, assignments: {device_id: {segment, frame, state}} }
// /esp/fleet/registry    → { devices: [{device_id, role, route_state, ...}] }
// /rehab/calibration/status → { state: "capturing"|"calibrated"|"uncalibrated", revision, model_hash }
// /rehab/opensim/input_validity → { device_validities: { device_id: bool } }
</interfaces>
</context>

<feature>
  <name>mappingStore — Zustand state for Mapping Workspace</name>
  <files>
    rehab-robotics-studio/src/state/mappingStore.ts,
    rehab-robotics-studio/src/state/mappingStore.test.ts
  </files>
  <behavior>
    RED phase — write these failing tests first:

    1. updateFromFleetRegistry with two devices creates two rows keyed by deviceId; calling again with the same devices upserts (does not duplicate); calling with an empty array does NOT remove existing rows (D-05).
    2. updateFromMappingCurrent sets backendSegment, backendFrame, backendState, mappingRevision, appliedRevision on matching rows; leaves draftSegment untouched if already set.
    3. updateFromCatalog sets catalogModelHash, catalogModelPath, catalogFrameList.
    4. updateInputValidity sets ikValid per device.
    5. mappingStatus computation (D-07):
       - row with backendState='assigned', appliedRevision==mappingRevision, ikValid=true → 'runtime_ready'
       - row with backendState='assigned', appliedRevision==mappingRevision, ikValid=false → 'applied'
       - row with backendState='assigned', appliedRevision < mappingRevision, draftSegment=null → 'saved'
       - row with draftSegment set → 'draft'
       - row with draftNotUsed=true → 'draft'
       - row with backendState='unassigned', no draft → 'unassigned'
    6. setDraftSegment sets draftSegment + draftFrame; setDraftNotUsed(deviceId, true) sets draftNotUsed=true AND sets draftSegment=null.
    7. clearDraft removes draftSegment, draftFrame, draftNotUsed for one device; clearAllDrafts does so for all.
    8. setApplyStatus('error', 'some error') → applyStatus='error', applyError='some error'.
    9. updateFromCalibrationStatus with state='capturing' → calibrationInterlocked=true; state='calibrated' → false.
    10. setIdentifyResult sets identifyResult on the named device; after 5 000 ms (fake timers) it is null.

    GREEN phase — implement the store to pass all tests.
  </behavior>
  <implementation>
    Create `mappingStore.ts` using `create` from zustand.

    Export snapshot types: `ModelCatalogSnapshot`, `MappingCurrentSnapshot`, `FleetRegistrySnapshot`, `NCalibrationStatusSnapshot`, `InputValiditySnapshot` — plain interfaces matching the topic payload schemas above.

    Export `MappingRow` and `MappingStore` interfaces per D-04 exactly. Add one additional field to `MappingStore`: `calibrationInterlocked: boolean` (initialized to false), updated by `updateFromCalibrationStatus`.

    `computeMappingStatus(row: MappingRow, mappingRevision: number, appliedRevision: number): MappingRow['mappingStatus']` — pure helper function, exported for test import. Priority order from D-07: runtime_ready > applied > saved > draft > unassigned.

    `updateFromFleetRegistry(devices: unknown[])`: iterate devices array. For each item that has a string `device_id`, upsert the row in `rows` — create with defaults if absent, overwrite only the fleet-sourced fields: `role`, `connectionState`, `routeState`, `rateHz`, `dropCount`. Never delete rows for devices not present in the list (D-05).

    `updateFromMappingCurrent(state: unknown)`: guard that payload is an object with `revision` (number), `applied_revision` (number), `assignments` (object). Set `mappingRevision`, `appliedRevision`, `mappingModelHash`. For each deviceId in assignments, upsert the row setting `backendSegment`, `backendFrame`, `backendState`. Do NOT touch `draftSegment`, `draftFrame`, or `draftNotUsed`. Recompute `mappingStatus` on every affected row.

    `setIdentifyResult(deviceId, result)`: sets `identifyResult` on the row, then calls `window.setTimeout(() => { /* clear */ }, 5000)` — use `window.setTimeout` so vitest fake timers can intercept it.

    All actions that change rows must recompute `mappingStatus` for affected rows using `computeMappingStatus`.

    Initial state: `rows: {}`, `catalogModelHash: null`, `catalogFrameList: []`, `catalogModelPath: null`, `mappingRevision: 0`, `appliedRevision: 0`, `mappingModelHash: null`, `applyStatus: 'idle'`, `applyError: null`, `calibrationInterlocked: false`.
  </implementation>
</feature>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED): Write failing vitest unit tests for all store actions and mappingStatus computation</name>
  <files>rehab-robotics-studio/src/state/mappingStore.test.ts</files>
  <read_first>
    - rehab-robotics-studio/src/state/systemStore.ts (Zustand create pattern, how tests reference the store)
    - rehab-robotics-studio/src/state/runtimeStore.ts (additional store reference)
    - rehab-robotics-studio/.planning/phases/24-rosbridge-and-studio-mapping-workspace/24-CONTEXT.md (D-04 through D-07, D-18)
  </read_first>
  <behavior>
    - Test: updateFromFleetRegistry with [{device_id: "esp32:aabbccddeeff", role: "master", route_state: "active", connection_state: "connected", rate_hz: 100, drop_count: 0}] creates one row with deviceId="esp32:aabbccddeeff", role="master", connectionState="connected"
    - Test: calling updateFromFleetRegistry with empty array leaves the row from the prior call intact (D-05)
    - Test: updateFromMappingCurrent sets backendSegment on matching row without clearing draftSegment="tibia"
    - Test: computeMappingStatus with backendState='assigned', appliedRevision==mappingRevision, ikValid=true → 'runtime_ready'
    - Test: computeMappingStatus with draftSegment set → 'draft' regardless of backendState
    - Test: clearDraft sets draftSegment=null, draftFrame=null, draftNotUsed=null on the named row
    - Test: setIdentifyResult sets identifyResult; after vi.advanceTimersByTime(5001) it is null (uses vi.useFakeTimers())
    - Test: updateFromCalibrationStatus({state:'capturing'}) → calibrationInterlocked=true; {state:'calibrated'} → false
    - All tests must FAIL before the store is created (import from non-existent file)
  </behavior>
  <action>
    Write `rehab-robotics-studio/src/state/mappingStore.test.ts`.

    Import pattern: `import { useMappingStore, computeMappingStatus } from './mappingStore';`

    Use vitest `describe` / `it` blocks. Use `beforeEach(() => { useMappingStore.setState(useMappingStore.getInitialState()); })` to reset between tests — check if zustand `getInitialState()` is available on the version in use; if not, use `useMappingStore.setState({ rows: {}, ... })` with the full initial state shape.

    For the auto-clear test: call `vi.useFakeTimers()` in `beforeEach`, `vi.useRealTimers()` in `afterEach`, then `vi.advanceTimersByTime(5001)` after calling `setIdentifyResult`.

    Cover all 10 behavior bullets from the feature `<behavior>` block.
  </action>
  <verify>
    <automated>cd "c:/Users/justi/Documents/# Rehab Robotics GUI Prototype__Here's a complete, single-file React + TypeScript app implementing the LabVIEW/rehab-robotics-studio" && npx vitest run src/state/mappingStore.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - mappingStore.test.ts exists at the specified path
    - Running vitest on the test file fails with "Cannot find module './mappingStore'" or similar import error (confirming RED state)
    - Test file imports `useMappingStore` and `computeMappingStatus` from `./mappingStore`
    - At least 10 `it(...)` test cases present covering all behavior bullets
    - vi.useFakeTimers() used in the auto-clear test
  </acceptance_criteria>
  <done>Test file exists with all cases; vitest run shows import failure (RED confirmed).</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 (GREEN): Implement mappingStore.ts to pass all tests</name>
  <files>rehab-robotics-studio/src/state/mappingStore.ts</files>
  <read_first>
    - rehab-robotics-studio/src/state/mappingStore.test.ts (the tests to satisfy — read this first)
    - rehab-robotics-studio/src/state/systemStore.ts (exact zustand pattern: import, interface, create, initial state, set/get usage)
    - rehab-robotics-studio/.planning/phases/24-rosbridge-and-studio-mapping-workspace/24-CONTEXT.md (D-04, D-05, D-06, D-07, D-18 — complete field list)
  </read_first>
  <behavior>
    All 10 test cases from Task 1 must pass. No test may be modified. Additional edge cases to satisfy:
    - updateFromFleetRegistry must NOT delete existing rows when devices list shrinks
    - setDraftNotUsed(deviceId, true) must set draftSegment=null AND draftNotUsed=true in one set() call
    - computeMappingStatus is a pure exported function (not closure over store state)
    - setIdentifyResult uses window.setTimeout (not global.setTimeout) so vitest fake timers intercept
    - updateFromMappingCurrent guard: if payload is not an object or missing revision/applied_revision, silently return without state change
  </behavior>
  <action>
    Create `rehab-robotics-studio/src/state/mappingStore.ts`.

    Export interfaces in this order: `ModelCatalogSnapshot`, `MappingCurrentSnapshot`, `FleetRegistrySnapshot`, `NCalibrationStatusSnapshot`, `InputValiditySnapshot`, `MappingRow`, `MappingStore`.

    `MappingRow` fields exactly per D-04 plus no additions needed for this plan (identifyBusy: boolean, identifyResult: string | null are in D-04 already).

    `MappingStore` fields per D-04 plus: `calibrationInterlocked: boolean`; `updateFromCalibrationStatus(status: unknown): void`.

    Export `computeMappingStatus(row: Pick<MappingRow, 'backendState' | 'draftSegment' | 'draftNotUsed' | 'ikValid'>, mappingRevision: number, appliedRevision: number): MappingRow['mappingStatus']` as a named export.

    Helper `defaultRow(deviceId: string): MappingRow` creates a row with all nullable fields set to null, backendState='unassigned', draftNotUsed=null, identifyBusy=false, identifyResult=null, mappingStatus='unassigned', rateHz=null, dropCount=0.

    Actions implementation guidance:
    - `updateFromFleetRegistry`: cast devices to `unknown[]`, filter for items where `isRecord(item) && typeof item.device_id === 'string'`. Use `set((s) => { const rows = { ...s.rows }; ... return { rows }; })`.
    - `updateFromMappingCurrent`: guard with `isRecord(payload) && typeof payload.revision === 'number' && typeof payload.applied_revision === 'number' && isRecord(payload.assignments)`. After updating each row, recompute mappingStatus.
    - `setIdentifyResult`: inside set() callback, update the row; then schedule `window.setTimeout(() => useMappingStore.setState(...)` for auto-clear. Note: the setTimeout callback reads store state again, so use `useMappingStore.setState((s) => ({ rows: { ...s.rows, [deviceId]: { ...s.rows[deviceId], identifyResult: null } } }))`.
  </action>
  <verify>
    <automated>cd "c:/Users/justi/Documents/# Rehab Robotics GUI Prototype__Here's a complete, single-file React + TypeScript app implementing the LabVIEW/rehab-robotics-studio" && npx vitest run src/state/mappingStore.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - mappingStore.ts exists and exports: useMappingStore, MappingRow, MappingStore, computeMappingStatus, ModelCatalogSnapshot, MappingCurrentSnapshot, FleetRegistrySnapshot, NCalibrationStatusSnapshot, InputValiditySnapshot
    - `npx vitest run src/state/mappingStore.test.ts` exits 0 with all test cases passing
    - mappingStore.ts does NOT import from any component file (no circular deps)
    - TypeScript compiles without errors: `npx tsc --noEmit` exits 0 in rehab-robotics-studio/
    - `grep -c "calibrationInterlocked" rehab-robotics-studio/src/state/mappingStore.ts` returns 1 or more
  </acceptance_criteria>
  <done>All vitest tests green; TypeScript clean; all exports confirmed present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rosbridge → store | Incoming topic payloads are untrusted strings parsed as JSON; store actions receive `unknown` typed payloads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Tampering | updateFromFleetRegistry | mitigate | Guard with `isRecord` + `typeof device_id === 'string'`; invalid items are silently skipped |
| T-24-02 | Tampering | updateFromMappingCurrent | mitigate | Guard revision/applied_revision as numbers and assignments as Record before touching state |
| T-24-03 | Denial of Service | setIdentifyResult setTimeout | accept | Single per-device timer; if deviceId not found in rows, setState no-ops harmlessly |
| T-24-04 | Information Disclosure | computeMappingStatus | accept | Pure function over client-visible state; no PII or credentials in mapping rows |
</threat_model>

<verification>
Run the full vitest suite after implementing the store:

```bash
cd "c:/Users/justi/Documents/# Rehab Robotics GUI Prototype__Here's a complete, single-file React + TypeScript app implementing the LabVIEW/rehab-robotics-studio"
npx vitest run src/state/mappingStore.test.ts
npx tsc --noEmit
```

All tests green. TypeScript exits 0.
</verification>

<success_criteria>
- mappingStore.ts exports useMappingStore, MappingRow, MappingStore, computeMappingStatus and all five snapshot types
- All 10+ vitest test cases pass
- TypeScript compiles clean (tsc --noEmit exit 0)
- No plan 24-03 or 24-04 integration needed for this plan to succeed
</success_criteria>

<output>
Create `.planning/phases/24-rosbridge-and-studio-mapping-workspace/24-01-SUMMARY.md` when done.
</output>
