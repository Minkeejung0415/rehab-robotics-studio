---
phase: 22-model-catalog-mapping-store-and-transactional-contracts
plan: 06
type: execute
wave: 4
depends_on: ["22-04", "22-05"]
files_modified:
  - backend/setup.py
  - backend/launch/rehab_robotics.launch.py
autonomous: true
requirements: [MODEL-01, MODEL-02, MODEL-03, MAP-01, MAP-02, MAP-03, MAP-04, MAP-05, MAP-06]

must_haves:
  truths:
    - "model_catalog_node and mapping_node appear as console_scripts entry points in setup.py"
    - "rehab_robotics.launch.py starts model_catalog_node when use_fleet_bridge=true or independently"
    - "rehab_robotics.launch.py starts mapping_node alongside model_catalog_node"
    - "Both new nodes accept opensim_model_path and store_path parameters from launch"
    - "All existing launch nodes (fleet_bridge_node, opensim_bridge, recorder, etc.) remain unchanged"
    - "Full test suite (python -m pytest backend/test/) still passes after launch wiring"
  artifacts:
    - path: "backend/setup.py"
      provides: "Entry point registration for new nodes"
      contains: "model_catalog_node"
    - path: "backend/launch/rehab_robotics.launch.py"
      provides: "Updated launch file with model_catalog and mapping nodes"
      contains: "mapping_node"
  key_links:
    - from: "backend/setup.py entry_points"
      to: "backend/rehab_robotics_bridge/model_catalog_node.py main()"
      via: "console_scripts"
      pattern: "model_catalog_node.*main"
    - from: "backend/launch/rehab_robotics.launch.py"
      to: "model_catalog_node executable"
      via: "Node(executable='model_catalog_node')"
      pattern: "model_catalog_node"
---

<objective>
Wire model_catalog_node and mapping_node into the project's entry points and launch file. Both nodes start alongside fleet_bridge_node and receive their parameters through launch arguments.

Purpose: Operators need both new nodes running in the standard launch path. Without this, the nodes exist as Python files but are unreachable via ros2 run or ros2 launch.

Output: Updated backend/setup.py and backend/launch/rehab_robotics.launch.py.
</objective>

<execution_context>
@C:/Users/justi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/justi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-model-catalog-mapping-store-and-transactional-contracts/22-CONTEXT.md

<interfaces>
<!-- Current entry points and launch structure. -->

From backend/setup.py — current console_scripts block:
```python
entry_points={
    'console_scripts': [
        'esp32_bridge_node = rehab_robotics_bridge.esp32_bridge_node:main',
        'fleet_bridge_node = rehab_robotics_bridge.fleet_bridge_node:main',
        'esp_filter = rehab_robotics_bridge.filter_node:main',
        'opensim_bridge = rehab_robotics_bridge.opensim_node:main',
        'esp_record = rehab_robotics_bridge.recorder_node:main',
        'esp_status = rehab_robotics_bridge.status_node:main',
        'processing_block_observer = rehab_robotics_bridge.processing_block_observer:main',
        'opensim_test_publisher = rehab_robotics_bridge.opensim_test_publisher:main',
    ],
},
```

From backend/launch/rehab_robotics.launch.py — existing launch pattern:
```python
from launch_ros.actions import Node
Node(
    package='rehab_robotics_bridge',
    executable='fleet_bridge_node',
    name='esp_fleet_bridge',
    output='screen',
    parameters=[{'routes_json': ..., 'alias_master_device_id': ...}],
)
# DeclareLaunchArgument pattern for new params:
DeclareLaunchArgument('model_path', default_value=''),
```

From 22-02-PLAN.md: ModelCatalogNode declares parameter "opensim_model_path" (string, default "").
From 22-03-PLAN.md: MappingNode declares parameter "store_path" (string, default "").
From 22-CONTEXT.md D-22: nodes run as separate ROS nodes, both added to rehab_robotics.launch.py.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register new entry points in setup.py</name>
  <files>backend/setup.py</files>
  <read_first>
    - backend/setup.py — full current content (to append without disrupting existing entries)
    - backend/rehab_robotics_bridge/model_catalog_node.py — confirm main() is defined
    - backend/rehab_robotics_bridge/mapping_node.py — confirm main() is defined
  </read_first>
  <action>
    Edit backend/setup.py to add two new console_script entries inside the existing entry_points console_scripts list. Append after the last existing entry:

      'model_catalog_node = rehab_robotics_bridge.model_catalog_node:main',
      'mapping_node = rehab_robotics_bridge.mapping_node:main',

    Do not alter any other line. All existing eight entries remain exactly as they are. Do not change name, version, packages, data_files, install_requires, or any other field.
  </action>
  <verify>
    <automated>python -c "
content = open('backend/setup.py').read()
assert 'model_catalog_node = rehab_robotics_bridge.model_catalog_node:main' in content
assert 'mapping_node = rehab_robotics_bridge.mapping_node:main' in content
assert 'fleet_bridge_node = rehab_robotics_bridge.fleet_bridge_node:main' in content
print('setup.py entry points: PASSED')
"</automated>
  </verify>
  <done>setup.py contains model_catalog_node and mapping_node in console_scripts. All pre-existing entries are intact.</done>
</task>

<task type="auto">
  <name>Task 2: Add model_catalog_node and mapping_node to rehab_robotics.launch.py</name>
  <files>backend/launch/rehab_robotics.launch.py</files>
  <read_first>
    - backend/launch/rehab_robotics.launch.py — full current content (understand generate_launch_description args and node list)
    - backend/rehab_robotics_bridge/model_catalog_node.py — parameter name: opensim_model_path
    - backend/rehab_robotics_bridge/mapping_node.py — parameter name: store_path
  </read_first>
  <action>
    Edit backend/launch/rehab_robotics.launch.py. Add two new DeclareLaunchArgument entries to the args list in generate_launch_description:

      DeclareLaunchArgument('enable_model_catalog', default_value='true', description='Start model_catalog_node'),
      DeclareLaunchArgument('enable_mapping_node', default_value='true', description='Start mapping_node'),
      DeclareLaunchArgument('mapping_store_path', default_value='', description='Path to mapping_store.json (empty = use default ~/.ros/rehab_robotics/mapping_store.json)'),

    Note: opensim_model_path for model_catalog_node reuses the existing 'model_path' launch argument — pass LaunchConfiguration('model_path') as the opensim_model_path parameter value. This avoids a new redundant argument.

    Define two new Node objects (use IfCondition on enable_model_catalog and enable_mapping_node):

    model_catalog = Node(
        package='rehab_robotics_bridge',
        executable='model_catalog_node',
        name='model_catalog_node',
        output='screen',
        parameters=[{'opensim_model_path': LaunchConfiguration('model_path')}],
        condition=IfCondition(LaunchConfiguration('enable_model_catalog')),
    )

    mapping = Node(
        package='rehab_robotics_bridge',
        executable='mapping_node',
        name='mapping_node',
        output='screen',
        parameters=[{'store_path': LaunchConfiguration('mapping_store_path')}],
        condition=IfCondition(LaunchConfiguration('enable_mapping_node')),
    )

    Add model_catalog and mapping to the LaunchDescription return list alongside the existing nodes. Do not remove or reorder any existing node. The new nodes appear after the existing roster (after processing_observer, before the closing bracket).

    All existing imports, OpaqueFunction, DeclareLaunchArgument entries, filter_node, opensim, recorder, status, rosbridge, etc. must remain exactly as they are.
  </action>
  <verify>
    <automated>python -c "
import ast
content = open('backend/launch/rehab_robotics.launch.py').read()
ast.parse(content)  # syntax check
assert 'model_catalog_node' in content, 'model_catalog_node missing'
assert 'mapping_node' in content, 'mapping_node missing'
assert 'enable_model_catalog' in content, 'enable_model_catalog arg missing'
assert 'enable_mapping_node' in content, 'enable_mapping_node arg missing'
assert 'fleet_bridge_node' in content, 'fleet_bridge_node removed — regression'
assert 'opensim_bridge' in content, 'opensim_bridge removed — regression'
assert 'esp_record' in content or 'recorder' in content, 'recorder removed — regression'
print('rehab_robotics.launch.py: PASSED')
"</automated>
  </verify>
  <done>rehab_robotics.launch.py contains model_catalog_node and mapping_node entries with IfCondition guards. All pre-existing nodes are present. File passes AST parse.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Launch arguments -> node parameters | Operator-supplied strings from launch command line |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-06-01 | Tampering | mapping_store_path launch arg | mitigate | MappingNode ignores empty string and uses default ~/.ros/rehab_robotics/ path; non-empty paths are user-controlled by design |
| T-22-06-02 | Information Disclosure | model_path exposed as opensim_model_path | accept | model_path is already visible in existing launch configuration; no new disclosure |
| T-22-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs; launch file is plain Python |
</threat_model>

<verification>
python -c "
import ast

# setup.py
setup_content = open('backend/setup.py').read()
assert 'model_catalog_node = rehab_robotics_bridge.model_catalog_node:main' in setup_content
assert 'mapping_node = rehab_robotics_bridge.mapping_node:main' in setup_content

# launch file
launch_content = open('backend/launch/rehab_robotics.launch.py').read()
ast.parse(launch_content)
assert 'model_catalog_node' in launch_content
assert 'mapping_node' in launch_content
assert 'fleet_bridge_node' in launch_content
assert 'opensim_bridge' in launch_content

print('Launch wiring: ALL CHECKS PASSED')
"

cd backend && python -m pytest test/ -v 2>&1 | tail -10
</verification>

<success_criteria>
- backend/setup.py has model_catalog_node and mapping_node in console_scripts
- backend/launch/rehab_robotics.launch.py has both new nodes with IfCondition guards and enable_model_catalog / enable_mapping_node args
- model_catalog_node receives opensim_model_path from existing model_path launch arg
- mapping_node receives store_path from mapping_store_path launch arg (default empty = use default path)
- rehab_robotics.launch.py passes AST parse (no syntax errors)
- python -m pytest backend/test/ passes (no regressions from prior plans)
</success_criteria>

<output>
Create .planning/phases/22-model-catalog-mapping-store-and-transactional-contracts/22-06-SUMMARY.md when done.
</output>
