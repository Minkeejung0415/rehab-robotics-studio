---
phase: 22-model-catalog-mapping-store-and-transactional-contracts
plan: 05
type: execute
wave: 3
depends_on: ["22-03"]
files_modified:
  - backend/test/test_mapping_node.py
autonomous: true
requirements: [MAP-01, MAP-02, MAP-03, MAP-04, MAP-05, MAP-06]

must_haves:
  truths:
    - "set_assignment accepts assigned/not_used/unassigned states"
    - "set_assignment rejects duplicate (segment, frame) pairs with duplicate_frame"
    - "set_assignment rejects malformed device_id (not esp32:<12hex>) with invalid_device_id"
    - "Assignments persist across MappingStore re-instantiation (atomic write verified)"
    - "Corruption recovery starts fresh when both main and .bak are invalid JSON"
    - "Apply returns revision_mismatch when expected_revision != stored revision"
    - "Apply returns blocked when recording_active is True"
    - "Apply returns blocked when calibration_active is True"
    - "Apply returns incomplete when any device has state=unassigned"
    - "Apply returns invalid_frame when a frame is not in the current frame_list"
    - "Apply succeeds and persists applied_revision when all checks pass"
    - "Reconnect re-attach: device in applied mapping stays assigned after fleet registry update"
    - "New device from fleet registry gets unassigned state"
    - "/rehab/mapping/current is published on every set_assignment call"
  artifacts:
    - path: "backend/test/test_mapping_node.py"
      provides: "Offline deterministic tests for MappingStore and MappingNode"
      min_lines: 200
---

<objective>
Write comprehensive deterministic offline tests for mapping_node.py. No live ROS, no hardware. Covers all MAP-01 through MAP-06 contracts including persistence, Apply transactions, interlock, and reconnect re-attach.

Purpose: Proves every mapping contract is implemented and provides regression protection for the full Apply state machine.

Output: backend/test/test_mapping_node.py
</objective>

<execution_context>
@C:/Users/justi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/justi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-model-catalog-mapping-store-and-transactional-contracts/22-CONTEXT.md

<interfaces>
<!-- Extracted from mapping_node.py contracts established in 22-03-PLAN.md. -->

MappingStore public API:
- __init__(store_path: Path)
- set_model_hash(new_hash: str) -> None
- set_frame_list(frame_list: list[dict]) -> None
- set_assignment(device_id: str, segment: str, frame: str, state: str) -> tuple[str, str]  # (outcome, detail)
- reset(model_hash: str) -> str  # outcome
- get_state_dict() -> dict
- Properties: model_hash, revision, applied_revision, assignments

MappingNode public API (via ROS service stubs):
- _on_set_assignment(request, response) -> response
- _on_apply_mapping(request, response) -> response
- _on_get_mapping_state(request, response) -> response
- _on_reset_mapping(request, response) -> response
- _on_fleet_registry(msg) -> None
- _on_recording_status(msg) -> None
- _on_calibration_status(msg) -> None
- _publish_current() -> None

ROS stub pattern (from test_fleet_bridge.py):
- sys.modules.setdefault for rclpy, rclpy.node, std_msgs, std_msgs.msg, rehab_robotics_interfaces, rehab_robotics_interfaces.srv
- Load module via importlib.util.spec_from_file_location
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write MappingStore unit tests — persistence, states, duplicate rejection, corruption recovery</name>
  <files>backend/test/test_mapping_node.py</files>
  <read_first>
    - backend/rehab_robotics_bridge/mapping_node.py — MappingStore class (full read to know exact field names and behavior)
    - backend/test/test_fleet_bridge.py — _install_ros_stubs and module loading pattern (lines 1-75)
  </read_first>
  <action>
    Create backend/test/test_mapping_node.py. At top: from __future__ import annotations, standard imports (hashlib, importlib.util, json, sys, tempfile, types, unittest, from pathlib import Path). Implement _install_ros_stubs() following test_fleet_bridge.py exactly — stubs for rclpy, rclpy.node, std_msgs, std_msgs.msg.String, rehab_robotics_interfaces.srv with SetAssignment/ApplyMapping/GetMappingState/ResetMapping types. Load mapping_node via importlib.util.spec_from_file_location('mapping_node_test', path). Module-level: mapping = _load_mapping_module().

    class MappingStoreTest(unittest.TestCase):

    setUp: self.tmp = tempfile.TemporaryDirectory(). self.store_path = Path(self.tmp.name) / "mapping_store.json". self.store = mapping.MappingStore(self.store_path). Call self.store.set_model_hash("deadbeef01234567" * 4).

    tearDown: self.tmp.cleanup().

    test_initial_state_is_empty: assert self.store.assignments == {}, self.store.revision == 0, self.store.applied_revision == 0.

    test_set_assignment_assigned_ok: call set_frame_list([{"segment":"femur_r","frame":"femur_r_imu"}]). set_assignment("esp32:aabbccddeeff", "femur_r", "femur_r_imu", "assigned"). Assert outcome=="ok", assignments has the device.

    test_set_assignment_not_used_ok: set_assignment("esp32:aabbccddeeff", "", "", "not_used"). Assert outcome=="ok".

    test_set_assignment_unassigned_ok: set_assignment("esp32:aabbccddeeff", "", "", "unassigned"). Assert outcome=="ok".

    test_set_assignment_duplicate_frame_rejected: set_frame_list with femur_r/femur_r_imu. set_assignment device A to (femur_r, femur_r_imu, assigned). set_assignment device B (esp32:112233445566) to same (femur_r, femur_r_imu, assigned). Assert outcome=="duplicate_frame".

    test_set_assignment_invalid_state_rejected: set_assignment with state="active". Assert outcome=="invalid_state".

    test_set_assignment_invalid_device_id_rejected: set_assignment("invalidid", ...). Assert outcome=="invalid_device_id". Also test "esp32:tooshort", "esp32:UPPERCASE00" (uppercase rejected), "notanesp32:aabbccddeeff".

    test_revision_increments_on_each_set_assignment: initial revision == 0. Two set_assignments -> revision == 2.

    test_persistence_across_instantiation: set_assignment device A. Create new MappingStore(same path). Assert device A in new_store.assignments.

    test_corruption_recovery_fresh_start: write invalid JSON to store_path, write invalid JSON to store_path.with_suffix('.json.bak'). Create new MappingStore(store_path). Assert store.revision == 0, store.assignments == {}.

    test_backup_on_save: after set_assignment, assert store_path.with_suffix('.json.bak').exists() or store_path.parent.joinpath('mapping_store.json.bak').exists(). (Check both patterns since naming may vary between path.with_suffix('.json.bak') and path.parent / 'mapping_store.json.bak' — verify which one the implementation uses.)

    test_reset_clears_assignments: set_assignment device A. reset(""). Assert assignments == {} (or revision resets to 0).
  </action>
  <verify>
    <automated>cd backend && python -m pytest test/test_mapping_node.py::MappingStoreTest -v 2>&1 | tail -20</automated>
  </verify>
  <done>All MappingStoreTest tests pass. Persistence, duplicate rejection, invalid state/device_id, revision increment, corruption recovery, and reset are all covered.</done>
</task>

<task type="auto">
  <name>Task 2: Write MappingNode service handler and interlock tests</name>
  <files>backend/test/test_mapping_node.py</files>
  <read_first>
    - backend/rehab_robotics_bridge/mapping_node.py — MappingNode service handler implementations (_on_apply_mapping, _on_fleet_registry, _on_recording_status, _on_calibration_status)
    - backend/test/test_opensim_node.py — _StubNode and _Publisher pattern (lines 1-100)
  </read_first>
  <action>
    Extend test_mapping_node.py with helper infrastructure and node tests.

    Define stub service request/response objects as simple namespaces:
    - make_set_assignment_req(device_id, segment, frame, state) -> types.SimpleNamespace
    - make_apply_req(expected_revision) -> types.SimpleNamespace
    - make_apply_resp() -> types.SimpleNamespace with outcome="", applied_revision=0, detail=""
    - make_set_resp() -> types.SimpleNamespace with outcome="", detail=""
    - make_get_resp() -> types.SimpleNamespace with state_json=""
    - make_reset_req(model_hash) -> types.SimpleNamespace
    - make_reset_resp() -> types.SimpleNamespace with outcome=""
    - make_str_msg(data: str) -> types.SimpleNamespace with data attribute

    Define _StubNode as a test-double Node base: implements declare_parameter (uses parameter_overrides dict), get_parameter, create_publisher (returns _Publisher with .messages list and .publish(msg) appending), create_subscription (returns SimpleNamespace), create_service (returns SimpleNamespace), create_timer (returns SimpleNamespace), get_logger (returns no-op logger with .info/.warning/.error).

    Instantiate MappingNode for each test by temporarily patching sys.modules['rclpy'].node.Node = _StubNode before instantiation, then restoring. Use a tempdir for store_path.

    class MappingNodeApplyTest(unittest.TestCase):

    setUp: set up stub node, instantiate MappingNode (with temp store_path param). Set model hash via _on_catalog to a fake catalog JSON: {"schema_version":"rehab.model_catalog.1","model_hash":"a"*64,"model_path":"/fake","frame_list":[{"segment":"femur_r","frame":"femur_r_imu"}],"error_reason":""}. Set one device assigned: call node._on_set_assignment with device_id="esp32:aabbccddeeff", segment="femur_r", frame="femur_r_imu", state="assigned".

    test_apply_ok: revision is now 1 (one set_assignment). Call _on_apply_mapping with expected_revision=1. Assert response.outcome == "applied", response.applied_revision == 1.

    test_apply_revision_mismatch: call with expected_revision=99. Assert response.outcome == "revision_mismatch".

    test_apply_blocked_recording: set node._recording_active = True. Call _on_apply_mapping(expected_revision=1). Assert response.outcome == "blocked", "recording" in response.detail.

    test_apply_blocked_calibration: set node._calibration_active = True. Call _on_apply_mapping(expected_revision=1). Assert response.outcome == "blocked", "calibration" in response.detail.

    test_apply_incomplete_unassigned_device: add second device esp32:112233445566 as unassigned (via _on_set_assignment with state=unassigned). Call apply with current revision. Assert response.outcome == "incomplete".

    test_apply_invalid_frame: set_assignment with (femur_r, unknown_frame, assigned). Frame list does not contain unknown_frame. Apply. Assert outcome == "invalid_frame".

    test_apply_preserves_applied_revision_on_failure: apply OK once (applied_revision=1). Add unassigned device (bumps revision). Apply fails with incomplete. Assert node._store.applied_revision still == 1 (not overwritten).

    class MappingNodeReconnectTest(unittest.TestCase):

    test_reconnect_reattach_stays_assigned: apply OK (applied_revision=1, device A assigned). Send fleet registry JSON with device A in connected state. Assert device A still "assigned" in store.assignments.

    test_new_device_from_fleet_registry_gets_unassigned: send fleet registry JSON with a new device_id "esp32:ffeeddccbbaa" (not in store). Assert it appears in store.assignments with state="unassigned".

    class MappingNodePublishTest(unittest.TestCase):

    test_publish_on_set_assignment: track published message count before and after _on_set_assignment. Assert count increased by 1.

    test_get_mapping_state_returns_json: call _on_get_mapping_state(empty_req, resp). Assert response.state_json is valid JSON. Assert "schema_version" in parsed or "model_hash" in parsed.
  </action>
  <verify>
    <automated>cd backend && python -m pytest test/test_mapping_node.py -v 2>&1 | tail -30</automated>
  </verify>
  <done>All tests in test_mapping_node.py pass. Apply transaction, interlock, revision mismatch, reconnect re-attach, new device registration, and publish-on-change are all covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test doubles -> MappingNode | No real ROS or hardware; all trust boundaries are internal to tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-05-01 | Tampering | Test double service requests | accept | Test doubles are test-only; adversarial inputs (malformed device_id, bad revision) are tested explicitly |
| T-22-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs; all test infrastructure is stdlib |
</threat_model>

<verification>
cd backend && python -m pytest test/test_mapping_node.py -v
</verification>

<success_criteria>
- test_mapping_node.py exists and all tests pass under python -m pytest
- Tests run without live ROS, hardware, or OpenSim installation
- Apply interlock (recording, calibration) explicitly tested
- Apply revision mismatch explicitly tested
- Apply applied_revision preserved on failure explicitly tested
- Reconnect re-attach contract explicitly tested
- New device from fleet registry gets unassigned state explicitly tested
- Publish-on-change tested via publisher message count
- Corruption recovery tested with both main and .bak invalid
</success_criteria>

<output>
Create .planning/phases/22-model-catalog-mapping-store-and-transactional-contracts/22-05-SUMMARY.md when done.
</output>
