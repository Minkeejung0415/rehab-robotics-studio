---
phase: 22-model-catalog-mapping-store-and-transactional-contracts
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - backend/rehab_robotics_bridge/model_catalog_node.py
autonomous: true
requirements: [MODEL-01, MODEL-02, MODEL-03]

must_haves:
  truths:
    - "model_catalog_node reads opensim_model_path ROS param and computes SHA-256 of raw file bytes"
    - "Compatible (non-Ground PhysicalFrame) frames are enumerated and published as frame_list with {segment, frame} pairs"
    - "Missing, ambiguous, or unsupported frame requests fail closed — no fuzzy match, no source model mutation"
    - "Publishes /rehab/model/catalog JSON String topic whenever model_path changes"
    - "Node handles missing file or OpenSim unavailable gracefully — publishes empty catalog with error_reason"
  artifacts:
    - path: "backend/rehab_robotics_bridge/model_catalog_node.py"
      provides: "ModelCatalogNode ROS node"
      exports: ["ModelCatalogNode", "main", "enumerate_compatible_frames", "compute_model_hash"]
      min_lines: 80
  key_links:
    - from: "backend/rehab_robotics_bridge/model_catalog_node.py"
      to: "/rehab/model/catalog"
      via: "String publisher"
      pattern: "rehab/model/catalog"
    - from: "enumerate_compatible_frames"
      to: "opensim.Model"
      via: "getBodySet or getFrameSet + PhysicalFrame filter"
      pattern: "PhysicalFrame"
---

<objective>
Implement model_catalog_node.py — a standalone ROS node that reads the opensim_model_path parameter, computes SHA-256, enumerates compatible non-Ground PhysicalFrames, and publishes /rehab/model/catalog as a JSON String.

Purpose: This node delivers MODEL-01 (SHA-256 identity), MODEL-02 (exact frame enumeration), and MODEL-03 (fail-closed on bad frames). mapping_node depends on its catalog topic to validate assignment choices at SetAssignment and Apply time (D-05).

Output: backend/rehab_robotics_bridge/model_catalog_node.py
</objective>

<execution_context>
@C:/Users/justi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/justi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-model-catalog-mapping-store-and-transactional-contracts/22-CONTEXT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Key patterns to reuse verbatim. -->

From 22-CONTEXT.md (D-01, D-02, D-03):
- SHA-256: hashlib.sha256(Path(model_path).read_bytes()).hexdigest()
- Compatible frames: non-Ground PhysicalFrame objects via opensim.Model getBodySet()/getFrameSet()
- Published topic: /rehab/model/catalog (String/JSON)
- Catalog JSON schema: {schema_version, model_hash, model_path, frame_list: [{segment, frame}], error_reason}

From backend/rehab_robotics_bridge/opensim_node.py — ROS node parameter pattern:
```python
parameter_defaults = {"model_path": "", ...}
values = {name: self.declare_parameter(name, default).value for name, default in parameter_defaults.items()}
```

From backend/rehab_robotics_bridge/opensim_adapter.py — OpenSim lazy import pattern:
```python
try:
    opensim_module = import_module("opensim")
except (ImportError, ModuleNotFoundError):
    return UnavailableVisualizerAdapter("opensim_bindings_unavailable", ...)
```

From backend/rehab_robotics_bridge/opensim_adapter.py — frame resolution (fail-closed):
```python
frame_type = getattr(self._opensim, "Frame", None)
safe_down_cast = getattr(frame_type, "safeDownCast", None)
if not callable(safe_down_cast):
    raise _AdapterInitializationError("frame_api_unsupported_by_bindings")
component = self._model.getComponent(component_path)
frame = safe_down_cast(component)
if frame is None or not frame:
    raise ...
```

From backend/rehab_robotics_bridge/fleet_bridge_node.py — JSON publish pattern:
```python
message = String()
message.data = json.dumps(payload, sort_keys=True, separators=(",", ":"))
self._publisher.publish(message)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement enumerate_compatible_frames and compute_model_hash as pure functions</name>
  <files>backend/rehab_robotics_bridge/model_catalog_node.py</files>
  <read_first>
    - backend/rehab_robotics_bridge/opensim_adapter.py — _resolve_frames method and OpenSim lazy import pattern (full read, extract frame resolution and import patterns)
    - .planning/phases/22-model-catalog-mapping-store-and-transactional-contracts/22-CONTEXT.md — D-01, D-02, D-03 (SHA-256, compatible frame definition, fail-closed contract)
    - examples/opensim_quaternion_demo.osim — reference model structure (skim for Body and Frame component names to understand what enumerate_compatible_frames must return)
  </read_first>
  <behavior>
    - compute_model_hash(model_path: str) -> str: reads raw bytes via Path(model_path).read_bytes(), returns hashlib.sha256(...).hexdigest(). Raises FileNotFoundError if path does not exist.
    - enumerate_compatible_frames(model_path: str, opensim_module: Any) -> list[dict]: loads opensim.Model(model_path), iterates getBodySet() to find Body objects (non-Ground), returns list of {segment: body_name, frame: body_name} for each Body. Also checks getComponentsList() for PhysicalOffsetFrame attached to a Body; for each such frame returns {segment: parent_body_name, frame: frame_name}. Ground body (name == "ground" case-insensitive) is excluded. Returns [] if model loads but has no compatible frames. Raises ValueError with reason string if model fails to load.
    - Frame enumeration does NOT modify the model object. It calls no setters and does not call initSystem(). Read-only access only (MODEL-03 no source mutation).
    - Missing frame path (model_path is empty or file absent): raises FileNotFoundError, not a fuzzy fallback.
  </behavior>
  <action>
    Create backend/rehab_robotics_bridge/model_catalog_node.py. Start with the module docstring and from __future__ import annotations. Import hashlib, json, time from stdlib. Import Path from pathlib. Import Any from typing. Do not import opensim at module level — use importlib.import_module lazily inside enumerate_compatible_frames.

    Implement compute_model_hash(model_path: str) -> str as a pure function: calls hashlib.sha256(Path(model_path).read_bytes()).hexdigest(). No caching — callers decide when to recompute.

    Implement enumerate_compatible_frames(model_path: str, opensim_module: Any) -> list[dict]: loads opensim_module.Model(model_path) (no setUseVisualizer, no initSystem — read-only). Iterates the body set via model.getBodySet(). For each index i in range(model.getBodySet().getSize()): gets body = model.getBodySet().get(i). Skips if body.getName().lower() == "ground". Adds {segment: body.getName(), frame: body.getName()} to results. Then iterates model.getComponentsList() looking for components whose class name contains "PhysicalOffsetFrame" (use type(comp).__name__ or comp.getConcreteClassName()). For each such component: gets its socket named "parent" or calls getParentFrame() to find the parent Body; if the parent is a non-Ground Body, adds {segment: parent_body.getName(), frame: comp.getName()}. Returns the list sorted by segment then frame name for deterministic output.

    Do not call any OpenSim method that may fork a process (no setUseVisualizer, no updVisualizer, no show). This prevents the simbody-visualizer fork hazard documented in opensim_adapter.py.

    The STATE.md concern "Phase 22 must prove pinned OpenSim 4.5.2 runtime Frame behavior or require model-authored Frames" is addressed by: the function tries getBodySet() first (universally available), then PhysicalOffsetFrame enumeration as a second pass. If getComponentsList() is unavailable (older binding), skip silently and return body-only results. This is the tightest safe filter that does not depend on unproven 4.5.2-specific APIs.
  </action>
  <verify>
    <automated>cd backend && python -c "
import sys, types
# Stub opensim
opensim = types.ModuleType('opensim')
sys.modules['opensim'] = opensim
# Test compute_model_hash import
import importlib.util
from pathlib import Path
spec = importlib.util.spec_from_file_location('mcn', 'rehab_robotics_bridge/model_catalog_node.py')
mod = importlib.util.module_from_spec(spec)
# Patch rclpy before loading
rclpy = types.ModuleType('rclpy')
rclpy.node = types.ModuleType('rclpy.node')
rclpy.node.Node = type('Node', (), {})
import sys; sys.modules.setdefault('rclpy', rclpy); sys.modules.setdefault('rclpy.node', rclpy.node)
std_msgs = types.ModuleType('std_msgs'); std_msgs.msg = types.ModuleType('std_msgs.msg')
std_msgs.msg.String = type('String', (), {'__init__': lambda s, **k: None})
sys.modules.setdefault('std_msgs', std_msgs); sys.modules.setdefault('std_msgs.msg', std_msgs.msg)
spec.loader.exec_module(mod)
import hashlib, tempfile, os
with tempfile.NamedTemporaryFile(delete=False, suffix='.osim') as f:
    f.write(b'test content')
    tmp = f.name
expected = hashlib.sha256(b'test content').hexdigest()
assert mod.compute_model_hash(tmp) == expected, 'hash mismatch'
os.unlink(tmp)
try:
    mod.compute_model_hash('/nonexistent/path.osim')
    assert False, 'should raise'
except FileNotFoundError:
    pass
print('compute_model_hash: PASSED')
"
</automated>
  </verify>
  <done>compute_model_hash returns correct SHA-256 hex for any file and raises FileNotFoundError for missing paths. enumerate_compatible_frames is present and calls no OpenSim setters. Module imports cleanly with ROS stubs.</done>
</task>

<task type="auto">
  <name>Task 2: Implement ModelCatalogNode ROS node and main entrypoint</name>
  <files>backend/rehab_robotics_bridge/model_catalog_node.py</files>
  <read_first>
    - backend/rehab_robotics_bridge/opensim_node.py — declare_parameter pattern, timer pattern, publisher creation (lines 104-293)
    - backend/rehab_robotics_bridge/fleet_bridge_node.py — JSON publish pattern (FLEET_REGISTRY_TOPIC publisher, build_fleet_registry call)
  </read_first>
  <action>
    Extend model_catalog_node.py with ModelCatalogNode(Node). The node name is "model_catalog". Declare one parameter: opensim_model_path (string, default ""). Create a String publisher on /rehab/model/catalog with queue depth 10. Create a timer at 1.0s interval calling _on_catalog_timer. Store _last_published_hash = "" and _last_model_path = "" to detect changes.

    _on_catalog_timer: reads current value of opensim_model_path parameter via self.get_parameter("opensim_model_path").value. If the path is unchanged from _last_model_path, skip publish (no-op). Otherwise, call _build_and_publish_catalog(new_path).

    _build_and_publish_catalog(model_path: str): builds the catalog dict and publishes it. If model_path is empty string: publish catalog with schema_version="rehab.model_catalog.1", model_hash="", model_path="", frame_list=[], error_reason="no_model_path". If path is non-empty but file does not exist: publish with error_reason="model_path_not_found". If path exists: call compute_model_hash(model_path). Then attempt to import opensim via importlib.import_module("opensim"); if ImportError, publish with error_reason="opensim_bindings_unavailable" and the hash. If opensim available: call enumerate_compatible_frames(model_path, opensim_module); on success publish with frame_list populated and error_reason="". On any exception from enumerate_compatible_frames: publish with frame_list=[], error_reason=str(exc) truncated to 200 chars.

    Published JSON structure (per D-02): {"schema_version": "rehab.model_catalog.1", "model_hash": "<64hex or empty>", "model_path": "<path>", "frame_list": [{"segment": "...", "frame": "..."}, ...], "error_reason": "<string or empty>"}.

    Always update _last_model_path after building (even on error) so the timer does not re-attempt every second on a broken path — only re-attempt if the operator changes the param. Use sort_keys=True, separators=(",", ":") for JSON serialization (matches existing pattern).

    Implement main(): calls rclpy.init(), creates ModelCatalogNode(), rclpy.spin(node), finally node.destroy_node(), rclpy.try_shutdown().
  </action>
  <verify>
    <automated>cd backend && python -c "
import ast, sys
from pathlib import Path
tree = ast.parse(Path('rehab_robotics_bridge/model_catalog_node.py').read_text())
classes = [n.name for n in ast.walk(tree) if isinstance(n, ast.ClassDef)]
funcs = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef)]
assert 'ModelCatalogNode' in classes, 'ModelCatalogNode class missing'
assert 'main' in funcs, 'main missing'
assert 'enumerate_compatible_frames' in funcs, 'enumerate_compatible_frames missing'
assert 'compute_model_hash' in funcs, 'compute_model_hash missing'
assert '_build_and_publish_catalog' in funcs, '_build_and_publish_catalog missing'
# Check topic string present
src = Path('rehab_robotics_bridge/model_catalog_node.py').read_text()
assert 'rehab/model/catalog' in src, 'topic string missing'
assert 'rehab.model_catalog.1' in src, 'schema version missing'
print('ModelCatalogNode structure: PASSED')
"
</automated>
  </verify>
  <done>ModelCatalogNode class exists with _on_catalog_timer, _build_and_publish_catalog methods. main() entrypoint present. Topic /rehab/model/catalog and schema_version "rehab.model_catalog.1" are present in source. Module passes AST parse.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ROS param (opensim_model_path) -> node | Operator-supplied file path crosses trust boundary |
| model file bytes -> SHA-256 | File content is read-only; hash is computed over raw bytes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-02-01 | Spoofing | model_hash | mitigate | SHA-256 computed from raw bytes at read time; path is validated before read per D-01 |
| T-22-02-02 | Information Disclosure | model_path in catalog topic | accept | Path is operator-supplied and visible to any rosbridge subscriber; no credentials in path |
| T-22-02-03 | Denial of Service | very large .osim file | accept | SHA-256 reads full file once; 4.5.2 models are < 10MB in practice; no streaming needed |
| T-22-02-04 | Tampering | enumerate_compatible_frames modifying model | mitigate | No setters called, no initSystem, no visualizer fork; read-only enumeration per D-03 and MODEL-03 |
| T-22-02-SC | Tampering | npm/pip/cargo installs | accept | No new package installs; opensim is already an existing dependency |
</threat_model>

<verification>
cd backend && python -c "
import ast
from pathlib import Path
src = Path('rehab_robotics_bridge/model_catalog_node.py').read_text()
tree = ast.parse(src)
assert 'ModelCatalogNode' in [n.name for n in ast.walk(tree) if isinstance(n, ast.ClassDef)]
assert 'rehab/model/catalog' in src
assert 'rehab.model_catalog.1' in src
assert 'compute_model_hash' in src
assert 'enumerate_compatible_frames' in src
assert 'error_reason' in src
assert 'setUseVisualizer' not in src, 'must not call setUseVisualizer'
assert 'initSystem' not in src, 'must not call initSystem'
print('model_catalog_node verification: PASSED')
"
</verification>

<success_criteria>
- model_catalog_node.py exists and passes AST parse with no syntax errors
- compute_model_hash returns the correct SHA-256 hex for a known file
- Topic /rehab/model/catalog and schema_version "rehab.model_catalog.1" present in source
- No calls to setUseVisualizer or initSystem (protects against simbody-visualizer fork)
- Frame enumeration is read-only (no OpenSim setters called)
- Error cases (missing file, opensim unavailable) publish catalog with error_reason and do not crash the node
</success_criteria>

<output>
Create .planning/phases/22-model-catalog-mapping-store-and-transactional-contracts/22-02-SUMMARY.md when done.
</output>
