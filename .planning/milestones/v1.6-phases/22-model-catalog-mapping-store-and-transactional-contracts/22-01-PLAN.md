---
phase: 22-model-catalog-mapping-store-and-transactional-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rehab_robotics_interfaces/srv/SetAssignment.srv
  - rehab_robotics_interfaces/srv/ApplyMapping.srv
  - rehab_robotics_interfaces/srv/GetMappingState.srv
  - rehab_robotics_interfaces/srv/ResetMapping.srv
  - rehab_robotics_interfaces/CMakeLists.txt
autonomous: true
requirements: [MAP-01, MAP-02, MAP-03, MAP-04, MAP-05, MAP-06]

must_haves:
  truths:
    - "SetAssignment.srv exists with device_id, segment, frame, state request fields and outcome, detail response fields"
    - "ApplyMapping.srv exists with expected_revision request and outcome, applied_revision, detail response fields"
    - "GetMappingState.srv exists with empty request and state_json response"
    - "ResetMapping.srv exists with model_hash request and outcome response"
    - "CMakeLists.txt lists all four srv files so colcon build generates Python stubs"
  artifacts:
    - path: "rehab_robotics_interfaces/srv/SetAssignment.srv"
      provides: "SetAssignment service interface"
      contains: "device_id"
    - path: "rehab_robotics_interfaces/srv/ApplyMapping.srv"
      provides: "ApplyMapping service interface"
      contains: "expected_revision"
    - path: "rehab_robotics_interfaces/srv/GetMappingState.srv"
      provides: "GetMappingState service interface"
      contains: "state_json"
    - path: "rehab_robotics_interfaces/srv/ResetMapping.srv"
      provides: "ResetMapping service interface"
      contains: "model_hash"
    - path: "rehab_robotics_interfaces/CMakeLists.txt"
      provides: "Updated build configuration"
      contains: "SetAssignment.srv"
  key_links:
    - from: "rehab_robotics_interfaces/CMakeLists.txt"
      to: "rehab_robotics_interfaces/srv/SetAssignment.srv"
      via: "rosidl_generate_interfaces"
      pattern: "SetAssignment\\.srv"
---

<objective>
Define the four new ROS service interface files that mapping_node.py will expose. These are the contracts — downstream plans implement against them.

Purpose: All mapping service calls in Phase 22 and Phase 24 (Studio UI) depend on these interface definitions. Without them, colcon cannot generate Python stubs and mapping_node.py cannot import the service types.

Output: Four .srv files + updated CMakeLists.txt.
</objective>

<execution_context>
@C:/Users/justi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/justi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-model-catalog-mapping-store-and-transactional-contracts/22-CONTEXT.md

<interfaces>
<!-- Existing service pattern from the codebase. -->

From rehab_robotics_interfaces/srv/IdentifyDevice.srv:
```
string command_id
string target_device_id
uint32 duration_ms
---
string command_id
string target_device_id
string outcome
uint32 applied_duration_ms
string detail
```

From rehab_robotics_interfaces/CMakeLists.txt (current):
```cmake
rosidl_generate_interfaces(${PROJECT_NAME}
  "msg/ProcessingBlockUpdate.msg"
  "srv/IdentifyDevice.srv"
  DEPENDENCIES std_msgs
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create four mapping service .srv files</name>
  <files>
    rehab_robotics_interfaces/srv/SetAssignment.srv,
    rehab_robotics_interfaces/srv/ApplyMapping.srv,
    rehab_robotics_interfaces/srv/GetMappingState.srv,
    rehab_robotics_interfaces/srv/ResetMapping.srv
  </files>
  <read_first>
    - rehab_robotics_interfaces/srv/IdentifyDevice.srv — existing srv pattern (fields, separator line format)
    - .planning/phases/22-model-catalog-mapping-store-and-transactional-contracts/22-CONTEXT.md — D-06 schema, D-09 state values, D-12 Apply fields, D-13 outcome strings
  </read_first>
  <action>
    Create each file under rehab_robotics_interfaces/srv/ following the ROS2 .srv format (request fields, three dashes, response fields). Use only primitive field types (string, int32, bool) — no custom message dependencies.

    SetAssignment.srv request: string device_id, string segment, string frame, string state. Response: string outcome, string detail. The state field accepts "assigned", "not_used", or "unassigned" per D-09 — no enum, plain string. Outcome values: "ok", "duplicate_frame", "invalid_frame", "unknown_device" per D-04 and D-10.

    ApplyMapping.srv request: int32 expected_revision. Response: string outcome, int32 applied_revision, string detail. Outcome values per D-13: "applied", "model_changed", "revision_mismatch", "incomplete", "duplicate_frame", "invalid_frame", "blocked".

    GetMappingState.srv request: (empty — no fields before ---). Response: string state_json. The state_json will carry the full mapping state serialized as JSON; using a single string avoids defining a custom message type for Phase 22.

    ResetMapping.srv request: string model_hash. Response: string outcome. Outcome values: "ok", "unknown_model". The model_hash field accepts the SHA-256 hex string for the target model or empty string to reset the current model's draft.
  </action>
  <verify>
    <automated>python -c "from pathlib import Path; files = ['rehab_robotics_interfaces/srv/SetAssignment.srv', 'rehab_robotics_interfaces/srv/ApplyMapping.srv', 'rehab_robotics_interfaces/srv/GetMappingState.srv', 'rehab_robotics_interfaces/srv/ResetMapping.srv']; [print(f) for f in files if '---' in Path(f).read_text()]"</automated>
  </verify>
  <done>All four .srv files exist, each contains a separator line (---), and the field names match the specification above.</done>
</task>

<task type="auto">
  <name>Task 2: Register all four services in CMakeLists.txt</name>
  <files>rehab_robotics_interfaces/CMakeLists.txt</files>
  <read_first>
    - rehab_robotics_interfaces/CMakeLists.txt — current rosidl_generate_interfaces block to extend
  </read_first>
  <action>
    Edit rehab_robotics_interfaces/CMakeLists.txt to add all four new .srv files to the rosidl_generate_interfaces() call. Add them after the existing "srv/IdentifyDevice.srv" line. The DEPENDENCIES std_msgs line remains — the new services use no custom message types. Do not change any other CMakeLists section.

    The result of the rosidl_generate_interfaces block must list exactly:
      "msg/ProcessingBlockUpdate.msg"
      "srv/IdentifyDevice.srv"
      "srv/SetAssignment.srv"
      "srv/ApplyMapping.srv"
      "srv/GetMappingState.srv"
      "srv/ResetMapping.srv"
    with DEPENDENCIES std_msgs.
  </action>
  <verify>
    <automated>python -c "
content = open('rehab_robotics_interfaces/CMakeLists.txt').read()
for name in ['SetAssignment', 'ApplyMapping', 'GetMappingState', 'ResetMapping']:
    assert name + '.srv' in content, f'Missing {name}.srv'
print('All four srv names present in CMakeLists.txt')
"</automated>
  </verify>
  <done>CMakeLists.txt lists all four new .srv files inside rosidl_generate_interfaces; existing entries (IdentifyDevice.srv, ProcessingBlockUpdate.msg) are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ROS service caller -> mapping_node | Service request fields are caller-supplied strings; mapping_node validates content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01-01 | Tampering | SetAssignment.srv state field | mitigate | mapping_node validates state value is one of {assigned, not_used, unassigned}; invalid values return outcome=invalid_state |
| T-22-01-02 | Tampering | ApplyMapping.srv expected_revision | mitigate | mapping_node rejects revision mismatch immediately per D-14; no partial apply |
| T-22-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; ROS .srv files are plain text |
</threat_model>

<verification>
python -c "
from pathlib import Path
for name in ['SetAssignment', 'ApplyMapping', 'GetMappingState', 'ResetMapping']:
    p = Path(f'rehab_robotics_interfaces/srv/{name}.srv')
    assert p.exists(), f'Missing {p}'
    assert '---' in p.read_text(), f'No separator in {p}'
cmake = Path('rehab_robotics_interfaces/CMakeLists.txt').read_text()
for name in ['SetAssignment', 'ApplyMapping', 'GetMappingState', 'ResetMapping']:
    assert name + '.srv' in cmake, f'{name}.srv not in CMakeLists.txt'
print('All interface contracts verified')
"
</verification>

<success_criteria>
- rehab_robotics_interfaces/srv/ contains SetAssignment.srv, ApplyMapping.srv, GetMappingState.srv, ResetMapping.srv
- Each .srv file has the correct request/response fields as specified
- CMakeLists.txt registers all four .srv files in rosidl_generate_interfaces
- Existing IdentifyDevice.srv and ProcessingBlockUpdate.msg entries are untouched
</success_criteria>

<output>
Create .planning/phases/22-model-catalog-mapping-store-and-transactional-contracts/22-01-SUMMARY.md when done.
</output>
