---
phase: 22-model-catalog-mapping-store-and-transactional-contracts
plan: 03
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - backend/rehab_robotics_bridge/mapping_node.py
autonomous: true
requirements: [MAP-01, MAP-02, MAP-03, MAP-04, MAP-05, MAP-06]

must_haves:
  truths:
    - "MappingNode exposes SetAssignment, ApplyMapping, GetMappingState, ResetMapping ROS services"
    - "Each device has exactly one of: assigned, not_used, unassigned state"
    - "Duplicate frame assignments are rejected at SetAssignment time with duplicate_frame outcome"
    - "Mapping store persists atomically to ~/.ros/rehab_robotics/mapping_store.json with backup and corruption recovery"
    - "Apply validates revision, frame list, completeness, and interlock before any write"
    - "Apply returns blocked when recording_active or calibration_active; does not stop the active session"
    - "Reconnected device with matching model_hash+applied_revision auto-reattaches as assigned"
    - "Publishing /rehab/mapping/current JSON occurs on every assignment state change"
  artifacts:
    - path: "backend/rehab_robotics_bridge/mapping_node.py"
      provides: "MappingNode ROS node"
      exports: ["MappingNode", "MappingStore", "main"]
      min_lines: 200
  key_links:
    - from: "backend/rehab_robotics_bridge/mapping_node.py"
      to: "/rehab/mapping/current"
      via: "String publisher on state change"
      pattern: "rehab/mapping/current"
    - from: "MappingStore.atomic_write"
      to: "mapping_store.json"
      via: "tmp file + os.replace()"
      pattern: "os.replace"
    - from: "MappingNode._on_fleet_registry"
      to: "MappingStore"
      via: "/esp/fleet/registry subscription"
      pattern: "fleet/registry"
---

<objective>
Implement mapping_node.py — the authoritative mapping state machine. Provides four ROS services (SetAssignment, ApplyMapping, GetMappingState, ResetMapping), persists state atomically to ~/.ros/rehab_robotics/mapping_store.json, publishes /rehab/mapping/current JSON on every state change, subscribes to /esp/fleet/registry for reconnect re-attach, and subscribes to /esp/recording/status + /rehab/calibration/status for Apply interlock.

Purpose: Covers all MAP-01 through MAP-06 requirements. This is the backend authoritative state for the operator mapping workflow.

Output: backend/rehab_robotics_bridge/mapping_node.py
</objective>

<execution_context>
@C:/Users/justi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/justi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-model-catalog-mapping-store-and-transactional-contracts/22-CONTEXT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Contracts and patterns this plan implements against. -->

From 22-CONTEXT.md:
D-06 store schema: {schema_version: "map.v1", model_hash: str, revision: int, assignments: {device_id: {segment: str, frame: str, state: str}}, applied_revision: int, backup_revision: int}
D-07: tmp = path.with_suffix('.tmp'); tmp.write_text(json.dumps(data)); tmp.replace(path). Backup to mapping_store.json.bak. Corruption recovery: if main invalid JSON load from .bak; if both corrupt start fresh revision=0.
D-08: Each model_hash gets its own revision counter starting at 1. Switching model_hash resets draft revision to 0 for that hash (but stored previous hash data is retained).
D-09: States: "assigned", "not_used", "unassigned". Unassigned = no decision yet.
D-10: Duplicate segment-frame rejected at SetAssignment with "duplicate_frame". Unknown device_id accepted with warning, stays unassigned by default.
D-11: Publish /rehab/mapping/current on every state change.
D-12: Apply validates: all fleet-session devices have assigned/not_used, no duplicate frames, all frames in current frame_list, revision == expected_revision.
D-13: Apply outcomes: "applied", "model_changed", "revision_mismatch", "incomplete", "duplicate_frame", "invalid_frame", "blocked".
D-14: Revision mismatch check first — before staging.
D-15: While recording_active or calibration_active, Apply returns "blocked". Does not stop active session.
D-16: SetAssignment is NOT blocked during recording/calibration.
D-17: Fleet reconnect: if newly bound device_id is in current mapping under current model_hash+applied_revision, state stays "assigned" (auto-reattach). Different device_id at old route registers as "unassigned".
D-18: Subscribe to /esp/fleet/registry (String/JSON) to track fleet events.
D-19/D-20: Solver sufficiency is best-effort warning in Phase 22 (not a hard block). Apply response detail field carries the warning if profile check fails.

Device ID canonical form: "esp32:aabbccddeeff" (12 hex lowercase, colon prefix) — Phase 20/21 decision.

From 22-01-PLAN.md interfaces (created in Wave 1):
SetAssignment.srv request: string device_id, string segment, string frame, string state
SetAssignment.srv response: string outcome, string detail
ApplyMapping.srv request: int32 expected_revision
ApplyMapping.srv response: string outcome, int32 applied_revision, string detail
GetMappingState.srv request: (empty)
GetMappingState.srv response: string state_json
ResetMapping.srv request: string model_hash
ResetMapping.srv response: string outcome

From test_fleet_bridge.py — ROS stub pattern (_install_ros_stubs):
- rclpy, rclpy.node.Node, std_msgs.msg.String, sensor_msgs.msg.Imu, rehab_robotics_interfaces.srv.IdentifyDevice all stubbed as types.ModuleType
- Node base class stubbed as type('Node', (), {})

From opensim_node.py — ROS service handler pattern:
def _on_calibration_capture(self, _request: Trigger.Request, response: Trigger.Response) -> Trigger.Response:
    ...
    return response
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement MappingStore — persistence, atomic write, and backup/corruption recovery</name>
  <files>backend/rehab_robotics_bridge/mapping_node.py</files>
  <read_first>
    - .planning/phases/22-model-catalog-mapping-store-and-transactional-contracts/22-CONTEXT.md — D-06, D-07, D-08 in full (store schema, atomic write, revision semantics)
    - backend/rehab_robotics_bridge/fleet_bridge_node.py — JSON publish pattern (build_fleet_registry, String msg)
  </read_first>
  <behavior>
    - MappingStore.__init__(store_path: Path): accepts path, creates parent dirs if needed. Loads existing data via _load().
    - MappingStore._load(): tries main file (valid JSON, schema_version=="map.v1"); on invalid JSON tries .bak; if both fail or absent, initializes fresh: {schema_version:"map.v1", model_hash:"", revision:0, assignments:{}, applied_revision:0, backup_revision:0}.
    - MappingStore._save(): atomic write via tmp=path.with_suffix('.tmp'), tmp.write_text(json.dumps(data)), tmp.replace(path). Before writing, copies current main to .bak if main exists (backup_revision updated to current revision).
    - MappingStore.set_assignment(device_id, segment, frame, state) -> tuple[str, str]: validates state in {"assigned","not_used","unassigned"}; if state=="assigned" checks no other device already has the same (segment, frame) pair (returns "duplicate_frame", reason); stores assignment; bumps revision by 1; calls _save(); returns ("ok", "").
    - MappingStore.reset(model_hash: str) -> str: clears assignments for the given model_hash (or current if empty); sets revision=0 for that hash; calls _save(); returns "ok".
    - MappingStore.get_state_dict() -> dict: returns the full in-memory state as a dict for JSON serialization in GetMappingState.
    - MappingStore has property applied_revision, revision, model_hash, assignments.
    - Per D-08: MappingStore holds one active model_hash at a time. set_assignment raises if model_hash is empty (no catalog loaded yet). Switching model_hash (via set_model_hash(hash)) clears draft assignments for the new hash (loads stored assignments for that hash if any) and resets revision counter for that hash to 0 (or stored value if previously saved).
  </behavior>
  <action>
    Create backend/rehab_robotics_bridge/mapping_node.py. Start with from __future__ import annotations. Stdlib imports: copy, json, os, threading, time from stdlib; Path from pathlib; Any from typing.

    Do NOT import rclpy or any ROS package at module level in this first task — MappingStore is pure Python with no ROS dependency. Place the class before the node class.

    DEFAULT_STORE_PATH = Path.home() / ".ros" / "rehab_robotics" / "mapping_store.json"

    MappingStore._data dict has the D-06 schema. All mutation goes through _save(). The _data["assignments"] is keyed by device_id. Each assignment entry: {"segment": str, "frame": str, "state": str}. When state is "unassigned", segment and frame may be empty strings.

    set_assignment rules in order:
    1. Normalize device_id: must start with "esp32:" and have exactly 12 hex chars after colon; if not, return ("invalid_device_id", "device_id must be esp32:<12hex>").
    2. Validate state value: if not in {"assigned","not_used","unassigned"} return ("invalid_state", "state must be assigned|not_used|unassigned").
    3. If state == "assigned" and model_hash is empty: return ("no_model", "no model loaded").
    4. If state == "assigned": check for duplicate (segment, frame) pair among other devices; if found return ("duplicate_frame", f"frame {frame} on segment {segment} already assigned to {other_device_id}").
    5. Store assignment; bump revision by 1; call _save(); return ("ok", "").

    set_model_hash(new_hash: str): if new_hash == self._data["model_hash"] return (no change). Load stored assignments for new_hash from _data.get("hash_assignments", {}).get(new_hash, {}). Set _data["model_hash"] = new_hash, _data["assignments"] = loaded_assignments, _data["revision"] = stored_revision for that hash or 0. Do not call _save() here — save happens at next set_assignment or apply. Store previous hash assignments back into _data["hash_assignments"][old_hash] before switching.

    frame_list property and set_frame_list(frame_list: list[dict]): stores the current frame_list in memory (not persisted — it comes from model_catalog). Used by Apply validation.
  </action>
  <verify>
    <automated>cd backend && python -c "
import sys, types, tempfile, json
from pathlib import Path

# Stub ROS modules before any import
for mod_name in ['rclpy', 'rclpy.node', 'std_msgs', 'std_msgs.msg', 'rehab_robotics_interfaces', 'rehab_robotics_interfaces.srv']:
    sys.modules.setdefault(mod_name, types.ModuleType(mod_name))
sys.modules['rclpy.node'].Node = type('Node', (), {})
sys.modules['std_msgs.msg'].String = type('String', (), {'__init__': lambda s, **k: None})
sys.modules['rehab_robotics_interfaces.srv'].SetAssignment = type('SetAssignment', (), {})
sys.modules['rehab_robotics_interfaces.srv'].ApplyMapping = type('ApplyMapping', (), {})
sys.modules['rehab_robotics_interfaces.srv'].GetMappingState = type('GetMappingState', (), {})
sys.modules['rehab_robotics_interfaces.srv'].ResetMapping = type('ResetMapping', (), {})

import importlib.util
spec = importlib.util.spec_from_file_location('mapping_node', 'rehab_robotics_bridge/mapping_node.py')
mod = importlib.util.module_from_spec(spec)
spec.loader.exec_module(mod)

with tempfile.TemporaryDirectory() as tmpdir:
    store = mod.MappingStore(Path(tmpdir) / 'test_store.json')
    store.set_model_hash('abc123')
    store.set_frame_list([{'segment': 'femur_r', 'frame': 'femur_r_imu'}])
    outcome, detail = store.set_assignment('esp32:aabbccddeeff', 'femur_r', 'femur_r_imu', 'assigned')
    assert outcome == 'ok', f'Expected ok, got {outcome}: {detail}'
    # duplicate frame
    outcome2, detail2 = store.set_assignment('esp32:112233445566', 'femur_r', 'femur_r_imu', 'assigned')
    assert outcome2 == 'duplicate_frame', f'Expected duplicate_frame, got {outcome2}'
    # persistence
    store2 = mod.MappingStore(Path(tmpdir) / 'test_store.json')
    assert 'esp32:aabbccddeeff' in store2.assignments
    print('MappingStore: PASSED')
"
</automated>
  </verify>
  <done>MappingStore loads, saves atomically, rejects duplicate frame assignments, normalizes device_id format, and persists assignments across instantiation. Corruption recovery logic is present in _load().</done>
</task>

<task type="auto">
  <name>Task 2: Implement MappingNode — ROS services, subscriptions, Apply interlock, and reconnect re-attach</name>
  <files>backend/rehab_robotics_bridge/mapping_node.py</files>
  <read_first>
    - backend/rehab_robotics_bridge/opensim_node.py — service creation pattern (self.create_service), handler signature, publish pattern (lines 274-293)
    - backend/rehab_robotics_bridge/fleet_bridge_node.py — /esp/fleet/registry subscription pattern and JSON parse (FLEET_REGISTRY_TOPIC constant, subscription callback)
    - .planning/phases/22-model-catalog-mapping-store-and-transactional-contracts/22-CONTEXT.md — D-11 through D-20 in full
  </read_first>
  <action>
    Extend mapping_node.py with the ROS imports block after the MappingStore class: import rclpy, from rclpy.node import Node, from std_msgs.msg import String, from rehab_robotics_interfaces.srv import SetAssignment, ApplyMapping, GetMappingState, ResetMapping.

    MAPPING_CATALOG_TOPIC = "/rehab/model/catalog"
    MAPPING_CURRENT_TOPIC = "/rehab/mapping/current"
    FLEET_REGISTRY_TOPIC = "/esp/fleet/registry"
    RECORDING_STATUS_TOPIC = "/esp/recording/status"
    CALIBRATION_STATUS_TOPIC = "/rehab/calibration/status"

    MappingNode(Node) class, node name "mapping_node":
    - Declare parameter: store_path (string, default ""). If empty, use DEFAULT_STORE_PATH.
    - Initialize self._store = MappingStore(store_path).
    - Initialize self._recording_active = False, self._calibration_active = False.
    - Initialize self._lock = threading.Lock() (all service handlers and subscription callbacks acquire this lock before touching self._store).
    - Create String publisher on MAPPING_CURRENT_TOPIC, depth 10.
    - Create String subscriber on MAPPING_CATALOG_TOPIC with _on_catalog callback.
    - Create String subscriber on FLEET_REGISTRY_TOPIC with _on_fleet_registry callback.
    - Create String subscriber on RECORDING_STATUS_TOPIC with _on_recording_status callback.
    - Create String subscriber on CALIBRATION_STATUS_TOPIC with _on_calibration_status callback.
    - Create four services: SetAssignment service -> _on_set_assignment, ApplyMapping -> _on_apply_mapping, GetMappingState -> _on_get_mapping_state, ResetMapping -> _on_reset_mapping.

    _on_catalog(msg): parse JSON, extract model_hash and frame_list. Call self._store.set_model_hash(model_hash) and self._store.set_frame_list(frame_list). Publish current state (call _publish_current()). If parse fails, log warning and return.

    _on_fleet_registry(msg): parse JSON, extract "devices" list. For each device with "route" state containing "connected" (check device["route"] == "connected" or device.get("route_state") == "connected"): get device_id = device.get("device_id"). Per D-17: if device_id in self._store.assignments and self._store.assignments[device_id]["state"] == "assigned" and self._store.applied_revision > 0: state remains "assigned" (no-op, auto-reattach). If device_id NOT in assignments: add with state="unassigned" (no save — only draft update, call _publish_current()). If parse fails, log warning and return.

    _on_recording_status(msg): parse JSON, set self._recording_active from "active" or "recording" field (check msg_data.get("active") or msg_data.get("state") in {"recording", "active"}). If parse fails, default False.

    _on_calibration_status(msg): parse JSON, set self._calibration_active from "active" field or state == "capturing". If parse fails, default False.

    _on_set_assignment(request, response): acquire lock. Delegate to self._store.set_assignment(request.device_id, request.segment, request.frame, request.state). Set response.outcome, response.detail. Call _publish_current(). Return response. Per D-16: NO interlock check here.

    _on_apply_mapping(request, response): acquire lock. Check interlock first: if self._recording_active return "blocked" with detail "recording_active". If self._calibration_active return "blocked" with detail "calibration_active". Revision check: if self._store.revision != request.expected_revision return "revision_mismatch" immediately. Model check: if self._store.model_hash == "" return "model_changed" with detail "no_model_loaded". Completeness check: verify all devices in self._store.assignments have state != "unassigned"; if any unassigned return "incomplete". Duplicate check: verify no two "assigned" devices share the same (segment, frame) pair; if duplicate return "duplicate_frame". Frame validity check: verify all "assigned" (segment, frame) pairs appear in self._store.frame_list; if any missing return "invalid_frame" with detail naming the unknown frame. Solver sufficiency check (D-20 best-effort): if no device is "assigned" (all are "not_used"), add "solver_insufficient" warning to detail but do NOT block apply. Atomic swap: update self._store._data["applied_revision"] = self._store.revision; call self._store._save(). Set response.outcome = "applied", response.applied_revision = self._store.applied_revision, response.detail = solver_warning_or_empty. Call _publish_current(). Return response.

    _on_get_mapping_state(request, response): acquire lock. Build state_dict from self._store.get_state_dict(). Set response.state_json = json.dumps(state_dict, sort_keys=True, separators=(",",":")). Return response.

    _on_reset_mapping(request, response): acquire lock. Call self._store.reset(request.model_hash). Call _publish_current(). response.outcome = "ok". Return response.

    _publish_current(): builds JSON from self._store.get_state_dict() plus {"schema": "rehab.mapping_current.1"}. Publishes to MAPPING_CURRENT_TOPIC.

    main(): rclpy.init(), MappingNode(), rclpy.spin(), finally destroy_node(), rclpy.try_shutdown().
  </action>
  <verify>
    <automated>cd backend && python -c "
import ast
from pathlib import Path
src = Path('rehab_robotics_bridge/mapping_node.py').read_text()
tree = ast.parse(src)
classes = [n.name for n in ast.walk(tree) if isinstance(n, ast.ClassDef)]
funcs = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef)]
assert 'MappingNode' in classes, 'MappingNode class missing'
assert 'MappingStore' in classes, 'MappingStore class missing'
assert 'main' in funcs, 'main missing'
assert 'rehab/mapping/current' in src, 'MAPPING_CURRENT_TOPIC missing'
assert 'rehab/model/catalog' in src, 'MAPPING_CATALOG_TOPIC missing'
assert 'esp/fleet/registry' in src, 'FLEET_REGISTRY_TOPIC missing'
assert 'esp/recording/status' in src, 'RECORDING_STATUS_TOPIC missing'
assert 'rehab/calibration/status' in src, 'CALIBRATION_STATUS_TOPIC missing'
assert 'os.replace' in src, 'atomic write pattern missing'
assert 'revision_mismatch' in src, 'revision_mismatch outcome missing'
assert 'blocked' in src, 'blocked outcome missing'
assert 'duplicate_frame' in src, 'duplicate_frame outcome missing'
assert 'invalid_frame' in src, 'invalid_frame outcome missing'
assert 'incomplete' in src, 'incomplete outcome missing'
print('MappingNode structure: PASSED')
"
</automated>
  </verify>
  <done>MappingNode class exists with all five service handlers and all four subscription callbacks. All required outcome strings and topic constants are present in source. Atomic write pattern (os.replace) is present. Module passes AST parse.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ROS service callers -> MappingNode | device_id, segment, frame strings are caller-supplied |
| /esp/fleet/registry topic -> MappingNode | Fleet JSON is ROS-internal but untrusted string content |
| ~/.ros/rehab_robotics/ directory -> MappingNode | File system persistence; backup recovery for corruption |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03-01 | Spoofing | device_id format validation | mitigate | set_assignment normalizes and validates "esp32:<12hex>" format; rejects malformed IDs with "invalid_device_id" |
| T-22-03-02 | Tampering | concurrent service calls | mitigate | threading.Lock acquired by all service handlers and subscription callbacks; no partial-write race |
| T-22-03-03 | Tampering | Apply partial write | mitigate | Stage+validate in memory first; only call _save() after all checks pass; previous applied_revision preserved on any failure path |
| T-22-03-04 | Tampering | Corruption of mapping_store.json | mitigate | .bak backup written before each atomic swap; corruption recovery starts fresh (revision=0) rather than using partial data |
| T-22-03-05 | Elevation of Privilege | Apply during recording | mitigate | _on_apply_mapping checks _recording_active and _calibration_active before any validation; returns "blocked" without reading/modifying store |
| T-22-03-06 | Information Disclosure | state_json in GetMappingState | accept | GetMappingState is a ROS service accessible to any rosbridge subscriber; no credentials stored in mapping state |
| T-22-03-SC | Tampering | npm/pip/cargo installs | accept | No new package installs; all imports are stdlib or existing ROS packages |
</threat_model>

<verification>
cd backend && python -c "
import ast
from pathlib import Path
src = Path('rehab_robotics_bridge/mapping_node.py').read_text()
tree = ast.parse(src)
classes = [n.name for n in ast.walk(tree) if isinstance(n, ast.ClassDef)]
assert 'MappingNode' in classes
assert 'MappingStore' in classes
for topic in ['rehab/mapping/current', 'rehab/model/catalog', 'esp/fleet/registry', 'esp/recording/status', 'rehab/calibration/status']:
    assert topic in src, f'Topic {topic} missing'
for outcome in ['applied', 'revision_mismatch', 'blocked', 'duplicate_frame', 'invalid_frame', 'incomplete', 'model_changed']:
    assert outcome in src, f'Outcome string {outcome} missing'
assert 'os.replace' in src
assert 'threading' in src
print('mapping_node.py: ALL CHECKS PASSED')
"
</verification>

<success_criteria>
- mapping_node.py exists and passes AST parse
- MappingStore implements atomic write (os.replace pattern), backup (.bak), and corruption recovery
- All four service handlers are present and return the specified outcome strings
- Apply interlock blocks on recording_active or calibration_active without altering the active session
- Reconnect re-attach logic present in _on_fleet_registry
- All five topic constants are in source
- threading.Lock used for concurrent access protection
</success_criteria>

<output>
Create .planning/phases/22-model-catalog-mapping-store-and-transactional-contracts/22-03-SUMMARY.md when done.
</output>
