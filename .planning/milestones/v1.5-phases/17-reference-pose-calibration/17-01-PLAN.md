---
phase: 17-reference-pose-calibration
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/rehab_robotics_bridge/opensim/calibration.py
  - backend/rehab_robotics_bridge/opensim/__init__.py
  - backend/test/test_opensim_calibration.py
autonomous: true
requirements:
  - IK-01
  - IK-02
  - IK-03
must_haves:
  truths:
    - "Calibration state machine exposes UNCALIBRATED, CAPTURING, CALIBRATED, FAILED with reason."
    - "Capture uses a multi-sample stable window and rejects motion above a dispersion threshold (not first-frame auto-cal)."
    - "Clear returns to UNCALIBRATED and drops active mounting offsets."
    - "may_publish_joint_states remains false for every non-CALIBRATED state; CALIBRATED alone does not invent joint angles."
  artifacts:
    - path: "backend/rehab_robotics_bridge/opensim/calibration.py"
      provides: "Pure-Python CalibrationController + mounting-offset math + status snapshot"
      exports: ["CalibrationController", "CalibrationArtifact", "DEFAULT_CAPTURE_WINDOW_S"]
    - path: "backend/test/test_opensim_calibration.py"
      provides: "Deterministic unit tests for window, reject, clear, gate"
  key_links:
    - from: "calibration.py"
      to: "ik_contracts.CalibrationState"
      via: "imports locked enum / may_publish_joint_states"
      pattern: "CalibrationState|may_publish_joint_states"
    - from: "CalibrationController.feed_pair"
      to: "stable window → offsets"
      via: "multi-sample average when dispersion within threshold"
      pattern: "CAPTURING|dispersion|offset"
---

<objective>
Build a pure-Python reference-pose calibration controller that implements the Phase 17 state machine, multi-sample stable-window capture, mounting-offset computation, and clear/invalidation — with no ROS or OpenSim solver imports.

Purpose: Lock the hard calibration gate and OpenSense-style offset math behind deterministic tests before wiring services/UI (D-17-03, D-17-06, D-17-07 core).
Output: `opensim/calibration.py` + `test_opensim_calibration.py`.
</objective>

<execution_context>
@C:/Users/justi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/justi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-reference-pose-calibration/17-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/research/SUMMARY.md
@docs/opensim-ik-contracts.md
@backend/rehab_robotics_bridge/opensim/ik_contracts.py
@backend/rehab_robotics_bridge/opensim_adapter.py

<locked_decisions>
- D-17-02: Known pose = standing, knees extended (encode as `known_pose="standing_knees_extended"`).
- D-17-03: Hard gate — no joint-state publish until CALIBRATED (`may_publish_joint_states`).
- D-17-04: Clear → UNCALIBRATED and invalidate offsets.
- D-17-05: States UNCALIBRATED / CAPTURING / CALIBRATED / FAILED (+ reason).
- D-17-06: Multi-sample stable window; reject if motion exceeds threshold.
- Deferred: multi-pose library, cross-session versioned save/load, clinical validation, InverseKinematicsSolver (Phase 18).
- Do NOT reuse browser `RelativeAngleStabilizer` / RosbridgeDataSource angle baseline as mounting calibration.
</locked_decisions>

<interfaces>
From `backend/rehab_robotics_bridge/opensim/ik_contracts.py`:
```python
class CalibrationState(str, Enum):
    UNCALIBRATED = "UNCALIBRATED"
    CAPTURING = "CAPTURING"
    CALIBRATED = "CALIBRATED"
    FAILED = "FAILED"

def may_publish_joint_states(state: CalibrationState | str) -> bool: ...
CALIBRATION_CAPTURE_SERVICE = "/opensim/calibration/capture"
CALIBRATION_CLEAR_SERVICE = "/opensim/calibration/clear"
CALIBRATION_STATUS_TOPIC = "/opensim/calibration_status"
JOINT_STATES_TOPIC = "/opensim/joint_states"
```

From `opensim_adapter.py` (reuse quaternion helpers only — not relative_orientation_angle_deg as product IK):
- `ros_xyzw_to_opensim_rotation(x,y,z,w)` for normalize/validate
- Quaternion multiply / conjugate patterns already used in `relative_orientation_angle_deg` (copy math into calibration module; do not call the debug angle API as the calibration product)

Discretion defaults (Claude — start conservative):
- `DEFAULT_CAPTURE_WINDOW_S = 1.5`
- `DEFAULT_MIN_SAMPLES = 10`
- `DEFAULT_MAX_DISPERSION_DEG = 8.0` (max angular deviation of any sample from the window’s running mean, per sensor)
- Persistence: in-memory `CalibrationArtifact` only
- Failed capture while already CALIBRATED: keep prior artifact active and leave state CALIBRATED (transactional); surface last_capture_error on status. First-time failure → FAILED with reason.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing tests for calibration controller</name>
  <files>backend/test/test_opensim_calibration.py</files>
  <behavior>
    - New controller starts UNCALIBRATED; may_publish_joint_states(controller.state) is False; artifact is None.
    - begin_capture() → CAPTURING; feeding fewer than min_samples keeps CAPTURING; no artifact yet.
    - Stable window of identical identity quaternions for master+slave over window_s + min_samples → CALIBRATED with non-None artifact containing master_xyzw and slave_xyzw offsets; may_publish True.
    - Unstable window (master yaw swings beyond DEFAULT_MAX_DISPERSION_DEG during capture) → FAILED with reason containing stability/dispersion language when no prior artifact; may_publish False.
    - clear() from CALIBRATED → UNCALIBRATED, artifact None, may_publish False.
    - begin_capture while already CAPTURING returns False / rejected (idempotent reject).
    - First-frame alone never yields CALIBRATED (assert after a single feed_pair).
  </behavior>
  <action>Create `backend/test/test_opensim_calibration.py` with unittest cases covering the behaviors above. Import from `rehab_robotics_bridge.opensim.calibration` and `rehab_robotics_bridge.opensim.ik_contracts`. Keep tests ROS-free (no rclpy). Run first and confirm RED (import/module missing or assertions fail).</action>
  <verify>
    <automated>$env:PYTHONPATH='backend'; python -m unittest backend.test.test_opensim_calibration -v</automated>
  </verify>
  <done>Failing tests exist that encode D-17-03/04/05/06 behaviors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement CalibrationController + mounting offsets</name>
  <files>backend/rehab_robotics_bridge/opensim/calibration.py, backend/rehab_robotics_bridge/opensim/__init__.py</files>
  <behavior>
    - Same behaviors as Task 1 must pass green.
    - status_dict() returns JSON-safe keys: state, reason, known_pose, sample_count, window_s, calibration_id, has_offsets (bool). Do not require publishing joint angles.
  </behavior>
  <action>
Implement `CalibrationController` in `backend/rehab_robotics_bridge/opensim/calibration.py` per D-17-02..06:

1. Dataclass `CalibrationArtifact` with calibration_id (uuid4 hex), known_pose=`standing_knees_extended`, master_xyzw, slave_xyzw (unit quaternions), captured_at_monotonic, sample_count, window_s, mean_dispersion_deg.
2. OpenSense-style mounting offsets for identity reference pose (standing / knees extended): average antipode-aware unit quaternions per sensor over the accepted window; store mean orientations as the sensor-to-model mounting offsets relative to identity model IMU frames. Document the formula in a short module docstring (R_mount ≈ R_sensor_mean for identity reference). Do not call OpenSim InverseKinematicsSolver.
3. API: `begin_capture() -> tuple[bool, str]` (accept/reject), `clear() -> None`, `feed_pair(master_xyzw, slave_xyzw, *, monotonic_time: float) -> CalibrationState` (only accumulates while CAPTURING), `state` property, `artifact` property, `status_dict() -> dict`.
4. Stability: while CAPTURING, track per-sensor angular deviation from running mean; if any sample exceeds DEFAULT_MAX_DISPERSION_DEG, abort capture (FAILED or restore prior per discretion above).
5. Use `CalibrationState` and `may_publish_joint_states` from ik_contracts — do not redefine divergent state strings.
6. Update `opensim/__init__.py` docstring to note calibration module exists (Phase 17).

Avoid: browser RelativeAngleStabilizer logic; publishing JointState; rclpy; full IK solver.
  </action>
  <verify>
    <automated>$env:PYTHONPATH='backend'; python -m unittest backend.test.test_opensim_calibration -v</automated>
  </verify>
  <done>Controller implements stable-window capture, clear, and CALIBRATED gate semantics; all new tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IMU quaternion samples → CalibrationController | Untrusted orientations; must be finite/unit before averaging |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | feed_pair inputs | mitigate | Reject non-finite / non-unit quaternions via ros_xyzw_to_opensim_rotation or equivalent normalize+validate before buffering |
| T-17-02 | Elevation of privilege | may_publish_joint_states | mitigate | Gate helper true only for CALIBRATED; tests assert UNCALIBRATED/CAPTURING/FAILED false |
| T-17-03 | Denial of service | begin_capture spam | accept | Reject while CAPTURING; low risk local operator tool |
| T-17-SC | Tampering | package installs | accept | No new npm/pip packages in this plan |
</threat_model>

<verification>
Unit tests prove multi-sample requirement, motion reject, clear invalidation, and publish gate without ROS.
</verification>

<success_criteria>
- `test_opensim_calibration` passes.
- Module has no rclpy/OpenSim solver imports.
- States and service-name constants remain aligned with ik_contracts.
</success_criteria>

<output>
Create `.planning/phases/17-reference-pose-calibration/17-01-SUMMARY.md` when done
</output>
