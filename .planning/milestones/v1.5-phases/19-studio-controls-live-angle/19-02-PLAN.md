---
phase: 19-studio-controls-live-angle
plan: "02"
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - rehab-robotics-studio/src/types/health.ts
  - rehab-robotics-studio/src/types/signals.ts
  - rehab-robotics-studio/src/data/liveKneeAngle.ts
  - rehab-robotics-studio/src/data/liveKneeAngle.test.ts
autonomous: true
requirements: [VIS-02, IK-06]
must_haves:
  truths:
    - "Only a calibrated, valid, identity-matched, finite, ordered, <=2,000 ms JointState sample derives a live knee value."
    - "PI/2 becomes 90 degrees once; valid zero remains zero; every unsafe state is null."
  artifacts:
    - path: "rehab-robotics-studio/src/data/liveKneeAngle.ts"
      provides: "Pure contract, tracker, monotonic stale timer, transition signatures"
      contains: "IK_ANGLE_STALE_MS"
    - path: "rehab-robotics-studio/src/data/liveKneeAngle.test.ts"
      provides: "Deterministic gate/timer coverage"
  key_links:
    - from: "OpenSim status/IK/JointState snapshot types"
      to: "LiveKneeAngleSnapshot"
      via: "pure fail-closed derivation"
      pattern: "LiveKneeAngle"
---

<objective>
Define and prove the pure frontend contract that turns official IK-06 JointState data into a nullable, timed product angle.

Purpose: Lock every trust gate before rosbridge, Zustand, graph, or React wiring can consume the value.
Output: Types, pure derivation/tracker, timer seam, and exhaustive targeted tests.
</objective>

<execution_context>
@C:/Users/justi/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/justi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-studio-controls-live-angle/19-CONTEXT.md
@.planning/phases/19-studio-controls-live-angle/19-UI-SPEC.md
@.planning/phases/19-studio-controls-live-angle/19-RESEARCH.md
@.planning/phases/18-realtime-opensim-ik-outputs/18-03-SUMMARY.md
@rehab-robotics-studio/src/types/health.ts
@rehab-robotics-studio/src/types/signals.ts
@rehab-robotics-studio/src/components/dashboard/calibrationStatus.ts
@rehab-robotics-studio/src/graph/productKneeReadout.test.ts

<locked_decisions>
- D-19-05/06/07: knee_angle_r only, one radians-to-degrees conversion, full calibration/validity gate, null not fake/stale zero.
- D-19-10/11: Exactly 2,000 ms local monotonic freshness and transition-only logging signatures.
</locked_decisions>

<interfaces>
Define `OpenSimIkStatusSnapshot`, validated `RosStamp {sec,nanosec}`, `OpenSimJointStateSnapshot`, and discriminated `LiveKneeAngleSnapshot`. Keep deprecated `Frame.jointAngleDeg` debug-only and introduce separate optional `Frame.openSimKneeAngleDeg`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define and prove the fail-closed live-angle contract</name>
  <files>rehab-robotics-studio/src/types/health.ts, rehab-robotics-studio/src/types/signals.ts, rehab-robotics-studio/src/data/liveKneeAngle.ts, rehab-robotics-studio/src/data/liveKneeAngle.test.ts</files>
  <read_first>
    - `19-WORKTREE-BASELINE.local.md` — re-check exact paths and preserve recorded content.
    - `types/health.ts` and `types/signals.ts` — current calibration/OpenSim/Frame contracts.
    - `calibrationStatus.ts` — pure formatter pattern.
    - `productKneeReadout.test.ts` — existing custom-angle fail-closed assertions.
    - UI-SPEC “Live Angle Data Contract” and “Angle state matrix”.
  </read_first>
  <action>Add explicit IK, JointState, ROS-stamp, visualizer-request, and discriminated live-angle types. Implement `IK_ANGLE_STALE_MS = 2_000`, safe lexicographic stamp validation/ordering, normalized reasons, and deterministic derivation/tracker seams with injected monotonic clock and scheduler. Require CALIBRATED, `solution_valid === true`, equal non-empty calibration IDs, equal-length arrays, named finite `knee_angle_r`, usable nondecreasing stamp, and receipt age <=2,000 ms. Convert radians once, clear cached JointState when calibration closes, cancel/replace stale timers, and emit transition notifications only when `{state,reason}` changes.</action>
  <verify>
    <automated>cd rehab-robotics-studio; npm exec -- tsx --test src/data/liveKneeAngle.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `[source]` All explicit types exist and `IK_ANGLE_STALE_MS` equals 2,000.
    - `[behavior]` PI/2 -> 90 and valid zero -> zero only with every gate open.
    - `[fail-closed]` Missing/malformed/mismatched/non-finite/out-of-order/stale/clear-cal states return null.
    - `[tests]` Targeted tests pass 1,999/2,001 ms, timer replacement, recovery, reordered coordinates, bad stamps, and transition coalescing.
    - `[security]` Stamps are bounded integer pairs; no raw JSON/reason object or unsafe nanosecond integer enters the view model.
    - `[preservation]` Baseline content remains and only four exact paths are staged.
  </acceptance_criteria>
  <done>The pure product-angle contract is exhaustive, deterministic, and independently green.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Severity | Disposition | Mitigation |
|-----------|----------|----------|-------------|------------|
| T-19-06 | Spoofing calibration identity | high | mitigate | Equal non-empty active/IK IDs required. |
| T-19-08 | Stale/out-of-order data | high | mitigate | Ordered stamp plus monotonic 2,000 ms timer. |
| T-19-19 | Fake zero | high | mitigate | Null union distinct from finite zero. |
| T-19-SC | Supply chain | low | accept | Installed tsx only; no install. |

No high-severity threat may remain unmitigated.
</threat_model>

<verification>Run the targeted live-angle test file.</verification>
<success_criteria>IK-06 has a tested fail-closed frontend contract independent of transport and UI.</success_criteria>
<output>Create `.planning/phases/19-studio-controls-live-angle/19-02-SUMMARY.md` when done.</output>
