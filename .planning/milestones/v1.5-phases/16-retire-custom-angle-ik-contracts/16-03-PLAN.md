---
phase: 16-retire-custom-angle-ik-contracts
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - docs/opensim-ik-contracts.md
  - backend/rehab_robotics_bridge/opensim/ik_contracts.py
  - backend/test/test_ik_contracts.py
autonomous: true
requirements:
  - IK-00
must_haves:
  truths:
    - "Documented contract locks /opensim/joint_states as sensor_msgs/JointState output for later phases."
    - "Documented contract locks hard calibration gate: no joint-state publication until CALIBRATED."
    - "Documented contract lists paired IMU inputs and calibration capture/clear services for Phases 17–18."
    - "Machine-checkable constants/tests assert the locked topic names and gate rule."
  artifacts:
    - path: "docs/opensim-ik-contracts.md"
      provides: "Human-readable IK/calibration ROS contracts for Phases 17–19"
    - path: "backend/rehab_robotics_bridge/opensim/ik_contracts.py"
      provides: "Frozen string constants for topics/services/states"
    - path: "backend/test/test_ik_contracts.py"
      provides: "Asserts locked names and gate policy text/constants"
  key_links:
    - from: "docs/opensim-ik-contracts.md"
      to: "/opensim/joint_states"
      via: "locked output topic name per D-16-03"
      pattern: "/opensim/joint_states"
    - from: "ik_contracts.py"
      to: "CALIBRATED gate"
      via: "constant documenting no JointState publish until CALIBRATED per D-16-04"
      pattern: "CALIBRATED|JOINT_STATES_TOPIC|publish.*calibrat"
---

<objective>
Lock the ROS contracts for calibration-gated OpenSim IK inputs/outputs without implementing the solver or calibration UI.

Purpose: Give Phases 17–19 a single authoritative contract (docs + constants) for `/opensim/joint_states`, pairing, status, and the hard calibration gate (D-16-03, D-16-04).
Output: `docs/opensim-ik-contracts.md`, `ik_contracts.py` constants, and unittest coverage of locked names/gate.
</objective>

<execution_context>
@C:/Users/justi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/justi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-retire-custom-angle-ik-contracts/16-CONTEXT.md
@.planning/research/SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
@.planning/ROADMAP.md
@docs/opensim-quaternion-live-link.md

<locked_decisions>
- D-16-03: Output contract for later phases is `sensor_msgs/JointState` on `/opensim/joint_states` (name locked). Prefer this over ARCHITECTURE.md draft `/joint_states`.
- D-16-04: Calibration gate is hard — no joint-state publication until CALIBRATED.
- Deferred: implementing solver (18) and Calibrate/visualizer UI (17, 19).
</locked_decisions>

<interfaces>
Research anchors to encode (contracts only — no runtime publishers/services in this plan):

Inputs (pairing later):
- Master/slave `sensor_msgs/Imu` on configurable topics (defaults `/esp32/master/imu`, `/esp32/slave/imu`).
- Frame defaults `femur_r_imu` / `tibia_r_imu`.

Controls (Phase 17):
- `/opensim/calibration/capture` — `std_srvs/Trigger`
- `/opensim/calibration/clear` — `std_srvs/Trigger`

Outputs (Phase 18):
- `/opensim/joint_states` — `sensor_msgs/JointState`, positions in radians, stamp = synchronized observation time; **publish only when calibration state is CALIBRATED and solution valid**.
- `/opensim/ik_status` (or typed equivalent) — validity, residuals/age, calibration identity.
- `/opensim/calibration_status` — UNCALIBRATED | CAPTURING | CALIBRATED | FAILED + reason.
- `/diagnostics` — standard health summary.

Explicit non-contract / retired:
- `/opensim/joint_angle` Float64 relative-quat degrees is NOT the product IK output.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Machine-checkable IK contract constants and tests</name>
  <files>backend/rehab_robotics_bridge/opensim/ik_contracts.py, backend/test/test_ik_contracts.py</files>
  <behavior>
    - JOINT_STATES_TOPIC equals "/opensim/joint_states".
    - JOINT_STATES_MSG_TYPE equals "sensor_msgs/msg/JointState" (or equivalent string constant used consistently).
    - CALIBRATION_CAPTURE_SERVICE equals "/opensim/calibration/capture"; CLEAR equals "/opensim/calibration/clear".
    - CalibrationState enum/constants include UNCALIBRATED, CAPTURING, CALIBRATED, FAILED.
    - Helper or documented constant asserts joint-state publication is allowed only when state == CALIBRATED (e.g. `may_publish_joint_states(state) -> bool` returns True only for CALIBRATED).
    - Deprecated PRODUCT_JOINT_ANGLE_TOPIC constant "/opensim/joint_angle" marked deprecated and tests assert it is not equal to JOINT_STATES_TOPIC.
  </behavior>
  <action>Create package module `backend/rehab_robotics_bridge/opensim/` if missing (`__init__.py` as needed). Write tests first, then constants + `may_publish_joint_states`. Pure Python only — no rclpy node, no OpenSim import, no service servers. Do not implement capture/clear handlers or JointState publishers.</action>
  <verify>
    <automated>$env:PYTHONPATH='backend'; python -m unittest backend.test.test_ik_contracts -v</automated>
  </verify>
  <done>Locked topic/service/gate constants exist and are asserted by unittest.</done>
</task>

<task type="auto">
  <name>Task 2: Write opensim-ik-contracts.md for later phases</name>
  <files>docs/opensim-ik-contracts.md</files>
  <action>Author `docs/opensim-ik-contracts.md` as the Phase 16 contract sheet. Include: purpose/boundary (contracts only); paired IMU inputs and defaults; hard calibration gate (D-16-04) with may-publish rule; locked `/opensim/joint_states` JointState semantics (radians, source stamp, no stale republish) per D-16-03 and STACK.md; calibration services capture/clear; status topics (ik_status, calibration_status, diagnostics); explicit retirement of `/opensim/joint_angle` as product IK; pointer that Phase 15 live-link doc remains for triad visualization only; "not in this milestone phase" list (solver, toolbar Calibrate, visualizer button). Cross-link `ik_contracts.py` as the machine-readable twin. Keep ARCHITECTURE `/joint_states` draft noted as superseded by D-16-03 `/opensim/joint_states`.</action>
  <verify>
    <automated>$env:PYTHONPATH='backend'; python -c "from pathlib import Path; t=Path('docs/opensim-ik-contracts.md').read_text(encoding='utf-8'); req=['/opensim/joint_states','CALIBRATED','/opensim/calibration/capture','/opensim/calibration/clear','/opensim/joint_angle','sensor_msgs']; missing=[s for s in req if s not in t]; assert not missing, missing; from rehab_robotics_bridge.opensim.ik_contracts import JOINT_STATES_TOPIC; assert JOINT_STATES_TOPIC=='/opensim/joint_states'"</automated>
  </verify>
  <done>Contract doc exists with locked names/gate and matches ik_contracts.py topic constant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Contract doc → future implementers | Wrong topic names would wire GUI/solver incorrectly |
| Calibration gate constant → Phase 18 publisher | Gate must remain hard-fail-closed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-07 | Tampering | Topic name drift (/joint_states vs /opensim/joint_states) | mitigate | Lock D-16-03 in docs + unittest constants |
| T-16-08 | Denial of integrity | Publishing angles while UNCALIBRATED | mitigate | Document + `may_publish_joint_states` only true for CALIBRATED |
| T-16-09 | Spoofing | Retired /opensim/joint_angle confused as IK | mitigate | Explicit retirement section in contracts doc |
| T-16-SC | Tampering | package installs | accept | No new third-party packages |
</threat_model>

<verification>
unittest for ik_contracts plus the doc content assertion command in Task 2.
</verification>

<success_criteria>
- `/opensim/joint_states` + JointState + hard CALIBRATED gate documented and constant-tested
- Calibration capture/clear service names documented
- Custom `/opensim/joint_angle` marked retired as product IK
- No solver or calibration UI code introduced
</success_criteria>

<output>
Create `.planning/phases/16-retire-custom-angle-ik-contracts/16-03-SUMMARY.md` when done
</output>
