---
phase: 16-retire-custom-angle-ik-contracts
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - rehab-robotics-studio/src/state/graphStore.ts
  - rehab-robotics-studio/src/graph/mockExecutor.ts
  - rehab-robotics-studio/src/graph/blockDefinitions.ts
  - rehab-robotics-studio/src/graph/productKneeReadout.test.ts
  - rehab-robotics-studio/src/data/RosbridgeDataSource.ts
  - rehab-robotics-studio/src/data/RosbridgeDataSource.test.ts
  - rehab-robotics-studio/src/components/dashboard/HealthPanel.tsx
  - rehab-robotics-studio/src/types/signals.ts
  - rehab-robotics-studio/src/types/health.ts
  - rehab-robotics-studio/package.json
autonomous: true
requirements:
  - IK-00
must_haves:
  truths:
    - "Default Studio graph does not use opensim_ik_live fed by custom /opensim/joint_angle as product OpenSim IK."
    - "Product knee readout stays waiting/empty when only custom jointAngleDeg would have been available."
    - "RosbridgeDataSource does not attach custom /opensim/joint_angle onto frames by default."
    - "HealthPanel does not present custom joint_angle_deg as OpenSim IK joint angle."
    - "Deterministic tests fail closed if default graph or executor would show custom angle as product knee."
  artifacts:
    - path: "rehab-robotics-studio/src/state/graphStore.ts"
      provides: "Default graph placeholder/waiting path without opensim_ik_live as product IK"
    - path: "rehab-robotics-studio/src/graph/productKneeReadout.test.ts"
      provides: "Fail-closed product knee readout tests"
    - path: "rehab-robotics-studio/src/data/RosbridgeDataSource.ts"
      provides: "No default subscription/attach of /opensim/joint_angle"
    - path: "rehab-robotics-studio/src/components/dashboard/HealthPanel.tsx"
      provides: "OpenSim section without custom relative-quat angle as product IK"
  key_links:
    - from: "graphStore DEFAULT_NODES B8"
      to: "joint_angle_display B9"
      via: "placeholder/waiting block or disconnected waiting display — not custom-angle opensim_ik_live"
      pattern: "opensim_ik_live|opensim_ik_waiting|joint_angle_display"
    - from: "RosbridgeDataSource"
      to: "Frame.jointAngleDeg"
      via: "subscription removed or gated off by default"
      pattern: "DEFAULT_OPENSIM_JOINT_ANGLE_TOPIC|jointAngleDeg"
---

<objective>
Stop the Studio GUI from presenting custom relative-quat `/opensim/joint_angle` as OpenSim IK.

Purpose: Enforce D-16-01/D-16-02 on the client so the default knee path is a waiting/placeholder state until Phases 18–19 wire calibrated `/opensim/joint_states`.
Output: Updated default graph, demoted/renamed live block semantics, rosbridge without default custom-angle attach, HealthPanel copy, and fail-closed tests.
</objective>

<execution_context>
@C:/Users/justi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/justi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-retire-custom-angle-ik-contracts/16-CONTEXT.md
@.planning/REQUIREMENTS.md
@rehab-robotics-studio/src/state/graphStore.ts
@rehab-robotics-studio/src/graph/mockExecutor.ts
@rehab-robotics-studio/src/graph/blockDefinitions.ts
@rehab-robotics-studio/src/data/RosbridgeDataSource.ts
@rehab-robotics-studio/src/components/dashboard/HealthPanel.tsx
@rehab-robotics-studio/src/types/signals.ts
@rehab-robotics-studio/src/types/health.ts

<locked_decisions>
- D-16-01: Custom `/opensim/joint_angle` must not be the default GUI knee/angle source.
- D-16-02: Default graph must stop using `opensim_ik_live` fed by that custom topic as “OpenSim IK.” Prefer placeholder/waiting until Phase 18–19 wire real joint states.
- Out of scope: Calibrate toolbar, JointState subscription wiring for live IK display (Phase 19), solver.
</locked_decisions>

<interfaces>
Current product coupling to remove:
- graphStore DEFAULT_NODES B8 = `opensim_ik_live`, edge B8→B9 joint_angle_display.
- mockExecutor `opensim_ik_live` returns `frame.jointAngleDeg` or `{ angles: 0 }` (0 falsely looks like a real angle).
- blockDefinitions `opensim_ik_live` description: "Live relative angle from opensim_bridge (/opensim/joint_angle)."
- RosbridgeDataSource subscribes to DEFAULT_OPENSIM_JOINT_ANGLE_TOPIC and sets Frame.jointAngleDeg.
- HealthPanel row "Joint angle" reads openSim.joint_angle_deg.

Chosen discretion for this plan:
- Replace default B8 with a waiting/placeholder block type (new `opensim_ik_waiting` OR repurpose B8) that never emits a finite product knee angle.
- Demote `opensim_ik_live` block: rename/description to debug/non-product if kept in palette; executor must not treat missing sample as angles=0 for product display.
- Remove default rosbridge subscribe/attach of `/opensim/joint_angle` (delete path preferred over gated ON).
- HealthPanel: replace "Joint angle" numeric row with waiting copy such as "OpenSim IK angles: Waiting (requires calibration)" — do not render custom joint_angle_deg as product.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fail-closed product knee readout tests</name>
  <files>rehab-robotics-studio/src/graph/productKneeReadout.test.ts, rehab-robotics-studio/package.json</files>
  <behavior>
    - Loading the default graph (same DEFAULT_NODES/DEFAULT_EDGES construction as graphStore) and running runMockExecutor with a Frame that includes jointAngleDeg=42.5 yields knee undefined/null/non-finite (product path must ignore custom angle).
    - Default graph node list does not include type opensim_ik_live as the B8 product IK source (assert no opensim_ik_live in default nodes, or B8 is the waiting placeholder type).
    - If opensim_ik_live remains in the palette, evaluating it alone must not return angles=0 when jointAngleDeg is absent (empty output or non-finite — fail closed, not fake zero).
    - A frame carrying only jointAngleDeg must not become the product knee via the default graph wiring.
  </behavior>
  <action>Create `productKneeReadout.test.ts` using node:test + assert (same style as RosbridgeDataSource.test.ts). Import runMockExecutor and the default graph builders — if DEFAULT_NODES is not exported, export a read-only helper or duplicate the expected default types assertion via a small exported `getDefaultGraphDocument()` from graphStore (prefer exporting existing defaults over duplicating). Add the new test file to package.json `test` script glob. Do not implement production code in this task beyond minimal exports needed for RED tests.</action>
  <verify>
    <automated>npm test --prefix rehab-robotics-studio -- src/graph/productKneeReadout.test.ts</automated>
  </verify>
  <done>Tests exist and fail against current opensim_ik_live + jointAngleDeg product wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Default graph, executor, rosbridge, and HealthPanel retirement</name>
  <files>rehab-robotics-studio/src/state/graphStore.ts, rehab-robotics-studio/src/graph/mockExecutor.ts, rehab-robotics-studio/src/graph/blockDefinitions.ts, rehab-robotics-studio/src/data/RosbridgeDataSource.ts, rehab-robotics-studio/src/data/RosbridgeDataSource.test.ts, rehab-robotics-studio/src/components/dashboard/HealthPanel.tsx, rehab-robotics-studio/src/types/signals.ts, rehab-robotics-studio/src/types/health.ts</files>
  <behavior>
    - Task 1 tests pass.
    - RosbridgeDataSource.test.ts no longer expects attaching /opensim/joint_angle to frames by default; update or replace that test to assert jointAngleDeg stays undefined when a Float64 arrives on that topic (or topic is not subscribed).
  </behavior>
  <action>Per D-16-02: change default B8 to a waiting/placeholder biomechanics block (recommended type id `opensim_ik_waiting`) whose executor returns no finite `angles`, keep edge to B9 so the display shows waiting/empty rather than a swinging mock. Update or demote `opensim_ik_live` definition/description so it is not labeled OpenSim IK from `/opensim/joint_angle`; if retained, remove the `{ angles: 0 }` fallback. In RosbridgeDataSource remove DEFAULT_OPENSIM_JOINT_ANGLE_TOPIC subscription, latestJointAngleDeg state, and emitWithJointAngle product attach (per D-16-01). Optionally leave Frame.jointAngleDeg typed as deprecated optional unused field with a comment pointing to future `/opensim/joint_states`. HealthPanel: stop rendering openSim.joint_angle_deg as product; show waiting/calibration-required copy for IK angles. Update health.ts comments if joint_angle_deg remains for backward status parsing — do not display it as IK. Update RosbridgeDataSource.test.ts accordingly.</action>
  <verify>
    <automated>npm test --prefix rehab-robotics-studio</automated>
  </verify>
  <done>Default GUI no longer sources product knee from custom joint angle; HealthPanel and rosbridge match; full Studio test script green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rosbridge WS → RosbridgeDataSource | Untrusted topic payloads enter the browser |
| graph executor → Front Panel knee | Displayed product angle is operator-facing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04 | Spoofing | /opensim/joint_angle via rosbridge | mitigate | Stop default subscribe/attach so spoofed Float64 cannot drive product knee |
| T-16-05 | Elevation of privilege / misuse | opensim_ik_live block | mitigate | Remove from default graph; demote naming; no fake-zero fallback |
| T-16-06 | Information disclosure | HealthPanel joint_angle_deg | mitigate | Do not present custom degrees as OpenSim IK |
| T-16-SC | Tampering | npm installs | accept | No new dependencies; only existing tsx --test |
</threat_model>

<verification>
npm test --prefix rehab-robotics-studio and npm run typecheck --prefix rehab-robotics-studio.
</verification>

<success_criteria>
- Default graph has no product opensim_ik_live←jointAngleDeg path
- Custom jointAngleDeg cannot become product knee through default wiring
- HealthPanel shows waiting/calibration-required for IK angles, not custom deg
- Rosbridge does not attach /opensim/joint_angle by default
</success_criteria>

<output>
Create `.planning/phases/16-retire-custom-angle-ik-contracts/16-02-SUMMARY.md` when done
</output>
