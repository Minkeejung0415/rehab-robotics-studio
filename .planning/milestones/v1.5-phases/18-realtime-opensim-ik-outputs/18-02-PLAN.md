---
phase: 18-realtime-opensim-ik-outputs
plan: "02"
type: tdd
wave: 2
depends_on:
  - "01"
files_modified:
  - backend/rehab_robotics_bridge/opensim/opensim_orientation_ik.py
  - backend/rehab_robotics_bridge/opensim/__init__.py
  - backend/test/test_opensim_orientation_ik_opensim.py
autonomous: true
requirements:
  - IK-05
must_haves:
  truths:
    - "create_orientation_ik_solver probes OpenSim Python bindings and returns either OpenSimOrientationIkSolver or UnavailableOrientationIkSolver — never a custom relative-quat product path."
    - "When InverseKinematicsSolver + orientation reference APIs exist, OpenSimOrientationIkSolver loads model_path, applies mounting offsets, and produces solution_valid joint coordinates for a synthetic known-pose fixture."
    - "When APIs or model_path are missing, solver remains fail-closed (solution_valid=False) with an actionable reason."
  artifacts:
    - path: "backend/rehab_robotics_bridge/opensim/opensim_orientation_ik.py"
      provides: "OpenSimOrientationIkSolver + create_orientation_ik_solver factory + capability probe"
      exports:
        - "OpenSimOrientationIkSolver"
        - "create_orientation_ik_solver"
        - "probe_opensim_orientation_ik_apis"
    - path: "backend/test/test_opensim_orientation_ik_opensim.py"
      provides: "skipUnless opensim integration + always-on factory unavailable path tests"
  key_links:
    - from: "create_orientation_ik_solver"
      to: "OpenSim InverseKinematicsSolver | Unavailable"
      via: "capability probe + model_path validation"
      pattern: "probe_opensim_orientation_ik_apis|UnavailableOrientationIkSolver|InverseKinematics"
    - from: "OpenSimOrientationIkSolver.solve"
      to: "apply_mounting_offsets"
      via: "correct sensor quats before orientation reference update"
      pattern: "apply_mounting_offsets|assemble|track"
---

<objective>
Implement the real OpenSim Python orientation-IK adapter and factory for WSL OpenSim 4.5.2, with capability probing and fail-closed Unavailable fallback — still never productizing custom relative-quat angle.

Purpose: Deliver IK-05 official OpenSim path on the existing Python bridge process (D-18-02, D-18-03) while keeping tests runnable without OpenSim installed.
Output: `opensim/opensim_orientation_ik.py` + integration tests.
</objective>

<execution_context>
@C:/Users/justi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/justi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-realtime-opensim-ik-outputs/18-CONTEXT.md
@.planning/phases/18-realtime-opensim-ik-outputs/18-01-SUMMARY.md
@backend/rehab_robotics_bridge/opensim/orientation_ik.py
@backend/rehab_robotics_bridge/opensim_adapter.py
@backend/test/test_opensim_adapter.py

<locked_decisions>
- D-18-02: Prefer OpenSim 4.5.2 Python inside opensim_bridge; C++ package deferred.
- D-18-03: Unavailable if APIs missing — no custom quat fallback.
- D-18-07: Apply mounting offsets before feeding OpenSim.
- D-18-08: Coordinate path(s) configurable; default knee_angle_r.
- Discretion: assemble-once + track when BufferedOrientationsReference exists; else OrientationsReference + assemble/track as bindings allow; re-assemble after reset/calibration change.
</locked_decisions>

<interfaces>
From plan 01 `orientation_ik.py`:
- `OrientationIkSolver`, `IkSolution`, `apply_mounting_offsets`, `UnavailableOrientationIkSolver`, `FakeOrientationIkSolver`

Factory contract for `opensim_orientation_ik.py`:
```python
def probe_opensim_orientation_ik_apis(opensim_module) -> dict[str, bool]:
    """Report presence of InverseKinematicsSolver, OrientationsReference,
    BufferedOrientationsReference, and any other symbols required."""

def create_orientation_ik_solver(
    *,
    model_path: str,
    master_frame: str,
    slave_frame: str,
    coordinate_paths: Sequence[str],
    opensim_module: Any | None = None,
) -> OrientationIkSolver:
    """Return OpenSimOrientationIkSolver when model+APIs OK; else Unavailable..."""
```

OpenSimOrientationIkSolver behaviors:
- Construct Model(model_path), initSystem, build orientation reference for master/slave IMU frames (match PhysicalOffsetFrame / OpenSense IMU frame names from parameters — strip leading `/` consistently with visualizer adapter).
- On first solve after reset/calibration_id change: assemble(); subsequent solves: track() when API supports sequential tracking; on track failure, attempt one reassemble then fail closed.
- Extract selected coordinates into positions_rad; compute orientation residual RMS/max via OpenSim orientation-error APIs when available, else None with reason note in status.
- Never import or call relative_orientation_angle_deg.

Synthetic fixture (tests): Follow `_write_installed_runtime_model` pattern in `test_opensim_adapter.py` but add a PinJoint (or equivalent) with one Coordinate named `knee_angle_r` between ground and a tibia body, plus femur_r_imu / tibia_r_imu frames — enough for InverseKinematics orientation goals if APIs exist. If probe shows APIs absent, skip the real-solve test and assert factory returns Unavailable with reason containing `opensim_ik_api_unavailable` or `missing_`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Factory/probe tests (always-on + skipUnless OpenSim)</name>
  <files>backend/test/test_opensim_orientation_ik_opensim.py</files>
  <behavior>
    - create_orientation_ik_solver(model_path="") returns Unavailable with reason model_path_empty (or equivalent).
    - create_orientation_ik_solver(model_path=missing file) returns Unavailable with model_path_not_found.
    - When opensim importable: probe_opensim_orientation_ik_apis returns a dict with boolean keys for InverseKinematicsSolver and OrientationsReference (and BufferedOrientationsReference).
    - When opensim importable AND probe indicates required APIs present: synthetic model fixture + CalibrationArtifact + identity/flex fixtures produce solution_valid solutions whose knee_angle_r moves in the expected direction (flexed > identity by a clear margin, flexed sign positive).
    - When opensim importable AND required APIs absent: factory returns Unavailable; document detected symbols in test message — do not invent JointState from Fake in this module.
  </behavior>
  <action>Write unittest file with always-on factory negative tests and `@unittest.skipUnless(importlib.util.find_spec("opensim"), ...)` class for binding/probe/solve. Keep ROS-free. Confirm RED before implementation.</action>
  <verify>
    <automated>$env:PYTHONPATH='backend'; python -m unittest backend.test.test_opensim_orientation_ik_opensim -v</automated>
  </verify>
  <done>Tests encode OpenSim factory probe and fail-closed / real-solve fixtures per D-18-02/03.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement OpenSimOrientationIkSolver + factory</name>
  <files>backend/rehab_robotics_bridge/opensim/opensim_orientation_ik.py, backend/rehab_robotics_bridge/opensim/__init__.py</files>
  <action>Implement probe + OpenSimOrientationIkSolver + create_orientation_ik_solver per interfaces and locked decisions. Use optional import_module("opensim") like opensim_adapter.create_visualizer_adapter. Single-thread ownership of Model/State/solver instances inside the adapter object. On any OpenSim exception during solve, return solution_valid=False with reason opensim_ik_solve_failed (or truncated exception type name) — never publish last-good coordinates as newly valid. Export factory from opensim package init. Update module docstring noting research preferred C++ 4.6 streaming APIs; this milestone uses Python 4.5.2 pragmatically (D-18-02).</action>
  <verify>
    <automated>$env:PYTHONPATH='backend'; python -m unittest backend.test.test_opensim_orientation_ik backend.test.test_opensim_orientation_ik_opensim -q</automated>
  </verify>
  <done>Factory probes APIs; real solver works when bindings allow; otherwise Unavailable fail-closed; plan 01 tests still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model_path filesystem → OpenSim Model | Untrusted path string loads native code |
| OpenSim native → IkSolution | Native solve results enter ROS publish path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04 | Tampering | model_path | mitigate | Require existing file; fail Unavailable on empty/missing; do not download remote models |
| T-18-05 | Denial of service | InverseKinematicsSolver.solve | mitigate | Catch native exceptions; return invalid solution; reset path on repeated failure (node plan may bound rate) |
| T-18-06 | Spoofing | Fake vs OpenSim factory | mitigate | Production factory never returns FakeOrientationIkSolver |
| T-18-SC | Tampering | package installs | accept | No new package-manager deps; uses existing opensim env |
</threat_model>

<verification>
- Always-on factory tests pass without opensim
- OpenSim skipUnless tests pass when micromamba opensim present, or skip cleanly documenting missing APIs
</verification>

<success_criteria>
- Official OpenSim orientation IK path exists behind factory
- Fail-closed Unavailable when APIs/model missing
- No custom relative-quat product path
</success_criteria>

<output>
Create `.planning/phases/18-realtime-opensim-ik-outputs/18-02-SUMMARY.md` when done
</output>
