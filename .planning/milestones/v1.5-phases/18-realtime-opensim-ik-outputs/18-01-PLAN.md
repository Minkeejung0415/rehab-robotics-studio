---
phase: 18-realtime-opensim-ik-outputs
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/rehab_robotics_bridge/opensim/orientation_ik.py
  - backend/rehab_robotics_bridge/opensim/__init__.py
  - backend/test/test_opensim_orientation_ik.py
autonomous: true
requirements:
  - IK-05
  - IK-07
must_haves:
  truths:
    - "OrientationIkSolver protocol returns IkSolution with solution_valid, joint names/positions (radians), residuals, calibration_id, and source stamp fields."
    - "FakeOrientationIkSolver maps a known flexed pose to a positive knee flexion direction (not custom relative_orientation_angle_deg)."
    - "UnavailableOrientationIkSolver never reports solution_valid=True."
    - "Mounting offsets from CalibrationArtifact are applied before solving (sensor orientations corrected for OpenSense-style mounts)."
  artifacts:
    - path: "backend/rehab_robotics_bridge/opensim/orientation_ik.py"
      provides: "IkSolution, IkStatusSnapshot, OrientationIkSolver protocol, Fake + Unavailable solvers, offset application"
      exports:
        - "IkSolution"
        - "IkStatusSnapshot"
        - "OrientationIkSolver"
        - "FakeOrientationIkSolver"
        - "UnavailableOrientationIkSolver"
        - "apply_mounting_offsets"
    - path: "backend/test/test_opensim_orientation_ik.py"
      provides: "ROS-free TDD for fake, unavailable, offsets, known-pose direction"
  key_links:
    - from: "FakeOrientationIkSolver.solve"
      to: "IkSolution.positions"
      via: "deterministic pose→coordinate mapping in radians"
      pattern: "solution_valid|knee_angle_r|radians"
    - from: "apply_mounting_offsets"
      to: "CalibrationArtifact master/slave xyzw"
      via: "conjugate/mount correction before solver input"
      pattern: "apply_mounting_offsets|master_xyzw|slave_xyzw"
---

<objective>
Define the ROS-free Orientation IK solver seam with Fake and Unavailable implementations, mounting-offset application, and deterministic known-pose fixtures — without calling OpenSim or relative_orientation_angle_deg as product IK.

Purpose: Lock IK-05/IK-07 data shapes and fail-closed behavior (D-18-01, D-18-03, D-18-07) before wiring OpenSim bindings or the ROS node.
Output: `opensim/orientation_ik.py` + `test_opensim_orientation_ik.py`.
</objective>

<execution_context>
@C:/Users/justi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/justi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-realtime-opensim-ik-outputs/18-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@docs/opensim-ik-contracts.md
@backend/rehab_robotics_bridge/opensim/ik_contracts.py
@backend/rehab_robotics_bridge/opensim/calibration.py
@backend/rehab_robotics_bridge/opensim_adapter.py

<locked_decisions>
- D-18-01: Product angles only via OrientationIkSolver; never relative_orientation_angle_deg for JointState.
- D-18-03: Unavailable solver fail-closed; Fake for tests only.
- D-18-04: Positions radians; source stamp fields on solution.
- D-18-05: solution_valid required for publish (node wiring in plan 03).
- D-18-07: Apply CalibrationArtifact mounting offsets before solve.
- D-18-08: Default joint name `knee_angle_r`.
- Deferred: C++ package, typed IkStatus.msg, Studio display, clinical validation.
</locked_decisions>

<interfaces>
From `ik_contracts.py`:
- `JOINT_STATES_TOPIC`, `IK_STATUS_TOPIC`, `CalibrationState`, `may_publish_joint_states`

From `calibration.py`:
```python
@dataclass(frozen=True)
class CalibrationArtifact:
    calibration_id: str
    known_pose: str
    master_xyzw: tuple[float, float, float, float]
    slave_xyzw: tuple[float, float, float, float]
    ...
```

From `opensim_adapter.py` (quaternion boundary only):
- `ros_xyzw_to_opensim_rotation` / scalar-first helpers — reuse for normalize; do NOT call `relative_orientation_angle_deg` as product IK.

Required types in `orientation_ik.py` (names may match exactly):
- `IkSolution`: frozen dataclass with `solution_valid: bool`, `reason: str`, `joint_names: list[str]`, `positions_rad: list[float]`, `source_timestamp_ns: int | None`, `orientation_residual_rms: float | None`, `orientation_residual_max: float | None`, `calibration_id: str | None`, `input_age_s: float | None`, `solve_duration_s: float | None`
- `IkStatusSnapshot`: dict-serializable status for `/opensim/ik_status` (schema `rehab.opensim_ik_status.1`) built from latest IkSolution + solver backend name
- Protocol `OrientationIkSolver` with:
  - `reset()` — clear assemble/track state after calibration clear/change
  - `solve(*, master_xyzw, slave_xyzw, calibration: CalibrationArtifact | None, source_timestamp_ns: int | None, input_age_s: float | None, joint_names: Sequence[str]) -> IkSolution`
- `apply_mounting_offsets(master_xyzw, slave_xyzw, artifact) -> (master_corr, slave_corr)` using OpenSense-style correction: treat artifact offsets as sensor orientations at identity model pose; corrected sensor orientation ≈ `q_sensor ⊗ conjugate(q_mount)` (document formula in module docstring; implement consistently for Fake and real adapters)
- `FakeOrientationIkSolver`: for identity-corrected pair → ~0 rad on `knee_angle_r`; for a documented +90° flexion slave relative orientation fixture → positive ≈ +π/2 rad (tolerance 1e-3). Must set `solution_valid=True` only when calibration is not None and stamps provided when required by tests.
- `UnavailableOrientationIkSolver(reason: str)`: always `solution_valid=False` with the given reason (e.g. `opensim_ik_api_unavailable`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing tests for Orientation IK seam</name>
  <files>backend/test/test_opensim_orientation_ik.py</files>
  <behavior>
    - UnavailableOrientationIkSolver.solve always returns solution_valid=False and never invents positions for publish.
    - Fake without calibration → solution_valid=False, reason mentions calibration.
    - Fake with CalibrationArtifact + identity master/slave matching mounts → knee_angle_r ≈ 0.0 rad, solution_valid=True when source_timestamp_ns provided.
    - Fake with documented flexed fixture (slave rotated +90° about model flexion axis after offset correction) → knee_angle_r ≈ +π/2 (positive flexion direction).
    - apply_mounting_offsets with identity mounts leaves orientations unchanged (within 1e-9).
    - Fake.reset() is callable without error; after reset, next solve still works.
    - Module must not import relative_orientation_angle_deg for product path (grep orientation_ik.py).
  </behavior>
  <action>Create ROS-free unittest module covering the behaviors above. Import from `rehab_robotics_bridge.opensim.orientation_ik` and `CalibrationArtifact` from calibration. Run first and confirm RED.</action>
  <verify>
    <automated>$env:PYTHONPATH='backend'; python -m unittest backend.test.test_opensim_orientation_ik -v</automated>
  </verify>
  <done>Failing tests encode Fake/Unavailable/offset/known-pose direction contracts per D-18-01/03/07/08.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement orientation_ik seam to green</name>
  <files>backend/rehab_robotics_bridge/opensim/orientation_ik.py, backend/rehab_robotics_bridge/opensim/__init__.py</files>
  <action>Implement IkSolution, IkStatusSnapshot (or `ik_status_dict(solution, *, backend: str) -> dict`), OrientationIkSolver Protocol, apply_mounting_offsets, FakeOrientationIkSolver, UnavailableOrientationIkSolver per interfaces. Fake must compute knee angle from offset-corrected relative orientation about a fixed axis (document axis) — this is a test double approximating OpenSense knee coordinate direction, not the product OpenSim solver. Export public symbols from `opensim/__init__.py`. Do not import rclpy or opensim.native in this module. Do not call relative_orientation_angle_deg.</action>
  <verify>
    <automated>$env:PYTHONPATH='backend'; python -m unittest backend.test.test_opensim_orientation_ik -q</automated>
  </verify>
  <done>All orientation_ik unit tests pass; Fake known-pose direction proven; Unavailable fail-closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IMU quaternion inputs → solver | Untrusted sensor numerics enter IK |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | orientation_ik inputs | mitigate | Reject non-finite / non-unit quats via existing ros_xyzw_to_opensim_rotation boundary before solve |
| T-18-02 | Elevation of privilege | N/A | accept | Local ROS lab process; no auth boundary in phase |
| T-18-03 | Information disclosure | IkStatusSnapshot | accept | No PII; lab kinematics only |
| T-18-SC | Tampering | package installs | accept | No new npm/pip/cargo packages in this plan |
</threat_model>

<verification>
- unittest orientation_ik green
- `orientation_ik.py` has no `relative_orientation_angle_deg` import
</verification>

<success_criteria>
- Fake proves identity→0 and flexed→+π/2 direction for knee_angle_r
- Unavailable never validates
- Offsets applied through shared helper
</success_criteria>

<output>
Create `.planning/phases/18-realtime-opensim-ik-outputs/18-01-SUMMARY.md` when done
</output>
