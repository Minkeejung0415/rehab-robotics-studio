---
phase: 18-realtime-opensim-ik-outputs
plan: "03"
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - backend/rehab_robotics_bridge/opensim_node.py
  - backend/test/test_opensim_node.py
  - backend/rehab_robotics_bridge/opensim/ik_contracts.py
  - docs/opensim-ik-contracts.md
  - backend/package.xml
  - backend/launch/opensim_live_link.launch.py
autonomous: true
requirements:
  - IK-05
  - IK-06
  - IK-07
must_haves:
  truths:
    - "When CALIBRATED and Fake/injected solver returns valid solution, /opensim/joint_states publishes JointState with matching name/position radians and source header.stamp."
    - "UNCALIBRATED, Clear cal, invalid solution, or missing source stamps produce no fresh JointState publication."
    - "/opensim/ik_status JSON exposes solution_valid, residuals, input age, calibration_id, and reason; /opensim/status embeds ik object."
    - "/diagnostics heartbeat remains observable even when JointState is suppressed."
  artifacts:
    - path: "backend/rehab_robotics_bridge/opensim_node.py"
      provides: "Solver wiring, stamped JointState publish, ik_status + diagnostics publishers"
    - path: "backend/test/test_opensim_node.py"
      provides: "Stubbed-node tests for IK publish gate, stamp, clear, ik_status"
    - path: "docs/opensim-ik-contracts.md"
      provides: "Phase 18 ik_status schema + JointState stamp rules"
  key_links:
    - from: "_on_*_imu"
      to: "OrientationIkSolver.solve"
      via: "after calibration feed, when both sensors live"
      pattern: "_ik_solver|solve\\(|_ik_solution"
    - from: "_maybe_publish_joint_states"
      to: "JOINT_STATES_TOPIC"
      via: "may_publish + solution_valid + header.stamp from source_timestamp_ns"
      pattern: "header.stamp|source_timestamp_ns|may_publish_joint_states"
    - from: "ik_status publisher"
      to: "IK_STATUS_TOPIC"
      via: "std_msgs/String JSON schema rehab.opensim_ik_status.1"
      pattern: "IK_STATUS_TOPIC|rehab.opensim_ik_status"
---

<objective>
Wire OrientationIkSolver into opensim_bridge: populate `_ik_solution` from official IK, publish stamped `/opensim/joint_states`, and expose `/opensim/ik_status` + `/diagnostics` observability — completing IK-05..07 on the ROS boundary.

Purpose: Make calibrated live orientations produce trustworthy OpenSim joint states for Phase 19 GUI consumption (D-18-04..09).
Output: Updated opensim_node + tests + contracts docs.
</objective>

<execution_context>
@C:/Users/justi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/justi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-realtime-opensim-ik-outputs/18-CONTEXT.md
@.planning/phases/18-realtime-opensim-ik-outputs/18-01-SUMMARY.md
@.planning/phases/18-realtime-opensim-ik-outputs/18-02-SUMMARY.md
@backend/rehab_robotics_bridge/opensim_node.py
@backend/rehab_robotics_bridge/opensim/orientation_ik.py
@backend/rehab_robotics_bridge/opensim/opensim_orientation_ik.py
@docs/opensim-ik-contracts.md
@backend/test/test_opensim_node.py

<locked_decisions>
- D-18-04: JointState on /opensim/joint_states; stamp = source observation time.
- D-18-05: Hard gate CALIBRATED + solution_valid; clear drops solution.
- D-18-06: /opensim/ik_status String JSON; embed ik in /opensim/status; diagnostics heartbeat.
- D-18-08: Parameterized joint names / coordinate paths; default knee_angle_r.
- D-18-09: Missing source timestamp → invalid solution, no wall-time stamp fabrication for product JointState.
- Do not implement Studio JointState subscription (Phase 19).
</locked_decisions>

<interfaces>
Existing node seam (extend, do not remove gate):
```python
self._ik_solution: dict[str, object] | None = None
def _maybe_publish_joint_states(self) -> None:
    if not may_publish_joint_states(self._calibration.state):
        return
    if self._ik_solution is None:
        return
    # Phase 18: also require solution_valid; set message.header.stamp from source_timestamp_ns
```

Constructor injection (add optional kwarg for tests):
- `ik_solver: OrientationIkSolver | None = None`
- Default production: `create_orientation_ik_solver(model_path=..., master_frame=..., slave_frame=..., coordinate_paths=...)`

New/updated parameters:
- `ik_joint_names` default `["knee_angle_r"]` (list or comma-separated string — match existing declare_parameter style)
- `ik_coordinate_paths` default same as joint names

`_ik_solution` dict shape for publisher:
- `name: list[str]`, `position: list[float]`, `source_timestamp_ns: int`, `solution_valid: bool`

ik_status JSON keys (minimum):
`schema`, `solution_valid`, `reason`, `calibration_id`, `orientation_residual_rms`, `orientation_residual_max`, `input_age_s`, `solve_duration_s`, `backend`, `joint_names`, `source_timestamp_ns`

Update `docs/opensim-ik-contracts.md` Phase 18 sections for ik_status schema and JointState stamp/stale policy. Keep machine twin constants in sync (`IK_STATUS_TOPIC` already exists).

Diagnostics: add `diagnostic_msgs` to package.xml if using DiagnosticArray; publish at status timer rate with level OK when solution_valid else WARN/ERROR and key values mirroring ik_status. If DiagnosticArray proves awkward in stubbed tests, String JSON on DIAGNOSTICS_TOPIC is acceptable per Claude discretion — document choice in SUMMARY.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend opensim_node tests for IK publish + status</name>
  <files>backend/test/test_opensim_node.py</files>
  <behavior>
    - Inject FakeOrientationIkSolver; after stable CALIBRATED path with stamped IMUs, JointState publishes with name knee_angle_r, position radians, header.stamp matching source time.
    - UNCALIBRATED / after clear: no JointState messages (or no new ones); _ik_solution cleared on clear.
    - Fake returning solution_valid=False → no JointState publish; ik_status still publishes solution_valid false.
    - IMU without usable header.stamp while CALIBRATED → no JointState; ik_status reason includes missing_source_timestamp (or equivalent).
    - status_snapshot contains ik object with solution_valid / calibration_id fields.
    - Existing Phase 17 tests remain green (including calibrated-without-valid-IK empty joint states when using Unavailable solver default).
  </behavior>
  <action>Extend stubbed-node tests. Prefer injecting Fake via new `ik_solver=` kwarg so tests stay OpenSim-free. Update the old calibrated-without-ik assertion to use Unavailable or Fake-invalid as appropriate. Keep using existing publisher/service stubs in the test harness.</action>
  <verify>
    <automated>$env:PYTHONPATH='backend'; python -m unittest backend.test.test_opensim_node -v</automated>
  </verify>
  <done>Failing/updated tests encode IK-06/IK-07 ROS behaviors and hard gate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire solver, stamped JointState, ik_status, diagnostics, docs</name>
  <files>backend/rehab_robotics_bridge/opensim_node.py, backend/rehab_robotics_bridge/opensim/ik_contracts.py, docs/opensim-ik-contracts.md, backend/package.xml, backend/launch/opensim_live_link.launch.py</files>
  <action>
Implement node wiring per locked decisions:
1. Construct/inject OrientationIkSolver; on each dual-live IMU update after calibration feed, call solve with latest xyzw, active CalibrationArtifact (or None), source_timestamp_ns (both sensors required per D-18-09), input_age_s from freshest sensor age, joint_names from params.
2. If solution_valid and may_publish: set `_ik_solution` and publish JointState with header.stamp from source_timestamp_ns; else set `_ik_solution=None` (or keep last only for status — do NOT publish JointState). Prefer clear `_ik_solution` when invalid so gate stays fail-closed.
3. On calibration clear / state leaving CALIBRATED: `ik_solver.reset()`, clear `_ik_solution`.
4. Publish `/opensim/ik_status` String JSON; embed `ik` in status_snapshot; publish diagnostics heartbeat on status timer.
5. Update contracts doc + any new constants; pass new launch args through opensim_live_link.launch.py.
6. Do not enable publish_joint_angle_enabled by default; do not feed relative_orientation_angle_deg into JointState.
  </action>
  <verify>
    <automated>$env:PYTHONPATH='backend'; python -m unittest backend.test.test_opensim_node backend.test.test_opensim_orientation_ik backend.test.test_ik_contracts backend.test.test_opensim_calibration -q</automated>
  </verify>
  <done>Stamped JointState publishes only when CALIBRATED+valid; ik_status/diagnostics observable; contracts updated; custom angle not product path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ROS IMU topics → opensim_bridge | Untrusted network/local messages |
| opensim_bridge → /opensim/joint_states consumers | Downstream GUI trusts validity gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-07 | Spoofing | JointState freshness | mitigate | Source stamp required; never wall-clock refresh of stale solutions (D-18-04/09) |
| T-18-08 | Tampering | calibration gate bypass | mitigate | Keep may_publish_joint_states + solution_valid dual gate; clear on Clear cal |
| T-18-09 | Information disclosure | ik_status JSON | accept | Lab kinematics/status only; no credentials |
| T-18-10 | Denial of service | high-rate IMU callbacks | mitigate | Latest-pair only (existing QoS depth 1); invalid solutions stop JointState |
| T-18-SC | Tampering | diagnostic_msgs dependency | mitigate | Add only official ROS distro package `diagnostic_msgs` if used; no pip/npm |
</threat_model>

<verification>
- Node + orientation_ik + ik_contracts + calibration unit tests green
- Docs describe ik_status schema and stamp policy
</verification>

<success_criteria>
- IK-05: OpenSim (or injected Fake in tests) solves after calibration
- IK-06: /opensim/joint_states stamped JointState
- IK-07: ik_status + diagnostics expose validity/residuals/age/calibration_id
- Hardware-free Fake fixture proves known pose → expected coordinate direction via node path
</success_criteria>

<output>
Create `.planning/phases/18-realtime-opensim-ik-outputs/18-03-SUMMARY.md` when done
</output>
