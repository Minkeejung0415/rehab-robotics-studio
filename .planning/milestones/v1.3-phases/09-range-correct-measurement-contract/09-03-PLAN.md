---
phase: 09-range-correct-measurement-contract
plan: 03
goal: >
  Run both full test suites (backend Python unittest discovery and frontend npm test
  plus typecheck), confirm zero failures, and conduct the manual UI-SPEC and phase-
  acceptance checklist to gate the phase complete. Also update ROADMAP.md plan count.
wave: 3
depends_on:
  - 09-01
  - 09-02
files_modified:
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - DATA-01
  - DATA-02

must_haves:
  truths:
    - "The full Python unittest suite (all test_*.py files) passes with zero failures"
    - "The full frontend npm test suite passes with zero failures"
    - "npm run typecheck reports zero TypeScript errors"
    - "Every Phase 9 acceptance criterion from VALIDATION.md is checkable and green"
    - "ROADMAP.md reflects the final plan count for Phase 9"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 9 plan count updated to 3"
    - path: ".planning/phases/09-range-correct-measurement-contract/09-03-SUMMARY.md"
      provides: "Phase 9 regression results and acceptance checklist"
  key_links:
    - from: "backend full suite"
      to: "test_measurement_contract.py, test_esp32_controls.py, test_pipeline.py"
      via: "python -m unittest discover"
      pattern: "OK.*[0-9]+ tests"
    - from: "frontend full suite"
      to: "measurementContract.test.ts, RosbridgeDataSource.test.ts, deployment/*.test.ts"
      via: "npm test"
      pattern: "pass.*[0-9]+"
---

<objective>
Regression gate for Phase 9. Both test suites must be green before the phase is
marked complete. This plan contains no new production code changes — it proves
that Plans 01 and 02 together satisfy DATA-01 and DATA-02, then captures the
acceptance evidence in the phase summary.

Purpose: Confirm the full measurement-contract chain is correct end to end:
backend tables → shared fixture → frontend tables agree, raw frames carry
metadata, native Imu values are range-correct, validation fails closed, warning
fires once, caches clear on reconnect, and range-change services target the
correct node names.

Output:
- All automated suites green (evidence captured in SUMMARY)
- Manual UI checklist completed and annotated
- ROADMAP.md plan count updated to 3
</objective>

<execution_context>
@C:\Users\justi\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\justi\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-range-correct-measurement-contract/09-CONTEXT.md
@.planning/phases/09-range-correct-measurement-contract/09-RESEARCH.md
@.planning/phases/09-range-correct-measurement-contract/09-UI-SPEC.md
@.planning/phases/09-range-correct-measurement-contract/09-VALIDATION.md
@.planning/phases/09-range-correct-measurement-contract/09-01-SUMMARY.md
@.planning/phases/09-range-correct-measurement-contract/09-02-SUMMARY.md
</context>

<tasks>

<task type="auto" id="09-03-01-auto">
  <name>Task 1 (09-03-01): Run full backend and frontend regression suites</name>
  <files>
    .planning/ROADMAP.md
  </files>
  <action>
    Run the full backend test suite from the project root:
      PYTHONPATH=backend python -m unittest discover -s backend/test -p "test_*.py" -v

    On Windows with cmd/PowerShell:
      set PYTHONPATH=backend && python -m unittest discover -s backend/test -p "test_*.py" -v

    Capture the output. All tests must pass (zero failures, zero errors).
    Expected test files covered:
      - test_measurement_contract.py  (09-01-01, 09-01-02 cases)
      - test_esp32_controls.py        (09-01-03 cases + prior cases)
      - test_pipeline.py              (existing pipeline cases, must not regress)
      - test_processing_block_contract.py  (must not regress)
      - test_stepesp_firmware_topology.py  (must not regress)

    Run the full frontend suite from rehab-robotics-studio/:
      npm test
      npm run typecheck

    npm test now covers:
      - src/deployment/*.test.ts  (existing deployment tests)
      - src/data/measurementContract.test.ts  (09-02-01)
      - src/data/RosbridgeDataSource.test.ts  (09-02-02, 09-02-03)

    If any test fails, do NOT proceed to the checkpoint. Fix the failing test(s)
    in the appropriate source file first, then re-run. Record the passing output.

    Update .planning/ROADMAP.md:
      Under Phase 9, change:
        **Plans**: TBD
      to:
        **Plans**: 3 plans

      Add a plan list immediately after:
        Plans:
        - [x] 09-PLAN-01.md — Backend measurement contract, bridge integration, and backend tests
        - [x] 09-PLAN-02.md — Frontend contract, RosbridgeDataSource integration, and frontend tests
        - [x] 09-PLAN-03.md — Full regression and phase acceptance gate

      Also update the progress table row for Phase 9:
        | 9. Range-Correct Measurement Contract | 3/3 | Complete | 2026-07-24 |
      (Use the current date.)
  </action>
  <verify>
    <automated>
      cd "C:\Users\justi\Documents\# Rehab Robotics GUI Prototype__Here's a complete, single-file React + TypeScript app implementing the LabVIEW" &amp;&amp; set PYTHONPATH=backend &amp;&amp; python -m unittest discover -s backend/test -p "test_*.py" -v 2>&amp;1 | tail -5
    </automated>
  </verify>
  <done>
    Python discover: zero failures, zero errors. Frontend npm test: zero failures.
    npm run typecheck: zero errors. ROADMAP.md updated with plan count 3 and plan list.
  </done>
</task>

<task type="checkpoint:human-verify" id="09-03-01-verify">
  <name>Task 2 (09-03-01): Phase Acceptance Checklist — Manual UI Verification</name>
  <what-built>
    Plan 01 added measurement_contract.py, nullable confirmed ranges, sensor_config
    on every raw frame, and range-correct native Imu conversion.
    Plan 02 added measurementContract.ts, validate-before-cache in RosbridgeDataSource,
    one-WARN-per-connection latch, cache reset on new socket, and correct service names.
    The automated suites proved DATA-01 and DATA-02 for all supported ranges and both roles.
  </what-built>
  <how-to-verify>
    Work through each item in the VALIDATION.md Phase Acceptance checklist.
    For items verifiable without hardware, confirm from the automated suite output
    and code inspection. For hardware items, note them as "pending hardware test".

    AUTOMATED (already confirmed by Task 1):
      [1] Raw counts remain integers and topic_schema remains oe_esp32.raw.v1
          → test_measurement_contract.py sensor_config tests confirm additive-only change
      [2] Every accepted raw/filtered frame has a complete, internally consistent sensor_config
          → test_09_01_02_sensor_config_survives_filter_json
      [3] Native ROS and GUI results match the shared fixture for all ranges and both roles
          → test_09_01_01 and 09-02-01 shared-fixture tests
      [4] Invalid metadata causes zero emissions, zero onFrameReceived calls, one WARN
          → 09-02-02 warning/cache/emission tests
      [5] Valid metadata later resumes without a success toast
          → 09-02-02 "valid frame after invalid" test
      [6] Rejected requests do not overwrite the confirming node's last confirmed scale
          → test_09_01_03_rejected_command_does_not_change_confirmed_state

    UI-SPEC VISUAL CHECK (requires running app, hardware optional):
      Open http://localhost:5173 (or built dist/index.html).
      Confirm no new panels, cards, modals, or diagnostics surfaces appeared.
      Confirm ACCEL and GYRO selectors still show 2g/4g/8g/16g and
      250dps/500dps/1000dps/2000dps options and disable while a command is pending.
      Confirm APPLYING amber text still shows during pending state.
      Confirm System Log uses the existing .log-row.level-warn treatment.
      Confirm status strip still shows "Awaiting data" before first valid frame.

    SERVICE NAME CHECK (from code inspection, no hardware needed):
      Open rehab-robotics-studio/src/data/RosbridgeDataSource.ts.
      Confirm requestImuControl targets /esp_bridge_master/set_parameters.
      Confirm accel_range_g and gyro_range_dps trigger both master and slave calls.
      Confirm /esp_master/set_parameters no longer appears in the file.

    HARDWARE (mark as pending if hardware unavailable):
      With both ESP32 devices connected, set each to a non-default range (e.g. 8g/1000dps).
      Inspect one /esp/raw/master message — confirm sensor_config field is present with
      accel_range_g=8, gyro_range_dps=1000, accel_lsb_per_g=4096.0, gyro_lsb_per_dps=32.768.
      Read one /imu/master message — compare a known raw count against the expected
      mps2/rad_s values from the shared fixture.
      Repeat for slave device.

    PHASE 7 CHECKLIST:
      [ ] All automated acceptance criteria green (from Task 1 output)
      [ ] No new UI surfaces visible
      [ ] Service names corrected in source
      [ ] Hardware smoke test completed OR deferred to next hardware session with a note
  </how-to-verify>
  <resume-signal>
    Type "approved" if all automated criteria are green and UI-SPEC visual checks pass.
    Type "hardware-deferred" if automated tests pass but hardware smoke test was not run
    (this is acceptable per VALIDATION.md "Manual-Only Verifications").
    Describe any failure with the failing item name and observed vs expected behavior.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test suite → production source | Regression run proves the measurement contract holds across all supported inputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | Full regression | mitigate | All metadata validation paths exercised by 32-case shared fixture and rejection partitions in both languages |
| T-09-02 | Tampering / DoS | Full regression | mitigate | Non-finite inputs rejected by both Python and TypeScript validators; proven by rejection-partition tests |
| T-09-03 | DoS | Full regression | mitigate | Warning-latch tests confirm exactly one WARN per connection regardless of invalid-frame volume |
| T-09-04 | Tampering | Full regression | mitigate | Cache-reset tests confirm cross-connection pair contamination is prevented |
| T-09-SC | Tampering | No package installs | accept | Plan 03 installs nothing; only runs existing test scripts |
</threat_model>

<verification>
This plan IS the verification gate. Task 1 runs both suites and records results.
The checkpoint (Task 2) captures the phase acceptance sign-off.

Summary of what the combined Phase 9 plans prove:

| VALIDATION.md Req | Automated Evidence | Status |
|-------------------|--------------------|--------|
| DATA-01 all ranges backend | test_09_01_01_all_ranges_both_roles_match_shared_fixture (32 cases) | Plan 01 |
| DATA-01 all ranges frontend | measurementContract.test.ts (32 shared cases) | Plan 02 |
| DATA-01 confirmed-range retention | test_09_01_03_* (5 cases) | Plan 01 |
| DATA-02 sensor_config on every frame | test_09_01_02_publish_frame_emits_sensor_config_for_all_ranges | Plan 01 |
| DATA-02 sensor_config survives filter | test_09_01_02_sensor_config_survives_filter_json | Plan 01 |
| DATA-02 invalid metadata: zero emissions | 09-02-02 tests | Plan 02 |
| DATA-02 one WARN per connection | 09-02-02 tests | Plan 02 |
| DATA-02 valid frame resumes | 09-02-02 tests | Plan 02 |
| DATA-02 new connection resets | 09-02-02 tests | Plan 02 |
| DATA-01 correct service names | 09-02-03 tests | Plan 02 |
| DATA-01 both services for range change | 09-02-03 tests | Plan 02 |
</verification>

<success_criteria>
1. python -m unittest discover -s backend/test -p "test_*.py" exits 0 with all tests OK.
2. npm test (from rehab-robotics-studio/) exits 0 with all tests passing.
3. npm run typecheck exits 0.
4. ROADMAP.md Phase 9 shows "3 plans" and plan list with all three checked.
5. Checkpoint approved (human confirms all automated criteria green, UI-SPEC visual pass,
   service name corrected; hardware smoke test completed or explicitly deferred).
6. 09-03-SUMMARY.md created with: test counts, pass/fail status, acceptance checklist,
   hardware smoke status, and any notes for the Phase 10 planner.
</success_criteria>

<output>
Create .planning/phases/09-range-correct-measurement-contract/09-03-SUMMARY.md when done.
</output>
