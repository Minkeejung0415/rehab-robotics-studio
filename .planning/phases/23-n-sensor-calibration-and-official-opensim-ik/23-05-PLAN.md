---
phase: 23-n-sensor-calibration-and-official-opensim-ik
plan: 05
type: execute
wave: 4
depends_on:
  - 23-01
  - 23-02
  - 23-03
  - 23-04
files_modified:
  - backend/launch/rehab_robotics.launch.py
  - backend/test/test_n_sensor_calibration.py
autonomous: true
requirements:
  - IK-01
  - IK-02
  - IK-03
  - IK-04
must_haves:
  truths:
    - "sync_skew_ms parameter is passed to opensim_node in launch file"
    - "All backend tests pass: python -m pytest backend/test/ -x -q"
    - "test_n_sensor_calibration.py has comprehensive offline tests for CalibrationArtifactStore"
  artifacts:
    - path: backend/launch/rehab_robotics.launch.py
      provides: "sync_skew_ms param wired to opensim_node"
      contains: "sync_skew_ms"
    - path: backend/test/test_n_sensor_calibration.py
      provides: "Complete offline artifact store tests (save/load, path, invalidation, corruption)"
      contains: "test_corruption_recovery"
  key_links:
    - from: "rehab_robotics.launch.py opensim_node params"
      to: "opensim_node sync_skew_ms parameter"
      via: "DeclareLaunchArgument + LaunchConfiguration pattern matching existing params"
---

<objective>
Wire sync_skew_ms into the launch file, complete test_n_sensor_calibration.py with corruption
recovery tests, and run the full test suite to confirm Phase 23 delivers a green baseline.

Purpose: Ensure all IK-01 through IK-04 work is launch-wired and fully tested offline.
This plan produces no new feature code — it finalizes integration and validates the phase.

Output:
- backend/launch/rehab_robotics.launch.py: sync_skew_ms argument wired to opensim_node
- backend/test/test_n_sensor_calibration.py: comprehensive tests including corruption recovery
- Full test suite passing: python -m pytest backend/test/ -x -q
</objective>

<execution_context>
@C:/Users/justi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/justi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/23-n-sensor-calibration-and-official-opensim-ik/23-CONTEXT.md
@.planning/phases/23-n-sensor-calibration-and-official-opensim-ik/23-01-SUMMARY.md
@.planning/phases/23-n-sensor-calibration-and-official-opensim-ik/23-02-SUMMARY.md
@.planning/phases/23-n-sensor-calibration-and-official-opensim-ik/23-03-SUMMARY.md
@.planning/phases/23-n-sensor-calibration-and-official-opensim-ik/23-04-SUMMARY.md

<interfaces>
<!-- Launch file pattern — read the actual file before modifying: -->
<!-- backend/launch/rehab_robotics.launch.py uses ROS 2 launch Python API. -->
<!-- Existing params use DeclareLaunchArgument and LaunchConfiguration. -->
<!-- Pattern to follow for opensim_node:
     DeclareLaunchArgument("sync_skew_ms", default_value="50", description="...")
     Then in Node(parameters=[{..., "sync_skew_ms": LaunchConfiguration("sync_skew_ms"), ...}])
-->

<!-- test_n_sensor_calibration.py (from Plan 23-02) already has basic tests.
     This plan adds the corruption recovery and edge-case tests listed below. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire sync_skew_ms into launch file</name>
  <files>backend/launch/rehab_robotics.launch.py</files>
  <action>
    Read the full launch file first to understand the existing opensim_node Node() declaration
    and parameter passing pattern.

    Add a DeclareLaunchArgument for sync_skew_ms with default_value="50" and description
    "Sync skew tolerance in ms for N-sensor IK gate" alongside the existing opensim_node
    launch arguments.

    In the opensim_node Node(...) parameters list, add:
      "sync_skew_ms": LaunchConfiguration("sync_skew_ms")

    If the launch file does not have an opensim_node Node() entry yet (it may be named
    opensim_bridge or similar), find the correct node name and add the parameter there.
    Use grep or read to confirm the node name before modifying.

    Do NOT change any other parameters or node configurations. Only add the sync_skew_ms
    argument and wire it to opensim_node. Follow the exact same indentation and quoting
    style as adjacent parameters.

    If the launch file imports are at top (generate_launch_description, DeclareLaunchArgument,
    LaunchConfiguration, Node, etc.), ensure all required symbols are already imported.
    If DeclareLaunchArgument or LaunchConfiguration are not imported, add them.
  </action>
  <verify>
    <automated>python -c "import ast, pathlib; ast.parse(pathlib.Path('backend/launch/rehab_robotics.launch.py').read_text()); print('syntax ok')" 2>&1</automated>
  </verify>
  <done>
    rehab_robotics.launch.py parses without syntax error. "sync_skew_ms" appears as a
    DeclareLaunchArgument and is referenced in opensim_node parameters. Default value is "50".
  </done>
</task>

<task type="auto">
  <name>Task 2: Complete test_n_sensor_calibration.py with corruption recovery and edge-case tests</name>
  <files>backend/test/test_n_sensor_calibration.py</files>
  <action>
    Read the current test_n_sensor_calibration.py (created in Plan 23-02) to see what tests
    already exist. Then ADD the following missing test cases without removing existing ones.

    Additional test cases to add (all offline, using tempfile.TemporaryDirectory or tmp_path):

    test_corruption_recovery():
      - Write a corrupt JSON string to artifact path directly (e.g., "{{not json")
      - Call CalibrationArtifactStore().load(path): assert returns None
      - Node can load None and remain in uncalibrated state (verify _n_calib_state handling)

    test_wrong_schema_version():
      - Write valid JSON but with schema_version="calib.v2" (wrong version)
      - load() returns None

    test_empty_device_order_invalid():
      - is_valid({...empty device_order...}, model_hash, revision, current_devices=[])
      - When both stored and current are empty sets: returns True (vacuously valid)
      - When stored empty but current has devices: returns False

    test_artifact_path_uses_hash_prefix():
      - compute_artifact_path("abc123def4567890", 5) -> filename contains "abc123de" (first 8 chars)
      - compute_artifact_path("abcdef", 0) -> filename contains "abcdef" (shorter hash, truncated to len)
        Actually: str("abcdef")[:8] == "abcdef" — acceptable

    test_save_creates_parent_dirs():
      - Use a deeply nested tmp path that does not exist
      - save() creates parent directories (mkdir parents=True) before writing

    test_atomic_write_no_tmp_residue():
      - Call save() with valid data
      - Confirm the .tmp file does NOT exist after save completes (was renamed to final)
      - Confirm the final file exists and contains valid JSON

    test_load_missing_file():
      - load() on a path that does not exist returns None (no exception)

    test_is_valid_all_match():
      - Build a minimal valid artifact dict
      - is_valid(artifact, same_hash, same_rev, same_devices) returns True

    All tests use only stdlib (json, pathlib, tempfile, unittest). No ROS required.
    Run with: python -m pytest backend/test/test_n_sensor_calibration.py -v
  </action>
  <verify>
    <automated>cd backend && python -m pytest test/test_n_sensor_calibration.py -v 2>&1 | tail -30</automated>
  </verify>
  <done>
    test_n_sensor_calibration.py has 10+ test cases covering save/load round-trip, corruption
    recovery, wrong schema version, path computation, parent dir creation, atomic write, missing
    file, and is_valid edge cases. All pass offline.
  </done>
</task>

<task type="auto">
  <name>Task 3: Full test suite green gate</name>
  <files></files>
  <action>
    Run the full backend test suite and verify all tests pass. This is the Phase 23 green gate.

    Command: python -m pytest backend/test/ -x -q

    If any test fails:
    1. Read the failure output carefully
    2. Identify which Phase 23 plan introduced the regression (23-01 through 23-04)
    3. Fix the regression in the appropriate file
    4. Re-run until all tests pass

    Common failure categories and fixes:
    - Import error for _DeviceInput: ensure opensim_node.py defines it at module level
    - Import error for CalibrationArtifactStore: ensure __init__.py in opensim/ does not block
    - AttributeError on _StubNode.destroy_subscription: ensure Plan 23-01 Task 3 added it
    - KeyError in _check_sync_skew: verify the median computation handles single-element lists
    - TypeError in _solve_and_publish_ik_n: verify solve_n signature matches caller

    Do NOT modify tests to make them pass. Fix the production code to satisfy the test contracts.

    After all tests pass, output the final pytest summary line (e.g., "42 passed in 3.1s").
  </action>
  <verify>
    <automated>cd backend && python -m pytest test/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    All backend tests pass. Zero failures. Zero errors. The pytest summary line shows N passed
    with no failures. Phase 23 IK-01, IK-02, IK-03, and IK-04 contracts are verified offline.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Launch file parameter sync_skew_ms | Operator-supplied at launch; no validation needed (bounds enforced in node) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-05-01 | Tampering | sync_skew_ms set to 0 at launch disabling IK | accept | Operator controls launch params; not a security boundary |
| T-23-05-02 | Denial of Service | Invalid sync_skew_ms value in launch | mitigate | opensim_node.__init__ clamps to max(1, int(value)) with fallback to 50 on parse error |
| T-23-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages; launch file and tests only |
</threat_model>

<verification>
Phase 23 acceptance criteria (all must be true simultaneously):

1. python -m pytest backend/test/ -x -q  → all pass, zero failures

2. grep "sync_skew_ms" backend/launch/rehab_robotics.launch.py → matches found

3. python -c "
   from rehab_robotics_bridge.opensim.n_sensor_calibration import CalibrationArtifactStore
   from rehab_robotics_bridge.opensim.ik_contracts import SOLVER_PROFILE_MIN_SENSORS
   assert SOLVER_PROFILE_MIN_SENSORS['lower_body'] == 2
   print('ik_contracts ok')
   "

4. python -m pytest backend/test/test_n_sensor_calibration.py -v → 10+ tests pass

5. Confirm IK-01 through IK-04 named tests present:
   python -m pytest backend/test/ --collect-only -q 2>&1 | grep -E "IkOne|IkTwo|IkThree|IkFour"
</verification>

<success_criteria>
- sync_skew_ms wired in launch file with default 50
- test_n_sensor_calibration.py has corruption recovery, missing file, atomic write, and is_valid edge case tests
- python -m pytest backend/test/ -x -q → all pass
- Phase 23 IK-01/02/03/04 requirements fully covered and verifiable offline
</success_criteria>

<output>
Create .planning/phases/23-n-sensor-calibration-and-official-opensim-ik/23-05-SUMMARY.md when done
</output>
