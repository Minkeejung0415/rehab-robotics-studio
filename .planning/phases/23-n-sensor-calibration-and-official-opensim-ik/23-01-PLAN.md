---
phase: 23-n-sensor-calibration-and-official-opensim-ik
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/rehab_robotics_bridge/opensim/opensim_orientation_ik.py
  - backend/rehab_robotics_bridge/opensim_node.py
  - backend/test/test_opensim_node.py
autonomous: true
requirements:
  - IK-01
must_haves:
  truths:
    - "Applying a mapping to opensim_node creates exactly one MAC-keyed subscription per assigned device"
    - "Re-applying a mapping with different devices destroys old subscriptions and creates only new ones — no orphans"
    - "Subscriptions are tracked in a dict keyed by device_id (canonical esp32:aabbccddeeff form)"
    - "All subscription dict mutations occur under self._input_lock"
    - "post_reconnect_fresh is set True on first frame after reconnect, False on fleet registry reconnect event"
  artifacts:
    - path: backend/rehab_robotics_bridge/opensim/opensim_orientation_ik.py
      provides: "solve_n() method alongside existing solve()"
      contains: "def solve_n"
    - path: backend/rehab_robotics_bridge/opensim_node.py
      provides: "_DeviceInput dataclass, _mac_inputs dict, _input_lock, _on_mapping_current, _on_mac_imu, _on_fleet_registry"
      contains: "_DeviceInput"
    - path: backend/test/test_opensim_node.py
      provides: "IK-01 contract tests covering subscription lifecycle, remap cleanup, name-based assertions"
      contains: "IK-01"
  key_links:
    - from: "opensim_node._on_mapping_current"
      to: "opensim_node._mac_inputs"
      via: "_input_lock acquire/release with destroy_subscription for removed devices"
    - from: "opensim_node._on_mac_imu"
      to: "_DeviceInput.post_reconnect_fresh"
      via: "set True on first frame arrival"
    - from: "opensim_node._on_fleet_registry"
      to: "_DeviceInput.post_reconnect_fresh"
      via: "set False on reconnect event"
---

<objective>
Wire dynamic N-sensor subscription management into opensim_node and add a solve_n() entry
point on the IK solver for N-column orientation tables.

Purpose: IK-01 — applying or replacing a mapping must create exactly one MAC-keyed subscription
per assigned device and tear down obsolete subscriptions without resource growth across repeated
remaps. The solver needs a multi-input path so downstream plans can drive the N-sensor IK loop.

Output:
- solve_n() on OpenSimOrientationIkSolver (builds N-column table; existing solve() preserved)
- _DeviceInput dataclass replacing _SensorState for dynamic device management
- _mac_inputs dict + _input_lock in OpenSimBridgeNode
- _on_mapping_current, _on_mac_imu, _on_fleet_registry callbacks
- IK-01 contract tests (RED before GREEN, then all green)
</objective>

<execution_context>
@C:/Users/justi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/justi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/23-n-sensor-calibration-and-official-opensim-ik/23-CONTEXT.md
@.planning/phases/23-n-sensor-calibration-and-official-opensim-ik/23-RESEARCH.md

<interfaces>
<!-- Existing patterns the executor must match exactly. -->

From backend/rehab_robotics_bridge/opensim_node.py (existing _SensorState):
```python
@dataclass
class _SensorState:
    topic: str
    frame: str
    waiting_since_monotonic: float
    state: str = "waiting"
    last_valid_monotonic: float | None = None
    last_source_timestamp_ns: int | None = None
    last_xyzw: tuple[float, float, float, float] | None = None
    updates: int = 0
    last_error: str = ""
```

Replacement _DeviceInput must carry all fields needed by IK-03 and IK-04 (per D-02):
```python
@dataclass
class _DeviceInput:
    device_id: str          # canonical "esp32:aabbccddeeff"
    frame: str              # OpenSim frame name e.g. "tibia_r_imu"
    subscription_handle: object   # ROS subscription returned by create_subscription
    last_xyzw: tuple[float, float, float, float] | None = None
    last_ts_ns: int | None = None
    post_reconnect_fresh: bool = False
    last_seen_monotonic: float | None = None
```

From backend/test/test_opensim_node.py (_StubNode):
```python
class _StubNode:
    def create_subscription(self, message_type, topic, callback, qos):
        subscription = types.SimpleNamespace(...)
        self.subscriptions.append(subscription)
        return subscription

    # MISSING — must be added in Task 3:
    def destroy_subscription(self, sub):
        if sub in self.subscriptions:
            self.subscriptions.remove(sub)
```

MAC topic naming pattern (from 21-CONTEXT.md):
  device_id "esp32:aabbccddeeff" -> strip "esp32:" -> "aabbccddeeff"
  topic: "/esp/raw/mac_aabbccddeeff"

Fleet registry reconnect event format (from 21-CONTEXT.md):
  Topic: "/esp/fleet/registry" (String JSON)
  Reconnect events have field "reconnected_devices": ["esp32:aabbccddeeff", ...]
  or individual device entries with "event": "reconnect"

solve_n signature to add (parallel to solve()):
```python
def solve_n(
    self,
    inputs: list[tuple[str, Sequence[float]]],  # [(frame_path, xyzw_tuple), ...]
    source_timestamp_ns: int | None,
    input_age_s: float | None,
    joint_names: Sequence[str],
) -> IkSolution:
```
Builds N-column TimeSeriesTableQuaternion from inputs list using same _make_quat_table logic.
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Add solve_n() to OpenSimOrientationIkSolver (RED then GREEN)</name>
  <files>backend/rehab_robotics_bridge/opensim/opensim_orientation_ik.py</files>
  <behavior>
    - solve_n(inputs=[], ...) returns IkSolution(solution_valid=False, reason="no_inputs")
    - solve_n(inputs=[("frame_a", (0,0,0,1)), ("frame_b", (0,0,0,1))], source_timestamp_ns=None, ...) returns IkSolution(solution_valid=False, reason="missing_source_timestamp")
    - solve_n with N inputs builds a TimeSeriesTableQuaternion with N columns in input order
    - solve_n preserves existing solve() untouched (no regression)
    - UnavailableOrientationIkSolver.solve_n() returns IkSolution(solution_valid=False, reason=self._reason)
    - FakeOrientationIkSolver.solve_n() with 2+ calibrated inputs returns solution_valid=True (uses first two frames for relative angle)
  </behavior>
  <action>
    Add solve_n() to OpenSimOrientationIkSolver, UnavailableOrientationIkSolver, and
    FakeOrientationIkSolver. Also add solve_n to the OrientationIkSolver Protocol.

    In OpenSimOrientationIkSolver.solve_n():
    - Accept inputs: list[tuple[str, Sequence[float]]] where each tuple is (frame_name, xyzw)
    - Return IkSolution(solution_valid=False, reason="no_inputs") if inputs is empty
    - Return IkSolution(solution_valid=False, reason="missing_source_timestamp") if source_timestamp_ns is None
    - Build a TimeSeriesTableQuaternion by calling self._osim.TimeSeriesTableQuaternion(),
      then setColumnLabels([frame for frame, _ in inputs]), then build a RowVectorQuaternion(len(inputs))
      using the same updElt loop pattern as _make_quat_table — convert each xyzw via self._xyzw_to_wxyz()
    - Call self._quats_to_rotations(table) to get rotations_table
    - Rebuild OrientationsReference and InverseKinematicsSolver from rotations_table (same pattern
      as _update_orientation_targets non-buffered path, since N-sensor does not use buffered path)
    - Increment self._time_s by 0.01, call self._state.setTime(self._time_s)
    - assemble if not self._assembled, else track (same as solve())
    - Call self._read_coordinates() and self._read_visualization_pose()
    - Return IkSolution identical in shape to solve() return

    In FakeOrientationIkSolver.solve_n():
    - If inputs has < 2 entries and calibration is None: return calibration_required
    - If source_timestamp_ns is None: return missing_source_timestamp
    - Use inputs[0][1] as master_xyzw, inputs[1][1] as slave_xyzw if len >= 2 (else identity for slave)
    - Call apply_mounting_offsets with a minimal artifact or skip if calibration is None (return calibration_required)
    - Return same IkSolution shape as FakeOrientationIkSolver.solve()

    In UnavailableOrientationIkSolver.solve_n(): return same IkSolution as solve() with self._reason.

    Write tests FIRST (RED), then implement (GREEN). Tests live alongside the existing test file
    or in a new test_opensim_orientation_ik_n.py in backend/test/.

    Do NOT remove or modify solve(), _make_quat_table(), _update_orientation_targets(), or any
    existing method. solve_n() is additive.
  </action>
  <verify>
    <automated>cd backend && python -m pytest test/test_opensim_orientation_ik_opensim.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    solve_n() exists on all three solver classes. Existing solve() tests still pass.
    New RED tests fail before implementation; GREEN after. solve_n([]) returns no_inputs.
    solve_n with N inputs and valid calibration succeeds on FakeOrientationIkSolver.
  </done>
</task>

<task type="tdd">
  <name>Task 2: Add _DeviceInput dataclass and dynamic subscription management to OpenSimBridgeNode</name>
  <files>backend/rehab_robotics_bridge/opensim_node.py</files>
  <behavior>
    - _DeviceInput dataclass exists with fields: device_id, frame, subscription_handle, last_xyzw, last_ts_ns, post_reconnect_fresh, last_seen_monotonic
    - OpenSimBridgeNode.__init__ creates self._input_lock = threading.Lock() and self._mac_inputs: dict[str, _DeviceInput] = {}
    - _on_mapping_current called with a String message whose .data contains JSON {assigned: [{device_id, frame}, ...]} correctly populates _mac_inputs
    - On remap: devices removed from assignment have destroy_subscription() called and are removed from _mac_inputs under lock
    - On remap: new devices get new subscriptions to /esp/raw/mac_<12hex>
    - _on_mac_imu(device_id, message) updates _DeviceInput.last_xyzw, last_ts_ns, last_seen_monotonic under lock; sets post_reconnect_fresh=True on first frame (when prior last_xyzw was None)
    - _on_fleet_registry called with JSON containing reconnect events sets post_reconnect_fresh=False for each reconnected device_id in _mac_inputs
    - Existing master/slave subscription and _sensor_states dict are preserved (no regression)
  </behavior>
  <action>
    Add a _DeviceInput dataclass above OpenSimBridgeNode (at module level, after _SensorState).
    Do NOT remove _SensorState — it is still used by the existing master/slave path.

    _DeviceInput fields (per D-02):
      device_id: str
      frame: str
      subscription_handle: object
      last_xyzw: tuple[float,float,float,float] | None = None
      last_ts_ns: int | None = None
      post_reconnect_fresh: bool = False
      last_seen_monotonic: float | None = None

    In OpenSimBridgeNode.__init__, after existing initialization:
    - Add: self._input_lock = threading.Lock()
    - Add: self._mac_inputs: dict[str, _DeviceInput] = {}
    - Add: self._n_mapping_revision: int = 0  (tracks current applied revision for IK-04)
    - Subscribe to MAPPING_CURRENT_TOPIC ("/rehab/mapping/current", String) with callback self._on_mapping_current using _IMU_QOS
    - Subscribe to FLEET_REGISTRY_TOPIC ("/esp/fleet/registry", String) with callback self._on_fleet_registry using _IMU_QOS
    - Import threading at top (already present in other bridge nodes; add if missing)
    - Import MAPPING_CURRENT_TOPIC and FLEET_REGISTRY_TOPIC constants from mapping_node or define locally:
        MAPPING_CURRENT_TOPIC = "/rehab/mapping/current"
        FLEET_REGISTRY_TOPIC = "/esp/fleet/registry"

    Implement _on_mapping_current(self, message: String) -> None:
      Parse message.data as JSON. Expected structure:
        {"assigned": [{"device_id": "esp32:aabbccddeeff", "frame": "tibia_r_imu", "segment": "..."}], "applied_revision": N}
      Under self._input_lock:
        new_device_ids = {entry["device_id"] for entry in assigned_list}
        for device_id in list(self._mac_inputs):
          if device_id not in new_device_ids:
            try: self.destroy_subscription(self._mac_inputs[device_id].subscription_handle)
            except Exception: pass
            del self._mac_inputs[device_id]
        for entry in assigned_list:
          device_id = entry["device_id"]
          frame = entry.get("frame", "")
          if device_id not in self._mac_inputs:
            mac_hex = device_id.replace("esp32:", "")
            topic = f"/esp/raw/mac_{mac_hex}"
            sub = self.create_subscription(
                Imu, topic,
                lambda msg, did=device_id: self._on_mac_imu(did, msg),
                _IMU_QOS,
            )
            self._mac_inputs[device_id] = _DeviceInput(
                device_id=device_id,
                frame=frame,
                subscription_handle=sub,
            )
      self._n_mapping_revision = int(message_json.get("applied_revision", 0))
      Log info: f"N-sensor mapping update: {len(self._mac_inputs)} devices"

    Implement _on_mac_imu(self, device_id: str, message: Imu) -> None:
      source_ts = _source_timestamp_ns(message) (reuse existing helper)
      now = self._monotonic_clock()
      with self._input_lock:
        entry = self._mac_inputs.get(device_id)
        if entry is None: return
        orientation = message.orientation
        try:
          xyzw = (float(orientation.x), float(orientation.y),
                  float(orientation.z), float(orientation.w))
        except (TypeError, AttributeError): return
        first_frame = (entry.last_xyzw is None)
        entry.last_xyzw = xyzw
        entry.last_ts_ns = source_ts
        entry.last_seen_monotonic = now
        if first_frame:
          entry.post_reconnect_fresh = True

    Implement _on_fleet_registry(self, message: String) -> None:
      Parse message.data as JSON. Look for reconnect events:
        If top-level "reconnected_devices" key: list of device_ids to mark not-fresh
        Or if top-level "devices" list with entries having "event": "reconnect"
      Under self._input_lock:
        for each reconnected device_id in parsed list:
          if device_id in self._mac_inputs:
            self._mac_inputs[device_id].post_reconnect_fresh = False
      On JSON parse error: log warning, return.

    All mutation of _mac_inputs must occur under self._input_lock.
    The existing _sensor_states, _on_master_imu, _on_slave_imu paths are NOT modified.
    Add threading import if not present.
  </action>
  <verify>
    <automated>cd backend && python -m pytest test/test_opensim_node.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    _DeviceInput dataclass is importable. OpenSimBridgeNode instantiates with _mac_inputs and _input_lock.
    _on_mapping_current creates subscriptions for each assigned device. Re-calling with different
    devices destroys old subscriptions. _on_mac_imu updates the entry under lock. _on_fleet_registry
    clears post_reconnect_fresh. All existing tests pass.
  </done>
</task>

<task type="tdd">
  <name>Task 3: IK-01 contract tests in test_opensim_node.py (RED then GREEN)</name>
  <files>backend/test/test_opensim_node.py</files>
  <behavior>
    - _StubNode.destroy_subscription(sub) removes the subscription from self.subscriptions
    - Subscription assertions by topic name, not by index/count (e.g., [s.topic for s in node.subscriptions])
    - IK-01-A: _on_mapping_current with 2 assigned devices creates 2 MAC-keyed subscriptions at /esp/raw/mac_<12hex> topics
    - IK-01-B: Re-calling _on_mapping_current with 1 new + 1 removed device: old subscription destroyed, new subscription created, total active subscriptions for mac_ topics = 1 for old + 1 for new = 1 remaining active
    - IK-01-C: After remap, _mac_inputs dict has exactly the new device_ids as keys, no stale entries
    - IK-01-D: _on_mac_imu sets last_xyzw and post_reconnect_fresh=True on first call; subsequent calls do not re-set post_reconnect_fresh
    - IK-01-E: _on_fleet_registry with reconnect event sets post_reconnect_fresh=False for matching device_id
    - IK-01-F: _on_mapping_current with malformed JSON logs a warning and does not raise
  </behavior>
  <action>
    Add destroy_subscription() to _StubNode in test_opensim_node.py:
      def destroy_subscription(self, sub):
          if sub in self.subscriptions:
              self.subscriptions.remove(sub)

    Update the existing subscription count assertion pattern from:
      self.assertEqual(len(node.subscriptions), N)
    to use topic name checks:
      topics = [s.topic for s in node.subscriptions]
      self.assertIn("/some/topic", topics)

    Add a new test class IkOneContractTests(unittest.TestCase) containing tests
    IK-01-A through IK-01-F as described in behavior section above.

    For each test: construct OpenSimBridgeNode with injected FakeOrientationIkSolver
    and a stub CalibrationController, call _on_mapping_current with appropriate
    String stub messages, then assert _mac_inputs keys and subscriptions topics.

    Helper: build a _String-like object with .data set to JSON string. The existing
    _String class already has a .data attribute — reuse it.

    Mapping JSON format to use in tests:
    {
      "applied_revision": 1,
      "assigned": [
        {"device_id": "esp32:aabbccddeeff", "frame": "tibia_r_imu", "segment": "tibia"},
        {"device_id": "esp32:112233445566", "frame": "femur_r_imu", "segment": "femur"}
      ]
    }

    Write IK-01 tests FIRST (they will fail RED since the implementation from Task 2 may
    not exist yet if tasks run sequentially). Commit RED, implement Task 2, then GREEN.
    If Task 2 is already complete, write tests to validate it (straight to GREEN).

    Tests must be runnable with: python -m pytest backend/test/test_opensim_node.py -x -q
    No live ROS or hardware required. All ROS stubs are already installed by _install_ros_stubs().
  </action>
  <verify>
    <automated>cd backend && python -m pytest test/test_opensim_node.py -x -q -k "IkOne" 2>&1 | tail -20</automated>
  </verify>
  <done>
    All IK-01-A through IK-01-F tests pass. destroy_subscription() works in _StubNode.
    Existing non-IK-01 tests still pass. No subscription topic assertions rely on bare count.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /rehab/mapping/current -> opensim_node | JSON from mapping_node; treated as trusted ROS peer but parsed defensively |
| /esp/fleet/registry -> opensim_node | JSON from fleet_bridge_node; treated as trusted ROS peer but parsed defensively |
| /esp/raw/mac_<12hex> -> opensim_node | IMU frames from relay; same trust level as existing master/slave inputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01-01 | Tampering | _on_mapping_current JSON parse | mitigate | Wrap json.loads in try/except; log warning on error; return without modifying _mac_inputs |
| T-23-01-02 | Denial of Service | Rapid remap flooding creating/destroying subs | accept | No rate limiting needed; destroy_subscription is bounded; each remap is O(N devices) |
| T-23-01-03 | Information Disclosure | device_id logged in topic names | accept | Topic names are not secrets; internal ROS bus only |
| T-23-01-04 | Tampering | _on_fleet_registry malformed JSON | mitigate | Wrap parse in try/except; log warning; return without modifying post_reconnect_fresh |
| T-23-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages introduced in this plan |
</threat_model>

<verification>
All three tasks together:
- python -m pytest backend/test/test_opensim_node.py -x -q (all existing + IK-01 tests green)
- python -m pytest backend/test/test_opensim_orientation_ik_opensim.py -x -q (solve_n tests green)
- python -m pytest backend/test/ -x -q (no regressions across suite)
</verification>

<success_criteria>
- solve_n() exists on OpenSimOrientationIkSolver, FakeOrientationIkSolver, UnavailableOrientationIkSolver, and OrientationIkSolver Protocol
- _DeviceInput dataclass defined in opensim_node.py with all D-02 fields
- _mac_inputs dict and _input_lock present in OpenSimBridgeNode.__init__
- _on_mapping_current correctly creates and destroys subscriptions per D-01
- _on_mac_imu sets post_reconnect_fresh=True on first frame per D-09
- _on_fleet_registry clears post_reconnect_fresh on reconnect event per D-09
- IK-01-A through IK-01-F tests pass offline without live ROS
- All pre-existing opensim_node tests still pass
</success_criteria>

<output>
Create .planning/phases/23-n-sensor-calibration-and-official-opensim-ik/23-01-SUMMARY.md when done
</output>
