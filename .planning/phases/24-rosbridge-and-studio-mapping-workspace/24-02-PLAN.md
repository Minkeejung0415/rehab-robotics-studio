---
phase: 24-rosbridge-and-studio-mapping-workspace
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - rehab-robotics-studio/src/data/RosbridgeDataSource.ts
  - rehab-robotics-studio/src/data/RosbridgeDataSource.test.ts
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04

must_haves:
  truths:
    - "RosbridgeDataSource constructor accepts 5 new optional callbacks at positions 12-16 without breaking existing 11 (D-08)"
    - "socket.onopen subscribes to all 5 new topics with type std_msgs/msg/String (D-09)"
    - "handleMessage dispatches to the correct callback using 5 new guard-parsed handlers; invalid JSON silently drops (D-10)"
    - "callSetAssignment calls /rehab/mapping/set_assignment with {device_id, segment, frame, state} and returns {success, outcome, detail} (D-11)"
    - "callApplyMapping calls /rehab/mapping/apply with {expected_revision} and returns {success, outcome, appliedRevision, detail} (D-11)"
    - "callResetMapping calls /rehab/mapping/reset with {model_hash} and returns {success, outcome} (D-11)"
    - "callIdentifyDevice calls /rehab/identify/device or reuses existing method; returns outcome string (D-17)"
    - "parseMappingCurrent, parseFleetRegistry, parseModelCatalog, parseNCalibrationStatus, parseInputValidity each return null on invalid input without throwing"
  artifacts:
    - path: "rehab-robotics-studio/src/data/RosbridgeDataSource.ts"
      provides: "Extended RosbridgeDataSource with 5 subscriptions + 4 service call methods"
      contains: "onModelCatalog"
    - path: "rehab-robotics-studio/src/data/RosbridgeDataSource.test.ts"
      provides: "Vitest unit tests covering parse functions and service call method signatures"
      contains: "parseMappingCurrent"
  key_links:
    - from: "rehab-robotics-studio/src/data/RosbridgeDataSource.ts"
      to: "rehab-robotics-studio/src/data/appDataSource.ts"
      via: "constructor callbacks wired in Plan 24-04"
      pattern: "onModelCatalog.*onMappingCurrent.*onFleetRegistry"
---

<objective>
Extend `RosbridgeDataSource` with the 5 new topic subscriptions and 4 new service call methods required by Phase 24 (D-08 through D-11, D-17). Also write vitest tests for the 5 guard-parse functions and verify service call method signatures compile correctly.

Purpose: Without these extensions, the Studio cannot receive mapping state from ROS topics or submit mapping changes. Plan 24-04 wires these into appDataSource.ts.
Output: Extended `RosbridgeDataSource.ts` + `RosbridgeDataSource.test.ts` (new file for Phase 24 additions).
</objective>

<execution_context>
@c:/Users/justi/Documents/# Rehab Robotics GUI Prototype__Here's a complete, single-file React + TypeScript app implementing the LabVIEW/rehab-robotics-studio/src/data/RosbridgeDataSource.ts
</execution_context>

<context>
@c:/Users/justi/Documents/# Rehab Robotics GUI Prototype__Here's a complete, single-file React + TypeScript app implementing the LabVIEW/.planning/phases/24-rosbridge-and-studio-mapping-workspace/24-CONTEXT.md
@c:/Users/justi/Documents/# Rehab Robotics GUI Prototype__Here's a complete, single-file React + TypeScript app implementing the LabVIEW/.planning/ROADMAP.md

<interfaces>
<!-- Existing constructor signature (positions 1-11): -->
```typescript
constructor(
  private readonly url?,                      // 1
  private readonly masterTopic?,              // 2
  private readonly slaveTopic?,               // 3
  private readonly onUnavailable?,            // 4
  private readonly onConnectionChange?,       // 5
  private readonly onFrameReceived?,          // 6
  private readonly onPairHealth?,             // 7
  private readonly onWarnScaleMissing?,       // 8
  private readonly onOpenSimStatus?,          // 9
  private readonly onOpenSimIkStatus?,        // 10
  private readonly onOpenSimJointState?,      // 11
) {}
```

<!-- Existing private callService signature (to replicate for new public methods): -->
```typescript
private callService(
  service: string,
  args: Record<string, unknown>,
  type?: string,
  toResult?: (values: RosbridgeEnvelope['values']) => RecordingCommandResult,
  timeoutMessage?: string,
): Promise<RecordingCommandResult>
```

<!-- Existing guard pattern (parseOpenSimStatus) to replicate: -->
```typescript
function parseOpenSimStatus(payload: unknown): OpenSimStatusSnapshot | null {
  if (!isRecord(payload)) return null;
  // ... field-by-field validation; return null on any type mismatch
}
```

<!-- Topic constants to add: -->
// '/rehab/model/catalog'       → onModelCatalog callback
// '/rehab/mapping/current'     → onMappingCurrent callback
// '/esp/fleet/registry'        → onFleetRegistry callback
// '/rehab/calibration/status'  → onCalibrationStatus callback
// '/rehab/opensim/input_validity' → onInputValidity callback

<!-- Service contracts: -->
// /rehab/mapping/set_assignment  type: rehab_robotics_interfaces/srv/SetAssignment
//   args: { device_id, segment, frame, state }
//   response fields: { outcome: string, detail: string }
// /rehab/mapping/apply           type: rehab_robotics_interfaces/srv/ApplyMapping
//   args: { expected_revision: number }
//   response fields: { outcome: string, applied_revision: number, detail: string }
// /rehab/mapping/reset           type: rehab_robotics_interfaces/srv/ResetMapping
//   args: { model_hash: string }
//   response fields: { outcome: string }
// /rehab/identify/device         type: rehab_robotics_interfaces/srv/IdentifyDevice
//   args: { device_id: string, timeout_ms?: number }
//   response fields: { outcome: string }
</interfaces>
</context>

<feature>
  <name>RosbridgeDataSource extensions — 5 topic subscriptions + 4 service call methods</name>
  <files>
    rehab-robotics-studio/src/data/RosbridgeDataSource.ts,
    rehab-robotics-studio/src/data/RosbridgeDataSource.test.ts
  </files>
  <behavior>
    RED phase test cases:

    Parse function tests (module-level, not class methods — export them for testability):
    1. parseModelCatalog({schema:'v1', model_hash:'abc', model_path:'/foo.osim', frame_list:[{path:'tibia', name:'Tibia'}]}) returns the snapshot with frame_list correctly shaped
    2. parseModelCatalog(null) returns null
    3. parseModelCatalog({model_hash:123}) returns null (hash not string)
    4. parseMappingCurrent({revision:1, applied_revision:0, model_hash:'abc', assignments:{'esp32:aa':{segment:'s',frame:'f',state:'assigned'}}}) returns the snapshot with revision=1, appliedRevision=0
    5. parseMappingCurrent({revision:'bad'}) returns null
    6. parseFleetRegistry({devices:[{device_id:'esp32:aa',role:'master',route_state:'active',connection_state:'connected',rate_hz:100,drop_count:0}]}) returns snapshot
    7. parseFleetRegistry({}) returns null (missing devices array)
    8. parseNCalibrationStatus({state:'capturing', revision:1, model_hash:'abc'}) returns snapshot with state='capturing'
    9. parseNCalibrationStatus({state:null}) returns null
    10. parseInputValidity({device_validities:{'esp32:aa':true, 'esp32:bb':false}}) returns snapshot with device_validities mapped correctly
    11. parseInputValidity({}) returns null (missing device_validities)

    Service call method signature test (smoke — does not call WebSocket):
    12. `callSetAssignment` is a public method on the class that returns Promise<RecordingCommandResult>
    13. `callApplyMapping` is a public method that accepts a number (expectedRevision)
    14. `callResetMapping` is a public method that accepts a string (modelHash)
    15. `callIdentifyDevice` is a public method that accepts a string (deviceId)

    GREEN phase: parse functions all return correct shapes or null; service methods delegate to private callService with correct service path, type string, and toResult extractor.
  </behavior>
  <implementation>
    Add 5 module-level parse functions immediately before the `RosbridgeDataSource` class declaration (to keep them testable without instantiation).

    Export them as named exports so tests can import directly: `export function parseModelCatalog(...)`, etc.

    Each parse function follows the `parseOpenSimStatus` guard pattern:
    - `parseModelCatalog(payload: unknown): ModelCatalogSnapshot | null` — require model_hash (string), model_path (string), frame_list (array of {path:string, name:string})
    - `parseMappingCurrent(payload: unknown): MappingCurrentSnapshot | null` — require revision (number), applied_revision (number), assignments (record)
    - `parseFleetRegistry(payload: unknown): FleetRegistrySnapshot | null` — require devices (array)
    - `parseNCalibrationStatus(payload: unknown): NCalibrationStatusSnapshot | null` — require state (one of: 'capturing','calibrated','uncalibrated')
    - `parseInputValidity(payload: unknown): InputValiditySnapshot | null` — require device_validities (record)

    Note: these parse functions use the snapshot types exported from `mappingStore.ts`. Import them from `../state/mappingStore`.

    Constructor extension (append after position 11, preserve existing params):
    ```
    private readonly onModelCatalog?: (catalog: ModelCatalogSnapshot) => void,   // 12
    private readonly onMappingCurrent?: (state: MappingCurrentSnapshot) => void, // 13
    private readonly onFleetRegistry?: (registry: FleetRegistrySnapshot) => void,// 14
    private readonly onCalibrationStatus?: (s: NCalibrationStatusSnapshot) => void, // 15
    private readonly onInputValidity?: (v: InputValiditySnapshot) => void,       // 16
    ```

    In `socket.onopen`, append to the subscriptions array (after existing entries):
    - `['/rehab/model/catalog', 'std_msgs/msg/String']`
    - `['/rehab/mapping/current', 'std_msgs/msg/String']`
    - `['/esp/fleet/registry', 'std_msgs/msg/String']`
    - `['/rehab/calibration/status', 'std_msgs/msg/String']`
    - `['/rehab/opensim/input_validity', 'std_msgs/msg/String']`

    In `handleMessage`, add 5 new topic dispatch blocks before the fallthrough to IMU topic handling. Each block: check envelope.topic === CONSTANT, parse with the guard function, call callback if non-null.

    Add 4 public methods to `RosbridgeDataSource`:

    `callSetAssignment(deviceId: string, segment: string, frame: string, state: string): Promise<RecordingCommandResult>` — calls `this.callService('/rehab/mapping/set_assignment', {device_id: deviceId, segment, frame, state}, 'rehab_robotics_interfaces/srv/SetAssignment', (values) => ({ success: values?.outcome === 'ok', message: String(values?.outcome ?? ''), outcome: String(values?.outcome ?? ''), detail: String(values?.detail ?? '') } as any), 'Timed out waiting for SetAssignment response')`. Return type must also expose `outcome` and `detail` — add a new exported type `MappingServiceResult = { success: boolean; message: string; outcome: string; detail: string }` and adjust the return type accordingly.

    `callApplyMapping(expectedRevision: number): Promise<RecordingCommandResult>` — service `/rehab/mapping/apply`, type `rehab_robotics_interfaces/srv/ApplyMapping`, args `{expected_revision: expectedRevision}`, toResult extracts `outcome` (string) and `applied_revision` (number).

    `callResetMapping(modelHash: string): Promise<RecordingCommandResult>` — service `/rehab/mapping/reset`, type `rehab_robotics_interfaces/srv/ResetMapping`, args `{model_hash: modelHash}`, toResult extracts `outcome`.

    `callIdentifyDevice(deviceId: string, timeoutMs = 5000): Promise<RecordingCommandResult>` — check if the method already exists on the class before adding; if it does, keep existing. If not, add: service `/rehab/identify/device`, type `rehab_robotics_interfaces/srv/IdentifyDevice`, args `{device_id: deviceId, timeout_ms: timeoutMs}`, toResult extracts `outcome`.

    For the toResult extractors on the mapping service calls, return an object that extends RecordingCommandResult with additional fields. Because `callService` is typed to return `Promise<RecordingCommandResult>`, the extra fields are carried via the resolved value — cast with `as RecordingCommandResult` for the private method signature.
  </implementation>
</feature>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED): Write failing tests for parse functions and service call stubs</name>
  <files>rehab-robotics-studio/src/data/RosbridgeDataSource.test.ts</files>
  <read_first>
    - rehab-robotics-studio/src/data/RosbridgeDataSource.ts (full file — understand existing parseOpenSimStatus guard pattern and callService signature)
    - rehab-robotics-studio/.planning/phases/24-rosbridge-and-studio-mapping-workspace/24-CONTEXT.md (D-08 through D-11, D-17 — exact service paths, arg shapes, response field names)
  </read_first>
  <behavior>
    - Tests import `parseModelCatalog`, `parseMappingCurrent`, `parseFleetRegistry`, `parseNCalibrationStatus`, `parseInputValidity` from `./RosbridgeDataSource` (fail because not yet exported)
    - Tests import `RosbridgeDataSource` class and instantiate with no-arg `new RosbridgeDataSource()` to verify instance has `callSetAssignment`, `callApplyMapping`, `callResetMapping`, `callIdentifyDevice` methods
    - All 15 behavior bullets from the feature block covered
  </behavior>
  <action>
    Create `rehab-robotics-studio/src/data/RosbridgeDataSource.test.ts`.

    Import parse functions and class: `import { parseModelCatalog, parseMappingCurrent, parseFleetRegistry, parseNCalibrationStatus, parseInputValidity, RosbridgeDataSource } from './RosbridgeDataSource';`

    Group tests in three `describe` blocks:
    1. `describe('parseModelCatalog', ...)` — 3 cases
    2. `describe('parseMappingCurrent / parseFleetRegistry / parseNCalibrationStatus / parseInputValidity', ...)` — 8 cases
    3. `describe('RosbridgeDataSource mapping service methods', ...)` — 4 cases checking `typeof instance.callSetAssignment === 'function'`, etc.

    Do NOT mock WebSocket or attempt to call service methods. Method-presence tests only for service calls.
  </action>
  <verify>
    <automated>cd "c:/Users/justi/Documents/# Rehab Robotics GUI Prototype__Here's a complete, single-file React + TypeScript app implementing the LabVIEW/rehab-robotics-studio" && npx vitest run src/data/RosbridgeDataSource.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - RosbridgeDataSource.test.ts file exists
    - Running vitest on the test file fails because parse functions are not yet exported from RosbridgeDataSource.ts
    - Test file contains at least 15 `it(...)` test cases
    - Tests import all 5 parse functions and the RosbridgeDataSource class
  </acceptance_criteria>
  <done>Test file exists with all cases; vitest shows import/missing-export failures (RED confirmed).</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 (GREEN): Extend RosbridgeDataSource.ts with subscriptions, parse functions, and service methods</name>
  <files>rehab-robotics-studio/src/data/RosbridgeDataSource.ts</files>
  <read_first>
    - rehab-robotics-studio/src/data/RosbridgeDataSource.test.ts (tests to satisfy — read first to know exactly what is expected)
    - rehab-robotics-studio/src/data/RosbridgeDataSource.ts (FULL FILE — understand all existing code before modifying; do not break existing behavior)
    - rehab-robotics-studio/src/state/mappingStore.ts (snapshot types to import: ModelCatalogSnapshot, MappingCurrentSnapshot, FleetRegistrySnapshot, NCalibrationStatusSnapshot, InputValiditySnapshot — created in Plan 24-01)
    - rehab-robotics-studio/.planning/phases/24-rosbridge-and-studio-mapping-workspace/24-CONTEXT.md (D-08 through D-11, D-17 — exact service paths)
  </read_first>
  <behavior>
    - All 15 test cases from Task 1 pass
    - Existing tests (if any) remain passing
    - TypeScript compiles without errors
    - constructor parameter count goes from 11 to 16 optional params; existing 11 remain at same positions
    - handleMessage dispatch: each new topic block must check the topic constant against envelope.topic, parse with the guard, invoke callback only when parse returns non-null; invalid payloads silently dropped (no throw, per D-10)
  </behavior>
  <action>
    Modify `rehab-robotics-studio/src/data/RosbridgeDataSource.ts`.

    Step 1: Add import at top — `import type { ModelCatalogSnapshot, MappingCurrentSnapshot, FleetRegistrySnapshot, NCalibrationStatusSnapshot, InputValiditySnapshot } from '../state/mappingStore';`

    Step 2: Add 5 topic string constants after existing DEFAULT_* constants:
    - `const MAPPING_CATALOG_TOPIC = '/rehab/model/catalog';`
    - `const MAPPING_CURRENT_TOPIC = '/rehab/mapping/current';`
    - `const FLEET_REGISTRY_TOPIC = '/esp/fleet/registry';`
    - `const CALIBRATION_STATUS_TOPIC = '/rehab/calibration/status';`
    - `const INPUT_VALIDITY_TOPIC = '/rehab/opensim/input_validity';`
    - `const MAPPING_SET_ASSIGNMENT_SERVICE = '/rehab/mapping/set_assignment';`
    - `const MAPPING_APPLY_SERVICE = '/rehab/mapping/apply';`
    - `const MAPPING_RESET_SERVICE = '/rehab/mapping/reset';`
    - `const IDENTIFY_DEVICE_SERVICE = '/rehab/identify/device';`

    Step 3: Add 5 exported parse functions before the class declaration (follow the isRecord + field-type guard pattern from parseOpenSimStatus exactly — return null on any validation failure, never throw).

    Step 4: Extend constructor — append 5 new private readonly callback params (positions 12-16). Existing 11 params unchanged.

    Step 5: In `socket.onopen`, append 5 new entries to the subscriptions array. The array uses `new Map(subscriptions.map(...)).values()` to deduplicate — append before the deduplication map line.

    Step 6: In `handleMessage`, add 5 `if (envelope.topic === CONSTANT)` dispatch blocks immediately after the existing `DEFAULT_OPENSIM_JOINT_STATES_TOPIC` block and before the `typeof envelope.msg.data !== 'string'` fallthrough check. Each block: parse the `envelope.msg.data` string as JSON, guard with the parse function, call the callback.

    Step 7: Add 4 public methods to the class. For `callIdentifyDevice`, first check if a method with that name already exists on the class; if it does, skip. The `callService` private method signature accepts a `toResult` callback of type `(values: RosbridgeEnvelope['values']) => RecordingCommandResult`. For mapping service calls, toResult extracts the specific response fields from `values` — note that rosbridge service responses for non-std_srvs services arrive differently. The `values` object on the envelope for these services will have fields from the srv response directly (outcome, detail, applied_revision). Access them as `(values as Record<string, unknown>)?.outcome` etc. Return type is `Promise<RecordingCommandResult>` with `success` set to `outcome === 'ok'` for SetAssignment/ResetMapping and `outcome === 'applied'` for ApplyMapping, `message` set to the outcome string.
  </action>
  <verify>
    <automated>cd "c:/Users/justi/Documents/# Rehab Robotics GUI Prototype__Here's a complete, single-file React + TypeScript app implementing the LabVIEW/rehab-robotics-studio" && npx vitest run src/data/RosbridgeDataSource.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/data/RosbridgeDataSource.test.ts` exits 0 with all 15+ test cases passing
    - `grep -c "onModelCatalog" rehab-robotics-studio/src/data/RosbridgeDataSource.ts` returns 1 or more (constructor param present)
    - `grep -c "callSetAssignment" rehab-robotics-studio/src/data/RosbridgeDataSource.ts` returns 1 or more (method present)
    - `grep -c "MAPPING_CATALOG_TOPIC\|/rehab/model/catalog" rehab-robotics-studio/src/data/RosbridgeDataSource.ts` returns 1 or more
    - `npx tsc --noEmit` exits 0 in rehab-robotics-studio/
    - Existing RosbridgeDataSource functionality is unchanged (no existing constructor param positions shifted)
  </acceptance_criteria>
  <done>All vitest tests green; TypeScript clean; all 5 subscriptions and 4 service methods confirmed in source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rosbridge WebSocket → handleMessage | All topic payloads arrive as untrusted JSON strings |
| Studio → ROS service | Service call args are formatted by client code; backend validates authoritatively |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-05 | Tampering | parseFleetRegistry / parseMappingCurrent | mitigate | Field-type guards return null on any type mismatch; callback never called with partial data |
| T-24-06 | Denial of Service | 5 new topic subscriptions | accept | Existing deduplication Map prevents double-subscription; topics are latched and low-frequency |
| T-24-07 | Spoofing | callSetAssignment device_id arg | accept | Backend validates device_id against its fleet registry authoritatively; client sends what store holds |
| T-24-08 | Tampering | callApplyMapping expectedRevision | mitigate | Backend validates expected_revision against current revision and rejects stale calls (outcome='stale_revision') |
| T-24-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages in this plan; existing vitest and zustand from package.json only |
</threat_model>

<verification>
Run after both tasks complete:

```bash
cd "c:/Users/justi/Documents/# Rehab Robotics GUI Prototype__Here's a complete, single-file React + TypeScript app implementing the LabVIEW/rehab-robotics-studio"
npx vitest run src/data/RosbridgeDataSource.test.ts
npx tsc --noEmit
```

Both exit 0.
</verification>

<success_criteria>
- All 15+ parse function and service method tests pass
- Constructor has 16 optional params; positions 1-11 unchanged
- 5 topic subscriptions present in socket.onopen
- 5 parse functions exported from the module
- 4 new public service call methods on RosbridgeDataSource
- TypeScript compiles clean
</success_criteria>

<output>
Create `.planning/phases/24-rosbridge-and-studio-mapping-workspace/24-02-SUMMARY.md` when done.
</output>
