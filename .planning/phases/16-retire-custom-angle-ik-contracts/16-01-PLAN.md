---
phase: 16-retire-custom-angle-ik-contracts
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/rehab_robotics_bridge/opensim_node.py
  - backend/rehab_robotics_bridge/opensim_adapter.py
  - backend/launch/rehab_robotics.launch.py
  - backend/launch/opensim_live_link.launch.py
  - backend/test/test_opensim_node.py
  - backend/test/test_opensim_launch.py
  - backend/test/test_opensim_adapter.py
  - docs/opensim-quaternion-live-link.md
autonomous: true
requirements:
  - IK-00
must_haves:
  truths:
    - "Default opensim_bridge does not publish product /opensim/joint_angle from relative-quat math."
    - "Status JSON no longer presents custom relative-quat degrees as the product OpenSim IK angle."
    - "relative_orientation_angle_deg is clearly non-product (debug/utility only) if retained."
    - "Deterministic backend tests assert the default product path never publishes custom joint angle."
  artifacts:
    - path: "backend/rehab_robotics_bridge/opensim_node.py"
      provides: "Custom joint_angle publisher demoted behind default-OFF flag; status without product joint_angle_deg"
    - path: "backend/test/test_opensim_node.py"
      provides: "Fail-closed tests for default non-publication of custom joint angle"
    - path: "docs/opensim-quaternion-live-link.md"
      provides: "Live-link doc updated: triad link is not IK; custom angle not product"
  key_links:
    - from: "backend/rehab_robotics_bridge/opensim_node.py"
      to: "/opensim/joint_angle"
      via: "publish_joint_angle_enabled parameter default false"
      pattern: "publish_joint_angle_enabled|joint_angle_topic"
    - from: "backend/rehab_robotics_bridge/opensim_node.py"
      to: "status_snapshot"
      via: "omitted or null product joint_angle_deg when debug publisher off"
      pattern: "joint_angle_deg"
---

<objective>
Retire the backend custom relative-quaternion `/opensim/joint_angle` path as product OpenSim IK.

Purpose: Enforce D-16-01 on the ROS side so the bridge stops advertising relative-quat math as live IK angles by default.
Output: Default-OFF (or removed) joint_angle publication, status without product angle, updated launch/tests/docs.
</objective>

<execution_context>
@C:/Users/justi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/justi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/16-retire-custom-angle-ik-contracts/16-CONTEXT.md
@.planning/research/SUMMARY.md
@backend/rehab_robotics_bridge/opensim_node.py
@backend/rehab_robotics_bridge/opensim_adapter.py
@backend/test/test_opensim_node.py
@docs/opensim-quaternion-live-link.md

<locked_decisions>
- D-16-01: Custom `/opensim/joint_angle` (relative quat math) is NOT OpenSim IK and must not be the default GUI knee/angle source (backend half: stop default product publish).
- Discretion (chosen): Keep publisher only behind `publish_joint_angle_enabled` launch/parameter flag, **default false**; do not use from product GUI. Quarantine `relative_orientation_angle_deg` as clearly named non-product debug utility if retained.
- Out of scope: OpenSim IK solver, `/opensim/joint_states` publisher implementation, calibration UI/services (Phases 17–19).
</locked_decisions>

<interfaces>
From backend/rehab_robotics_bridge/opensim_node.py (current — to change):
- Parameter defaults include `joint_angle_topic: "/opensim/joint_angle"`.
- `_publish_joint_angle_if_ready()` always publishes `std_msgs/Float64` when both sensors live.
- `status_snapshot()` includes `"joint_angle_deg": self._last_joint_angle_deg`.

From backend/rehab_robotics_bridge/opensim_adapter.py:
- `relative_orientation_angle_deg(master_xyzw, slave_xyzw) -> float` — pure helper used only by the custom path.

Contract after this plan:
- Default node constructs **no** active product joint-angle publisher (or publisher exists but never publishes unless `publish_joint_angle_enabled=true`).
- Default paired live IMUs do **not** publish Float64 on `/opensim/joint_angle`.
- Default `status_snapshot()` does not expose a product `joint_angle_deg` number (omit key or set null; if debug enabled, may include a clearly named debug field — prefer omit/null for product schema).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fail-closed backend tests for retired product joint angle</name>
  <files>backend/test/test_opensim_node.py, backend/test/test_opensim_launch.py, backend/test/test_opensim_adapter.py</files>
  <behavior>
    - Default-constructed OpenSimBridgeNode has no joint_angle publisher among default publishers, OR has publish_joint_angle_enabled=false and paired live IMUs leave joint_angle publisher message list empty.
    - Existing test_paired_live_imus_publish_zeroed_relative_joint_angle is rewritten: default path never publishes; optional test with publish_joint_angle_enabled=true may keep debug behavior if flag retained.
    - Default status_snapshot after paired live IMUs does not contain a numeric product joint_angle_deg (assert key absent or value is None).
    - Launch defaults declare publish_joint_angle_enabled false (or omit joint_angle_topic from required product args).
    - If relative_orientation_angle_deg retained, adapter tests still cover it as a named debug/utility function; docstring/name must not call it OpenSim IK.
  </behavior>
  <action>Update unittest expectations first (RED). Replace assertions that require publishers[1]==/opensim/joint_angle and default relative-angle publication. Add an explicit fail-closed test name such as test_default_path_never_publishes_custom_joint_angle_as_product. Keep hardware-free stubs. Do not add OpenSim IK solver tests.</action>
  <verify>
    <automated>$env:PYTHONPATH='backend'; python -m unittest backend.test.test_opensim_node backend.test.test_opensim_launch backend.test.test_opensim_adapter -v</automated>
  </verify>
  <done>Updated tests fail on current product joint_angle behavior and describe the retired default contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Demote custom joint_angle publisher and status field</name>
  <files>backend/rehab_robotics_bridge/opensim_node.py, backend/rehab_robotics_bridge/opensim_adapter.py, backend/launch/rehab_robotics.launch.py, backend/launch/opensim_live_link.launch.py, docs/opensim-quaternion-live-link.md</files>
  <behavior>
    - Same behaviors as Task 1 must pass GREEN.
  </behavior>
  <action>Implement per D-16-01 and chosen discretion: add `publish_joint_angle_enabled` (default false) to opensim_node parameter_defaults and both launch files; gate `_publish_joint_angle_if_ready` and publisher creation on that flag. When disabled, do not call relative_orientation_angle_deg on the product path and do not put a product `joint_angle_deg` in status_snapshot. If the helper remains in opensim_adapter.py, document it as non-product relative-orientation debug utility (not OpenSim IK). Update docs/opensim-quaternion-live-link.md to state custom Float64 joint_angle is not product IK, default OFF, and live link remains triad orientations only. Do not implement JointState publication or calibration services.</action>
  <verify>
    <automated>$env:PYTHONPATH='backend'; python -m unittest backend.test.test_opensim_node backend.test.test_opensim_launch backend.test.test_opensim_adapter -v</automated>
  </verify>
  <done>Default opensim_bridge no longer publishes or status-advertises custom relative-quat angle as product IK; optional debug flag defaults OFF; docs match.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IMU topics → opensim_bridge | Untrusted/sensor orientations enter the node |
| opensim_bridge → rosbridge/GUI | Status and any debug topics cross to the client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Spoofing | /opensim/joint_angle Float64 | mitigate | Default-disable publisher so GUI cannot treat spoofable custom degrees as calibrated IK |
| T-16-02 | Tampering | status_snapshot joint_angle_deg | mitigate | Omit/null product field when debug publisher off so health UI cannot present fake IK |
| T-16-03 | Information disclosure | relative_orientation_angle_deg | accept | Pure local math; no PII; may remain as debug utility |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
Run the three unittest modules above after both tasks. Confirm no new publisher of `/opensim/joint_states` was added in this plan (contracts-only later).
</verification>

<success_criteria>
- Default node: paired live IMUs → zero Float64 messages on joint_angle topic path
- Default status: no numeric product joint_angle_deg
- Launch default: publish_joint_angle_enabled is false
- Live-link doc explicitly rejects custom angle as OpenSim IK
</success_criteria>

<output>
Create `.planning/phases/16-retire-custom-angle-ik-contracts/16-01-SUMMARY.md` when done
</output>
