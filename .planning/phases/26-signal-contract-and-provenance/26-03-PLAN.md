---
phase: 26-signal-contract-and-provenance
plan: 03
type: execute
wave: 2
depends_on: [26-01]
files_modified:
  - backend/rehab_robotics_bridge/fleet_bridge_node.py
  - backend/test/test_fleet_bridge.py
  - backend/test/test_mapping_node.py
autonomous: true
requirements: [SIG-01, SIG-02, SIG-03, SIG-04, SIG-05]
must_haves:
  truths:
    - "Every verified per-MAC publication contains the additive canonical envelope while legacy fields remain compatible."
    - "Reconnect and applied-mapping changes create independent epochs and affect only subsequent samples."
    - "Capabilities come from an explicit verified declaration, never channel values."
  artifacts:
    - path: "backend/rehab_robotics_bridge/fleet_bridge_node.py"
      provides: "Canonical envelope emission at the verified per-MAC publication seam"
    - path: "backend/test/test_fleet_bridge.py"
      provides: "Synthetic baseline/draft/apply/reconnect publication proof"
  key_links:
    - from: "backend/rehab_robotics_bridge/fleet_bridge_node.py"
      to: "backend/rehab_robotics_bridge/signal_contract.py"
      via: "canonical builder before `publish_session_raw`"
      pattern: "build_canonical_signal_sample"
---

<objective>
Attach the canonical signal envelope to the verified backend fleet stream with honest timing, capability, reconnect, and applied-mapping provenance.

Purpose: Ensure every later consumer receives sample-owned meaning at ingestion rather than reconstructing it from mutable state.
Output: Additive backend publication and deterministic integration coverage.
</objective>

<execution_context>
@C:/Users/justi/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/justi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-signal-contract-and-provenance/26-CONTEXT.md
@.planning/phases/26-signal-contract-and-provenance/26-RESEARCH.md
@.planning/phases/26-signal-contract-and-provenance/26-PATTERNS.md
@.planning/phases/26-signal-contract-and-provenance/26-VALIDATION.md
@.planning/phases/26-signal-contract-and-provenance/26-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove explicit session metadata and additive publication</name>
  <files>backend/test/test_fleet_bridge.py</files>
  <read_first>
    - backend/test/test_fleet_bridge.py
    - backend/rehab_robotics_bridge/fleet_bridge_node.py
    - backend/rehab_robotics_bridge/signal_contract.py
    - backend/rehab_robotics_bridge/mapping_node.py
    - .planning/phases/26-signal-contract-and-provenance/26-VALIDATION.md
  </read_first>
  <behavior>
    - Verified route/session metadata carries explicit channel capabilities and optional magnetometer calibration; missing declarations fail closed rather than being inferred.
    - Legacy `oe_esp32.raw.v1` fields remain and nested `sample_contract.schema` is `rehab.signal_sample.1`.
    - Current bridge counter/time are labelled `bridge_session` and bridge monotonic, with acquisition time unavailable.
  </behavior>
  <action>Extend existing fleet stubs with RED assertions for D-01..D-08 and D-13..D-16. Choose the repository-compatible explicit declaration seam: validate a bounded `signal_contract` object on each `routes_json` row, keyed inherently by that row's verified `expected_device_id`, carrying capability booleans and optional validated magnetometer sensitivity/calibration. Do not infer capabilities from zero/nonzero channels or role. Assert missing/malformed declarations prevent canonical emission with a bounded logged reason while legacy publication behavior is preserved only where safe.</action>
  <acceptance_criteria>
    - Tests cover canonical full MAC, sequence/time origins, reconnect epoch, int16 raw preservation, capability declaration, quaternion invalidity, and additive legacy compatibility.
    - Fabricated capability from channel values is impossible in the fixture API.
    - Tests fail before production wiring and do not require ROS installation or hardware.
  </acceptance_criteria>
  <verify>
    <automated>$env:PYTHONPATH='backend'; python -m pytest backend/test/test_fleet_bridge.py -k "signal_contract or canonical" -q</automated>
  </verify>
  <done>Backend publication integration has a deterministic RED contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Snapshot authoritative applied mapping and independent epochs</name>
  <files>backend/rehab_robotics_bridge/fleet_bridge_node.py, backend/test/test_fleet_bridge.py, backend/test/test_mapping_node.py</files>
  <read_first>
    - backend/rehab_robotics_bridge/fleet_bridge_node.py
    - backend/test/test_fleet_bridge.py
    - backend/rehab_robotics_bridge/mapping_node.py
    - backend/test/test_mapping_node.py
    - backend/rehab_robotics_bridge/signal_contract.py
    - .planning/phases/26-signal-contract-and-provenance/26-CONTEXT.md
  </read_first>
  <behavior>
    - Mapping cache consumes `applied_assignments`, `applied_revision`, and `model_hash`, never draft `assignments`.
    - Mapping provenance epoch increments only when the authoritative applied snapshot changes.
    - Draft/save leaves sample label unchanged; Apply changes subsequent samples only; reconnect epoch changes independently.
  </behavior>
  <action>Add a strict bounded applied-snapshot cache at the fleet node's existing mapping-current integration seam (subscribe to `/rehab/mapping/current` String if no cache exists), and copy its exact full-MAC entry into each canonical sample per D-09..D-12. Treat absence as explicit unassigned. Increment a local mapping provenance epoch on a changed applied revision/assignment/model-hash signature, not on draft revision/save. Read reconnect generation from the verified registry row. Tests must retain an earlier serialized sample and prove it is not mutated after Apply or reconnect.</action>
  <acceptance_criteria>
    - Intentionally divergent `assignments` and `applied_assignments` never leak draft labels.
    - Revision zero, unassigned, exact segment/frame, model hash, mapping epoch, and reconnect epoch are serialized explicitly.
    - Baseline → draft/save → Apply → reconnect produces the expected immutable before/after provenance sequence.
  </acceptance_criteria>
  <verify>
    <automated>$env:PYTHONPATH='backend'; python -m pytest backend/test/test_mapping_node.py backend/test/test_fleet_bridge.py -k "applied or provenance or reconnect" -q</automated>
  </verify>
  <done>Applied provenance and reconnect provenance are explicit, immutable, independent, and draft-safe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Emit canonical envelopes at the verified per-MAC seam</name>
  <files>backend/rehab_robotics_bridge/fleet_bridge_node.py, backend/test/test_fleet_bridge.py</files>
  <read_first>
    - backend/rehab_robotics_bridge/fleet_bridge_node.py
    - backend/test/test_fleet_bridge.py
    - backend/rehab_robotics_bridge/signal_contract.py
    - backend/rehab_robotics_bridge/measurement_contract.py
    - .planning/phases/26-signal-contract-and-provenance/26-01-SUMMARY.md
  </read_first>
  <behavior>
    - `_publish_fleet_frame` builds canonical data only after verified session binding and strict metadata validation.
    - Rejected canonical updates emit a bounded reason/counter and do not publish a misleading canonical envelope.
    - Existing alias and typed IMU paths retain their established compatibility behavior.
  </behavior>
  <action>Call `build_canonical_signal_sample` from `_publish_fleet_frame` and add its deterministic serialization under `sample_contract` while retaining `topic_schema: oe_esp32.raw.v1` and existing legacy fields. Use bridge-session sequence and bridge monotonic time honestly; acquisition fields remain unavailable unless genuinely supplied. Preserve raw counts, use validated config/calibration, and validate topic identity before publish. Record bounded rejection telemetry/system log context without raw payload detail. Do not change firmware framing, charts, exports, or OpenSim behavior.</action>
  <acceptance_criteria>
    - Accepted per-MAC payloads carry complete canonical envelopes and unchanged legacy fields.
    - Missing required declarations, MAC disagreement, malformed values, draft injection, and fabricated capability are blocked with allowlisted reasons.
    - Full focused backend suite and legacy fleet tests pass.
  </acceptance_criteria>
  <verify>
    <automated>$env:PYTHONPATH='backend'; python -m pytest backend/test/test_signal_contract.py backend/test/test_measurement_contract.py backend/test/test_fleet_bridge.py -q</automated>
  </verify>
  <done>The authoritative backend publication boundary emits trustworthy sample-owned canonical meaning.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route/session config → fleet session | Operator/deployment declarations become capability and calibration facts |
| mapping topic → applied cache | Mutable ROS JSON attempts to become authoritative provenance |
| decoded stream → per-MAC publisher | Device bytes become browser-visible JSON |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-11 | Spoofing | session/topic binding | mitigate | Block canonical emission until verified self equals configured full MAC and derived topic. |
| T-26-12 | Tampering | decoded sample/config | mitigate | Validate finite/range/domain through the pure builder before nesting the envelope. |
| T-26-13 | Tampering / Repudiation | mapping-current callback | mitigate | Parse only bounded `applied_*` fields; signature and epoch ignore draft changes. |
| T-26-14 | Denial of Service / Injection | mapping labels/log detail | mitigate | Bound strings and log allowlisted reason codes only. |
| T-26-15 | Spoofing | capability declarations | mitigate | Require explicit per-verified-MAC route contract; reject missing declarations and never inspect values to infer support. |
| T-26-SC | Tampering | package installs | mitigate | No packages are installed. |
</threat_model>

<verification>
Run the Phase 26 quick backend contract/integration suite and full backend tests after Wave 2 completes.
</verification>

<success_criteria>
- Every accepted per-MAC sample carries trustworthy identity, timing, capability, units, and applied mapping.
- Draft changes and reconnect changes have independent, test-proven provenance effects.
- Rejections are bounded and cannot overwrite or masquerade as accepted canonical data.
</success_criteria>

<output>
Create `.planning/phases/26-signal-contract-and-provenance/26-03-SUMMARY.md` when done.
</output>
