---
phase: 26-signal-contract-and-provenance
plan: 04
type: execute
wave: 3
depends_on: [26-02, 26-03]
files_modified:
  - rehab-robotics-studio/src/state/mappingStore.ts
  - rehab-robotics-studio/src/state/mappingStore.test.ts
  - rehab-robotics-studio/src/data/RosbridgeDataSource.ts
  - rehab-robotics-studio/src/data/RosbridgeDataSource.test.ts
autonomous: true
requirements: [SIG-01, SIG-04, SIG-05]
must_haves:
  truths:
    - "Rosbridge accepts canonical samples only after strict topic-aware parsing and emits bounded rejection events separately."
    - "Draft and applied mapping remain separate browser concepts, and canonical samples never late-join either store."
  artifacts:
    - path: "rehab-robotics-studio/src/data/RosbridgeDataSource.ts"
      provides: "Dynamic per-MAC subscription and canonical accept/reject callbacks"
    - path: "rehab-robotics-studio/src/state/mappingStore.ts"
      provides: "Separately named immutable applied mapping snapshot"
  key_links:
    - from: "rehab-robotics-studio/src/data/RosbridgeDataSource.ts"
      to: "rehab-robotics-studio/src/data/signalContract.ts"
      via: "strict topic-aware parser"
      pattern: "parseCanonicalSignalSample"
---

<objective>
Route canonical per-MAC rosbridge payloads through strict parsing while preserving a separately named applied mapping snapshot.

Purpose: Prevent untrusted JSON, dynamic topic spoofing, and draft mapping state from entering canonical browser state.
Output: Safe dynamic transport callbacks, rejection metadata, and draft/applied store separation.
</objective>

<execution_context>
@C:/Users/justi/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/justi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-signal-contract-and-provenance/26-CONTEXT.md
@.planning/phases/26-signal-contract-and-provenance/26-UI-SPEC.md
@.planning/phases/26-signal-contract-and-provenance/26-PATTERNS.md
@.planning/phases/26-signal-contract-and-provenance/26-02-SUMMARY.md
@.planning/phases/26-signal-contract-and-provenance/26-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve applied mapping separately from drafts</name>
  <files>rehab-robotics-studio/src/state/mappingStore.ts, rehab-robotics-studio/src/state/mappingStore.test.ts, rehab-robotics-studio/src/data/RosbridgeDataSource.ts</files>
  <read_first>
    - rehab-robotics-studio/src/state/mappingStore.ts
    - rehab-robotics-studio/src/state/mappingStore.test.ts
    - rehab-robotics-studio/src/data/RosbridgeDataSource.ts
    - backend/rehab_robotics_bridge/mapping_node.py
    - .planning/phases/26-signal-contract-and-provenance/26-CONTEXT.md
  </read_first>
  <behavior>
    - `parseMappingCurrent` validates `applied_assignments` independently of draft `assignments`.
    - Store exposes an immutable applied snapshot without changing editor-row draft semantics.
    - Malformed/overlong applied labels are rejected/no-op and cannot partially update state.
  </behavior>
  <action>Extend `MappingCurrentSnapshot`, `parseMappingCurrent`, and mapping store state with separately named `appliedAssignments`, `appliedRevision`, and bounded `modelHash` per D-09..D-12. Keep `backendSegment`/`backendFrame` editor behavior tied to drafts. Add intentionally divergent draft/applied tests, revision-zero/unassigned cases, and atomic malformed-payload no-op assertions. This snapshot is diagnostic/editor state only; canonical samples already carry their own applied labels and must not read this store during render.</action>
  <acceptance_criteria>
    - Draft edits and Save can change editor rows without changing the applied snapshot.
    - Apply payload updates the separate applied snapshot atomically.
    - Unbounded segment/frame/hash and malformed assignments cannot enter state.
  </acceptance_criteria>
  <verify>
    <automated>Push-Location rehab-robotics-studio; try { & npm exec -- tsx --test src/state/mappingStore.test.ts src/data/RosbridgeDataSource.test.ts; if ($LASTEXITCODE -ne 0) { throw 'mapping and rosbridge tests failed' } } finally { Pop-Location }</automated>
  </verify>
  <done>The browser represents draft and applied mapping as distinct, bounded state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Subscribe to canonical per-MAC topics and emit accept/reject events</name>
  <files>rehab-robotics-studio/src/data/RosbridgeDataSource.ts, rehab-robotics-studio/src/data/RosbridgeDataSource.test.ts</files>
  <read_first>
    - rehab-robotics-studio/src/data/RosbridgeDataSource.ts
    - rehab-robotics-studio/src/data/RosbridgeDataSource.test.ts
    - rehab-robotics-studio/src/data/signalContract.ts
    - rehab-robotics-studio/src/state/mappingStore.ts
    - .planning/phases/26-signal-contract-and-provenance/26-UI-SPEC.md
  </read_first>
  <behavior>
    - Fleet registry rows create deduplicated `/esp/raw/mac_<12hex>` subscriptions for canonical samples.
    - Accepted sample callback and rejected update callback are distinct; a rejection cannot emit/replace an accepted sample.
    - Identical repeated rejection codes for one source are deduplicated for alert/log behavior while counts remain accurate.
  </behavior>
  <action>Add narrow canonical accept/rejection callback registration to `RosbridgeDataSource`. Derive expected topic tokens only from strictly parsed fleet full-MAC rows; deduplicate dynamic subscriptions and remove/replace them safely on reconnect generations. Parse nested `sample_contract` with `parseCanonicalSignalSample(raw, expectedTopicToken)` before dispatch. On rejection emit only full MAC when safely derivable, allowlisted code, bounded timestamp, and count; never emit raw JSON, exception text, `numeric()` defaults, `performance.now()` acquisition fallback, role aliases, or an identity quaternion. Leave the compatibility `Frame` path unchanged.</action>
  <acceptance_criteria>
    - Topic/payload MAC mismatch, malformed JSON, non-finite values, draft fields, and fabricated capabilities never reach accepted callbacks.
    - Accepted payloads retain exact per-sample applied mapping and timing facts.
    - Rejected input leaves the last accepted callback state untouched and produces bounded, deduplicable metadata.
    - Existing rosbridge tests remain green.
  </acceptance_criteria>
  <verify>
    <automated>Push-Location rehab-robotics-studio; try { & npm exec -- tsx --test src/data/RosbridgeDataSource.test.ts src/data/signalContract.test.ts src/state/mappingStore.test.ts; if ($LASTEXITCODE -ne 0) { throw 'canonical ingress tests failed' }; & npm run typecheck; if ($LASTEXITCODE -ne 0) { throw 'frontend typecheck failed' } } finally { Pop-Location }</automated>
  </verify>
  <done>Only strictly validated canonical samples cross rosbridge into browser application callbacks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fleet registry → dynamic subscriptions | Untrusted registry data influences subscribed topic names |
| websocket message → app callbacks | Untrusted JSON becomes persistent browser state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-16 | Spoofing | dynamic topic dispatch | mitigate | Derive only canonical topics from validated full MACs and recheck payload agreement. |
| T-26-17 | Tampering | canonical parser dispatch | mitigate | Block malformed/non-finite/domain-invalid payloads before callbacks. |
| T-26-18 | Tampering / Repudiation | mapping store | mitigate | Parse applied assignments separately and never late-join samples to mutable state. |
| T-26-19 | Denial of Service / Injection | rejection and labels | mitigate | Bound strings, allowlist codes, deduplicate announcements, omit raw detail. |
| T-26-20 | Spoofing | capability fields | mitigate | Delegate only strict parser-accepted declarations; no value-based inference. |
| T-26-SC | Tampering | package installs | mitigate | No installs. |
</threat_model>

<verification>
Run focused rosbridge/mapping/contract tests and frontend typecheck.
</verification>

<success_criteria>
- Dynamic subscriptions are identity-safe and deduplicated.
- Rejected data never replaces or masquerades as an accepted canonical sample.
- Draft and applied mapping are distinct and bounded throughout browser state.
</success_criteria>

<output>
Create `.planning/phases/26-signal-contract-and-provenance/26-04-SUMMARY.md` when done.
</output>
