---
phase: 26-signal-contract-and-provenance
plan: 02
type: execute
wave: 2
depends_on: [26-01]
files_modified:
  - rehab-robotics-studio/src/types/signals.ts
  - rehab-robotics-studio/src/data/measurementContract.ts
  - rehab-robotics-studio/src/data/measurementContract.test.ts
  - rehab-robotics-studio/src/data/signalContract.ts
  - rehab-robotics-studio/src/data/signalContract.test.ts
autonomous: true
requirements: [SIG-01, SIG-02, SIG-03, SIG-04, SIG-05]
must_haves:
  truths:
    - "Python and TypeScript accept and reject the same canonical samples with identical reason codes and conversions."
    - "Browser code receives an immutable canonical type without using legacy Frame defaults."
  artifacts:
    - path: "rehab-robotics-studio/src/data/signalContract.ts"
      provides: "Strict unknown-to-canonical browser parser"
      exports: ["parseCanonicalSignalSample"]
    - path: "rehab-robotics-studio/src/types/signals.ts"
      provides: "Readonly canonical signal interfaces alongside unchanged legacy Frame"
    - path: "rehab-robotics-studio/src/data/signalContract.test.ts"
      provides: "Cross-language fixture parity"
  key_links:
    - from: "rehab-robotics-studio/src/data/signalContract.test.ts"
      to: "backend/test/fixtures/signal_contract_cases.json"
      via: "direct shared fixture load"
      pattern: "signal_contract_cases\\.json"
---

<objective>
Implement the TypeScript half of the canonical signal contract with exact Python parity.

Purpose: Make untrusted rosbridge JSON cross a strict, typed browser boundary before any state or UI consumer sees it.
Output: Canonical types, deterministic conversions, strict parser, and cross-language tests.
</objective>

<execution_context>
@C:/Users/justi/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/justi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/26-signal-contract-and-provenance/26-CONTEXT.md
@.planning/phases/26-signal-contract-and-provenance/26-RESEARCH.md
@.planning/phases/26-signal-contract-and-provenance/26-PATTERNS.md
@.planning/phases/26-signal-contract-and-provenance/26-VALIDATION.md
@.planning/phases/26-signal-contract-and-provenance/26-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define readonly browser contracts and parity tests</name>
  <files>rehab-robotics-studio/src/types/signals.ts, rehab-robotics-studio/src/data/signalContract.test.ts</files>
  <read_first>
    - rehab-robotics-studio/src/types/signals.ts
    - rehab-robotics-studio/src/data/signalContract.test.ts (new file)
    - rehab-robotics-studio/src/data/measurementContract.test.ts
    - backend/test/fixtures/signal_contract_cases.json
    - backend/rehab_robotics_bridge/signal_contract.py
    - .planning/phases/26-signal-contract-and-provenance/26-UI-SPEC.md
  </read_first>
  <behavior>
    - Canonical interfaces express exact schema literals, nullable acquisition facts, independent epochs, raw tuples, capabilities, validity, conversions, and applied snapshots.
    - Shared cases assert exact accepted shapes and exact rejection codes.
  </behavior>
  <action>Add readonly `CanonicalSignalSample`, timing/epoch, channel, conversion, applied mapping, rejection, and availability reason types without changing legacy `Frame`/`ImuData`. Build `node:test` cases directly from the Plan 01 fixture, covering D-01..D-16. Do not use Vitest and do not duplicate fixture values in TypeScript.</action>
  <acceptance_criteria>
    - Legacy signal types remain source compatible.
    - New types cannot represent a label without its applied revision/epoch or acquisition time without its clock/origin.
    - Test discovery runs and fails only because parser/conversion parity is not complete.
  </acceptance_criteria>
  <verify>
    <automated>Push-Location rehab-robotics-studio; npm exec -- tsx --test src/data/signalContract.test.ts; $red=$LASTEXITCODE; Pop-Location; if ($red -eq 0) { throw 'Expected RED contract tests before parser implementation' }</automated>
  </verify>
  <done>The browser contract surface and cross-language RED tests are committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mirror deterministic measurement validation</name>
  <files>rehab-robotics-studio/src/data/measurementContract.ts, rehab-robotics-studio/src/data/measurementContract.test.ts</files>
  <read_first>
    - rehab-robotics-studio/src/data/measurementContract.ts
    - rehab-robotics-studio/src/data/measurementContract.test.ts
    - backend/rehab_robotics_bridge/measurement_contract.py
    - backend/test/fixtures/signal_contract_cases.json
    - .planning/phases/26-signal-contract-and-provenance/26-01-SUMMARY.md
  </read_first>
  <behavior>
    - Validation order, accepted config, unavailability reasons, units, and numeric conversions match Python.
    - Sensitivity alone never authorizes magnetometer µT.
  </behavior>
  <action>Extend the existing discriminated `ValidateResult` pattern to mirror Plan 01 names, literal tables, bounds, calibration provenance validation, and formulas per D-05..D-08. Preserve exact raw counts and return explicit unavailable results rather than zero/NaN. Keep conversion in shared contract code; no component formulas.</action>
  <acceptance_criteria>
    - Existing measurement tests remain green.
    - Shared fixture values match Python within the fixture tolerance.
    - Invalid config and missing/invalid calibration retain raw counts and suppress SI with the exact reason code.
  </acceptance_criteria>
  <verify>
    <automated>Push-Location rehab-robotics-studio; npm exec -- tsx --test src/data/measurementContract.test.ts; Pop-Location</automated>
  </verify>
  <done>TypeScript measurement validation and conversions are deterministic and Python-compatible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement the strict canonical parser</name>
  <files>rehab-robotics-studio/src/data/signalContract.ts, rehab-robotics-studio/src/data/signalContract.test.ts</files>
  <read_first>
    - rehab-robotics-studio/src/data/signalContract.ts (new file)
    - rehab-robotics-studio/src/data/signalContract.test.ts
    - rehab-robotics-studio/src/data/RosbridgeDataSource.ts
    - rehab-robotics-studio/src/data/measurementContract.ts
    - rehab-robotics-studio/src/types/signals.ts
    - backend/test/fixtures/signal_contract_cases.json
    - .planning/phases/26-signal-contract-and-provenance/26-PATTERNS.md
  </read_first>
  <behavior>
    - `parseCanonicalSignalSample(raw, expectedTopicToken)` returns a copied canonical value or bounded rejection.
    - Guard order and exact codes match Python for every shared fixture.
    - No `numeric()` default, browser receipt-time fallback, late mapping join, inferred capability, or quaternion normalization occurs.
  </behavior>
  <action>Implement ordered `unknown` guards for schema, canonical MAC/topic agreement, timing, epochs, int16 raw values, explicit capabilities and validity, validated conversions, and bounded applied mapping per D-01..D-16. Reject unknown availability codes and unbounded strings. Copy nested tuples/objects so future store mutation cannot relabel buffered samples. Keep legacy rosbridge parsing untouched in this plan.</action>
  <acceptance_criteria>
    - Python and TypeScript return identical accept/reject results and codes for the shared fixture.
    - Valid acquisition absence stays `null`/Unavailable while bridge monotonic time remains separately labelled.
    - No accepted object references mutable raw input and no failure contains raw JSON or exception text.
  </acceptance_criteria>
  <verify>
    <automated>Push-Location rehab-robotics-studio; npm exec -- tsx --test src/data/signalContract.test.ts src/data/measurementContract.test.ts; npm run typecheck; Pop-Location</automated>
  </verify>
  <done>The strict browser canonical boundary passes all shared SIG-01..05 cases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rosbridge JSON → TypeScript parser | Fully untrusted JSON becomes typed application data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-06 | Spoofing | `parseCanonicalSignalSample` | mitigate | Require exact payload MAC/subscribed topic agreement. |
| T-26-07 | Tampering | numeric and epoch guards | mitigate | Reject non-finite, fractional, negative, and out-of-domain values before copying. |
| T-26-08 | Tampering / Repudiation | applied mapping parse | mitigate | Accept only applied snapshot fields; reject draft-shaped injection and keep immutable copies. |
| T-26-09 | Denial of Service / Injection | strings/rejections | mitigate | Bound labels/hashes and map failures to allowlisted codes without backend detail. |
| T-26-10 | Spoofing | capability/validity | mitigate | Require explicit booleans and allowlisted validity; never infer or fabricate channels. |
| T-26-SC | Tampering | package installs | mitigate | Use existing TypeScript/tsx stack; no installs. |
</threat_model>

<verification>
Run both Python and TypeScript against the same fixture, then run frontend typecheck.
</verification>

<success_criteria>
- Cross-language contract parity is exact for acceptance, reason codes, conversion, and provenance.
- Canonical browser types are immutable and distinct from legacy `Frame`.
- All untrusted strings and values are bounded before entering application state.
</success_criteria>

<output>
Create `.planning/phases/26-signal-contract-and-provenance/26-02-SUMMARY.md` when done.
</output>
