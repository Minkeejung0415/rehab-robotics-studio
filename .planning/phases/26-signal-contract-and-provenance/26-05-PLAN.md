---
phase: 26-signal-contract-and-provenance
plan: 05
type: execute
wave: 4
depends_on: [26-04]
files_modified:
  - rehab-robotics-studio/src/data/DataSource.ts
  - rehab-robotics-studio/src/data/appDataSource.ts
  - rehab-robotics-studio/src/data/signalBus.ts
  - rehab-robotics-studio/src/data/signalBus.test.ts
autonomous: true
requirements: [SIG-01, SIG-02, SIG-03, SIG-04, SIG-05]
must_haves:
  truths:
    - "Current-session latest accepted samples are keyed by full MAC and published to React at bounded render rate."
    - "A rejection preserves the last accepted sample while recording bounded persistent rejection metadata."
    - "No mock canonical source is fabricated in fallback mode."
  artifacts:
    - path: "rehab-robotics-studio/src/data/signalBus.ts"
      provides: "Immutable latest-by-MAC canonical snapshot and rejection state"
    - path: "rehab-robotics-studio/src/data/signalBus.test.ts"
      provides: "High-rate ingestion / bounded publication and rejection retention tests"
  key_links:
    - from: "rehab-robotics-studio/src/data/appDataSource.ts"
      to: "rehab-robotics-studio/src/data/signalBus.ts"
      via: "canonical accept/reject subscriptions"
      pattern: "subscribeCanonical"
---

<objective>
Carry accepted canonical samples and bounded rejection state through the existing high-rate/low-render-rate data boundary.

Purpose: Give the UI immutable current-session truth without coupling inspector presentation to acquisition, recording, mapping, or OpenSim paths.
Output: Data-source contracts and a tested latest-by-MAC signal snapshot.
</objective>

<execution_context>
@C:/Users/justi/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/justi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-signal-contract-and-provenance/26-CONTEXT.md
@.planning/phases/26-signal-contract-and-provenance/26-UI-SPEC.md
@.planning/phases/26-signal-contract-and-provenance/26-PATTERNS.md
@.planning/phases/26-signal-contract-and-provenance/26-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add narrow canonical data-source subscriptions</name>
  <files>rehab-robotics-studio/src/data/DataSource.ts, rehab-robotics-studio/src/data/appDataSource.ts, rehab-robotics-studio/src/data/signalBus.test.ts</files>
  <read_first>
    - rehab-robotics-studio/src/data/DataSource.ts
    - rehab-robotics-studio/src/data/appDataSource.ts
    - rehab-robotics-studio/src/data/signalBus.test.ts (new file)
    - rehab-robotics-studio/src/data/RosbridgeDataSource.ts
    - rehab-robotics-studio/src/types/signals.ts
    - rehab-robotics-studio/src/data/signalBus.ts
  </read_first>
  <behavior>
    - DataSource exposes separate canonical accepted and rejected subscriptions alongside unchanged `subscribe(Frame)`.
    - `appDataSource` forwards rosbridge canonical events and returns cleanup functions.
    - Mock mode emits no invented canonical devices or samples.
  </behavior>
  <action>Define narrow callback/unsubscribe contracts for `CanonicalSignalSample` and bounded rejection metadata, then wire the Plan 04 rosbridge callbacks through `appDataSource`. Preserve legacy Frame/OpenSim APIs. Add test doubles proving cleanup, callback isolation, and explicit empty mock behavior. Do not subscribe to mapping drafts and do not alter acquisition lifecycle.</action>
  <acceptance_criteria>
    - Existing Frame subscribers behave unchanged.
    - Canonical accept and reject streams cannot be confused at the type or runtime boundary.
    - Mock fallback has an empty canonical stream, satisfying the UI empty-state contract.
  </acceptance_criteria>
  <verify>
    <automated>Push-Location rehab-robotics-studio; npm exec -- tsx --test src/data/signalBus.test.ts src/data/RosbridgeDataSource.test.ts; Pop-Location</automated>
  </verify>
  <done>The application data facade exposes canonical events without contaminating legacy or mock paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publish immutable latest accepted samples and bounded rejections</name>
  <files>rehab-robotics-studio/src/data/signalBus.ts, rehab-robotics-studio/src/data/signalBus.test.ts</files>
  <read_first>
    - rehab-robotics-studio/src/data/signalBus.ts
    - rehab-robotics-studio/src/data/signalBus.test.ts (new file)
    - rehab-robotics-studio/src/hooks/useSignals.ts
    - rehab-robotics-studio/src/data/appDataSource.ts
    - .planning/phases/26-signal-contract-and-provenance/26-UI-SPEC.md
  </read_first>
  <behavior>
    - Latest accepted canonical samples are keyed by exact full MAC and copied into each published `SignalSnapshot`.
    - High-rate accepts update internal latest state but React listeners publish at the existing ~30 fps cadence.
    - Rejection increments bounded per-source/panel metadata and preserves the prior accepted object.
    - Reconnect sequence reset at a higher reconnect epoch is accepted and shown as the new latest sample.
  </behavior>
  <action>Extend `SignalSnapshot` and `SignalBus` with current-browser-session `canonicalSamplesByMac`, accepted count, bounded rejected count, and latest rejection metadata. Use copy-on-publish maps/objects so `useSyncExternalStore` sees immutable references. Ingest canonical events independently of graph execution and legacy Frame history; a rejected update must not replace the last accepted sample. Deduplicate alert signatures while still maintaining bounded counts. Do not add waveform buffers, cross-source unit state, offline fleet rows, recording, or OpenSim coupling.</action>
  <acceptance_criteria>
    - Multiple MACs remain independent and fully named.
    - Rejection after acceptance leaves values/provenance intact and supplies last-update-rejected metadata.
    - Rejection before any acceptance creates only rejection metadata, not a source value card.
    - Maps and nested sample objects are immutable to consumers and render publication remains bounded.
  </acceptance_criteria>
  <verify>
    <automated>Push-Location rehab-robotics-studio; npm exec -- tsx --test src/data/signalBus.test.ts src/data/signalContract.test.ts; npm run typecheck; Pop-Location</automated>
  </verify>
  <done>The React snapshot contains trustworthy current-session latest samples and persistent bounded rejection truth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validated transport callbacks → persistent signal state | Accepted/rejected events become long-lived UI state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-21 | Spoofing | latest-by-MAC map | mitigate | Key solely by parser-validated canonical full MAC. |
| T-26-22 | Tampering | sample/rejection ingestion | mitigate | Copy immutable accepted values and allowlisted rejection metadata; never accept raw JSON. |
| T-26-23 | Tampering / Repudiation | provenance retention | mitigate | Store sample-owned applied mapping; never read mapping drafts/current state during ingest. |
| T-26-24 | Denial of Service | rejection/listener updates | mitigate | Bound counters/metadata, deduplicate announcements, and retain the ~30 fps publication gate. |
| T-26-25 | Spoofing | mock/capability state | mitigate | Emit no mock canonical source and never synthesize capabilities. |
| T-26-SC | Tampering | package installs | mitigate | No installs. |
</threat_model>

<verification>
Run signal bus, rosbridge, and canonical tests plus typecheck; after Wave 4 run the full frontend suite.
</verification>

<success_criteria>
- UI consumers receive immutable full-MAC-keyed current-session canonical truth.
- Rejections are persistent but bounded and cannot erase accepted data.
- Display publication stays decoupled from acquisition rate and authoritative paths.
</success_criteria>

<output>
Create `.planning/phases/26-signal-contract-and-provenance/26-05-SUMMARY.md` when done.
</output>
