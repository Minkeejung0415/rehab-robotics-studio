---
phase: 26-signal-contract-and-provenance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/test/fixtures/signal_contract_cases.json
  - backend/test/test_signal_contract.py
  - backend/rehab_robotics_bridge/signal_contract.py
  - backend/rehab_robotics_bridge/measurement_contract.py
  - backend/test/test_measurement_contract.py
autonomous: true
requirements: [SIG-01, SIG-02, SIG-03, SIG-04, SIG-05]
must_haves:
  truths:
    - "Required identity, topic, timing, epoch, raw-channel, capability, and applied-provenance fields fail closed with stable bounded reason codes."
    - "Raw signed counts remain exact while SI values exist only behind validated sensitivity and calibration facts."
    - "Quaternion capability absence and per-sample invalidity are distinct and neither path fabricates orientation."
  artifacts:
    - path: "backend/rehab_robotics_bridge/signal_contract.py"
      provides: "Pure immutable canonical sample builder and validator"
      exports: ["CanonicalSignalSample", "build_canonical_signal_sample"]
    - path: "backend/test/fixtures/signal_contract_cases.json"
      provides: "Language-neutral SIG-01..05 acceptance, rejection, conversion, and provenance cases"
    - path: "backend/rehab_robotics_bridge/measurement_contract.py"
      provides: "Validated accel, gyro, and magnetometer conversion contract"
  key_links:
    - from: "backend/test/test_signal_contract.py"
      to: "backend/test/fixtures/signal_contract_cases.json"
      via: "table-driven fixture loading"
      pattern: "signal_contract_cases\\.json"
    - from: "backend/rehab_robotics_bridge/signal_contract.py"
      to: "backend/rehab_robotics_bridge/measurement_contract.py"
      via: "validated conversion snapshot"
      pattern: "measurement_contract"
---

<objective>
Define the fail-closed, immutable Python source of truth and shared fixture for every canonical signal sample.

Purpose: Establish the irreversible wire meaning before backend or browser integration so plausible defaults cannot masquerade as measured data.
Output: A ROS-free canonical validator, complete cross-language fixture, and validated measurement conversions.
</objective>

<execution_context>
@C:/Users/justi/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/justi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/26-signal-contract-and-provenance/26-CONTEXT.md
@.planning/phases/26-signal-contract-and-provenance/26-RESEARCH.md
@.planning/phases/26-signal-contract-and-provenance/26-PATTERNS.md
@.planning/phases/26-signal-contract-and-provenance/26-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the canonical fixture and rejection taxonomy</name>
  <files>backend/test/fixtures/signal_contract_cases.json, backend/test/test_signal_contract.py, backend/rehab_robotics_bridge/signal_contract.py</files>
  <read_first>
    - backend/test/fixtures/signal_contract_cases.json (new file)
    - backend/test/test_signal_contract.py (new file)
    - backend/rehab_robotics_bridge/signal_contract.py (new file)
    - backend/test/fixtures/measurement_contract_cases.json
    - backend/test/test_measurement_contract.py
    - backend/rehab_robotics_bridge/opensim_adapter.py
    - .planning/phases/26-signal-contract-and-provenance/26-CONTEXT.md
    - .planning/phases/26-signal-contract-and-provenance/26-VALIDATION.md
  </read_first>
  <behavior>
    - Exact canonical full MAC and matching topic token accept; malformed/mismatched identity rejects.
    - Sequence origin, nullable acquisition time/clock, bridge monotonic time, reconnect epoch, and mapping epoch have bounded integer/domain validation.
    - Int16 boundaries round-trip; fractional, out-of-range, NaN-equivalent, and missing required raw fields reject.
    - Draft/applied divergence proves only applied revision/segment/frame enters a sample and earlier samples remain immutable.
    - Quaternion capability-absent, stale, missing, malformed, non-finite, zero-norm, norm-out-of-range, and available cases stay distinct.
  </behavior>
  <action>Create the shared JSON fixture, ROS-free Python tests, and the public frozen type/builder interface needed for behavioral RED tests. The initial builder raises exactly `NotImplementedError('canonical_validation_unimplemented')` after accepting its documented arguments, so focused identity/time cases execute the target API and fail for the one intended missing behavior rather than import or collection errors. Normalize the 16 unnumbered locked context bullets as trace IDs D-01 through D-16 in their existing order; cover D-01..D-04 identity/timing, D-05..D-08 raw/SI, D-09..D-12 applied mapping, and D-13..D-16 quaternion policy. Use only allowlisted codes (`schema_invalid`, `device_id_invalid`, `topic_device_mismatch`, timing/epoch/raw-field codes, and the UI-SPEC availability codes); expected detail must be bounded and fixtures must include overlong labels/model hashes/error detail to prove rejection. Do not add waveform cases.</action>
  <acceptance_criteria>
    - The fixture contains accepted and rejected partitions for every SIG-01..05 requirement and every D-01..D-16 decision family.
    - Tests assert exact reason codes, canonical normalized results, immutable applied snapshots, int16 edge values, and no quaternion identity fallback.
    - Focused identity/time execution reaches the builder and fails with the exact `canonical_validation_unimplemented` marker, establishing the intended behavioral RED state.
  </acceptance_criteria>
  <verify>
    <automated>$env:PYTHONPATH='backend'; $redOutput = (& python -m pytest backend/test/test_signal_contract.py -k identity_time -q 2>&1 | Out-String); $redCode = $LASTEXITCODE; if ($redCode -ne 1 -or $redOutput -notmatch 'canonical_validation_unimplemented') { throw "Expected focused behavioral RED failure; exit=$redCode output=$redOutput" }</automated>
  </verify>
  <done>The shared contract fixture is exhaustive, language-neutral, and fails for missing production behavior rather than fixture/test errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement immutable canonical validation</name>
  <files>backend/rehab_robotics_bridge/signal_contract.py, backend/test/test_signal_contract.py</files>
  <read_first>
    - backend/rehab_robotics_bridge/signal_contract.py (new file; inspect after Task 1 scaffold)
    - backend/test/test_signal_contract.py
    - backend/rehab_robotics_bridge/measurement_contract.py
    - backend/rehab_robotics_bridge/esp32_bridge_node.py
    - backend/rehab_robotics_bridge/opensim_adapter.py
    - backend/test/fixtures/signal_contract_cases.json
    - .planning/phases/26-signal-contract-and-provenance/26-RESEARCH.md
  </read_first>
  <behavior>
    - `build_canonical_signal_sample` returns one immutable `rehab.signal_sample.1` object whose nested values are copied.
    - Required envelope failures reject; channel-level capability/validity failures remain explicit unavailable states.
    - Full MAC/topic agreement, bounded labels, finite values, non-negative epochs, int16 counts, and quaternion norm policy are enforced.
  </behavior>
  <action>Implement the pure-stdlib frozen canonical model and builder against the fixture. Use established `normalize_device_id`/`device_topic_token` helpers and align quaternion finite/near-zero/norm tolerance with `opensim_adapter.py` while preserving received component order and values. Represent honest timing with `sequence_origin` (`device` or `bridge_session`), nullable acquisition time/clock, and separately named bridge monotonic time per D-02/D-04. Copy only the supplied authoritative applied snapshot per D-09..D-12; reject draft-shaped provenance fields and bound segment/frame/model-hash strings. Capability must be explicit rather than inferred from nonzero channels per D-03/D-13. Never normalize or synthesize a quaternion.</action>
  <acceptance_criteria>
    - Accepted samples serialize deterministically with schema, canonical identity, timing origins, epochs, raw values, capabilities, validity, conversion metadata, and applied mapping.
    - Topic/MAC disagreement, malformed/non-finite values, negative/fractional epochs, draft provenance fields, overlong strings, and unsupported reason detail fail closed.
    - Capability absence and transient quaternion invalidity produce distinct machine-readable states, with no `[1,0,0,0]` fallback.
  </acceptance_criteria>
  <verify>
    <automated>$env:PYTHONPATH='backend'; python -m pytest backend/test/test_signal_contract.py -k "identity_time or quaternion or applied" -q</automated>
  </verify>
  <done>The canonical Python contract passes identity, timing, applied-provenance, boundary, and quaternion partitions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend deterministic SI conversion and magnetometer provenance</name>
  <files>backend/rehab_robotics_bridge/measurement_contract.py, backend/test/test_measurement_contract.py, backend/test/fixtures/signal_contract_cases.json, backend/test/test_signal_contract.py</files>
  <read_first>
    - backend/rehab_robotics_bridge/measurement_contract.py
    - backend/test/test_measurement_contract.py
    - backend/test/fixtures/measurement_contract_cases.json
    - backend/test/fixtures/signal_contract_cases.json
    - backend/test/test_signal_contract.py
    - .planning/REQUIREMENTS.md
    - .planning/phases/26-signal-contract-and-provenance/26-CONTEXT.md
  </read_first>
  <behavior>
    - Accel/gyro SI converts deterministically only for validated declared range/sensitivity.
    - Magnetometer sensitivity without a valid bounded calibration provenance artifact yields raw counts plus `calibration_missing`/`calibration_invalid`, never µT.
    - Valid magnetometer sensitivity and calibration provenance yields deterministic mx/my/mz µT while retaining original raw counts.
  </behavior>
  <action>Extend `MeasurementConfig`, validation, deterministic serialization, and conversion helpers per D-05..D-08. Keep existing accel/gyro fixture behavior backward compatible. Model magnetometer sensitivity and calibration provenance as separate validated facts including sensor model, axis convention, calibration ID/hash, and finite coefficients required by the chosen representation. Do not authorize µT from the nominal `0.15` sensitivity alone. Reuse the canonical builder to expose explicit SI availability and units (`counts`, `m/s^2`, `rad/s`, `µT`) without UI formulas.</action>
  <acceptance_criteria>
    - Existing accel/gyro cases remain green and int16 raw values are unchanged.
    - Invalid accel/gyro config exposes raw-only availability; valid config matches current formulas.
    - Magnetometer raw remains available when capable, while µT exists only when sensitivity and calibration provenance both validate.
    - No test or production path labels an unavailable value with `µT`.
  </acceptance_criteria>
  <verify>
    <automated>$env:PYTHONPATH='backend'; python -m pytest backend/test/test_measurement_contract.py backend/test/test_signal_contract.py -q</automated>
  </verify>
  <done>The backend contract delivers lossless raw counts and fail-closed deterministic SI availability for accel, gyro, and magnetometer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device/session input → canonical builder | Binary-decoded and configuration-derived values become trusted sample facts |
| canonical builder → JSON consumer | Validated values and bounded reason codes cross into transport |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Spoofing | MAC/topic identity | mitigate | Block acceptance unless normalized full MAC exactly matches canonical topic token. |
| T-26-02 | Tampering | raw/timing/quaternion fields | mitigate | Block malformed, non-finite, fractional/out-of-range counts and epochs with ordered strict guards. |
| T-26-03 | Tampering / Repudiation | applied mapping snapshot | mitigate | Reject draft/current assignment keys and copy only bounded applied revision/segment/frame/model hash. |
| T-26-04 | Denial of Service / Injection | labels and error detail | mitigate | Enforce maximum lengths and emit only allowlisted reason codes; never interpolate untrusted detail. |
| T-26-05 | Spoofing | capability and SI availability | mitigate | Require explicit capability and validated sensitivity/calibration; never infer from channel values. |
| T-26-SC | Tampering | package installs | mitigate | No package installation is permitted by this plan. |
</threat_model>

<verification>
Run the Plan 01 focused suite, then the Phase 26 quick backend subset from `26-VALIDATION.md`.
</verification>

<success_criteria>
- Shared fixture covers SIG-01..05 and D-01..D-16 with exact cross-language-ready expectations.
- Pure Python validation is fail closed, immutable, deterministic, bounded, and ROS-independent.
- Raw values remain authoritative; µT and quaternion availability cannot be fabricated.
</success_criteria>

<output>
Create `.planning/phases/26-signal-contract-and-provenance/26-01-SUMMARY.md` when done.
</output>
