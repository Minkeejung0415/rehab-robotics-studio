---
phase: 26-signal-contract-and-provenance
plan: 06
type: execute
wave: 5
depends_on: [26-05]
files_modified:
  - rehab-robotics-studio/src/components/dashboard/SignalContractPanel.tsx
  - rehab-robotics-studio/src/components/dashboard/SignalContractPanel.test.tsx
  - rehab-robotics-studio/src/components/dashboard/Dashboard.tsx
  - rehab-robotics-studio/src/styles/app.css
autonomous: true
requirements: [SIG-01, SIG-02, SIG-03, SIG-04, SIG-05]
must_haves:
  truths:
    - "Operator sees each current-session source by full MAC with applied mapping, independent epochs, availability, units, and provenance."
    - "Raw/SI switching changes presentation only and unavailable values show an em dash plus exact bounded explanation."
    - "Rejected updates remain visible without replacing the last accepted values."
  artifacts:
    - path: "rehab-robotics-studio/src/components/dashboard/SignalContractPanel.tsx"
      provides: "Accessible latest-sample contract inspector"
    - path: "rehab-robotics-studio/src/styles/app.css"
      provides: "Compact responsive Phase 26 layout using existing tokens"
  key_links:
    - from: "rehab-robotics-studio/src/components/dashboard/SignalContractPanel.tsx"
      to: "rehab-robotics-studio/src/data/signalBus.ts"
      via: "`useSignals` immutable snapshot"
      pattern: "useSignals"
    - from: "rehab-robotics-studio/src/components/dashboard/Dashboard.tsx"
      to: "SignalContractPanel"
      via: "first panel before HealthPanel"
      pattern: "SignalContractPanel"
---

<objective>
Deliver the compact, accessible Signal Contract inspector specified for the existing Front Panel.

Purpose: Make canonical identity, units, capability, validity, and applied provenance directly verifiable without introducing Phase 29 waveform scope.
Output: Tested source cards, unit controls, provenance disclosure, rejection feedback, and responsive styling.
</objective>

<execution_context>
@C:/Users/justi/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/justi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/26-signal-contract-and-provenance/26-CONTEXT.md
@.planning/phases/26-signal-contract-and-provenance/26-UI-SPEC.md
@.planning/phases/26-signal-contract-and-provenance/26-PATTERNS.md
@.planning/phases/26-signal-contract-and-provenance/26-VALIDATION.md
@.planning/phases/26-signal-contract-and-provenance/26-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build testable channel presentation and accessibility states</name>
  <files>rehab-robotics-studio/src/components/dashboard/SignalContractPanel.tsx, rehab-robotics-studio/src/components/dashboard/SignalContractPanel.test.tsx</files>
  <read_first>
    - rehab-robotics-studio/src/components/dashboard/SignalContractPanel.tsx (new file)
    - rehab-robotics-studio/src/components/dashboard/HealthPanel.tsx
    - rehab-robotics-studio/src/components/mapping/MappingWorkspace.tsx
    - rehab-robotics-studio/src/data/signalBus.ts
    - rehab-robotics-studio/src/types/signals.ts
    - .planning/phases/26-signal-contract-and-provenance/26-UI-SPEC.md
  </read_first>
  <behavior>
    - Raw mode is default per new source and renders exact counts.
    - SI mode renders accel/gyro only from validated conversions and magnetometer µT only with sensitivity plus calibration provenance.
    - Capability absent, raw-only, transient-invalid, and rejected states use exact badges/copy and em dashes without false units.
    - Valid quaternion preserves received qw/qx/qy/qz; every invalid partition renders no orientation.
  </behavior>
  <action>Export pure presentation helpers/view-model builders beside the component and test them with `node:test` plus `react-dom/server` markup assertions. Implement the UI-SPEC exact copy, reason-code map, units, status distinctions, assigned/unassigned applied labels, epoch labels including zero, provenance field list, and raw/SI mode behavior per D-01..D-16. Unit controls use `aria-pressed`; disabled SI has `aria-describedby`; disclosure uses `aria-expanded`; source sections are labelled by safe full-MAC IDs. Local unit selection must remain selected when later SI validity disappears, showing em dashes and reasons rather than silently switching to raw. Never calculate SI, read mapping drafts, normalize quaternion, render raw error detail, or synthesize sources.</action>
  <acceptance_criteria>
    - Tests cover all rows of the UI-SPEC availability matrix and exact copy mappings.
    - `µT` never appears beside an unavailable value; invalid quaternion never prints `[1, 0, 0, 0]` unless received and valid.
    - Full MAC, applied revision/segment/frame, mapping epoch, reconnect epoch, and availability badge coexist in each card's accessible markup.
    - Provenance disclosure includes every required field and acquisition time is explicitly Unavailable when null.
  </acceptance_criteria>
  <verify>
    <automated>Push-Location rehab-robotics-studio; try { & npm exec -- tsx --test src/components/dashboard/SignalContractPanel.test.tsx; if ($LASTEXITCODE -ne 0) { throw 'signal panel tests failed' }; & npm run typecheck; if ($LASTEXITCODE -ne 0) { throw 'frontend typecheck failed' } } finally { Pop-Location }</automated>
  </verify>
  <done>The source-card behavior is deterministic, fail closed, accessible, and contract-tested.</done>
</task>

<task type="auto">
  <name>Task 2: Compose the panel, persistent rejection feedback, and empty state</name>
  <files>rehab-robotics-studio/src/components/dashboard/SignalContractPanel.tsx, rehab-robotics-studio/src/components/dashboard/SignalContractPanel.test.tsx, rehab-robotics-studio/src/components/dashboard/Dashboard.tsx</files>
  <read_first>
    - rehab-robotics-studio/src/components/dashboard/SignalContractPanel.tsx
    - rehab-robotics-studio/src/components/dashboard/SignalContractPanel.test.tsx
    - rehab-robotics-studio/src/components/dashboard/Dashboard.tsx
    - rehab-robotics-studio/src/components/dashboard/LogsPanel.tsx
    - rehab-robotics-studio/src/hooks/useSignals.ts
    - .planning/phases/26-signal-contract-and-provenance/26-UI-SPEC.md
  </read_first>
  <action>Compose `SignalContractPanel` from `useSignals()` current-session latest samples sorted by canonical full MAC. Render the exact empty state and do not synthesize a device; accepted/rejected summary uses `role=status` and polite count announcements, not per-sample announcements. Keep the last accepted card visible with `Last update rejected` and bounded reason/timestamp; when no accepted sample exists, render only `Sample rejected`. Use `role=alert` only when the reason signature changes and feed the existing System Log through its established bounded text action. Insert the panel first in `Dashboard`, before `HealthPanel`. Do not add a tab, toast-only feedback, charts, history, comparison, scrolling controls, or export controls.</action>
  <acceptance_criteria>
    - Dashboard source order places Signal Contract before Acquisition Health.
    - Summary renders `{n} accepted` or `{n} accepted · {n} rejected`; empty state copy is exact.
    - Accepted updates do not move focus; repeated identical rejection codes are not repeatedly announced.
    - No Phase 29 waveform/history/control vocabulary or component is introduced.
  </acceptance_criteria>
  <verify>
    <automated>Push-Location rehab-robotics-studio; try { & npm exec -- tsx --test src/components/dashboard/SignalContractPanel.test.tsx src/data/signalBus.test.ts; if ($LASTEXITCODE -ne 0) { throw 'panel composition tests failed' }; & npm run typecheck; if ($LASTEXITCODE -ne 0) { throw 'frontend typecheck failed' } } finally { Pop-Location }</automated>
  </verify>
  <done>The existing Front Panel exposes persistent canonical trust feedback before derived health readouts.</done>
</task>

<task type="auto">
  <name>Task 3: Apply the responsive compact visual contract and run the phase gate</name>
  <files>rehab-robotics-studio/src/styles/app.css, rehab-robotics-studio/src/components/dashboard/SignalContractPanel.tsx, rehab-robotics-studio/src/components/dashboard/SignalContractPanel.test.tsx</files>
  <read_first>
    - rehab-robotics-studio/src/styles/app.css
    - rehab-robotics-studio/src/theme/tokens.ts
    - rehab-robotics-studio/src/components/dashboard/SignalContractPanel.tsx
    - .planning/phases/26-signal-contract-and-provenance/26-UI-SPEC.md
    - .planning/phases/26-signal-contract-and-provenance/26-VALIDATION.md
  </read_first>
  <action>Add only UI-SPEC-prescribed classes using existing token colors: 8px panel inset override, square source cards with 16px padding/24px gaps, 112px group and 168px availability desktop columns, tabular monospace values, and existing status colors. Below 768px stack groups/provenance and make controls at least 44px; below 620px wrap full MAC, exact segment/frame, hashes, revision, and both epochs without ellipsis. Add 2px inset accent focus-visible treatment and reduced-motion compliance. Keep maximum width at the existing 960px and never hide reason text at any breakpoint. Finish with the complete validation suite.</action>
  <acceptance_criteria>
    - `.dash-panel.signal-contract-panel` is exactly 8px and no new hex values appear.
    - Full identity/provenance wraps and remains inspectable at narrow widths; no horizontal scroll is required for channel groups.
    - State is never communicated by color alone and numeric values are outside tab order.
    - Quick and full Phase 26 commands pass.
  </acceptance_criteria>
  <verify>
    <automated>$env:PYTHONPATH='backend'; & python -m pytest backend/test/test_signal_contract.py backend/test/test_measurement_contract.py backend/test/test_fleet_bridge.py -q; if ($LASTEXITCODE -ne 0) { throw 'backend phase tests failed' }; Push-Location rehab-robotics-studio; try { & npm test; if ($LASTEXITCODE -ne 0) { throw 'frontend test suite failed' }; & npm run typecheck; if ($LASTEXITCODE -ne 0) { throw 'frontend typecheck failed' }; & npm run build; if ($LASTEXITCODE -ne 0) { throw 'frontend build failed' } } finally { Pop-Location }</automated>
  </verify>
  <done>The inspector matches the complete UI-SPEC and all backend/frontend Phase 26 regressions pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical signal snapshot → DOM/accessibility tree | Validated data becomes operator-visible text and interactive state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-26 | Spoofing | identity header | mitigate | Render canonical full MAC and bound topic-derived sample identity; never role/IP/order aliases. |
| T-26-27 | Tampering | channel presentation | mitigate | Consume validated representation only; unavailable/non-finite paths render em dash and reason. |
| T-26-28 | Tampering / Repudiation | applied label | mitigate | Render only sample-owned applied snapshot and independent epochs; no mapping-store join. |
| T-26-29 | Denial of Service / Injection | labels/rejection DOM | mitigate | Render bounded typed strings as React text, allowlist reason copy, deduplicate alerts. |
| T-26-30 | Spoofing | capability and unit badges | mitigate | Derive badges solely from canonical capability/validity/conversion states; never infer or fabricate. |
| T-26-SC | Tampering | package installs | mitigate | Existing React/CSS only; no registry or package install. |
</threat_model>

<verification>
Run server-render/presentation tests, all Phase 26 contract tests, frontend typecheck/build, and the full backend/frontend suites.
</verification>

<success_criteria>
- Operators can inspect exact full-MAC identity, timing, capabilities, raw/SI availability, applied mapping, and validity for every accepted current-session source.
- Unavailable and rejected states are explicit, persistent, bounded, accessible, and never fabricated as plausible data.
- The panel meets every UI-SPEC layout, copy, responsive, and accessibility obligation without Phase 29 waveform scope.
</success_criteria>

<source_audit>

| Source | ID | Feature / Requirement | Plan | Status |
|--------|----|-----------------------|------|--------|
| GOAL | — | Trust identity, timing, validity, units, capabilities, and applied mapping for every sample | 01-06 | COVERED |
| REQ | SIG-01 | Full MAC, time/sequence, reconnect epoch, capabilities | 01-06 | COVERED |
| REQ | SIG-02 | Lossless raw axes and validated accel/gyro SI | 01,02,03,05,06 | COVERED |
| REQ | SIG-03 | Validated magnetometer µT contract | 01,02,03,05,06 | COVERED |
| REQ | SIG-04 | Applied-only exact mapping labels | 01-06 | COVERED |
| REQ | SIG-05 | Declared/valid quaternion only, no fabrication | 01-06 | COVERED |
| RESEARCH | R-01 | Immutable additive canonical envelope and compatibility boundary | 01-04 | COVERED |
| RESEARCH | R-02 | Explicit clock/sequence origins and reconnect generation | 01,03-06 | COVERED |
| RESEARCH | R-03 | Explicit capability/validity, bounded reason taxonomy | 01-06 | COVERED |
| RESEARCH | R-04 | Shared deterministic cross-language contract fixture | 01,02 | COVERED |
| RESEARCH | R-05 | Applied snapshot per sample, no draft late join | 01-06 | COVERED |
| RESEARCH | R-06 | No new package; existing high-rate/low-render boundary | 02,05,06 | COVERED |
| CONTEXT | D-01..D-04 | Full-MAC identity and honest fail-closed timing metadata | 01-06 | COVERED |
| CONTEXT | D-05..D-08 | Lossless raw and validated SI/magnetometer provenance | 01-03,05,06 | COVERED |
| CONTEXT | D-09..D-12 | Authoritative applied mapping and immutable epochs | 01,03-06 | COVERED |
| CONTEXT | D-13..D-16 | Quaternion capability/validity distinction and no fabrication | 01-06 | COVERED |
| UI-SPEC | UI-01 | Complete Signal Contract panel hierarchy, layout, copy, interaction, a11y, responsive behavior | 05,06 | COVERED |

Deferred waveform/history/comparison controls, full-rate export, full-body model, calibration, IK, and physical evidence are explicitly excluded and not audit gaps.
</source_audit>

<output>
Create `.planning/phases/26-signal-contract-and-provenance/26-06-SUMMARY.md` when done.
</output>
