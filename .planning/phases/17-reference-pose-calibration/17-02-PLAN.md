---
phase: 17-reference-pose-calibration
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - backend/rehab_robotics_bridge/opensim_node.py
  - backend/test/test_opensim_node.py
  - docs/opensim-ik-contracts.md
autonomous: true
requirements:
  - IK-01
  - IK-02
  - IK-03
  - IK-04
must_haves:
  truths:
    - "/opensim/calibration/capture and /opensim/calibration/clear Trigger services exist on opensim_bridge."
    - "Capture feeds live master/slave orientations into CalibrationController until CALIBRATED or FAILED."
    - "No sensor_msgs/JointState is published on /opensim/joint_states while not CALIBRATED."
    - "Calibration state+reason appear in /opensim/status and /opensim/calibration_status."
  artifacts:
    - path: "backend/rehab_robotics_bridge/opensim_node.py"
      provides: "ROS service servers + status publish + joint_states gate wrapper"
    - path: "backend/test/test_opensim_node.py"
      provides: "Stubbed-node tests for capture/clear/gate/status"
  key_links:
    - from: "OpenSimBridgeNode"
      to: "CalibrationController"
      via: "begin_capture/clear/feed_pair on IMU path"
      pattern: "CalibrationController|calibration/capture|feed_pair"
    - from: "_maybe_publish_joint_states"
      to: "may_publish_joint_states"
      via: "hard gate before JointState publish"
      pattern: "may_publish_joint_states|JOINT_STATES_TOPIC"
---

<objective>
Wire CalibrationController into `opensim_bridge`: Trigger capture/clear services, feed pairs during CAPTURING, publish calibration status, and enforce the hard joint_states publish gate (no solver — Phase 18).

Purpose: Deliver D-17-07 ROS surface + D-17-03 gate on the live bridge so Studio can call services and show status.
Output: Updated `opensim_node.py`, extended node tests, contract doc note that Phase 17 implements services/status.
</objective>

<execution_context>
@C:/Users/justi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/justi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-reference-pose-calibration/17-CONTEXT.md
@.planning/phases/17-reference-pose-calibration/17-01-SUMMARY.md
@docs/opensim-ik-contracts.md
@backend/rehab_robotics_bridge/opensim/calibration.py
@backend/rehab_robotics_bridge/opensim/ik_contracts.py
@backend/rehab_robotics_bridge/opensim_node.py
@backend/test/test_opensim_node.py
@backend/rehab_robotics_bridge/esp32_bridge_node.py

<locked_decisions>
- D-17-03: Hard gate — no joint angles published until CALIBRATED.
- D-17-07: Services `/opensim/calibration/capture` and `/opensim/calibration/clear` (`std_srvs/Trigger`).
- D-17-05/IK-04: Status UNCALIBRATED|CAPTURING|CALIBRATED|FAILED + reason must be observable (publish here; GUI in plan 03).
- Phase 18 owns InverseKinematicsSolver — do not add it here.
</locked_decisions>

<interfaces>
Existing `OpenSimBridgeNode` patterns:
- Stores latest `last_xyzw` per role in `_SensorState` after valid IMU updates.
- Publishes JSON String on `/opensim/status` via `status_snapshot()`.
- Optional debug Float64 `/opensim/joint_angle` behind `publish_joint_angle_enabled` (unchanged; not product IK).

`esp32_bridge_node.py` service pattern:
- `from std_srvs.srv import SetBool` + `self.create_service(...)`.
- Use `from std_srvs.srv import Trigger` similarly.

Test harness (`test_opensim_node.py`):
- Stubs Node with `create_publisher` / `create_subscription` / `create_timer` — **extend stub with `create_service`** that records (srv_type, name, callback).
- Stub `std_srvs.srv.Trigger` Request/Response with `success` and `message` fields if importing real msgs is awkward under the existing fake-module pattern.

Constants to import from ik_contracts:
- CALIBRATION_CAPTURE_SERVICE, CALIBRATION_CLEAR_SERVICE, CALIBRATION_STATUS_TOPIC, JOINT_STATES_TOPIC, may_publish_joint_states
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend opensim_node tests for services and gate</name>
  <files>backend/test/test_opensim_node.py</files>
  <behavior>
    - Node constructs services named `/opensim/calibration/capture` and `/opensim/calibration/clear`.
    - status_snapshot includes `calibration` object with at least `state` and `reason` keys; initial state UNCALIBRATED.
    - Invoking clear service callback → state UNCALIBRATED.
    - With both sensors live (feed two IMU msgs) then capture service → CAPTURING or CALIBRATED after enough timer/IMU feeds of stable quats; never CALIBRATED from a single pair alone when window/min_samples unmet.
    - `_maybe_publish_joint_states` / joint_states publisher: while UNCALIBRATED, JointState publisher message list stays empty even if a helper tries to publish.
    - When CALIBRATED but no IK solution available, still no fabricated JointState positions (gate allows publish only when CALIBRATED AND solution present — Phase 17 solution always absent).
  </behavior>
  <action>
Extend `backend/test/test_opensim_node.py` stub Node with `create_service`. Add tests for the behaviors above using the existing fake rclpy/sensor_msgs pattern. Prefer calling service callbacks directly with Trigger Request/Response objects. Keep tests ROS-daemon-free. Expect RED until Task 2 lands.
  </action>
  <verify>
    <automated>$env:PYTHONPATH='backend'; python -m unittest backend.test.test_opensim_node -v</automated>
  </verify>
  <done>New failing tests encode capture/clear/status/gate contracts on the node.</done>
</task>

<task type="auto">
  <name>Task 2: Wire CalibrationController into OpenSimBridgeNode</name>
  <files>backend/rehab_robotics_bridge/opensim_node.py, docs/opensim-ik-contracts.md</files>
  <action>
Implement runtime wiring in `opensim_node.py` per D-17-03/05/07:

1. Construct `CalibrationController` (injectable optional kwarg for tests).
2. `create_service(Trigger, CALIBRATION_CAPTURE_SERVICE, handler)` and clear service. Capture handler: if both sensors have `last_xyzw` and are live, `begin_capture()` and return Trigger success=True with message `capturing` or reject with success=False + reason (stale/missing). Clear handler: `clear()` and success=True.
3. On each successful `_on_imu` after both `last_xyzw` present, if state is CAPTURING call `feed_pair(master, slave, monotonic_time=...)`.
4. Publish String JSON on `CALIBRATION_STATUS_TOPIC` whenever calibration status changes and on the status timer (same payload as `status_snapshot()["calibration"]`).
5. Embed `calibration: controller.status_dict()` inside `status_snapshot()` so existing `/opensim/status` consumers (Studio) see state without a second subscription (also keep dedicated topic for contract).
6. Create JointState publisher on `JOINT_STATES_TOPIC`. Implement `_maybe_publish_joint_states` that returns immediately unless `may_publish_joint_states(state)` AND a future solution payload exists. In Phase 17, never invent angles — method is the gate seam for Phase 18. Do not change default of `publish_joint_angle_enabled` (debug path stays OFF).
7. Optional ROS params: `calibration_window_s`, `calibration_max_dispersion_deg` forwarded into controller if easy; otherwise use module defaults.
8. Update `docs/opensim-ik-contracts.md` Calibration services / status sections: mark Phase 17 as implementing capture/clear + calibration_status JSON schema keys (`state`, `reason`, `known_pose`, …). Keep Phase 18 as solver owner.

Avoid: InverseKinematicsSolver; browser-side calibration; auto-calibrate on first IMU without service.
  </action>
  <verify>
    <automated>$env:PYTHONPATH='backend'; python -m unittest backend.test.test_opensim_node backend.test.test_opensim_calibration backend.test.test_ik_contracts -v</automated>
  </verify>
  <done>Services, status publish, and joint_states gate exist; node + calibration + contracts tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rosbridge/GUI → Trigger services | Untrusted operator commands on local ROS graph |
| opensim_bridge → /opensim/joint_states | Must not emit uncalibrated product kinematics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | Spoofing | capture/clear services | accept | Local lab ROS graph; no auth layer in v1.5 |
| T-17-05 | Tampering | JointState publish path | mitigate | may_publish_joint_states gate + no fabricated solution in Phase 17 |
| T-17-06 | Information disclosure | calibration_status offsets | accept | Lab-only orientations; optional has_offsets without full matrices in status |
| T-17-SC | Tampering | package installs | accept | No new packages; std_srvs already in package.xml |
</threat_model>

<verification>
Unittest suite for opensim_node proves service names, status embedding, clear, and empty joint_states while uncalibrated.
</verification>

<success_criteria>
- Capture/clear Trigger services registered at locked names.
- Calibration state visible on `/opensim/status` and `/opensim/calibration_status`.
- JointState publisher never emits while UNCALIBRATED/CAPTURING/FAILED.
</success_criteria>

<output>
Create `.planning/phases/17-reference-pose-calibration/17-02-SUMMARY.md` when done
</output>
