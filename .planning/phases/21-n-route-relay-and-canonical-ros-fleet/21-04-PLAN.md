---
phase: 21-n-route-relay-and-canonical-ros-fleet
plan: "04"
type: execute
wave: 4
depends_on: ["21-01", "21-02", "21-03"]
files_modified:
  - scripts/stepesp_tcp_udp_relay.py
  - backend/rehab_robotics_bridge/fleet_bridge_node.py
  - backend/rehab_robotics_bridge/esp32_bridge_node.py
  - backend/test/test_stepesp_udp_relay.py
  - backend/test/test_fleet_bridge.py
  - backend/test/test_esp32_controls.py
  - docs/stepesp-wireless-setup.md
autonomous: true
requirements: [FLEET-03, FLEET-01, ID-02]
must_haves:
  truths:
    - "A failed, stale, or reconnecting route does not stop acquisition, health, Identify, or recording for other devices."
    - "Per-route bounded UDP queues expose drop_count; reconnect diagnostics appear on health and registry rows."
    - "Auto-reconnect affects only the failed route; registry marks reconnecting/stale without a global relay restart."
    - "Deterministic tests prove multi-route isolation, DHCP topic stability, drop counters, and registry layered diagnostics without STEP_ESP32 Wi-Fi."
  artifacts:
    - path: "scripts/stepesp_tcp_udp_relay.py"
      provides: "drop_count on UdpRouter queues"
    - path: "backend/rehab_robotics_bridge/fleet_bridge_node.py"
      provides: "Per-session reconnect isolation + diagnostics in registry/health"
    - path: "backend/test/test_fleet_bridge.py"
      provides: "Isolation and diagnostics automated coverage"
    - path: "backend/test/test_stepesp_udp_relay.py"
      provides: "Drop-count and multi-route failure isolation coverage"
  key_links:
    - from: "UdpRouter.route_datagram"
      to: "health/registry drop_count"
      via: "increment on drop-oldest"
      pattern: "drop_count"
    - from: "session reconnect loop"
      to: "other sessions"
      via: "independent tasks; no global cancel"
      pattern: "reconnect|CancelledError|gather"
---

<objective>
Expose per-route drop/reconnect diagnostics and prove failure isolation with deterministic automated tests.

Purpose: Satisfy FLEET-03 observability and ensure N-device acquisition remains resilient.
Output: drop_count metrics, per-route reconnect diagnostics in health/registry, and isolation test suite — no live STEP_ESP32 required.
</objective>

<execution_context>
@C:/Users/justi/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/justi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-n-route-relay-and-canonical-ros-fleet/21-CONTEXT.md
@.planning/phases/21-n-route-relay-and-canonical-ros-fleet/21-RESEARCH.md
@.planning/phases/21-n-route-relay-and-canonical-ros-fleet/21-01-PLAN.md
@.planning/phases/21-n-route-relay-and-canonical-ros-fleet/21-02-PLAN.md
@.planning/phases/21-n-route-relay-and-canonical-ros-fleet/21-03-PLAN.md
@scripts/stepesp_tcp_udp_relay.py
@backend/rehab_robotics_bridge/fleet_bridge_node.py

<locked_decisions>
- D-21-09: Isolate failures per identity; failed/stale/reconnecting route must not stop acquisition, health, Identify, or recording for others.
- D-21-10: Keep per-route bounded UDP queues (maxsize=256, drop-oldest) and expose drop_count (and reconnect diagnostics) in health/registry.
- D-21-11: Auto-reconnect only the affected route; registry marks reconnecting/stale without global relay restart.
- D-21-12: Retain offline/stale MAC rows with last-seen.
- D-21-14: Layered registry fields include drops/reconnects.
</locked_decisions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-route drop_count and reconnect diagnostics</name>
  <files>scripts/stepesp_tcp_udp_relay.py, backend/rehab_robotics_bridge/fleet_bridge_node.py, backend/rehab_robotics_bridge/esp32_bridge_node.py, backend/test/test_stepesp_udp_relay.py, backend/test/test_fleet_bridge.py</files>
  <read_first>
    - scripts/stepesp_tcp_udp_relay.py — UdpRouter.route_datagram drop-oldest path
    - fleet_bridge_node / esp32_bridge_node — health_snapshot, reconnect_count, registry builder
    - 21-RESEARCH.md diagnostics fields
  </read_first>
  <behavior>
    - "When a per-host UDP queue is full, the oldest datagram is dropped and drop_count increments by one for that route only."
    - "Health JSON for mac_ topics and registry device rows include drop_count and reconnect_count."
    - "Registry route state becomes reconnecting or stale for the affected device without removing the row."
    - "Other routes' drop_count values remain unchanged when one route drops."
  </behavior>
  <action>Per D-21-10, D-21-11, D-21-14: add drop_count tracking to UdpRouter (per host). Propagate relay-visible drop stats to fleet health if a control/status channel exists, or have the fleet session track its own ingress drops if UDP drops are only relay-local — prefer exposing relay drop_count via a lightweight relay status line or by having tests assert relay counters directly and fleet counters for session-level reconnects. Extend oe_esp32.health (additive fields) and registry rows with drop_count and reconnect_count. Write failing tests first for overflow drop increments and registry field presence.</action>
  <verify>
    <automated>python -m unittest backend.test.test_stepesp_udp_relay backend.test.test_fleet_bridge -v</automated>
  </verify>
  <done>drop_count and reconnect diagnostics are visible on the affected route only and covered by tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Deterministic multi-route isolation suite</name>
  <files>backend/test/test_fleet_bridge.py, backend/test/test_stepesp_udp_relay.py, backend/test/test_esp32_controls.py, backend/rehab_robotics_bridge/fleet_bridge_node.py, docs/stepesp-wireless-setup.md</files>
  <read_first>
    - Existing isolation test test_malformed_inventory_cannot_poison_an_unrelated_live_route
    - fleet_bridge_node session task structure
    - ROADMAP Phase 21 success criteria
  </read_first>
  <behavior>
    - "Simulated failure/reconnect on slave B does not stop frame publish, health publish, or Identify handling on master/slave A."
    - "DHCP/IP remap keeps canonical mac_ topic names unchanged (ID-02)."
    - "Recording/acquisition control path is not globally cancelled when one session errors (source or behavioral assertion)."
    - "Offline MAC remains in registry with last_seen after TCP death."
  </behavior>
  <action>Per D-21-09, D-21-11, D-21-12 and ROADMAP success criterion 3: add deterministic async/unit tests that inject session failure on one fake route while asserting continued publish callbacks on others; assert Identify on a healthy device still completes correlation; assert topic tokens unchanged across endpoint refresh; assert registry retains offline rows. Update docs with diagnostic field names and note that live STEP_ESP32 validation remains operator-run. Keep all verifies hardware-free.</action>
  <verify>
    <automated>python -m unittest backend.test.test_stepesp_udp_relay backend.test.test_fleet_bridge backend.test.test_esp32_controls -v</automated>
  </verify>
  <done>Isolation, DHCP topic stability, offline retention, and diagnostics are proven by automated tests without STEP_ESP32 Wi-Fi.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Per-route queues → shared relay process | One noisy sender must not exhaust others |
| Session tasks → shared ROS node | One failed session must not cancel the executor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-12 | Denial of service | UdpRouter queues | mitigate | maxsize=256 drop-oldest + per-route drop_count visibility |
| T-21-13 | Denial of service | session reconnect | mitigate | Reconnect only affected route; no global gather cancel |
| T-21-14 | Tampering | diagnostics inflation | accept | Counters are local telemetry; not security audit log |
| T-21-15 | Elevation | Identify during partial outage | mitigate | Identify remains MAC-targeted on healthy sessions only |
| T-21-SC | Tampering | package installs | accept | No new packages |
</threat_model>

<verification>
python -m unittest backend.test.test_stepesp_udp_relay backend.test.test_fleet_bridge backend.test.test_esp32_controls -v
</verification>

<success_criteria>
- drop_count and reconnect diagnostics visible per identity
- Failed route does not stop other devices' acquisition/health/Identify/recording paths
- Registry retains offline rows and layered states
- Full deterministic suite green without STEP_ESP32
</success_criteria>

<output>
Create `.planning/phases/21-n-route-relay-and-canonical-ros-fleet/21-04-SUMMARY.md` when done
</output>
