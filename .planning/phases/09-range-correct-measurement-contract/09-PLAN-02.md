---
phase: 09-range-correct-measurement-contract
plan: 02
goal: >
  Create the TypeScript measurement-contract seam, extend RosbridgeDataSource to
  validate sensor_config before caching or emitting frames, fix the service name
  mismatch for range changes, and add frontend tests covering all rejection
  partitions, warning-latch behavior, cache-reset on reconnect, and ACK-driven
  range coordination.
wave: 2
depends_on:
  - 09-plan-01
files_modified:
  - rehab-robotics-studio/src/data/measurementContract.ts
  - rehab-robotics-studio/src/data/measurementContract.test.ts
  - rehab-robotics-studio/src/data/RosbridgeDataSource.ts
  - rehab-robotics-studio/src/data/RosbridgeDataSource.test.ts
  - rehab-robotics-studio/package.json
autonomous: true
requirements:
  - DATA-01
  - DATA-02

must_haves:
  truths:
    - "The TypeScript validator rejects the same partitions as the Python validator: missing field, unsupported range, zero/negative/non-finite sensitivity, range/sensitivity mismatch, wrong unit token"
    - "TypeScript SI values for all 8 supported ranges match the shared JSON fixture values to 1e-9 tolerance"
    - "An incoming frame with invalid sensor_config causes zero subscriber emissions, zero onFrameReceived callbacks, and exactly one WARN callback per connection"
    - "Subsequent invalid frames on the same connection produce no additional WARN callbacks"
    - "A valid frame later on the same connection resumes emission without a success callback"
    - "A new WebSocket connection resets the warning latch, masterRaw/slaveRaw caches, and RelativeAngleStabilizer"
    - "Range service calls target /esp_bridge_master/set_parameters and /esp_bridge_slave/set_parameters, not /esp_master/set_parameters"
    - "Pair-relative math (frameFromPair) is not attempted when either device's frame is invalid"
  artifacts:
    - path: "rehab-robotics-studio/src/data/measurementContract.ts"
      provides: "SensorConfig type; ACCEL_LSB_PER_G, GYRO_LSB_PER_DPS maps; validateSensorConfig(); accelCountToMps2(); gyroCountToRad_s()"
    - path: "rehab-robotics-studio/src/data/measurementContract.test.ts"
      provides: "09-02-01: shared-fixture table tests and rejection partitions"
    - path: "rehab-robotics-studio/src/data/RosbridgeDataSource.ts"
      provides: "Validate-before-cache handleMessage; one-warn-per-connection latch; cache/latch reset on new socket; corrected service names"
    - path: "rehab-robotics-studio/src/data/RosbridgeDataSource.test.ts"
      provides: "09-02-02: warning/cache/emission tests; 09-02-03: service-name and ACK tests"
    - path: "rehab-robotics-studio/package.json"
      provides: "test script extended to include src/data/*.test.ts"
  key_links:
    - from: "RosbridgeDataSource.handleMessage"
      to: "validateSensorConfig(raw.sensor_config)"
      via: "Called per-device before masterRaw/slaveRaw assignment"
      pattern: "validateSensorConfig"
    - from: "warnScaleOnce(role)"
      to: "onWarnScaleMissing callback"
      via: "Boolean latch per-connection; set on first invalid frame only"
      pattern: "warnScaleOnce|_warnedScale"
    - from: "RosbridgeDataSource.start()"
      to: "new WebSocket()"
      via: "Resets latch, caches, pair stabilizer before creating socket"
      pattern: "masterRaw.*null|slaveRaw.*null|_warnedScale.*false"
    - from: "requestImuControl"
      to: "/esp_bridge_master/set_parameters and /esp_bridge_slave/set_parameters"
      via: "Two separate callService calls for accel_range_g and gyro_range_dps"
      pattern: "esp_bridge_master.*set_parameters"
---

<objective>
Port the measurement contract to TypeScript, integrate strict validation into the
rosbridge data source, fix the service-name bug that has prevented range changes
from reaching the launched nodes, and prove the corrected behavior with a
comprehensive Node node:test suite that reads the same JSON fixture as the Python
backend.

Purpose: The current RosbridgeDataSource uses fixed ACC_SCALE/GYRO_SCALE constants
and caches raw messages before any validation. This plan makes the browser fail
closed on untrusted frames (DATA-02), and proves it produces the same SI values as
the backend for all supported ranges (DATA-01).

Output:
- measurementContract.ts: pure TS validator/converter seam
- measurementContract.test.ts: 09-02-01 shared-fixture and rejection tests
- RosbridgeDataSource.ts: validate-before-cache, one-warn latch, cache reset,
  corrected service names, coordinated master+slave range changes
- RosbridgeDataSource.test.ts: 09-02-02 warning/cache/emission; 09-02-03 service ACK
- package.json: test script extended to src/data/*.test.ts
</objective>

<execution_context>
@C:\Users\justi\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\justi\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-range-correct-measurement-contract/09-CONTEXT.md
@.planning/phases/09-range-correct-measurement-contract/09-RESEARCH.md
@.planning/phases/09-range-correct-measurement-contract/09-UI-SPEC.md
@.planning/phases/09-range-correct-measurement-contract/09-VALIDATION.md
@.planning/phases/09-range-correct-measurement-contract/09-01-SUMMARY.md

<interfaces>
<!-- Key symbols the executor needs. Extracted from source files. -->

From rehab-robotics-studio/src/data/RosbridgeDataSource.ts (current):

Type used by handleMessage — needs sensor_config added:
  type RawEspMessage = {
    topic_schema?: string;
    time_us?: number;
    imu?: Partial<Record<'ax'|'ay'|'az'|'gx'|'gy'|'gz', number>>;
    quat?: Partial<Record<'qw'|'qx'|'qy'|'qz', number>>;
    // ADD: sensor_config?: unknown;
  };

Current constants to REPLACE:
  const ACC_SCALE = 9.80665 / 16384;    // fixed ±2g
  const GYRO_SCALE = (Math.PI / 180) / 131.072;   // fixed ±250dps

Existing service name BUG (must fix):
  requestImuControl calls: '/esp_master/set_parameters'
  Correct names from launch file: '/esp_bridge_master/set_parameters'
                                   '/esp_bridge_slave/set_parameters'

Current handleMessage logic (problematic flow):
  const raw = JSON.parse(envelope.msg.data) as RawEspMessage;
  if (envelope.topic === this.masterTopic) this.masterRaw = raw;     // caches BEFORE validation
  if (envelope.topic === this.slaveTopic) this.slaveRaw = raw;       // caches BEFORE validation
  const frame = frameFromPair(this.masterRaw ?? raw, ...);           // pair uses cached invalid

Existing callbacks the executor must NOT remove or rename:
  onUnavailable?: () => void
  onConnectionChange?: (connected: boolean) => void
  onFrameReceived?: () => void
  onPairHealth?: (health: PairHealthSnapshot) => void

New callback to ADD to constructor:
  onWarnScaleMissing?: (deviceList: string) => void
  (Used by UI-SPEC warning: "Live IMU measurements paused: scale metadata is
   missing or invalid for {device list}. Confirm ACCEL and GYRO ranges, then
   reconnect ROS.")
  deviceList values: 'MASTER', 'SLAVE', or 'MASTER and SLAVE'

Current frameFromRaw uses:
  numeric(raw.imu?.ax) * ACC_SCALE  → must change to per-config conversion
  numeric(raw.imu?.gx) * GYRO_SCALE → must change to per-config conversion

frameFromPair signature (must NOT change):
  function frameFromPair(masterRaw, slaveRaw, stabilizer): Frame | null

RelativeAngleStabilizer.reset() must be called when a new WebSocket is created
(already called in start() and stop() but NOT on reconnect with existing socket).

From rehab-robotics-studio/package.json (current test script):
  "test": "tsx --test src/deployment/*.test.ts"
  Change to: "tsx --test src/deployment/*.test.ts src/data/*.test.ts"

Node node:test import pattern (from existing test files):
  import assert from 'node:assert/strict';
  import { describe, it } from 'node:test';

Shared fixture path for TypeScript tests:
  Read from: backend/test/fixtures/measurement_contract_cases.json
  Using: import { readFileSync } from 'node:fs';
         import { join, dirname } from 'node:path';
         import { fileURLToPath } from 'node:url';
  Path: join(dirname(fileURLToPath(import.meta.url)),
             '../../../../backend/test/fixtures/measurement_contract_cases.json')
  (From rehab-robotics-studio/src/data/, go up four levels to project root,
   then into backend/test/fixtures/)

Supported ranges (match Python constants exactly):
  ACCEL_LSB_PER_G: Record<number,number> = {2:16384,4:8192,8:4096,16:2048}
  GYRO_LSB_PER_DPS: Record<number,number> = {250:131.072,500:65.536,1000:32.768,2000:16.384}
  GRAVITY = 9.80665
  DEG_TO_RAD = Math.PI / 180

UI-SPEC copywriting contract for WARN message:
  "Live IMU measurements paused: scale metadata is missing or invalid for
   {device list}. Confirm ACCEL and GYRO ranges, then reconnect ROS."
  {device list} = 'MASTER', 'SLAVE', or 'MASTER and SLAVE'
</interfaces>
</context>

<tasks>

<task type="auto" id="09-02-01">
  <name>Task 1 (09-02-01): Create measurementContract.ts and shared-fixture tests</name>
  <files>
    rehab-robotics-studio/src/data/measurementContract.ts
    rehab-robotics-studio/src/data/measurementContract.test.ts
    rehab-robotics-studio/package.json
  </files>
  <action>
    Create rehab-robotics-studio/src/data/measurementContract.ts as a pure TypeScript
    module with no React/Zustand/DOM imports. It must export:

    1. Type SensorConfig:
         export type SensorConfig = {
           accel_range_g: number;
           gyro_range_dps: number;
           accel_lsb_per_g: number;
           gyro_lsb_per_dps: number;
           units: Record<string, string>;
         };

    2. Constants (match Python measurement_contract.py exactly):
         export const GRAVITY = 9.80665;
         export const DEG_TO_RAD = Math.PI / 180;
         export const ACCEL_LSB_PER_G: Readonly<Record<number, number>> =
           { 2: 16384, 4: 8192, 8: 4096, 16: 2048 };
         export const GYRO_LSB_PER_DPS: Readonly<Record<number, number>> =
           { 250: 131.072, 500: 65.536, 1000: 32.768, 2000: 16.384 };

    3. type ValidateResult<T> = { ok: true; value: T } | { ok: false; reason: string };

    4. export function validateSensorConfig(raw: unknown): ValidateResult<SensorConfig>
       Strict validation — returns { ok: false, reason: string } for each failure:
         - raw not an object or null
         - Missing any of: accel_range_g, gyro_range_dps, accel_lsb_per_g,
           gyro_lsb_per_dps, units
         - accel_range_g not in [2, 4, 8, 16]
         - gyro_range_dps not in [250, 500, 1000, 2000]
         - !Number.isFinite(accel_lsb_per_g) || accel_lsb_per_g <= 0
         - !Number.isFinite(gyro_lsb_per_dps) || gyro_lsb_per_dps <= 0
         - Math.abs(accel_lsb_per_g - ACCEL_LSB_PER_G[accel_range_g]) /
           ACCEL_LSB_PER_G[accel_range_g] > 1e-9  (range/sensitivity mismatch)
         - Same cross-check for gyro
         - units not an object or missing 'raw' key
       On success returns { ok: true, value: SensorConfig }.
       NEVER throw; always return a result object.

    5. export function accelCountToMps2(count: number, config: SensorConfig): number
       Returns count / config.accel_lsb_per_g * GRAVITY

    6. export function gyroCountToRad_s(count: number, config: SensorConfig): number
       Returns count / config.gyro_lsb_per_dps * DEG_TO_RAD

    Create rehab-robotics-studio/src/data/measurementContract.test.ts using
    node:test and node:assert/strict:

    Read the shared fixture:
      const casesPath = join(dirname(fileURLToPath(import.meta.url)),
        '../../../../backend/test/fixtures/measurement_contract_cases.json');
      const cases: Array<{
        role: string; raw_count: number; accel_range_g: number; gyro_range_dps: number;
        expected_accel_mps2: number; expected_gyro_rad_s: number;
      }> = JSON.parse(readFileSync(casesPath, 'utf-8'));

    Test group: 'measurementContract — 09-02-01'

    test '09-02-01 all 32 shared-fixture cases match expected SI values':
      For each case, build a fake SensorConfig:
        const config: SensorConfig = {
          accel_range_g: c.accel_range_g,
          gyro_range_dps: c.gyro_range_dps,
          accel_lsb_per_g: ACCEL_LSB_PER_G[c.accel_range_g],
          gyro_lsb_per_dps: GYRO_LSB_PER_DPS[c.gyro_range_dps],
          units: { raw: 'count' },
        };
      Assert Math.abs(accelCountToMps2(c.raw_count, config) - c.expected_accel_mps2) < 1e-9.
      Assert Math.abs(gyroCountToRad_s(c.raw_count, config) - c.expected_gyro_rad_s) < 1e-9.

    test '09-02-01 validateSensorConfig accepts a canonical round-trip object':
      Build a canonical config for 8g/2000dps and assert ok=true.

    test '09-02-01 validateSensorConfig rejects each required rejection partition':
      Use assert.equal(validateSensorConfig(x).ok, false) for:
        - null
        - {} (missing all fields)
        - object with accel_range_g=3 (unsupported)
        - object with gyro_range_dps=42 (unsupported)
        - object with accel_lsb_per_g=0
        - object with accel_lsb_per_g=NaN
        - object with accel_lsb_per_g=Infinity
        - object with accel_range_g=8, accel_lsb_per_g=16384 (8g range vs 2g sensitivity)
        - object missing 'units' key
        - valid fields but units does not have 'raw' key

    In package.json, change the "test" script from:
      "tsx --test src/deployment/*.test.ts"
    to:
      "tsx --test src/deployment/*.test.ts src/data/*.test.ts"
    No other package.json changes; do not add dependencies.
  </action>
  <verify>
    <automated>
      cd "C:\Users\justi\Documents\# Rehab Robotics GUI Prototype__Here's a complete, single-file React + TypeScript app implementing the LabVIEW\rehab-robotics-studio" &amp;&amp; npm exec -- tsx --test src/data/measurementContract.test.ts
    </automated>
  </verify>
  <done>
    measurementContract.ts exports all six symbols. All 32 shared-fixture cases pass
    to 1e-9 tolerance. All 10 rejection partitions return ok=false. package.json test
    script updated.
  </done>
</task>

<task type="auto" id="09-02-02-09-02-03">
  <name>Task 2 (09-02-02, 09-02-03): Integrate contract into RosbridgeDataSource and add tests</name>
  <files>
    rehab-robotics-studio/src/data/RosbridgeDataSource.ts
    rehab-robotics-studio/src/data/RosbridgeDataSource.test.ts
  </files>
  <action>
    Modify rehab-robotics-studio/src/data/RosbridgeDataSource.ts:

    IMPORTS — add at the top:
      import {
        validateSensorConfig,
        accelCountToMps2,
        gyroCountToRad_s,
        type SensorConfig,
      } from './measurementContract';

    TYPE — extend RawEspMessage to accept sensor_config:
      type RawEspMessage = {
        topic_schema?: string;
        time_us?: number;
        sensor_config?: unknown;
        imu?: Partial<Record<'ax'|'ay'|'az'|'gx'|'gy'|'gz', number>>;
        quat?: Partial<Record<'qw'|'qx'|'qy'|'qz', number>>;
      };

    REMOVE the old fixed-constant lines:
      const ACC_SCALE = 9.80665 / 16384;
      const GYRO_SCALE = (Math.PI / 180) / 131.072;
    Keep GRAVITY, QUAT_SCALE, CALIBRATION_WINDOW_SECONDS, STILL_RANGE_RADIANS,
    ANGLE_CUTOFF_HZ, REST_DEADBAND_RADIANS unchanged (they are used elsewhere).

    REWRITE frameFromRaw to accept a pre-validated SensorConfig as a second
    parameter and use accelCountToMps2/gyroCountToRad_s instead of fixed scales:
      function frameFromRaw(raw: RawEspMessage, config: SensorConfig): Frame | null {
        if (raw.topic_schema !== 'oe_esp32.raw.v1') return null;
        const t = numeric(raw.time_us) / 1_000_000 || performance.now() / 1_000;
        const imu: ImuData = {
          accel: [
            accelCountToMps2(numeric(raw.imu?.ax), config),
            accelCountToMps2(numeric(raw.imu?.ay), config),
            accelCountToMps2(numeric(raw.imu?.az), config),
          ],
          gyro: [
            gyroCountToRad_s(numeric(raw.imu?.gx), config),
            gyroCountToRad_s(numeric(raw.imu?.gy), config),
            gyroCountToRad_s(numeric(raw.imu?.gz), config),
          ],
          quat: [...],  // unchanged Q15 conversion
          t,
        };
        return { t, imu, force: ..., emg: ..., motor: ... };  // same as before
      }

    REWRITE frameFromPair to accept pre-validated frames (Frame) instead of
    raw messages — since handleMessage will now validate and convert each device
    independently before calling pair math:
      function frameFromPair(
        masterFrame: Frame,
        slaveFrame: Frame | undefined,
        stabilizer: RelativeAngleStabilizer,
      ): Frame
      (remove the inner frameFromRaw calls — the caller now passes already-converted Frames)
      If slaveFrame is undefined, return masterFrame directly.
      Otherwise apply the existing inclination + stabilizer + gyro difference math
      using the pre-converted frame values.

    CLASS CHANGES to RosbridgeDataSource:

    1. Add new instance fields:
         private _warnedScale = false;
         private masterFrame: Frame | null = null;   // replaces masterRaw for pair math
         private slaveFrame: Frame | null = null;    // replaces slaveRaw for pair math
       Keep masterRaw and slaveRaw fields if pair health still needs them;
       otherwise remove if only pair math needed them.

    2. Add new constructor parameter:
         private readonly onWarnScaleMissing?: (deviceList: string) => void,
       Add it as the last optional parameter after onPairHealth.

    3. In start(), reset all connection state before creating the new WebSocket:
         this._warnedScale = false;
         this.masterFrame = null;
         this.slaveFrame = null;
         this.masterRaw = null;   // if kept
         this.slaveRaw = null;    // if kept
         this.receivedFrame = false;
         this.angleStabilizer.reset();

    4. Rewrite handleMessage's raw-frame branch:
         // Parse topic role
         const isMaster = envelope.topic === this.masterTopic;
         const isSlave  = envelope.topic === this.slaveTopic;
         if (!isMaster && !isSlave) return;

         const raw = JSON.parse(envelope.msg.data) as RawEspMessage;
         const role = isMaster ? 'MASTER' : 'SLAVE';

         // Validate sensor_config before any caching
         const configResult = validateSensorConfig(raw.sensor_config);
         if (!configResult.ok) {
           if (!this._warnedScale) {
             this._warnedScale = true;
             this.onWarnScaleMissing?.(role);
           }
           return;   // Do NOT update masterFrame/slaveFrame; do NOT emit
         }

         // Convert independently from validated config
         const frame = frameFromRaw(raw, configResult.value);
         if (!frame) return;

         if (isMaster) this.masterFrame = frame;
         if (isSlave)  this.slaveFrame  = frame;

         // Pair math on independently converted frames
         const paired = frameFromPair(
           this.masterFrame ?? frame,
           isMaster ? this.slaveFrame ?? undefined : this.masterFrame ? this.slaveFrame ?? undefined : undefined,
           this.angleStabilizer,
         );
         // Simplified: emit master frame solo if no slave, pair if both available
         const emission = (isMaster && this.masterFrame !== null)
           ? frameFromPair(this.masterFrame, this.slaveFrame ?? undefined, this.angleStabilizer)
           : (isSlave && this.masterFrame !== null)
             ? frameFromPair(this.masterFrame, this.slaveFrame ?? undefined, this.angleStabilizer)
             : frame;

         if (!this.receivedFrame) {
           this.receivedFrame = true;
           this.onFrameReceived?.();
         }
         this.listeners.forEach((listener) => listener(emission));

       Note on pair logic: emit whenever we have a valid master (with or without slave).
       Rewrite this section cleanly to avoid the nested ternary shown above.
       The correct logic:
         - After updating masterFrame or slaveFrame with the valid converted frame,
           if masterFrame is non-null, emit frameFromPair(masterFrame, slaveFrame ?? undefined, stabilizer).
         - If masterFrame is null and we just set slaveFrame, emit nothing (slave without master is not useful).

    5. FIX the service name in requestImuControl:
       Change '/esp_master/set_parameters' to '/esp_bridge_master/set_parameters'.
       For accel_range_g and gyro_range_dps specifically (DATA-01), the confirmed
       value must match both master and slave firmware. Extend requestImuControl for
       these two parameter names to call both master and slave services:
         If name === 'accel_range_g' or name === 'gyro_range_dps':
           Call '/esp_bridge_master/set_parameters' AND '/esp_bridge_slave/set_parameters'
           sequentially (or in parallel with Promise.all).
           Report success: true only when both succeed.
           Report success: false with a combined message if either fails.
           If master succeeds and slave fails, attempt to restore master to the
           prior value (compensating request). Log the outcome regardless.
         For all other ImuControlParameter values (sample_rate_hz, filter_enabled,
         effective_sample_rate_hz), call only '/esp_bridge_master/set_parameters'
         (these propagate via ESP-NOW without a separate slave ACK requirement).

    Create rehab-robotics-studio/src/data/RosbridgeDataSource.test.ts using
    node:test and node:assert/strict:

    The test file must NOT import from DOM or React. All WebSocket and callback
    interactions are tested using minimal stubs.

    Create a helper makeMinimalRaw(role: 'master'|'slave', accelRange=8, gyroRange=2000):
      Builds a valid raw RawEspMessage JSON string with a correct sensor_config object.
      Uses config_as_json values from the corresponding ACCEL_LSB_PER_G/GYRO_LSB_PER_DPS.

    Create a helper makeBadRaw(kind: string): string
      Returns JSON strings for each rejection partition:
        'missing_sensor_config' → raw without sensor_config key
        'unsupported_range'     → raw with accel_range_g=3
        'zero_sensitivity'      → raw with accel_lsb_per_g=0
        'nan_sensitivity'       → raw with accel_lsb_per_g=NaN (serialize as null since JSON can't encode NaN — or string; validator must reject)
        'range_mismatch'        → raw with accel_range_g=8, accel_lsb_per_g=16384

    Test group: 'RosbridgeDataSource — 09-02-02 warning/cache/emission'

    test '09-02-02 invalid frame: no emission, no onFrameReceived, one WARN':
      Create a RosbridgeDataSource stub without a real WebSocket. Inject a fake
      handleMessage call with a malformed raw (makeBadRaw('missing_sensor_config'))
      for the master topic. Assert:
        - onFrameReceived was NOT called
        - onWarnScaleMissing was called exactly once with 'MASTER'
        - masterFrame remains null

    test '09-02-02 subsequent invalid frames produce no additional WARN':
      Call handleMessage three times with invalid master frames on the same connection.
      Assert onWarnScaleMissing was called exactly once total.

    test '09-02-02 valid frame after invalid: emission resumes, no success callback':
      Call handleMessage with invalid frame (WARN fires), then with valid frame.
      Assert listeners received exactly one frame from the valid call.
      Assert onWarnScaleMissing was called exactly once (not again on valid).

    test '09-02-02 invalid slave does not emit using cached valid master':
      Call handleMessage with valid master frame (emits master solo frame).
      Then call handleMessage with invalid slave frame.
      Assert listeners received exactly one frame total (the valid master solo).
      Assert onWarnScaleMissing was called once with 'SLAVE'.

    test '09-02-02 new connection resets latch and caches':
      Simulate a start() call (or directly invoke the reset logic) after WARN has
      fired. Assert _warnedScale is false, masterFrame is null, slaveFrame is null.
      Then send a valid frame and confirm emission resumes.

    test '09-02-02 both roles valid: pair frame emitted after both arrive':
      Send valid master, then valid slave. Assert two emissions (one solo master,
      one pair). Alternatively assert that when both are present the latest emission
      reflects pair math (gyro difference is non-zero when the two gyro channels differ).

    Test group: 'RosbridgeDataSource — 09-02-03 service names and ACK'

    test '09-02-03 requestImuControl sends to correct master service path':
      Create a RosbridgeDataSource with a fake WebSocket that captures outgoing JSON.
      Call requestImuControl('sample_rate_hz', 100).
      Assert the sent JSON's service field is '/esp_bridge_master/set_parameters'.
      Assert it is NOT '/esp_master/set_parameters'.

    test '09-02-03 accel_range_g sends to both master and slave services':
      Capture all outgoing messages. Call requestImuControl('accel_range_g', 8).
      Assert two service call messages are sent, one targeting
      '/esp_bridge_master/set_parameters' and one targeting
      '/esp_bridge_slave/set_parameters'.

    test '09-02-03 both-service success reported as success':
      Fake WebSocket resolves both service calls as successful.
      Assert requestImuControl('accel_range_g', 8) resolves with success=true.

    test '09-02-03 slave rejection reported as failure (master stays confirmed)':
      Fake WebSocket resolves master as successful and slave as failed.
      Assert requestImuControl('accel_range_g', 8) resolves with success=false.
      Assert the compensating call to restore master to prior value was attempted
      (check for a third outgoing service message targeting master with the old value).
  </action>
  <verify>
    <automated>
      cd "C:\Users\justi\Documents\# Rehab Robotics GUI Prototype__Here's a complete, single-file React + TypeScript app implementing the LabVIEW\rehab-robotics-studio" &amp;&amp; npm exec -- tsx --test src/data/measurementContract.test.ts src/data/RosbridgeDataSource.test.ts
    </automated>
  </verify>
  <done>
    All 09-02-02 and 09-02-03 tests pass. RosbridgeDataSource validates sensor_config
    before caching. Warning fires exactly once per connection. Caches clear on new
    start(). Service name is /esp_bridge_master/set_parameters. Range changes for
    accel_range_g and gyro_range_dps call both master and slave services.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rosbridge WebSocket → handleMessage | Raw JSON string from network; sensor_config field is untrusted |
| validateSensorConfig → frame cache | Validation result gates whether masterFrame/slaveFrame is updated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | validateSensorConfig | mitigate | Reject all non-finite, non-positive, unsupported-enum, and range/sensitivity-mismatched values before conversion or caching |
| T-09-02 | Tampering / DoS | accelCountToMps2 / gyroCountToRad_s | accept | Inputs are integer counts from validated bridge source; NaN/Infinity is prevented by SensorConfig validation |
| T-09-03 | DoS | onWarnScaleMissing callback | mitigate | Boolean _warnedScale latch per WebSocket connection; fires at most once; reset only when start() creates a new socket |
| T-09-04 | Tampering | masterFrame/slaveFrame across connections | mitigate | Reset masterFrame, slaveFrame, _warnedScale, and angleStabilizer at start of every start() before new WebSocket creation |
| T-09-SC | Tampering | No npm package installs | accept | Plan 02 installs no new packages; tsx and node:test are already in devDependencies |
</threat_model>

<verification>
After both tasks pass:

Run full frontend test suite from rehab-robotics-studio/:
  npm test
  (now runs src/deployment/*.test.ts AND src/data/*.test.ts)

Run TypeScript type check:
  npm run typecheck

Both must pass with zero errors.
</verification>

<success_criteria>
1. measurementContract.ts exports SensorConfig, ACCEL_LSB_PER_G, GYRO_LSB_PER_DPS,
   validateSensorConfig, accelCountToMps2, gyroCountToRad_s.
2. measurementContract.test.ts: all 32 shared-fixture cases pass to 1e-9; all 10
   rejection partitions return ok=false.
3. RosbridgeDataSource.test.ts: all 09-02-02 warning/cache/emission tests pass.
4. RosbridgeDataSource.test.ts: all 09-02-03 service-name and ACK tests pass.
5. npm test passes (all deployment and data tests green).
6. npm run typecheck: zero errors.
7. RosbridgeDataSource uses /esp_bridge_master/set_parameters (not /esp_master/).
8. accel_range_g and gyro_range_dps changes call both master and slave services.
</success_criteria>

<output>
Create .planning/phases/09-range-correct-measurement-contract/09-02-SUMMARY.md when done.
</output>
