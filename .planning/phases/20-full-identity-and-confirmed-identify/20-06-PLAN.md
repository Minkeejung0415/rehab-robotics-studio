---
phase: 20-full-identity-and-confirmed-identify
plan: "06"
type: execute
wave: 5
depends_on: ["20-05"]
files_modified: []
autonomous: true
requirements: [ID-01, ID-03]
must_haves:
  truths:
    - "The complete local regression and both official XIAO ESP32S3 firmware compiles pass without modifying source or the dirty worktree."
    - "Deterministic fixtures cover one Master plus at least two peer identities, low-32 collisions, wrong-target and ACK failures, and unchanged acquisition/recording state."
    - "Physical one-target blink, non-target stillness, 1/3/5-second timing, prior-state restoration, and live-work continuity remain explicitly human-needed until observed."
  artifacts:
    - path: ".planning/phases/20-full-identity-and-confirmed-identify/20-06-SUMMARY.md"
      provides: "Final automated evidence, source hashes, and explicit pending HUMAN-UAT record"
  key_links:
    - from: "focused local test suites and official firmware builds"
      to: "20-06-SUMMARY.md"
      via: "captured commands, results, source hashes, and evidence classification"
      pattern: "automated|human_needed|HUMAN-UAT"
---

<objective>
Finish Phase 20 with an autonomous local verification gate and a verifier-harvested physical UAT request.

Purpose: Deliver all software, documentation, and deterministic proof now without silently auto-approving evidence that requires deployed hardware.
Output: A final summary of passing local evidence and an explicit HUMAN-UAT item for one-target LED behavior; Phase 25 retains final fleet capacity/promotion ownership.
</objective>

<execution_context>
@C:/Users/justi/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/justi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-full-identity-and-confirmed-identify/20-05-SUMMARY.md
@docs/stepesp-identity-identify.md
@backend/test/test_stepesp_firmware_topology.py
@backend/test/test_stepesp_udp_relay.py
@backend/test/test_esp32_controls.py
@.planning/phases/20-full-identity-and-confirmed-identify/20-CONTEXT.md

<worktree_rules>
This plan is verification-only. Inspect `git status --short` before and after commands, do not edit source or tests, do not switch branches, and do not stage user files. Record commands, results, and hashes only in the required plan summary.
</worktree_rules>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the final local gate and surface physical Identify as HUMAN-UAT</name>
  <files>None; create only the required 20-06-SUMMARY.md execution record</files>
  <read_first>
    - `20-05-SUMMARY.md` — final cross-layer test and runbook results.
    - `docs/stepesp-identity-identify.md` — exact local commands and one-target physical observation checklist.
    - All three focused test files — test names covering inventory, binding, targeting, timing-state, and phase-boundary behavior.
    - `20-CONTEXT.md` — software scope, physical evidence boundary, and Phase 25 capacity/promotion ownership.
  </read_first>
  <action>Capture the pre-run dirty-worktree status and hashes for the firmware sketches, relay, bridge, interfaces, tests, launcher, and runbook. Run the full focused test matrix and compile both firmware roles with the official XIAO ESP32S3 FQBN. Confirm test output includes one self plus at least two peers, low-32 collision safety, self-only binding, count/terminator failures, exact-target unicast, wrong target, duplicate/lost/late ACK, all seven outcomes, guarded GPIO configuration, prior-state restoration logic, acquisition/recording non-mutation, and absence of Phase 20 per-MAC publisher lifecycle. Capture post-run status and prove no source/test file changed. In the summary, classify these as automated software/configuration evidence only. Do not perform, infer, or auto-approve physical observations. The `<human-check>` below is intentionally left for execute-phase verification to report as `human_needed` and create HUMAN-UAT. Phase 25 remains the final Master-plus-multiple-Slave capacity and default-promotion gate.</action>
  <verify>
    <automated>python -m unittest backend.test.test_stepesp_firmware_topology backend.test.test_stepesp_udp_relay backend.test.test_esp32_controls -v; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; arduino-cli compile --fqbn esp32:esp32:XIAO_ESP32S3 firmware/step_node; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; arduino-cli compile --fqbn esp32:esp32:XIAO_ESP32S3 firmware/step_node_slave</automated>
    <human-check>On the official XIAO ESP32S3 hardware, with at least one other device powered as a non-target witness, select exactly one full-MAC target and observe 1 s, 3 s, and 5 s Identify runs: only the selected onboard LED blinks, durations are bounded, the exact prior LED state restores, and repeating Identify during streaming and SD recording does not stop or alter either operation. Record the target/non-target IDs, firmware hash, observed timing/restoration, and continuity evidence in HUMAN-UAT; do not treat missing observation as an automated pass.</human-check>
  </verify>
  <acceptance_criteria>
    - `[automated]` All focused suites and both official firmware compiles pass with captured commands/results.
    - `[coverage]` Local evidence covers the required Master-plus-two-peer inventory, collision, malformed protocol, targeting, outcome, and live-state invariants.
    - `[preservation]` Pre/post status and hashes show the verification run changed no source, tests, or unrelated dirty-worktree content.
    - `[human-needed]` Physical blink, non-target stillness, timing, restoration, and live-work observations are emitted as pending HUMAN-UAT, never silently approved.
    - `[phase boundary]` The summary makes no Phase 20 per-MAC publisher or fleet-capacity claim; Phase 21 and Phase 25 ownership remains explicit.
  </acceptance_criteria>
  <done>All autonomous Phase 20 evidence is complete, and the remaining physical observation is explicitly handed to HUMAN-UAT.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Automated fixtures/compiles -> acceptance summary | Local evidence must not be promoted into claims about physical LED behavior. |
| Human hardware observation -> HUMAN-UAT | Physical evidence must remain explicit, attributable, and non-auto-approvable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-23 | Repudiation | final automated evidence | high | mitigate | Record exact commands, results, source hashes, and pre/post worktree state in the summary. |
| T-20-24 | Tampering | physical acceptance status | high | mitigate | Use `<verify><human-check>` on an auto task so verifier emits `human_needed`; no checkpoint or auto-chain can approve it. |
| T-20-25 | Denial of service | live acquisition/recording | high | mitigate | Static/dynamic local invariants run automatically; physical continuity remains a specific HUMAN-UAT observation. |
| T-20-26 | Elevation of privilege | milestone promotion | high | mitigate | Reserve fleet-capacity and default-promotion claims for Phase 25. |
| T-20-SC | Tampering | package/tool installs | low | accept | No install is authorized. |
</threat_model>

<verification>The executor completes all software tests and official builds autonomously. The verifier, not an auto-chain checkpoint, records the physical LED/timing/restoration/continuity check as `human_needed` and creates HUMAN-UAT.</verification>
<success_criteria>Phase 20 software and documentation are locally proven, the dirty worktree is preserved, and every remaining physical claim is visible as pending human UAT rather than silently approved.</success_criteria>
<output>Create `.planning/phases/20-full-identity-and-confirmed-identify/20-06-SUMMARY.md` with automated evidence and the explicit `human_needed` item.</output>
