---
phase: 01-block-wire-selection-deletion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useKeyboardDelete.ts
  - src/components/canvas/GraphCanvas.tsx
autonomous: true
requirements:
  - GRAPH-01
  - GRAPH-02
user_setup: []
must_haves:
  truths:
    - "GRAPH-01: User can click a block, press Delete or Backspace, and the block disappears."
    - "GRAPH-01: Deleting a selected block also removes all connected wires."
    - "GRAPH-02: User can click a wire and see it highlight as the selected wire."
    - "GRAPH-02: User can press Delete or Backspace after selecting a wire and the wire is removed."
    - "Clicking empty canvas space clears selected block and selected wire state."
    - "Delete and Backspace do not delete graph items while focus is inside INPUT, TEXTAREA, or SELECT."
  artifacts:
    - path: "src/hooks/useKeyboardDelete.ts"
      provides: "Global Delete/Backspace shortcut for selected blocks and wires"
      exports:
        - "useKeyboardDelete"
      contains: "useGraphStore.getState()"
    - path: "src/components/canvas/GraphCanvas.tsx"
      provides: "Wire selection wiring and keyboard-delete hook mount"
      contains: "useKeyboardDelete()"
  key_links:
    - from: "src/hooks/useKeyboardDelete.ts"
      to: "src/state/graphStore.ts"
      via: "imperative Zustand state read inside keydown handler"
      pattern: "useGraphStore\\.getState\\(\\)"
    - from: "src/components/canvas/GraphCanvas.tsx"
      to: "src/components/canvas/Wire.tsx"
      via: "selected and onClick props passed to each Wire"
      pattern: "selected=\\{edge\\.id === selectedEdgeId\\}"
---

<objective>
Implement block and wire selection deletion as a single canvas interaction slice.

Purpose: Phase 1 makes the existing graph canvas editable at the most basic level: selected blocks and wires can be removed with standard keyboard keys, and wire selection becomes visible and actionable.
Output: A new keyboard-delete hook mounted by the canvas, plus GraphCanvas wiring that selects and highlights wires using the existing Zustand store and Wire component props.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-block-wire-selection-deletion/01-RESEARCH.md

@src/state/graphStore.ts
@src/components/canvas/GraphCanvas.tsx
@src/components/canvas/Wire.tsx
@src/components/canvas/BlockNode.tsx
@src/styles/app.css
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add keyboard deletion hook</name>
  <files>src/hooks/useKeyboardDelete.ts</files>
  <read_first>src/state/graphStore.ts, src/components/canvas/GraphCanvas.tsx, .planning/phases/01-block-wire-selection-deletion/01-RESEARCH.md</read_first>
  <action>Create `src/hooks/useKeyboardDelete.ts`. Export `useKeyboardDelete(): void`. Inside a `useEffect`, add a `window` `keydown` listener and remove it during cleanup. The handler must ignore keys other than `Delete` and `Backspace`; must ignore events whose target tag is `INPUT`, `TEXTAREA`, or `SELECT`; must read `selectedId`, `selectedEdgeId`, `removeNode`, and `removeEdge` from `useGraphStore.getState()` at keydown time; must call `e.preventDefault()` only when a selected block or selected wire will actually be removed; must prefer node deletion when `selectedId` is truthy and otherwise delete `selectedEdgeId` when truthy. Do not add dependencies or new store methods.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - `src/hooks/useKeyboardDelete.ts` exports `useKeyboardDelete`.
    - The hook contains `window.addEventListener('keydown', handler)` and removes the same handler in cleanup.
    - The handler contains guards for `Delete`, `Backspace`, `INPUT`, `TEXTAREA`, and `SELECT`.
    - The handler reads selection using `useGraphStore.getState()` rather than closing over reactive selector values.
    - The handler calls `removeNode(selectedId)` for selected blocks and `removeEdge(selectedEdgeId)` for selected wires.
  </acceptance_criteria>
  <done>Keyboard deletion logic exists, is input-safe, and passes TypeScript.</done>
</task>

<task type="auto">
  <name>Task 2: Mount deletion hook and wire wire selection</name>
  <files>src/components/canvas/GraphCanvas.tsx</files>
  <read_first>src/components/canvas/GraphCanvas.tsx, src/components/canvas/Wire.tsx, src/state/graphStore.ts, src/styles/app.css</read_first>
  <action>Import `useKeyboardDelete` from `../../hooks/useKeyboardDelete` and call `useKeyboardDelete()` once inside `GraphCanvas`. Add Zustand selectors for `selectedEdgeId` and `selectEdge`. On the `<svg className="wire-layer">`, add `style={{ pointerEvents: 'all' }}` so SVG wire clicks are not blocked by the existing `.wire-layer { pointer-events: none }` CSS. In each `<Wire>` render, pass `selected={edge.id === selectedEdgeId}` and `onClick={selectEdge}`. Keep the existing block selection, movement bounds, canvas background deselect, and wire geometry unchanged.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - `GraphCanvas.tsx` imports and calls `useKeyboardDelete()`.
    - `GraphCanvas.tsx` reads `selectedEdgeId` and `selectEdge` from `useGraphStore`.
    - The wire SVG has `style={{ pointerEvents: 'all' }}`.
    - Each `Wire` receives `selected={edge.id === selectedEdgeId}` and `onClick={selectEdge}`.
    - Existing `select(null)` canvas background behavior remains present so empty canvas clicks clear selection.
  </acceptance_criteria>
  <done>Wire clicks select/highlight wires, keyboard deletion is mounted while the canvas is mounted, and TypeScript passes.</done>
</task>

<task type="auto">
  <name>Task 3: Verify Phase 1 behavior</name>
  <files>src/hooks/useKeyboardDelete.ts, src/components/canvas/GraphCanvas.tsx</files>
  <read_first>package.json, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</read_first>
  <action>Run `npm run typecheck` after implementation. Start the app with `npm run dev` if manual browser verification is needed. In the browser, verify: selecting a block and pressing Delete removes it; connected wires disappear with the deleted block; selecting a wire highlights it; pressing Delete or Backspace removes the selected wire; clicking empty canvas clears selection; Delete/Backspace in form fields does not remove graph items.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - `npm run typecheck` exits successfully.
    - GRAPH-01 is manually verified: selected block deletion works and connected wires are pruned.
    - GRAPH-02 is manually verified: wire click selection highlights the wire, and keyboard deletion removes it.
    - Empty canvas click deselection remains working for both blocks and wires.
    - Keyboard deletion is suppressed while typing in INPUT, TEXTAREA, or SELECT.
  </acceptance_criteria>
  <done>All automated and manual Phase 1 verification checks pass.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck` succeeds.
- [ ] Click an existing block, press Delete, and confirm the block disappears.
- [ ] Delete a block with connected wires and confirm no orphaned wires remain.
- [ ] Click an existing wire and confirm it highlights blue/thicker.
- [ ] Press Delete or Backspace after selecting a wire and confirm the wire disappears.
- [ ] Click empty canvas space and confirm selected block or wire is cleared.
- [ ] Focus an INPUT, TEXTAREA, or SELECT if present and confirm Delete/Backspace does not delete graph items.
</verification>

<success_criteria>
- GRAPH-01 is covered by a working selected-block keyboard deletion path.
- GRAPH-02 is covered by clickable, highlighted, keyboard-deletable wires.
- The implementation uses existing `graphStore` actions and does not introduce new dependencies.
- `npm run typecheck` passes after implementation.
- A summary is written to `.planning/phases/01-block-wire-selection-deletion/01-01-SUMMARY.md` after execution.
</success_criteria>

<output>
After completion, create `.planning/phases/01-block-wire-selection-deletion/01-01-SUMMARY.md`
</output>
