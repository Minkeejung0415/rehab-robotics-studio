---
phase: 04-runtime-feedback-deploy-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/components/common/Toast.tsx
  - src/styles/app.css
  - src/components/chrome/Toolbar.tsx
autonomous: false
requirements:
  - RT-02
  - DEP-01
must_haves:
  truths:
    - "Toolbar shows Rec after Stop and before the Validate separator; labels toggle ○ Rec / ● Rec"
    - "Clicking Rec toggles systemStore recording so the status-strip Recording light shows On (fault) / Off (idle)"
    - "Rec is disabled when runtime is estopped or fault; Rec On uses fault-family pressed chrome"
    - "Deploy Mock still logs Deploy (mock) — graph would be pushed to Jetson and also shows the locked toast copy"
    - "Toast is a single top-center portal banner, auto-dismisses at 2500ms, click-to-dismiss, replace-on-retrigger"
  artifacts:
    - path: "src/components/common/Toast.tsx"
      provides: "Hand-rolled portal toast with status role and dismiss"
      exports: ["Toast"]
    - path: "src/styles/app.css"
      provides: ".toast and .btn-rec-on LabVIEW chrome"
      contains: ".toast"
    - path: "src/components/chrome/Toolbar.tsx"
      provides: "Rec button + Deploy toast host wiring"
      contains: "Rec"
  key_links:
    - from: "Toolbar Rec"
      to: "systemStore.setRecording"
      via: "onClick toggle"
      pattern: "setRecording"
    - from: "Toolbar Deploy Mock"
      to: "actions.deployMock + Toast"
      via: "onClick shows locked toast message"
      pattern: "deployMock"
    - from: "Toast"
      to: "document.body"
      via: "createPortal"
      pattern: "createPortal"
---

<objective>
Ship RT-02 and DEP-01: toolbar Recording toggle wired to the existing status-strip indicator, plus a hand-rolled Deploy Mock toast confirmation (in addition to the existing log entry).

Purpose: Complete Phase 4 user-visible feedback on top of Plan 01 badge sync.
Output: Toast component + CSS, Rec button, Deploy toast host in Toolbar; browser-verified checklist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-runtime-feedback-deploy-polish/04-CONTEXT.md
@.planning/phases/04-runtime-feedback-deploy-polish/04-RESEARCH.md
@.planning/phases/04-runtime-feedback-deploy-polish/04-UI-SPEC.md
@.planning/phases/04-runtime-feedback-deploy-polish/04-01-PLAN.md
@.planning/phases/03-context-menu-block-management/03-02-SUMMARY.md

<interfaces>
From Plan 01 / runtime (already done — do not redo):
- setAllNodeStatuses wired from runtimeStore for RT-01

From src/state/systemStore.ts:
- `setRecording(on: boolean): void` — sets recording.value On/Off and level fault/idle
- StatusStrip already renders status.recording

From src/state/actions.ts:
- `deployMock()` logs exactly: `Deploy (mock) — graph would be pushed to Jetson` — keep this string; toast is additional

From src/components/common/ContextMenu.tsx (analog):
- `createPortal(..., document.body)`, fixed positioning, z-index 1000

From src/components/chrome/Toolbar.tsx (current order):
- Run, Pause, Stop, sep, Validate, Deploy Mock, Save, Load, … E-STOP
- `blocked = state === 'estopped' || state === 'fault'`

Locked copy (UI-SPEC):
- Toast: `Deploy (mock) started — graph would be pushed to Jetson`
- Rec Off: `○ Rec` / Rec On: `● Rec` / title: `Toggle recording`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Toast component and chrome CSS</name>
  <files>src/components/common/Toast.tsx, src/styles/app.css</files>
  <read_first>
    - src/components/common/ContextMenu.tsx
    - src/styles/app.css (context-menu and btn-estop rules)
    - src/theme/tokens.ts
    - .planning/phases/04-runtime-feedback-deploy-polish/04-UI-SPEC.md (Deploy toast + Color/Spacing)
    - .planning/phases/04-runtime-feedback-deploy-polish/04-RESEARCH.md (Pattern 4–5)
    - .planning/phases/04-runtime-feedback-deploy-polish/04-CONTEXT.md
  </read_first>
  <action>
  Implement DEP-01 overlay shell (hand-rolled, no new npm deps — per CONTEXT / RESEARCH Pattern 4). Analog: ContextMenu portal.

  1. Create `src/components/common/Toast.tsx` exporting `Toast` with props `{ message: string; onDismiss: () => void }`:
     - `createPortal` to `document.body`
     - Root: `role="status"`, `aria-live="polite"`, class `toast`
     - Position via CSS (top-center); z-index 1100
     - onClick on the toast root calls `onDismiss` (click-to-dismiss)
     - useEffect: start 2500ms timer → onDismiss; clear on unmount / message change (restart when message identity changes — parent replaces by remounting or keying)
     - Optional Escape → onDismiss (recommended in UI-SPEC; not required for acceptance)
     - Render `message` as React text children only — never dangerouslySetInnerHTML (per D-toast / T-4-04)
     - Do not hardcode the deploy string inside Toast — parent passes message

  2. Append CSS to `src/styles/app.css` per UI-SPEC exactly:
     - `.toast`: position fixed; top 24px; left 50%; transform translateX(-50%); z-index 1100; max-width 480px; padding 8px 12px; border 1px solid #30383d; border-radius 0; background #1a1f23; box-shadow 0 8px 18px rgba(0,0,0,0.25); color #dfe6ea; font-size 12px; font-weight 400; font-family ui-monospace / Cascadia Code / monospace; cursor pointer
     - `.btn-rec-on`: border-color #7a3030 (or #ec5a5a); background #3a2020; color #ffd9d9 — match `.btn-estop` family for Rec pressed chrome (RT-02)
  3. No package.json changes.
  </action>
  <acceptance_criteria>
    - File `src/components/common/Toast.tsx` exists and exports `Toast`
    - `grep -n "createPortal\\|role=\\"status\\"\\|aria-live\\|2500\\|onDismiss" src/components/common/Toast.tsx` shows portal, a11y, timer, dismiss
    - `grep -n "\\.toast\\|btn-rec-on" src/styles/app.css` shows toast and Rec-on rules
    - No package.json dependency changes
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Reusable portal Toast and Rec/toast CSS chrome exist without new dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Rec button and Deploy toast in Toolbar</name>
  <files>src/components/chrome/Toolbar.tsx</files>
  <read_first>
    - src/components/chrome/Toolbar.tsx
    - src/components/common/Toast.tsx
    - src/state/systemStore.ts
    - src/state/actions.ts
    - .planning/phases/04-runtime-feedback-deploy-polish/04-CONTEXT.md
    - .planning/phases/04-runtime-feedback-deploy-polish/04-UI-SPEC.md (RT-02, DEP-01, Copywriting)
    - .planning/phases/04-runtime-feedback-deploy-polish/04-RESEARCH.md (Patterns 3–4)
  </read_first>
  <action>
  Implement RT-02 and DEP-01 end-user wiring in Toolbar (per CONTEXT Recording Toggle + Deploy Toast; UI-SPEC interaction contract).

  1. Rec button (RT-02):
     - Insert after Stop and **before** the existing separator that precedes Validate (order: Run · Pause · Stop · Rec · sep · Validate · Deploy · …)
     - Subscribe to recording: `useSystemStore` for `status.recording.value === 'On'` (or equivalent) and `setRecording`
     - Label: On → `● Rec`; Off → `○ Rec`; `title="Toggle recording"`
     - onClick: `setRecording(!isRecording)` — immediate, no modal
     - `disabled={blocked}` same as Run/Deploy (`estopped` || `fault`)
     - When On, add class `btn-rec-on` (with base `btn`) for pressed fault chrome

  2. Deploy toast host (DEP-01):
     - Local React state: `toastMessage: string | null` (Toolbar host per RESEARCH discretion)
     - Deploy button onClick: call `actions.deployMock()` (preserve existing log string), then set toast message to exact locked copy `Deploy (mock) started — graph would be pushed to Jetson`
     - Single instance: setting message again while visible replaces content and remounts/restarts 2500ms timer (use a `key` timestamp or clear-then-set pattern)
     - When `toastMessage` non-null, render `<Toast message={toastMessage} onDismiss={() => setToastMessage(null)} />`
     - Do not change `actions.deployMock` log text
     - Do not modify StatusStrip or BlockNode

  3. Do not add npm packages. Do not implement real Jetson deploy or wire-flow animation.
  </action>
  <acceptance_criteria>
    - `grep -n "Rec\\|setRecording\\|btn-rec-on" src/components/chrome/Toolbar.tsx` shows Rec control wired to setRecording with active class
    - `grep -n "Toast\\|deployMock\\|Deploy (mock) started" src/components/chrome/Toolbar.tsx` shows toast host + locked copy + deployMock call
    - Toolbar button order places Rec between Stop and the Validate separator
    - `npm run typecheck && npm run build` exits 0
    - package.json dependencies unchanged (no new toast library)
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <done>Rec toggles strip Recording; Deploy Mock shows toast plus existing log; chrome matches UI-SPEC.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Browser-verify runtime feedback and deploy toast</name>
  <files></files>
  <read_first>
    - .planning/phases/04-runtime-feedback-deploy-polish/04-RESEARCH.md (Browser acceptance checklist)
    - .planning/phases/04-runtime-feedback-deploy-polish/04-UI-SPEC.md
    - .planning/phases/04-runtime-feedback-deploy-polish/04-PLAYWRIGHT-ISSUES.md
  </read_first>
  <action>
  Do not change production code unless a checklist item fails (then fix and re-verify). Run `npm run typecheck && npm run build`, start preview on 127.0.0.1:4173 (avoid Vite `dev` when the path contains `#`), and walk the browser checklist. Record pass/fail in the plan SUMMARY; log Playwright failures in `04-PLAYWRIGHT-ISSUES.md` without auto-fixing until reviewed.
  </action>
  <what-built>
  Plan 01 badge sync (Run/Pause/Stop/E-STOP/Reset) plus Plan 02 Rec toolbar toggle and Deploy Mock portal toast.
  </what-built>
  <how-to-verify>
  Prefer `npm run build && npm run preview -- --host 127.0.0.1 --port 4173`. Then confirm:

  1. Run → every block badge shows `running` (green).
  2. Pause → RUNTIME PAUSED; badges still `running`.
  3. Run again (resume) → RUNTIME RUNNING; badges `running`.
  4. Stop → all badges `idle`.
  5. Run then E-STOP → badges `idle`; Reset → badges `idle`.
  6. `○ Rec` → `● Rec` and strip Recording On (red/fault); toggle back to Off / `○ Rec`.
  7. E-STOP engaged → Rec, Deploy, Run disabled.
  8. Deploy Mock → log `Deploy (mock) — graph would be pushed to Jetson` AND toast `Deploy (mock) started — graph would be pushed to Jetson`.
  9. Toast auto-dismiss ~2.5s; click dismisses; second Deploy replaces (no stack).
  10. Phase 1–3 regression: select/delete, wiring, palette drop, context menu still work.
  </how-to-verify>
  <resume-signal>Type "approved" or list any failing checklist items</resume-signal>
  <acceptance_criteria>
    - User confirms checklist items 1–10 pass, or executor records Playwright/browser evidence covering the same RT-01/RT-02/DEP-01 behaviors
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
    <human-check>Browser checklist in how-to-verify (RT-01, RT-02, DEP-01, regression)</human-check>
  </verify>
  <done>Phase 4 interactive behaviors verified in the browser against ROADMAP success criteria.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → Toolbar / Toast | Button clicks and toast display are client-only; deploy is mock |
| Toast message → DOM | Locked constant string rendered as text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-04 | Tampering | Toast message render | mitigate | Pass locked constant only; React text children; never dangerouslySetInnerHTML |
| T-4-05 | Spoofing | Deploy toast implying real Jetson push | mitigate | Keep "(mock)" in toast and existing log copy; no network/deploy API |
| T-4-06 | Elevation of Privilege (UX) | Rec while estopped/fault | mitigate | Disable Rec when blocked (same gate as Run/Deploy) |
| T-4-07 | Denial of Service (UX) | Stacked or stuck toasts | mitigate | Single-instance replace; 2500ms auto-dismiss; click-to-dismiss |
| T-4-SC | Tampering | npm installs | mitigate | No new packages this plan — skip install; legitimacy N/A |
</threat_model>

<verification>
After Tasks 1–2: `npm run typecheck && npm run build`. Task 3 walks the RESEARCH browser checklist for RT-01/RT-02/DEP-01.
</verification>

<success_criteria>
- Rec toggles strip Recording On/Off with correct labels and disabled/pressed chrome
- Deploy Mock shows locked toast plus unchanged log
- Toast single-instance, 2500ms auto-dismiss, click-to-dismiss
- Typecheck/build green; no new npm dependencies
</success_criteria>

<output>
Create `.planning/phases/04-runtime-feedback-deploy-polish/04-02-SUMMARY.md` when done
</output>
