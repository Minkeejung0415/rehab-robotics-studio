---
phase: 04-runtime-feedback-deploy-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/state/graphStore.ts
  - src/state/runtimeStore.ts
autonomous: true
requirements:
  - RT-01
must_haves:
  truths:
    - "Clicking Run sets every node.status to running (including resume from paused)"
    - "Clicking Pause leaves every node.status as running"
    - "Clicking Stop, E-STOP, or Reset sets every node.status to idle"
    - "Empty canvas bulk status update is a no-op"
  artifacts:
    - path: "src/state/graphStore.ts"
      provides: "setAllNodeStatuses(status) bulk badge writer"
      contains: "setAllNodeStatuses"
    - path: "src/state/runtimeStore.ts"
      provides: "run/resume → running; stop/estop/reset/raiseFault → idle; pause untouched"
      contains: "setAllNodeStatuses"
  key_links:
    - from: "src/state/runtimeStore.ts"
      to: "src/state/graphStore.ts"
      via: "useGraphStore.getState().setAllNodeStatuses"
      pattern: "setAllNodeStatuses"
    - from: "nodes[].status"
      to: "BlockNode .status-badge"
      via: "existing statusColor paint (no BlockNode change this plan)"
      pattern: "status-badge"
---

<objective>
Wire RT-01: add `setAllNodeStatuses` on the graph store and call it from runtimeStore so Run/Stop/E-STOP/Reset (and resume) synchronously update every block status badge.

Purpose: Make canvas badges reflect execution without changing BlockNode paint or Toolbar buttons yet.
Output: graphStore bulk setter + runtimeStore sync on all locked transition paths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-runtime-feedback-deploy-polish/04-CONTEXT.md
@.planning/phases/04-runtime-feedback-deploy-polish/04-RESEARCH.md
@.planning/phases/04-runtime-feedback-deploy-polish/04-UI-SPEC.md
@.planning/phases/03-context-menu-block-management/03-01-SUMMARY.md

<interfaces>
From src/types/blocks.ts:
- `export type BlockStatus = 'idle' | 'running' | 'warning' | 'error'`
- `BlockInstance.status: BlockStatus`

From src/state/graphStore.ts (extend — do not remove Phase 3 APIs):
- GraphStore: nodes, edges, selectedId, selectedIds, select*, renameNode, duplicateNode, removeNodes, …
- Nodes already carry `status: 'idle'` on create/duplicate
- No status setter today

From src/state/runtimeStore.ts (current behavior):
- `run()`: if paused → `resume()`; else idle→running + mockDataSource.start + log
- `pause()`: running→paused; does not touch graph
- `resume()`: paused→running + mockDataSource.resume + log
- `stop()`: → idle + mockDataSource.stop
- `estop()`: → estopped (no can-guard) + motor fault + SAFETY log
- `reset()`: only from estopped/fault → idle
- Cross-store pattern already used: `useSystemStore.getState()`

From src/components/canvas/BlockNode.tsx (do not modify this plan):
- Renders `<span className="status-badge" style={{ color: statusColor[node.status] }}>{node.status}</span>`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add setAllNodeStatuses to graphStore</name>
  <files>src/state/graphStore.ts</files>
  <read_first>
    - src/state/graphStore.ts
    - src/types/blocks.ts
    - .planning/phases/04-runtime-feedback-deploy-polish/04-CONTEXT.md
    - .planning/phases/04-runtime-feedback-deploy-polish/04-RESEARCH.md (Pattern 1)
    - .planning/phases/04-runtime-feedback-deploy-polish/04-UI-SPEC.md (RT-01 table)
  </read_first>
  <action>
  Implement locked CONTEXT Run/Stop badge model and RESEARCH Pattern 1 / Open Question 1 (API name `setAllNodeStatuses` per D-discretion / UI-SPEC).

  1. Import or reuse `BlockStatus` from `../types/blocks` (already used via BlockInstance).
  2. Add to GraphStore interface: `setAllNodeStatuses(status: BlockStatus): void`.
  3. Implement: one `set` that maps `nodes` to `{ ...n, status }` for every node (immutable). Empty `nodes` array is a no-op (per UI-SPEC empty canvas rule). Do not touch edges, selection, pendingWire, or params.
  4. Do not install packages. Do not wire runtimeStore in this task (Task 2).
  </action>
  <acceptance_criteria>
    - `grep -n "setAllNodeStatuses" src/state/graphStore.ts` shows the method on the interface and the create() implementation
    - Implementation maps all nodes' `status` field in a single set()
    - No package.json dependency changes
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>graphStore exports setAllNodeStatuses and can bulk-update every node.status.</done>
</task>

<task type="auto">
  <name>Task 2: Sync runtimeStore transitions to node badges</name>
  <files>src/state/runtimeStore.ts</files>
  <read_first>
    - src/state/runtimeStore.ts
    - src/state/graphStore.ts (after Task 1)
    - .planning/phases/04-runtime-feedback-deploy-polish/04-CONTEXT.md
    - .planning/phases/04-runtime-feedback-deploy-polish/04-RESEARCH.md (Pattern 2, Open Questions 3–4, 9)
    - .planning/phases/04-runtime-feedback-deploy-polish/04-UI-SPEC.md (Run/Stop → block status badges)
  </read_first>
  <action>
  Wire RT-01 from runtime methods using RESEARCH Pattern 2 (per CONTEXT: synchronous bulk setter from runtime actions). Use `useGraphStore.getState().setAllNodeStatuses(...)` — same getState style as existing `useSystemStore` calls. Do not import graphStore into a cycle (graphStore must not import runtimeStore).

  1. After successful idle→running in `run`, call `setAllNodeStatuses('running')`.
  2. In `resume`, after setting state to running, call `setAllNodeStatuses('running')` (re-assert per UI-SPEC / RESOLVED Open Question 3). Because `run` from paused delegates to `resume`, badges are covered.
  3. `pause`: do **not** change node statuses (CONTEXT / Open Question 4).
  4. After successful `stop` → idle: `setAllNodeStatuses('idle')`.
  5. In `estop`: `setAllNodeStatuses('idle')` (always, alongside existing motor/log side effects).
  6. After successful `reset`: `setAllNodeStatuses('idle')`.
  7. In `raiseFault`: also `setAllNodeStatuses('idle')` (RESEARCH Open Question 9 discretion — consistency with estop).
  8. Only call setters on paths that actually change runtime state (respect existing `can()` / early returns so failed transitions do not flip badges).
  9. Do not modify Toolbar, BlockNode, or Toast in this plan.
  </action>
  <acceptance_criteria>
    - `grep -n "setAllNodeStatuses" src/state/runtimeStore.ts` shows calls for running and idle paths
    - `grep -n "pause" src/state/runtimeStore.ts` pause handler does not call setAllNodeStatuses
    - `grep -n "useGraphStore" src/state/runtimeStore.ts` shows getState import usage
    - `npm run typecheck && npm run build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <done>Run/resume set all badges running; stop/estop/reset/raiseFault set idle; pause leaves badges running.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runtime UI → graphStore | Toolbar/runtime actions mutate in-memory node.status only — no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | Tampering | setAllNodeStatuses | mitigate | Accept only BlockStatus union values from typed call sites; never parse free-form user strings into status |
| T-4-02 | Spoofing | running badges implying real hardware | accept | Mock-only product; badges are UI feedback, not a safety interlock |
| T-4-03 | Denial of Service | bulk map on large graphs | accept | In-memory mock graphs are small; no remote amplification |
| T-4-SC | Tampering | npm installs | mitigate | No new packages this plan — skip install; legitimacy N/A |
</threat_model>

<verification>
After both tasks: `npm run typecheck && npm run build`. Confirm setAllNodeStatuses exists and runtimeStore call sites match the RT-01 transition table.
</verification>

<success_criteria>
- setAllNodeStatuses exists on graphStore
- run/resume → all nodes running; pause unchanged; stop/estop/reset/raiseFault → idle
- Typecheck and build pass with no new dependencies
</success_criteria>

<output>
Create `.planning/phases/04-runtime-feedback-deploy-polish/04-01-SUMMARY.md` when done
</output>
