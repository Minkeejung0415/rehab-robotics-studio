---
phase: 02-interactive-wiring-palette-drag-drop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/canvas/Port.tsx
  - src/components/canvas/BlockNode.tsx
  - src/components/canvas/GraphCanvas.tsx
  - src/components/library/LibraryItem.tsx
  - src/components/library/BlockLibrary.tsx
  - src/styles/app.css
autonomous: true
requirements:
  - GRAPH-03
  - GRAPH-04
user_setup: []
must_haves:
  truths:
    - "GRAPH-03: Dragging from an output terminal shows a dashed preview wire that follows the pointer."
    - "GRAPH-03: Releasing a pending wire on a compatible input creates a permanent edge, while empty canvas release and Escape cancel it."
    - "GRAPH-03: Wiring does not start node movement or create an edge to an incompatible input."
    - "GRAPH-04: Dragging a palette block type over the canvas and dropping it creates a selected block near the drop location."
    - "Existing palette plus-button/double-click placement and Phase 1 selection/deletion behavior still work."
  artifacts:
    - path: "src/components/canvas/GraphCanvas.tsx"
      provides: "Pending-wire preview, port-to-port lifecycle wiring, cancellation, and canvas drop handling"
      contains: "PendingWireOverlay"
    - path: "src/components/canvas/Port.tsx"
      provides: "Interactive terminal callbacks with port identity and signal metadata"
    - path: "src/components/library/LibraryItem.tsx"
      provides: "Native drag source carrying a block definition type"
  key_links:
    - from: "src/components/canvas/GraphCanvas.tsx"
      to: "src/state/graphStore.ts"
      via: "startWire, finishWire, and cancelWire actions"
      pattern: "startWire|finishWire|cancelWire"
    - from: "src/components/canvas/GraphCanvas.tsx"
      to: "src/components/canvas/Wire.tsx"
      via: "PendingWireOverlay SVG preview"
      pattern: "PendingWireOverlay"
    - from: "src/components/library/LibraryItem.tsx"
      to: "src/components/library/BlockLibrary.tsx"
      via: "dragged block type is consumed by palette add callback"
      pattern: "onDragStart"
---

<objective>
Implement the Phase 2 graph-building interaction slice.

Purpose: Allow users to create typed graph connections from output terminals and place new block instances exactly where they drop a palette item.
Output: Interactive port callbacks, a canvas-managed dashed wire preview/cancellation path, and native palette drag-drop that uses the existing Zustand graph actions.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-interactive-wiring-palette-drag-drop/02-RESEARCH.md
@.planning/phases/02-interactive-wiring-palette-drag-drop/02-VALIDATION.md
@.planning/phases/01-block-wire-selection-deletion/01-01-SUMMARY.md

@src/state/graphStore.ts
@src/components/canvas/GraphCanvas.tsx
@src/components/canvas/BlockNode.tsx
@src/components/canvas/Port.tsx
@src/components/canvas/Wire.tsx
@src/components/library/BlockLibrary.tsx
@src/components/library/LibraryItem.tsx
@src/graph/blockDefinitions.ts
@src/styles/app.css
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make canvas terminals initiate and finish typed wire interactions</name>
  <files>src/components/canvas/Port.tsx, src/components/canvas/BlockNode.tsx, src/components/canvas/GraphCanvas.tsx, src/styles/app.css</files>
  <read_first>src/state/graphStore.ts, src/components/canvas/Port.tsx, src/components/canvas/BlockNode.tsx, src/components/canvas/GraphCanvas.tsx, src/components/canvas/Wire.tsx, .planning/phases/02-interactive-wiring-palette-drag-drop/02-RESEARCH.md</read_first>
  <action>Extend `Port` and `BlockNode` with narrowly typed callbacks that expose each terminal's `PortDefinition` and stop terminal pointer events from triggering the existing node-move handler. In `GraphCanvas`, select `pendingWire`, `startWire`, `finishWire`, and `cancelWire` from `useGraphStore`; begin a wire only from an output; calculate source and pointer endpoints in canvas coordinates; render `PendingWireOverlay` in the existing SVG while pending; and call `finishWire` only when a matching-signal input terminal receives the release. Register cleanup-safe pointer/mouse move and Escape handling while a wire is pending. Releasing on canvas background, an incompatible input, or leaving the canvas must clear the preview through `cancelWire`. Preserve existing node movement, wire selection, SVG hit targets, and empty-canvas deselection.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - Output terminal drag starts `pendingWire` with semantic block and port IDs.
    - A dashed `PendingWireOverlay` follows the pointer until completion or cancellation.
    - A compatible input release calls `finishWire`; incompatible input, background release, and Escape call `cancelWire` without an edge.
    - Terminal events do not also invoke `BlockNode` node movement.
    - Existing wire selection and empty-canvas deselection remain intact.
  </acceptance_criteria>
  <done>Typed port-to-port interaction is wired to the existing store lifecycle and compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Add native palette drag-drop placement on the graph canvas</name>
  <files>src/components/library/LibraryItem.tsx, src/components/library/BlockLibrary.tsx, src/components/canvas/GraphCanvas.tsx, src/styles/app.css</files>
  <read_first>src/components/library/LibraryItem.tsx, src/components/library/BlockLibrary.tsx, src/components/canvas/GraphCanvas.tsx, src/state/graphStore.ts, src/components/canvas/Wire.tsx</read_first>
  <action>Make `LibraryItem` draggable and publish `def.type` through a named native `dataTransfer` payload while preserving its existing double-click and plus-button behavior (the button itself must not accidentally begin a palette drag). Have `BlockLibrary` pass an add callback that accepts explicit coordinates and retains the existing staggered fallback for click-based additions. Add `dragover`, `dragleave`, and `drop` handling to `GraphCanvas`: prevent the browser default only for supported block payloads, convert the client drop point into the canvas coordinate system using the graph canvas rectangle and scroll offsets, clamp placement with the same bounds as node movement, and invoke the existing `addNode`. Add only restrained CSS state feedback needed to show the valid canvas drop target; do not add dependencies or alter the graph store's data model.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - Palette items expose a draggable block type payload.
    - Dropping that payload on the canvas calls `addNode` with bounded canvas-relative coordinates.
    - The new node is selected by the existing store behavior and appears near the drop point.
    - Palette double-click and plus-button still add blocks using their staggered placement fallback.
    - Unsupported drops do not create a node or leave a persistent drop-target visual state.
  </acceptance_criteria>
  <done>Palette block definitions can be placed by drag-drop without regressing existing add controls.</done>
</task>

<task type="auto">
  <name>Task 3: Verify end-to-end graph construction behavior</name>
  <files>src/components/canvas/GraphCanvas.tsx, src/components/library/LibraryItem.tsx, src/components/library/BlockLibrary.tsx</files>
  <read_first>package.json, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/02-interactive-wiring-palette-drag-drop/02-VALIDATION.md</read_first>
  <action>Run `npm run typecheck` and `npm run build`. Start a production preview because this project path contains `#`, which has made the Vite dev server unreliable. In a browser, verify a dashed preview starts from an output and tracks movement; completing on a compatible input produces one permanent edge; empty-canvas release and Escape cancel without adding an edge; and dragging a palette item onto the visible canvas creates a selected node at the intended location. Also verify existing palette plus/double-click placement and Phase 1 wire selection/deletion remain functional.</action>
  <verify>npm run typecheck && npm run build</verify>
  <acceptance_criteria>
    - `npm run typecheck` and `npm run build` both succeed.
    - GRAPH-03 browser checks pass for preview, compatible completion, and both cancellation paths.
    - GRAPH-04 browser check passes for palette drop position and selected new node.
    - Existing palette add controls and Phase 1 deletion/selection still work.
  </acceptance_criteria>
  <done>Phase 2 requirements have automated build confidence and browser-level acceptance evidence.</done>
</task>

</tasks>

<verification>
- [ ] `npm run typecheck` succeeds after each task.
- [ ] `npm run build` succeeds before phase verification.
- [ ] Drag a source output to a matching input and confirm a permanent wire is created.
- [ ] Confirm the dashed preview follows the cursor while wiring.
- [ ] Release over empty canvas and press Escape in separate attempts; confirm neither adds an edge.
- [ ] Drop a palette item on the canvas and confirm a new selected block appears near the pointer.
- [ ] Confirm palette plus/double-click placement and Phase 1 selection/deletion remain working.
</verification>

<success_criteria>
- GRAPH-03 is implemented using the existing wire lifecycle state and `PendingWireOverlay`.
- GRAPH-04 is implemented through native palette drag-drop using existing `addNode` state behavior.
- No dependencies or graph data-model changes are introduced.
- Typecheck, production build, and browser acceptance checks pass.
</success_criteria>

<output>
After completion, create `.planning/phases/02-interactive-wiring-palette-drag-drop/02-01-SUMMARY.md`.
</output>
