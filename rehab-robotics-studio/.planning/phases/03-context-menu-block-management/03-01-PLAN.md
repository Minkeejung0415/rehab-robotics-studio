---
phase: 03-context-menu-block-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/state/graphStore.ts
  - src/hooks/useKeyboardDelete.ts
  - src/components/canvas/GraphCanvas.tsx
autonomous: true
requirements:
  - CTX-03
  - BLK-01
  - BLK-02
must_haves:
  truths:
    - "selectAll fills selectedIds with every block id and clears wire selection"
    - "select(id) sets primary selectedId and selectedIds to [id]; select(null) clears both block selection fields and edge selection"
    - "selectEdge clears selectedId and selectedIds"
    - "duplicateNode clones type/params, appends ' copy' to name, offsets +40/+40 clamped to canvas, new B{nextId}, no new edges, selects the copy"
    - "renameNode updates node.name in the store so canvas labels can re-render"
    - "Delete/Backspace removes every id in selectedIds (and connected edges) when multi-select is active"
  artifacts:
    - path: "src/state/graphStore.ts"
      provides: "selectedIds, selectAll, renameNode, duplicateNode, removeNodes, selection hygiene"
      contains: "selectedIds"
    - path: "src/hooks/useKeyboardDelete.ts"
      provides: "Multi-select-aware keyboard delete"
      contains: "selectedIds"
    - path: "src/components/canvas/GraphCanvas.tsx"
      provides: "Block selected chrome driven by selectedIds.includes"
      contains: "selectedIds"
  key_links:
    - from: "src/hooks/useKeyboardDelete.ts"
      to: "src/state/graphStore.ts"
      via: "removeNodes(selectedIds) via getState()"
      pattern: "removeNodes"
    - from: "src/components/canvas/GraphCanvas.tsx"
      to: "selectedIds"
      via: "BlockNode selected prop"
      pattern: "selectedIds\\.includes"
---

<objective>
Extend the Zustand graph store with minimal multi-select (`selectedIds`), Select All, rename, duplicate, and batch remove — then wire keyboard Delete and canvas selected chrome to that model.

Purpose: Unlock CTX-03 / BLK-01 / BLK-02 store contracts and make Select All useful with Delete before the context menu UI lands.
Output: Selection-hygienic store APIs + multi-delete keyboard path + multi-select visual chrome on blocks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-context-menu-block-management/03-CONTEXT.md
@.planning/phases/03-context-menu-block-management/03-RESEARCH.md
@.planning/phases/03-context-menu-block-management/03-PATTERNS.md
@.planning/phases/03-context-menu-block-management/03-UI-SPEC.md
@.planning/phases/03-context-menu-block-management/03-VALIDATION.md
@.planning/phases/01-block-wire-selection-deletion/01-01-SUMMARY.md
@.planning/phases/02-interactive-wiring-palette-drag-drop/02-01-SUMMARY.md

<interfaces>
From src/state/graphStore.ts (current — extend, do not replace unrelated APIs):
- GraphStore fields: nodes, edges, selectedId, selectedEdgeId, pendingWire, validationIssues, nextId, nextEdgeId
- select(id: string | null), selectEdge(id: string | null), addNode, removeNode, removeEdge, updateParam, startWire, load
- addNode uses id `B${nextId}` and increments nextId
- removeNode already prunes connected edges
- CANVAS clamp used by GraphCanvas: CANVAS_WIDTH=980, CANVAS_HEIGHT=720, NODE_WIDTH=220; clamp x to [8, 980-220-8], y to [8, 720-140]

From src/hooks/useKeyboardDelete.ts:
- Window keydown Delete/Backspace; ignores INPUT/TEXTAREA/SELECT; currently deletes selectedId then selectedEdgeId

From src/components/canvas/GraphCanvas.tsx:
- selected={node.id === selectedId} must become selectedIds.includes(node.id)
- Empty canvas mousedown already calls select(null) when target === currentTarget
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend graphStore for multi-select, rename, duplicate, batch remove</name>
  <files>src/state/graphStore.ts</files>
  <read_first>
    - src/state/graphStore.ts
    - .planning/phases/03-context-menu-block-management/03-CONTEXT.md
    - .planning/phases/03-context-menu-block-management/03-RESEARCH.md (Pattern 2, Pattern 5, store sketches)
    - .planning/phases/03-context-menu-block-management/03-PATTERNS.md (graphStore section)
  </read_first>
  <action>
  Implement locked CONTEXT decisions D-select-all / D-duplicate / D-rename in `graphStore.ts` (per D-01–D-04 decision groups in 03-CONTEXT). Follow analogs in `03-PATTERNS.md` § `graphStore.ts` (self):

  1. Add `selectedIds: string[]` to state. Initialize from current primary: if `selectedId` is `'B1'`, set `selectedIds: ['B1']`.
  2. Update selection methods (per CONTEXT Select All & Selection Model; PATTERNS “Selection mutual exclusion” / Phase 3 target sketches):
     - `select(id)` → `{ selectedId: id, selectedIds: id ? [id] : [], selectedEdgeId: null }`
     - `selectEdge(id)` → `{ selectedEdgeId: id, selectedId: null, selectedIds: [] }`
     - `selectAll()` → set `selectedIds` to all `nodes.map(n => n.id)`, `selectedEdgeId: null`, and `selectedId` to previous primary if still in ids else `ids[0] ?? null` (research A1 / RESOLVED Open Question 1).
  3. Add dedicated `renameNode(nodeId, name)` — analog: `updateParam`→`renameNode` (PATTERNS “Field-update pattern” / “Dedicated rename”); map nodes and set `name` only (Claude discretion: dedicated method, not generic updateNode).
  4. Add `duplicateNode(nodeId)` per CONTEXT Duplicate Behavior / BLK-02 — analog: `addNode`→`duplicateNode` (PATTERNS “Insert + auto-select” + “Duplicate core” + “ID allocation”):
     - Clone `type`, `params` via `{ ...src.params }`, `name: \`${src.name} copy\`` (single space + literal `copy`)
     - New id via `B${nextId}` (same as `addNode`); increment `nextId`
     - Position `src.position + 40/+40`, clamped with GraphCanvas drop constants from PATTERNS (980 / 720 / NODE_WIDTH 220 / margins 8 and 140)
     - `status: 'idle'`; do NOT copy any edges
     - Set `selectedId` / `selectedIds: [newId]` / `selectedEdgeId: null`
     - If source missing, return unchanged state (silent no-op — same style as `addEdge` alreadyConnected guard in PATTERNS)
  5. Add `removeNodes(ids: string[])` — analog: `removeNode` “Delete + edge prune” in PATTERNS — in one `set`: filter nodes not in ids, filter edges whose source/target is in ids, clear `selectedId`/`selectedIds`/`selectedEdgeId` appropriately (primary cleared if removed; filter selectedIds to survivors or clear all if batch delete of selection). Prefer clearing selection fully after multi-delete of the active set.
  6. Selection hygiene — also clear or sync `selectedIds` in (PATTERNS “Also clear selectedIds in”):
     - `removeNode` (filter id out of selectedIds; clear selectedId if it was that node)
     - `addNode` (set `selectedIds: [id]` alongside selectedId)
     - `startWire` (selectedIds: [])
     - `load` (selectedIds: first node id array or [])
  7. Do not install packages. Do not implement context menu UI here.
  </action>
  <acceptance_criteria>
    - `grep -n "selectedIds" src/state/graphStore.ts` shows field + updates in select/selectEdge/selectAll/duplicateNode/addNode/startWire/load/remove*
    - `grep -n "renameNode\\|duplicateNode\\|selectAll\\|removeNodes" src/state/graphStore.ts` finds all four method names on the store interface and implementation
    - `grep -n " copy" src/state/graphStore.ts` shows name suffix template with space before `copy`
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Store exports selectedIds, selectAll, renameNode, duplicateNode, removeNodes with selection mutual exclusion and duplicate offset/naming locked by CONTEXT.</done>
</task>

<task type="auto">
  <name>Task 2: Multi-delete keyboard path and selectedIds canvas chrome</name>
  <files>src/hooks/useKeyboardDelete.ts, src/components/canvas/GraphCanvas.tsx</files>
  <read_first>
    - src/hooks/useKeyboardDelete.ts
    - src/components/canvas/GraphCanvas.tsx
    - src/state/graphStore.ts (after Task 1)
    - .planning/phases/03-context-menu-block-management/03-UI-SPEC.md (Selection model keyboard Delete row)
  </read_first>
  <action>
  Per CONTEXT Claude's Discretion on keyboard Delete with multi-select and UI-SPEC “Keyboard Delete with multi-select → Remove all selected blocks”. Follow analogs in `03-PATTERNS.md` § `useKeyboardDelete.ts` (self) and § `GraphCanvas.tsx` (“Selected chrome wiring”):

  1. In `useKeyboardDelete.ts`, keep the existing window-keydown + INPUT/TEXTAREA/SELECT guard pattern from PATTERNS (required so Name field Backspace does not delete blocks). Change branch order per PATTERNS “Phase 3 core change”:
     - If `selectedIds.length > 0`, preventDefault and call `removeNodes(selectedIds)` via `useGraphStore.getState()` (batch — do not loop removeNode unless removeNodes unavailable).
     - Else if `selectedEdgeId`, removeEdge as today.
     - Do not also call removeNode(selectedId) separately when selectedIds already covers the primary (aligns with RESEARCH RESOLVED Open Question 2: keyboard multi-delete path).
  2. In `GraphCanvas.tsx`, subscribe to `selectedIds` from the store and pass `selected={selectedIds.includes(node.id)}` to each `BlockNode` (replacing `node.id === selectedId` — PATTERNS “Selected chrome wiring”). Keep wire selection via `selectedEdgeId` unchanged. Empty-canvas `select(null)` already clears multi-select once Task 1 updates `select`.
  3. Do not add context menu handlers in this plan.
  </action>
  <acceptance_criteria>
    - `grep -n "selectedIds\\|removeNodes" src/hooks/useKeyboardDelete.ts` shows multi-select-first delete path
    - `grep -n "selectedIds\\.includes" src/components/canvas/GraphCanvas.tsx` matches BlockNode selected prop
    - INPUT/TEXTAREA/SELECT guard still present in useKeyboardDelete.ts
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Select All-ready multi-delete works from the keyboard; all selectedIds show selected chrome on the canvas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → graph store | Block names and selection ids are untrusted UI strings/ids mutated only in-memory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Tampering | renameNode / node.name | mitigate | Store plain strings only; canvas/properties render via React text nodes — never dangerouslySetInnerHTML |
| T-3-02 | Tampering | duplicateNode params clone | mitigate | Shallow-copy `{ ...params }` of known ParamValue primitives only; do not deep-merge untrusted prototypes |
| T-3-03 | Elevation of Privilege (UX) | removeNodes / keyboard multi-delete | accept | No confirm modal (parity with Phase 1 Delete); undo deferred to v2 EDIT-01 |
| T-3-04 | Denial of Service | selectAll on large graphs | accept | In-memory mock graphs are small; no remote amplification |
| T-3-SC | Tampering | npm installs | mitigate | No new packages this phase — skip install; legitimacy N/A |
</threat_model>

<verification>
After both tasks: `npm run typecheck && npm run build`. Confirm store methods exist and GraphCanvas uses selectedIds for block chrome.
</verification>

<success_criteria>
- selectedIds / selectAll / renameNode / duplicateNode / removeNodes exist and keep selection fields consistent
- Keyboard Delete removes the full multi-select set
- Multiple blocks can show `.is-selected` via selectedIds
</success_criteria>

<output>
Create `.planning/phases/03-context-menu-block-management/03-01-SUMMARY.md` when done
</output>
